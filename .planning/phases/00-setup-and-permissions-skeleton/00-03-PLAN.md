---
phase: 00-setup-and-permissions-skeleton
plan: 3
title: Permission probes (FDA / Automation / Accessibility) and the doctor MCP tool
type: execute
wave: 3
depends_on: [1, 2]
requirements: [SETUP-04, SETUP-02]
files_modified:
  - src/whatsapp_desktop_mcp/permissions/osascript.py
  - src/whatsapp_desktop_mcp/permissions/fda.py
  - src/whatsapp_desktop_mcp/permissions/automation.py
  - src/whatsapp_desktop_mcp/permissions/accessibility.py
  - src/whatsapp_desktop_mcp/tools/doctor.py
  - src/whatsapp_desktop_mcp/server.py
autonomous: true
must_haves:
  truths:
    - "`tools/list` over the stdio JSON-RPC channel returns exactly one tool named `doctor` with `readOnlyHint: true`"
    - "`tools/call doctor` returns a `DoctorReport`-shaped JSON object (three keys: `full_disk_access`, `automation_whatsapp`, `accessibility`)"
    - "Each `PermissionStatus` payload includes `binary_path` (= `sys.executable`), `system_settings_url` (matching exception class attribute), and a `remediation` string when state != `granted`"
    - "Automation probe uses `id of application \"WhatsApp\"` (NOT `count windows`) and treats exit_code==0 OR error_code==-1708 OR error_code==-600 as `granted`"
    - "Accessibility probe matches numeric error codes (-1719, -25211) — NOT English prose (locale-stable per P-PHASE0-02)"
    - "FDA probe distinguishes `whatsapp_not_installed` (FileNotFoundError) from `denied` (PermissionError EACCES/EPERM)"
    - "Every osascript invocation runs via `asyncio.create_subprocess_exec` with `asyncio.wait_for(timeout=3)` (D-10) — synchronous `subprocess.run` is forbidden"
  artifacts:
    - path: src/whatsapp_desktop_mcp/permissions/osascript.py
      provides: "Async osascript runner with hard timeout and locale-blind (-NNNN)-only error parser"
      contains: "async def run_osascript"
    - path: src/whatsapp_desktop_mcp/permissions/fda.py
      provides: "FDA probe via os.stat() on ChatStorage.sqlite"
      contains: "async def check"
    - path: src/whatsapp_desktop_mcp/permissions/automation.py
      provides: "Automation probe via id of application WhatsApp"
      contains: "async def check_whatsapp"
    - path: src/whatsapp_desktop_mcp/permissions/accessibility.py
      provides: "Accessibility probe via tell System Events to count processes"
      contains: "async def check"
    - path: src/whatsapp_desktop_mcp/tools/doctor.py
      provides: "@mcp.tool() decorated doctor orchestrator returning a DoctorReport"
      contains: "@mcp.tool"
  key_links:
    - from: src/whatsapp_desktop_mcp/server.py
      to: src/whatsapp_desktop_mcp/tools/doctor.py
      via: "from whatsapp_desktop_mcp.tools import doctor as _doctor (registration side-effect)"
      pattern: "from whatsapp_desktop_mcp\\.tools import doctor as _doctor"
    - from: src/whatsapp_desktop_mcp/tools/doctor.py
      to: src/whatsapp_desktop_mcp/server.py
      via: "from whatsapp_desktop_mcp.server import mcp (decorator target)"
      pattern: "from whatsapp_desktop_mcp\\.server import mcp"
    - from: src/whatsapp_desktop_mcp/permissions/automation.py
      to: osascript probe for WhatsApp bundle id
      via: "asyncio.create_subprocess_exec /usr/bin/osascript -e <PROBE>"
      pattern: 'id of application "WhatsApp"'
    - from: src/whatsapp_desktop_mcp/permissions/osascript.py
      to: locale-stable error parsing
      via: 'regex r"\\((-?\\d+)\\)\\s*\\Z"'
      pattern: '\\\\\\(\\(-\\?\\\\d\\+\\)\\\\\\)'
---

<objective>
Wire the three permission probes and the `doctor` MCP tool. After this plan, calling `doctor` over MCP returns a structured `DoctorReport` listing the live state of Full Disk Access, Apple Events / Automation (for WhatsApp), and Accessibility — each with `binary_path`, `db_path` (FDA only), `system_settings_url`, and a `remediation` string for any non-granted bucket. This is the user-visible vertical slice of Phase 0: the four-step quickstart in the README ends with this tool returning a report.

Purpose: Implement SETUP-04's structured-error contract for missing permissions. Encode the empirically-corrected probe technique from CONTEXT.md D-09 (patched 2026-05-13: `id of application "WhatsApp"` with `-1708` treated as granted) verbatim from RESEARCH.md §"Code Examples / Automation probe (refined to handle -1708)" — the planner reviewed the corrected probe and selected it over the original `count windows` form.

Output: A single MCP tool registered with `readOnlyHint: true`, callable from Claude Desktop, returning a Pydantic-validated `DoctorReport` JSON. End-to-end: user adds the `claude_desktop_config.json` entry from Plan 05, restarts Claude Desktop, asks "call the WhatsApp doctor tool", receives the structured report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-setup-and-permissions-skeleton/00-CONTEXT.md
@.planning/phases/00-setup-and-permissions-skeleton/00-RESEARCH.md
@.planning/phases/00-setup-and-permissions-skeleton/00-01-SUMMARY.md
@.planning/phases/00-setup-and-permissions-skeleton/00-02-SUMMARY.md
@CLAUDE.md

# Decisions implemented by this plan
- D-06 (DoctorReport with three permission checks only — no schema/version probes)
- D-07 (NO SQLite schema probing, NO WhatsApp.app version detection — those are Phase 1)
- D-08 (`doctor` is the only `tools/list` entry in Phase 0)
- D-09 PATCHED (Automation probe uses `id of application "WhatsApp"`, treats `-1708` as granted; verified empirically 2026-05-13 — the original `count windows` form would mis-classify granted as denied per P-PHASE0-03)
- D-10 (every probe runs in `asyncio.create_subprocess_exec` + `asyncio.wait_for(timeout=3)` — sync `subprocess.run` is forbidden)
- D-11 (every `denied` response includes binary_path, db_path-or-None, system_settings_url, remediation)

# Phase-0-specific pitfalls this plan must mitigate
- **P-PHASE0-02:** Match `(-NNNN)` numeric tail only — never English prose (user's machine returns French stderr). RESEARCH.md §"Pattern 2 / osascript.py" provides the locale-stable regex.
- **P-PHASE0-03:** Use `id of application "WhatsApp"` as the primary probe; treat `-1708` as granted. The `count windows` form (CONTEXT.md D-09 original wording) is empirically broken on WhatsApp Catalyst.
- **P-PHASE0-06:** Tool module imports `from whatsapp_desktop_mcp.server import mcp`; server module imports `from whatsapp_desktop_mcp.tools import doctor as _doctor` AFTER `mcp = FastMCP(...)`. Plan 02 left a `# Phase 0 Plan 03 inserts:` comment marking the exact insertion line; this plan adds the import directly below that comment.

# Interfaces consumed (from Plan 02's frozen surface)

From `whatsapp_desktop_mcp.server`: `mcp: FastMCP` (already instantiated; decorator target). `def run() -> None`.

From `whatsapp_desktop_mcp.exceptions`: `FullDiskAccessRequired.system_settings_url`, `AutomationPermissionRequired.system_settings_url`, `AccessibilityPermissionRequired.system_settings_url` (class attributes — single source of truth for the URLs).

From `whatsapp_desktop_mcp.models.doctor`: `PermissionStatus(bucket, state, binary_path, db_path=None, system_settings_url, remediation="")` and `DoctorReport(full_disk_access, automation_whatsapp, accessibility)`.

From `whatsapp_desktop_mcp.paths`: `resolve_chatstorage_path() -> str`.

From `mcp.types` (provided by `mcp[cli]==1.27.1`, verified): `ToolAnnotations(readOnlyHint, destructiveHint, idempotentHint, openWorldHint)`.

# AppleScript probe error code map (verified empirically 2026-05-13 on user's Mac, fr_FR locale)

| Outcome | Automation status |
|---------|-------------------|
| `exit_code == 0` | granted |
| `error_code == -1708` (handler not found, but app received the event) | granted |
| `error_code == -600` (app installed but not running) | granted |
| `error_code == -1743` (errAEEventNotPermitted) | denied |
| `error_code == -1728` (errAENoSuchObject — for `id of application "WhatsApp"`) | whatsapp_not_installed |
| timeout / unknown | denied (with "WhatsApp may be hung" remediation) |

| Accessibility outcome | status |
|-----------------------|--------|
| `exit_code == 0` | granted |
| `error_code == -1719` (errAEAccessibilityNotEnabled) | denied |
| `error_code == -25211` (variant accessibility-deny) | denied |

# Hard rules carried from CLAUDE.md
- Stdout = JSON-RPC; logging to stderr (Plan 02 set up basicConfig; this plan keeps that invariant — no `print()`).
- No HTTP listener (this plan adds zero transport code; FastMCP runs stdio per Plan 02 default).
- Reader / Sender stay empty (this plan touches neither).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Async osascript runner with locale-stable error parser</name>
  <files>src/whatsapp_desktop_mcp/permissions/osascript.py</files>
  <read_first>
    .planning/phases/00-setup-and-permissions-skeleton/00-RESEARCH.md §"Pattern 2: Async osascript runner with localized-stderr-safe error parsing" (verbatim source),
    .planning/phases/00-setup-and-permissions-skeleton/00-RESEARCH.md §"Common Pitfalls / P-PHASE0-02",
    .planning/phases/00-setup-and-permissions-skeleton/00-CONTEXT.md §"Permission Probe Technique" (D-09, D-10)
  </read_first>
  <action>
    Implement `src/whatsapp_desktop_mcp/permissions/osascript.py` verbatim from RESEARCH.md §"Pattern 2 / osascript.py". Required invariants:
    - `OsascriptResult` is a `@dataclass(frozen=True)` with fields `exit_code: int`, `stdout: str`, `stderr: str`, `error_code: int | None`.
    - `_ERR_RE = re.compile(r"\((-?\d+)\)\s*\Z", re.MULTILINE)` — matches the trailing parenthesised signed integer at end of any line. Locale-blind by construction.
    - `async def run_osascript(script: str, timeout: float = 3.0) -> OsascriptResult` invokes `asyncio.create_subprocess_exec("/usr/bin/osascript", "-e", script, stdout=PIPE, stderr=PIPE)` then `await asyncio.wait_for(proc.communicate(), timeout=timeout)`. On `asyncio.TimeoutError`: kill the proc, await its termination, return `OsascriptResult(exit_code=-1, stdout="", stderr="timeout", error_code=None)`. On `FileNotFoundError` (no `/usr/bin/osascript`, e.g., a non-Mac CI runner): return `OsascriptResult(exit_code=-1, stdout="", stderr="osascript-missing", error_code=None)`.
    - Decode stdout/stderr as UTF-8 with `errors="replace"`. Parse `error_code` ONLY when `proc.returncode != 0` AND the regex matches.
    - `assert proc.returncode is not None` after `communicate()` — guards mypy strict's narrowing.
    - Module logger via `logger = logging.getLogger(__name__)`. NEVER `print()`. NEVER use synchronous `subprocess.run` (D-10).
  </action>
  <verify>
    <automated>uv run python -c "import asyncio; from whatsapp_desktop_mcp.permissions.osascript import run_osascript, OsascriptResult; r = asyncio.run(run_osascript('return 1+1', timeout=3.0)); assert r.exit_code == 0 and r.stdout.strip() == '2' and r.error_code is None" && uv run python -c "import asyncio; from whatsapp_desktop_mcp.permissions.osascript import run_osascript; r = asyncio.run(run_osascript('error \"forced\" number -42', timeout=3.0)); assert r.exit_code != 0 and r.error_code == -42" && python3 -c "import re,sys; src=open('src/whatsapp_desktop_mcp/permissions/osascript.py').read(); sys.exit(0 if re.search(r'r\"\\\\\\(\\(-\\?\\\\d\\+\\)\\\\\\)\\\\s\\*\\\\Z\"', src) else 1)" && python3 -c "import sys; src=open('src/whatsapp_desktop_mcp/permissions/osascript.py').read(); sys.exit(0 if 'subprocess.run' not in src else 1)" && uv run ruff check src/whatsapp_desktop_mcp/permissions/osascript.py && uv run mypy src/whatsapp_desktop_mcp/permissions/osascript.py</automated>
  </verify>
  <acceptance_criteria>
    - **Source assertion:** Regex pattern `\((-?\d+)\)\s*\Z` is present (locale-stable numeric tail matcher per P-PHASE0-02).
    - **Source assertion (D-10 enforcement):** No occurrence of `subprocess.run` anywhere in the file. Only `asyncio.create_subprocess_exec` is used.
    - **Behavior assertion (live osascript on macOS):** `run_osascript("return 1+1", timeout=3.0)` returns `OsascriptResult(exit_code=0, stdout="2\n", stderr="", error_code=None)`. *(Skip on non-Mac CI — but Plan 05's CI runs on `macos-14`, so this gate fires.)*
    - **Behavior assertion (forced error):** `run_osascript('error "forced" number -42', timeout=3.0)` returns `exit_code != 0` and `error_code == -42` (regex extraction works).
    - **Behavior assertion (timeout):** `run_osascript("delay 10", timeout=0.5)` returns `OsascriptResult(exit_code=-1, stderr="timeout", error_code=None)` within ~1s wall-clock — confirms the kill+wait path.
    - **Lint+type assertion:** ruff + mypy strict exit 0.
  </acceptance_criteria>
  <done>
    The osascript primitive is async, timeout-bounded, locale-blind, and reusable. Phase 2's sender will reuse this same module unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Three permission probe modules (FDA, Automation, Accessibility)</name>
  <files>
    src/whatsapp_desktop_mcp/permissions/fda.py,
    src/whatsapp_desktop_mcp/permissions/automation.py,
    src/whatsapp_desktop_mcp/permissions/accessibility.py
  </files>
  <read_first>
    .planning/phases/00-setup-and-permissions-skeleton/00-RESEARCH.md §"Code Examples / FDA probe" (verbatim source),
    .planning/phases/00-setup-and-permissions-skeleton/00-RESEARCH.md §"Code Examples / Automation probe (refined to handle -1708)" (verbatim source — the EMPIRICALLY CORRECTED form),
    .planning/phases/00-setup-and-permissions-skeleton/00-RESEARCH.md §"Code Examples / Accessibility probe" (verbatim source),
    .planning/phases/00-setup-and-permissions-skeleton/00-RESEARCH.md §"AppleScript Probe Error Code Map" (decision matrix),
    .planning/phases/00-setup-and-permissions-skeleton/00-RESEARCH.md §"Common Pitfalls / P-PHASE0-03" (the -1708 trap),
    .planning/phases/00-setup-and-permissions-skeleton/00-CONTEXT.md §"Permission Probe Technique" D-09 PATCHED (the empirical override notice — `id of application "WhatsApp"` is the agreed primary probe)
  </read_first>
  <action>
    Implement all three probe modules verbatim from the corresponding RESEARCH.md §"Code Examples" subsections. Critical invariants:

    **`fda.py`** (verbatim from §"Code Examples / FDA probe"):
    - `async def check() -> PermissionStatus` resolves the path via `from whatsapp_desktop_mcp.paths import resolve_chatstorage_path` then dispatches the blocking `os.stat` call to `asyncio.to_thread(_check_blocking, db_path)` (D-10).
    - `_check_blocking(db_path: str) -> PermissionStatus`: try `os.stat(db_path)`. `FileNotFoundError` → return `PermissionStatus(bucket="fda", state="whatsapp_not_installed", ...)`. `PermissionError` with `errno.EACCES`/`errno.EPERM` → return `state="denied"` with the remediation block from RESEARCH.md (the multi-line "Open System Settings → Privacy & Security → Full Disk Access" instruction). Other errno → log a warning and also return `state="denied"`. No exception → return `state="granted"`.
    - All `PermissionStatus` payloads: `binary_path=sys.executable` (D-11), `db_path=db_path`, `system_settings_url=FullDiskAccessRequired.system_settings_url` (read off the exception class — single source of truth).

    **`automation.py`** (verbatim from §"Code Examples / Automation probe (refined to handle -1708)" — the CORRECTED form, NOT the original `count windows` form):
    - `_PROBE = 'id of application "WhatsApp"'` (NOT `tell application "WhatsApp" to count windows` — the latter mis-classifies granted-Automation as denied per P-PHASE0-03; the patched probe is what RESEARCH.md §"Final probe-result decision matrix" requires).
    - `async def check_whatsapp() -> PermissionStatus`: call `await run_osascript(_PROBE, timeout=3.0)`.
    - Map per the decision matrix:
      - `exit_code == 0` OR `error_code == -1708` OR `error_code == -600` → `state="granted"` (no remediation).
      - `error_code == -1743` → `state="denied"` with the multi-line remediation including the `tccutil reset AppleEvents` recovery hint (verbatim from RESEARCH.md).
      - `error_code == -1728` → `state="whatsapp_not_installed"` with "Install WhatsApp Desktop from the App Store" remediation.
      - Anything else (timeout, unknown) → `state="denied"` with the "osascript probe returned an unexpected result" remediation.
    - `binary_path=sys.executable`, `db_path=None` (FDA only carries db_path), `system_settings_url=AutomationPermissionRequired.system_settings_url`.

    **`accessibility.py`** (verbatim from §"Code Examples / Accessibility probe"):
    - `_PROBE = 'tell application "System Events" to count processes'`.
    - `async def check() -> PermissionStatus`.
    - Map: `exit_code == 0` → `state="granted"`. `error_code in (-1719, -25211)` → `state="denied"` with the multi-line remediation. Anything else → `state="denied"` with the unexpected-result remediation.
    - `system_settings_url=AccessibilityPermissionRequired.system_settings_url`.

    **All three files:** `from __future__ import annotations`. Module logger via `logging.getLogger(__name__)`. No `print()`. Strict mypy compliance.
  </action>
  <verify>
    <automated>uv run python -c "import asyncio; from whatsapp_desktop_mcp.permissions import fda, automation, accessibility; r1 = asyncio.run(fda.check()); r2 = asyncio.run(automation.check_whatsapp()); r3 = asyncio.run(accessibility.check()); assert r1.bucket == 'fda' and r1.state in ('granted','denied','whatsapp_not_installed'); assert r2.bucket == 'automation' and r2.state in ('granted','denied','whatsapp_not_installed'); assert r3.bucket == 'accessibility' and r3.state in ('granted','denied'); assert r1.binary_path and r2.binary_path and r3.binary_path; assert 'Privacy_AllFiles' in r1.system_settings_url and 'Privacy_Automation' in r2.system_settings_url and 'Privacy_Accessibility' in r3.system_settings_url" && grep -F 'id of application "WhatsApp"' src/whatsapp_desktop_mcp/permissions/automation.py && python3 -c "import sys; src=open('src/whatsapp_desktop_mcp/permissions/automation.py').read(); sys.exit(0 if 'count windows' not in src else 1)" && grep -E 'error_code == -1708' src/whatsapp_desktop_mcp/permissions/automation.py && uv run ruff check src/whatsapp_desktop_mcp/permissions/fda.py src/whatsapp_desktop_mcp/permissions/automation.py src/whatsapp_desktop_mcp/permissions/accessibility.py && uv run mypy src/whatsapp_desktop_mcp/permissions/fda.py src/whatsapp_desktop_mcp/permissions/automation.py src/whatsapp_desktop_mcp/permissions/accessibility.py</automated>
  </verify>
  <acceptance_criteria>
    - **Source assertion (P-PHASE0-03 enforcement):** `automation.py` contains the literal probe string `id of application "WhatsApp"` and does NOT contain `count windows`. The `-1708 → granted` mapping is present (`error_code == -1708` appears in a granted-state branch).
    - **Source assertion:** `fda.py` uses `asyncio.to_thread(_check_blocking, ...)` (D-10 — no synchronous os.stat in the async event loop). `automation.py` and `accessibility.py` both call `await run_osascript(...)`.
    - **Source assertion (single-source-of-truth URLs):** Each probe module reads its `system_settings_url` from the matching exception class attribute (e.g., `_FDA_URL = FullDiskAccessRequired.system_settings_url`), NOT from a re-typed string literal.
    - **Behavior assertion (live, on user's Mac):** Calling each `check()` returns a `PermissionStatus` with `bucket` matching the module, `state` from the allowed Literal set, `binary_path` non-empty (= `sys.executable`), and `system_settings_url` containing the right `Privacy_*` token.
    - **Behavior assertion (FDA path resolution):** `fda.check()` returns a payload whose `db_path` ends with `Library/Group Containers/group.net.whatsapp.WhatsApp.shared/ChatStorage.sqlite`.
    - **Lint+type assertion:** ruff + mypy strict exit 0 on all three files.
  </acceptance_criteria>
  <done>
    Three independent probes, each pure-async, each producing a `PermissionStatus` with the full D-11 payload (binary_path + db_path-or-None + system_settings_url + remediation). The empirically-corrected D-09 wording is in the source.
  </done>
</task>

<task type="auto">
  <name>Task 3: doctor MCP tool registration and server-side import wiring</name>
  <files>
    src/whatsapp_desktop_mcp/tools/doctor.py,
    src/whatsapp_desktop_mcp/server.py
  </files>
  <read_first>
    .planning/phases/00-setup-and-permissions-skeleton/00-RESEARCH.md §"Pattern 1: FastMCP minimal stdio server / tools/doctor.py" (verbatim source),
    .planning/phases/00-setup-and-permissions-skeleton/00-RESEARCH.md §"Common Pitfalls / P-PHASE0-06" (circular import order),
    .planning/phases/00-setup-and-permissions-skeleton/00-CONTEXT.md §"`doctor` Tool Scope" (D-06, D-07, D-08),
    .planning/phases/00-setup-and-permissions-skeleton/00-02-SUMMARY.md (the line number of the `# Phase 0 Plan 03 inserts:` marker in server.py)
  </read_first>
  <action>
    1. **`src/whatsapp_desktop_mcp/tools/doctor.py`** (verbatim from RESEARCH.md §"Pattern 1 / tools/doctor.py"):
       - `from mcp.types import ToolAnnotations` (NOT from `mcp.server.fastmcp` — `ToolAnnotations` lives at `mcp.types` per RESEARCH.md §"Standard Stack" / verified API surface).
       - `from whatsapp_desktop_mcp.models.doctor import DoctorReport`.
       - `from whatsapp_desktop_mcp.permissions import accessibility, automation, fda` (package-import; the three probe modules are siblings under `whatsapp_desktop_mcp.permissions`).
       - `from whatsapp_desktop_mcp.server import mcp` — this is the import that closes the loop. `mcp = FastMCP(...)` in `server.py` was instantiated BEFORE Plan 02's `# Phase 0 Plan 03 inserts:` marker, so the Plan 03 edit to `server.py` (step 2 below) does NOT introduce a circular import (P-PHASE0-06 mitigation: definition before use).
       - Decorator: `@mcp.tool(name="doctor", title="Doctor — preflight permission check", description="...", annotations=ToolAnnotations(readOnlyHint=True, destructiveHint=False, idempotentHint=True, openWorldHint=False))` — verbatim description text from RESEARCH.md (the multi-line "Reports whether the three macOS permissions ..." block).
       - `async def doctor() -> DoctorReport`: returns `DoctorReport(full_disk_access=await fda.check(), automation_whatsapp=await automation.check_whatsapp(), accessibility=await accessibility.check())`. The three probes can run concurrently — using `asyncio.gather` is permissible but not required for Phase 0; the verbatim source uses sequential `await`s, which is what to ship (simpler; total wall-clock ≤ 3 × 3s timeout = 9s, well under any tool timeout).
    2. **`src/whatsapp_desktop_mcp/server.py`** (single-line edit):
       - Locate the marker `# Phase 0 Plan 03 inserts: from whatsapp_desktop_mcp.tools import doctor as _doctor  # noqa: E402, F401` (left by Plan 02 Task 2).
       - Insert directly below it (or replace the marker comment with) the literal line:
         `from whatsapp_desktop_mcp.tools import doctor as _doctor  # noqa: E402, F401`
       - The `# noqa: E402` is required because the import comes AFTER `logging.basicConfig(...)` (D-05 ordering); the `# noqa: F401` documents that the side-effect (decorator registration) is the point of the import, not the bound name.
       - This is the ONLY change to `server.py` in this plan.
    Per D-08: this is the only `tools/list` entry in Phase 0 — do not add a `ping` tool, a `version` tool, or any other module under `whatsapp_desktop_mcp/tools/`.
  </action>
  <verify>
    <automated>uv run python -c "import asyncio; from whatsapp_desktop_mcp.server import mcp; tools = asyncio.run(mcp.list_tools()); assert len(tools) == 1, f'expected 1 tool, got {len(tools)}: {[t.name for t in tools]}'; t = tools[0]; assert t.name == 'doctor'; assert t.annotations is not None and t.annotations.readOnlyHint is True and t.annotations.destructiveHint in (False, None)" && uv run python -c "import asyncio; from whatsapp_desktop_mcp.tools.doctor import doctor; r = asyncio.run(doctor()); d = r.model_dump(); assert set(d.keys()) == {'full_disk_access', 'automation_whatsapp', 'accessibility'}; assert d['full_disk_access']['bucket'] == 'fda'; assert d['automation_whatsapp']['bucket'] == 'automation'; assert d['accessibility']['bucket'] == 'accessibility'" && grep -E '^from whatsapp_desktop_mcp\.tools import doctor as _doctor' src/whatsapp_desktop_mcp/server.py && grep -E '@mcp\.tool\(' src/whatsapp_desktop_mcp/tools/doctor.py && grep -E 'readOnlyHint=True' src/whatsapp_desktop_mcp/tools/doctor.py && uv run ruff check src/whatsapp_desktop_mcp/tools/doctor.py src/whatsapp_desktop_mcp/server.py && uv run mypy src/whatsapp_desktop_mcp/tools/doctor.py src/whatsapp_desktop_mcp/server.py</automated>
  </verify>
  <acceptance_criteria>
    - **Behavior assertion (registration):** `await mcp.list_tools()` returns exactly one `Tool` named `doctor` with `annotations.readOnlyHint == True` and `annotations.destructiveHint` in `{False, None}` (D-08 — exactly one tool in Phase 0).
    - **Behavior assertion (call shape):** Calling `doctor()` returns a `DoctorReport` whose `.model_dump()` has exactly the three keys `full_disk_access`, `automation_whatsapp`, `accessibility` (D-06; no `schema_fingerprint`, no `whatsapp_app_version`, no `coverage` field — those are Phase 1 per D-07).
    - **Behavior assertion (Pydantic v2 buckets):** Each nested payload's `bucket` field matches the parent key (`full_disk_access.bucket == "fda"`, `automation_whatsapp.bucket == "automation"`, `accessibility.bucket == "accessibility"`).
    - **Source assertion (server wiring):** `src/whatsapp_desktop_mcp/server.py` contains the line `from whatsapp_desktop_mcp.tools import doctor as _doctor` (with appropriate `# noqa` comment). The Plan 02 marker comment is either removed or replaced by this import.
    - **Source assertion (decorator):** `tools/doctor.py` contains `@mcp.tool(` and the keyword argument `readOnlyHint=True` somewhere within the `ToolAnnotations(...)` call.
    - **Lint+type assertion:** ruff + mypy strict exit 0 on both files.
  </acceptance_criteria>
  <done>
    The `doctor` MCP tool is the sole registered tool in Phase 0, returning a Pydantic-validated three-bucket report. The empirically-corrected probe technique is in the source. The user-visible vertical slice of Phase 0 is complete from the server side; Plan 04 will gate it with the stdout-purity CI test and Plan 05 will wire the README quickstart that exercises it end-to-end.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → `tools/call doctor` | Claude Desktop (or any MCP client) can invoke the doctor tool with no arguments — minimal attack surface |
| `whatsapp_desktop_mcp` process → `osascript` subprocess | Spawned process inherits same TCC grants and runs locale-dependent code |
| process → macOS TCC system | Reads filesystem metadata via `os.stat`; sends Apple Events to WhatsApp and System Events |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-08 | Information disclosure | `doctor` enumerates filesystem paths that the LLM can then exfiltrate via prompt injection — specifically `db_path` (under user's home) and `binary_path` (under `~/.local/share/uv/...`) | accept | These are paths the user already has on disk; no message content, no SQLite data, no chat metadata. CONTEXT.md D-11 mandates exactly these fields. Phase 0 has no other surfaces to enumerate |
| T-00-09 | Tampering | A malicious WhatsApp build or a system in unusual state returns a confused/spoofed osascript stderr that maps to `granted` when permission is actually denied | mitigate | Locale-blind `(-NNNN)` matching (P-PHASE0-02); the decision matrix only accepts `0 / -1708 / -600` as granted (closed set). A truly anomalous case (e.g., new error code Apple introduces) falls through to `state="denied"` with the unexpected-result remediation — safe default |
| T-00-10 | DoS | An osascript invocation hangs (e.g., WhatsApp is frozen mid-prompt), blocking the stdio event loop forever | mitigate | Every probe wraps `osascript` in `asyncio.wait_for(timeout=3)`; on timeout the proc is killed and `state="denied"` returned with a "WhatsApp may be hung; restart it" remediation. Worst case: a `doctor` call returns in ~9 seconds (three 3s timeouts), still well under any reasonable tool timeout |
| T-00-11 | Elevation of privilege | The doctor tool's `readOnlyHint:true` annotation is forgotten or set wrong, causing the LLM to treat it as destructive (or vice versa, treating destructive tools as read-only) | mitigate | Verify gate greps `readOnlyHint=True` and asserts the annotation at runtime via `mcp.list_tools()`. Pydantic+FastMCP enforce the shape; the test is belt-and-braces |
</threat_model>

<verification>
After all three tasks:
1. `uv run python -c "import asyncio; from whatsapp_desktop_mcp.server import mcp; t = asyncio.run(mcp.list_tools()); assert len(t) == 1 and t[0].name == 'doctor'"` exits 0.
2. `uv run python -c "import asyncio; from whatsapp_desktop_mcp.tools.doctor import doctor; r = asyncio.run(doctor()); print(r.model_dump_json(indent=2), file=__import__('sys').stderr)"` writes a JSON DoctorReport to **stderr** (no stdout output — preserves P-PHASE0-01).
3. `uv run ruff check src tests` exits 0 (no `print`, no other rule violations).
4. `uv run mypy` exits 0 in strict mode against the whole package.
5. `grep -F 'id of application "WhatsApp"' src/whatsapp_desktop_mcp/permissions/automation.py` returns the line (D-09 PATCHED enforcement).
6. `grep -c subprocess.run src/whatsapp_desktop_mcp/permissions/*.py` returns 0 (D-10 — async-only).
7. End-to-end live (on user's Mac): `uv run whatsapp-desktop-mcp` boots stdio; manually piping a JSON-RPC `initialize` → `tools/list` → `tools/call doctor` sequence produces three response frames on stdout, all valid JSON-RPC, the third containing a `DoctorReport` with the user's actual permission states. (Plan 04 automates this in `test_stdout_purity.py`.)
</verification>

<success_criteria>
- SETUP-04 satisfied: missing permissions produce structured payloads with binary_path + db_path + system_settings_url + remediation. The exception classes (Plan 02) and the probe modules (Plan 03) share a single source of truth for the System Settings URLs.
- SETUP-02 strengthened: server registers `doctor` cleanly; Claude Desktop sees the tool advertisement; `tools/call doctor` returns a structured Pydantic response (not a Python traceback). The "looks broken" class of permission bugs is now diagnosable from inside Claude Desktop.
- D-06, D-07, D-08 honored: exactly one tool registered (doctor); exactly three permission buckets reported; no schema/version/coverage probing in Phase 0.
- D-09 PATCHED implemented as agreed (2026-05-13 empirical correction).
- D-10 enforced: zero synchronous subprocess calls; every external invocation is timeout-bounded.
- P-PHASE0-02, P-PHASE0-03, P-PHASE0-06 all mitigated in source.
</success_criteria>

<output>
After completion, create `.planning/phases/00-setup-and-permissions-skeleton/00-03-SUMMARY.md` recording:
- Confirmation that `mcp.list_tools()` returns exactly one tool, with the exact annotation flags observed.
- A live `doctor` invocation transcript on the user's Mac (the actual `DoctorReport` JSON, with permission states as currently configured — for the Phase 1 baseline reference).
- Confirmation that automation.py uses `id of application "WhatsApp"` (NOT `count windows`).
- The total number of lines changed in `server.py` by this plan (should be exactly 1 import line added, optionally 1 marker comment removed).
</output>
