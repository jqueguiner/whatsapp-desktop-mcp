---
phase: 02-send-ui-automation-guardrails
plan: 01
title: Sender primitives — pyobjc deps, deeplink builder, osascript_send wrapper, AX preflight (Wave-0 spikes first)
type: execute
wave: 1
depends_on: []
requirements: [SEND-03, SEND-04]
files_modified:
  - pyproject.toml
  - uv.lock
  - src/whatsapp_desktop_mcp/exceptions.py
  - src/whatsapp_desktop_mcp/sender/deeplink.py
  - src/whatsapp_desktop_mcp/sender/osascript_send.py
  - src/whatsapp_desktop_mcp/sender/ax_assert.py
  - .planning/phases/02-send-ui-automation-guardrails/02-01-SPIKES.md
autonomous: true
must_haves:
  truths:
    - "pyobjc-core 12.1, pyobjc-framework-Cocoa 12.1, pyobjc-framework-ApplicationServices 12.1 install via `uv sync --extra dev` and are listed in `[project] dependencies` (NOT optional-dev) per D-05."
    - "`from whatsapp_desktop_mcp.sender.deeplink import build_send_url` returns `whatsapp://send?phone=33612345678&text=Hello%21` for `build_send_url('+33 612 345 678', 'Hello!')` (digits-only normalization + urllib.parse.quote)."
    - "`from whatsapp_desktop_mcp.sender.osascript_send import press_return` raises `AutomationRevoked` when the underlying osascript returns error_code=-1743 and `OsascriptError` on any other non-zero exit."
    - "`from whatsapp_desktop_mcp.sender.ax_assert import assert_focused_chat_matches, _assert_first_search_result_matches` both raise `AccessibilityAPIUnavailable` when pyobjc imports fail, and raise `ChatHeaderMismatch` when their respective AX target (focused chat header for the former, sidebar-search first result for the latter) does not match the expected (bidi-stripped, casefold-substring) name."
    - "`whatsapp_desktop_mcp.exceptions` exports five new classes: `ChatHeaderMismatch`, `AccessibilityAPIUnavailable`, `OsascriptError`, `SendTimeout`, `AutomationRevoked` — all inheriting from `WhatsAppMCPError`."
    - "REL-05 D-24 invariant holds: `sender/deeplink.py`, `sender/osascript_send.py`, `sender/ax_assert.py` import nothing from `whatsapp_desktop_mcp.reader.*` (verified by `grep -rE 'whatsapp_desktop_mcp\\.reader' src/whatsapp_desktop_mcp/sender/` returning 0)."
  artifacts:
    - path: "src/whatsapp_desktop_mcp/sender/deeplink.py"
      provides: "build_send_url(phone_e164, body) -> str + send_deeplink(phone_e164, body) async coroutine that runs `/usr/bin/open -g <url>` + 1.5s settle-poll via osascript front-window-name probe"
    - path: "src/whatsapp_desktop_mcp/sender/osascript_send.py"
      provides: "press_return(timeout=3.0) async coroutine — wraps Phase 0's run_osascript('tell application \"System Events\" to keystroke return', timeout=...); type_string(text, timeout=3.0) for the group-fallback body keystroke (BMP-only validation)"
    - path: "src/whatsapp_desktop_mcp/sender/ax_assert.py"
      provides: "_strip_bidi(s) + assert_focused_chat_matches(expected_chat_name) + _assert_first_search_result_matches(chat_name) — both raise ChatHeaderMismatch | AccessibilityAPIUnavailable; D-06 try/except ImportError guard around all pyobjc symbols; the second helper exists so Plan 02-03's group-fallback can preflight the sidebar-search FIRST result BEFORE pressing Return"
    - path: "src/whatsapp_desktop_mcp/exceptions.py"
      provides: "Appended classes ChatHeaderMismatch, AccessibilityAPIUnavailable, OsascriptError, SendTimeout, AutomationRevoked (all WhatsAppMCPError subclasses)"
    - path: "pyproject.toml"
      provides: "Three pyobjc deps appended to `[project] dependencies` (NOT dev) per D-05"
    - path: ".planning/phases/02-send-ui-automation-guardrails/02-01-SPIKES.md"
      provides: "Empirical results of the 4 Wave-0 spikes: open -g foreground behavior, AXHeading presence when a chat is open, pyobjc 12.1 AXUIElementCopyAttributeValue return shape, sidebar-search shortcut (Cmd-F vs AX-click)"
  key_links:
    - from: "src/whatsapp_desktop_mcp/sender/osascript_send.py"
      to: "src/whatsapp_desktop_mcp/permissions/osascript.py:run_osascript"
      via: "from whatsapp_desktop_mcp.permissions.osascript import run_osascript"
      pattern: "from whatsapp_desktop_mcp.permissions.osascript import run_osascript"
    - from: "src/whatsapp_desktop_mcp/sender/ax_assert.py"
      to: "src/whatsapp_desktop_mcp/exceptions.py"
      via: "from whatsapp_desktop_mcp.exceptions import AccessibilityAPIUnavailable, ChatHeaderMismatch"
      pattern: "AccessibilityAPIUnavailable|ChatHeaderMismatch"
    - from: "src/whatsapp_desktop_mcp/sender/deeplink.py"
      to: "src/whatsapp_desktop_mcp/permissions/osascript.py:run_osascript"
      via: "front-window-name settle-poll uses run_osascript"
      pattern: "run_osascript"
---

<objective>
Land the three **MCP-decoupled** sender primitive modules that Plan 02-03 will compose into the unified send path: the deep-link URL builder + foregrounding wrapper (D-01 / SEND-03 1:1 path), the osascript keystroke-return wrapper (reused by both 1:1 and group fallback), and the pyobjc-based AX state assertion (D-03 / SEND-04 — the load-bearing P5 mitigation). Add the 3 pyobjc runtime dependencies (D-05) and append the 5 new exception classes the sender surface needs.

Phase 2's Wave 0 lives inside Task 1 of this plan: run the 4 empirical spikes the research surfaced (A1-A4 / open questions 1-4) and pin the results in `02-01-SPIKES.md` BEFORE writing the dependent module code. Locking these tactical specifics here means Plans 02-03 / 02-05 can assume them.

Purpose: hand Plans 02-02 (guardrails) and 02-03 (orchestration + tool) a stable, RO-tier-coupling-free set of primitives so they can run in parallel with this plan's Wave-2 sibling (02-02) and then both feed 02-03 in Wave-3.

Output: 4 new `.py` files (3 sender primitives + 1 spike-results doc) + amended `pyproject.toml` + `uv.lock` + amended `exceptions.py`. Zero tool registrations. Zero reader imports. Zero MCP coupling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-send-ui-automation-guardrails/02-CONTEXT.md
@.planning/phases/02-send-ui-automation-guardrails/02-RESEARCH.md
@CLAUDE.md
@src/whatsapp_desktop_mcp/exceptions.py
@src/whatsapp_desktop_mcp/permissions/osascript.py
@src/whatsapp_desktop_mcp/permissions/automation.py
@pyproject.toml

<interfaces>
<!-- The Phase 0 / Phase 1 surface Plan 02-01 builds on. Verbatim signatures lifted from the shipped codebase via grep — executor does not need to re-read. -->

From `src/whatsapp_desktop_mcp/permissions/osascript.py`:
```python
@dataclass(frozen=True)
class OsascriptResult:
    exit_code: int
    stdout: str
    stderr: str
    error_code: int | None   # parsed from trailing "(-NNNN)"; locale-blind

async def run_osascript(script: str, timeout: float = 3.0) -> OsascriptResult: ...
# Synthetic outcomes:
#   timeout       -> OsascriptResult(exit_code=-1, stdout="", stderr="timeout", error_code=None)
#   no osascript  -> OsascriptResult(exit_code=-1, stdout="", stderr="osascript-missing", error_code=None)
```

From `src/whatsapp_desktop_mcp/exceptions.py` (frozen Phase 0/1 surface):
```python
class WhatsAppMCPError(Exception): ...
class PermissionRequired(WhatsAppMCPError):
    bucket: str = "unknown"
    system_settings_url: str = ""
    def __init__(self, message, *, binary_path, db_path=None, remediation=""): ...
class FullDiskAccessRequired(PermissionRequired): ...
class AutomationPermissionRequired(PermissionRequired): ...
class AccessibilityPermissionRequired(PermissionRequired): ...
class ReadOnlyMode(WhatsAppMCPError): ...
```

From `src/whatsapp_desktop_mcp/permissions/automation.py` (the D-09 PATCHED probe — reused by Plan 02-03's T-6 re-check):
```python
_PROBE = 'id of application "WhatsApp"'
# Decision matrix:
#   exit 0 OR error_code in (-1708, -600) -> granted
#   error_code == -1743 -> denied
#   error_code == -1728 -> whatsapp_not_installed
#   anything else        -> denied (unexpected_result)
async def check_whatsapp() -> PermissionStatus: ...
```

From `pyproject.toml` current dependencies block (TO BE EXTENDED, NOT REWRITTEN):
```toml
dependencies = [
    "mcp[cli]==1.27.1",
    "pydantic>=2.7,<3",
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 spikes + pyobjc dependency wiring</name>
  <files>pyproject.toml, uv.lock, .planning/phases/02-send-ui-automation-guardrails/02-01-SPIKES.md</files>
  <read_first>
    - .planning/phases/02-send-ui-automation-guardrails/02-RESEARCH.md §"Environment Availability" + §"Open Questions" (entries 1-4) + §"Assumptions Log" A1-A4
    - .planning/phases/02-send-ui-automation-guardrails/02-CONTEXT.md D-05 (pyobjc to `[project] dependencies` NOT dev)
    - pyproject.toml (current `[project] dependencies` block — extend, do not rewrite)
  </read_first>
  <action>
Run the four Wave-0 spikes against the user's live WhatsApp Desktop 26.16.74 / macOS 26.4 to resolve the empirical assumptions A1-A4 from 02-RESEARCH.md §"Assumptions Log" BEFORE wiring dependent code. Each spike is a single command or short heredoc; record observed output verbatim in `02-01-SPIKES.md` along with the resulting locked decision (which way the implementation will branch).

Spike SP-1 (A1 — sidebar-search shortcut): with WhatsApp.app in foreground, run `osascript -e 'tell application "System Events" to keystroke "f" using {command down}'` then immediately `osascript -e 'tell application "System Events" to tell process "WhatsApp" to get description of (focused UI element)'`. Record what focuses. If sidebar search field focuses: lock D-02 group fallback to keystroke Cmd-F. If chat-pane search-within-chat focuses (or nothing): lock D-02 to AX-click on the `AXGenericElement` whose description contains "Recherch" or "Search" via pyobjc.

Spike SP-2 (A2/A3 — `open -g` foreground behavior): with WhatsApp.app backgrounded (e.g. Cmd-Tab to Terminal), run `/usr/bin/open -g whatsapp://send?phone=33612345678&text=test`. Observe whether WhatsApp comes to front within 1.5s (poll `osascript -e 'tell application "System Events" to tell process "WhatsApp" to get name of front window'` at 50ms intervals up to 30 polls). Record the actual settle time. If `-g` keeps WhatsApp completely background such that the settle-poll never finds a WhatsApp front window, lock the implementation to drop `-g` and accept the focus-steal tradeoff. DO NOT actually send — do this against a known-safe self-chat or use a non-routable phone like 99999999999 so the deep-link opens the "Send to ... ?" prompt without ever firing.

Spike SP-3 (open question 3 — AXHeading when a chat is open): manually open any chat in WhatsApp, then run `osascript -e 'tell application "System Events" to tell process "WhatsApp" to get entire contents of front window'` and scan the output for `AXHeading` entries. Record whether the open chat's name appears as an `AXHeading` description, or only as `AXStaticText`. If only as `AXStaticText`, widen the `_walk_for_heading` collection in Task 3 to also collect AXStaticText nodes within the first 50 visited nodes.

Spike SP-4 (A6 — pyobjc 12.1 AX-API return signature): install pyobjc temporarily in a throwaway venv (`uv venv .venv-spike && .venv-spike/bin/pip install pyobjc-core==12.1 pyobjc-framework-Cocoa==12.1 pyobjc-framework-ApplicationServices==12.1`) and run a minimal probe via a `.venv-spike/bin/python -c '...'` heredoc that imports `AXUIElementCreateApplication`, `AXUIElementCopyAttributeValue`, `kAXFocusedWindowAttribute`, resolves WhatsApp's PID via `NSWorkspace.sharedWorkspace().runningApplications()` filtering by `bundleIdentifier() == "net.whatsapp.WhatsApp"`, calls `AXUIElementCreateApplication(pid)` then `AXUIElementCopyAttributeValue(app_ref, kAXFocusedWindowAttribute, None)`. Record the EXACT return shape — whether it is `(err: int, value)` tuple, raises, or returns just `value`. Tear down `.venv-spike` after recording. Lock Task 3's call-shape against the observed reality.

Spike SP-5 (sidebar-search AX role for first-result preflight — W-5 follow-up for Task 3's `_assert_first_search_result_matches`): with WhatsApp.app foregrounded and sidebar search invoked (per SP-1 locked decision), type a known chat name (e.g. "Discussions" or any contact certain to match). Observe the resulting sidebar list elements via `osascript -e 'tell application "System Events" to tell process "WhatsApp" to get entire contents of front window'` AND a minimal pyobjc walk script that enumerates the first 50 AX nodes under the focused window's AXScrollArea, printing each node's role + title + description. Record (a) the AX role that backs each sidebar result (AXButton vs AXCell vs AXStaticText vs AXGenericElement), (b) which attribute carries the resolved chat display name (AXTitle vs AXDescription vs AXValue). Lock the result so Task 3's `_assert_first_search_result_matches` uses the correct role filter and attribute fetch. If sidebar results expose the chat name as AXTitle on AXButton nodes, the helper can reuse `_walk_for_heading` with a widened role set. If they only surface via AXDescription on AXGenericElement nodes, ship a parallel `_walk_for_first_button` with the appropriate role + attribute pair.

After all five spikes are recorded in `02-01-SPIKES.md`, extend `[project] dependencies` in `pyproject.toml` (preserve every existing line) appending three lines: `"pyobjc-core>=12.1"`, `"pyobjc-framework-Cocoa>=12.1"`, `"pyobjc-framework-ApplicationServices>=12.1"`. The deps go in `[project] dependencies` per D-05 (NOT `[project.optional-dependencies] dev`). Run `uv sync --extra dev` to re-resolve and commit the updated `uv.lock`.

Format of `02-01-SPIKES.md` — one section per spike (SP-1..SP-5) with: command run, observed output (verbatim or trimmed to relevant lines), conclusion (locked decision affecting Task 2/3), date. Treat this file as a permanent artifact Plans 02-03 and 02-05 will read.
  </action>
  <verify>
    <automated>cd /Users/jlqueguiner/dev/whatsapp-desktop-mcp && uv sync --extra dev &gt;/dev/null 2&gt;&amp;1 &amp;&amp; uv run python - &lt;&lt;'PY'
import ApplicationServices
import Cocoa
import objc
assert objc.__version__ &gt;= "12.1", f"pyobjc-core {objc.__version__} &lt; 12.1"
print("OK pyobjc", objc.__version__)
PY</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^\s*"pyobjc-core' pyproject.toml | grep -v '^#'` returns ≥1 AND `grep -cE '^\s*"pyobjc-framework-Cocoa' pyproject.toml | grep -v '^#'` returns ≥1 AND `grep -cE '^\s*"pyobjc-framework-ApplicationServices' pyproject.toml | grep -v '^#'` returns ≥1.
    - Same three pyobjc tokens are absent from any `[project.optional-dependencies]` block — verify via `grep -nE 'pyobjc' pyproject.toml` and confirm every line is inside the `[project] dependencies` array, not the `dev = [` array.
    - `uv.lock` contains `name = "pyobjc-core"` (i.e. `grep -cE 'name = "pyobjc-core"' uv.lock` returns ≥1).
    - File `.planning/phases/02-send-ui-automation-guardrails/02-01-SPIKES.md` exists, is at least 60 lines, and contains literal headings for SP-1, SP-2, SP-3, SP-4, SP-5 — verify via `grep -cE '^##? SP-[1-5]' .planning/phases/02-send-ui-automation-guardrails/02-01-SPIKES.md` returning 5.
    - `uv run python -c 'import ApplicationServices, Cocoa; print("OK")'` prints OK and exits 0.
    - `cd /Users/jlqueguiner/dev/whatsapp-desktop-mcp && uv run ruff check pyproject.toml || true` does not error on the dependency file itself (pyproject.toml is not Python; ruff skips it).
    - The throwaway `.venv-spike` directory used by SP-4 is removed (or never created if SP-4 was executed inside `uv run`). Verify via `test ! -d .venv-spike`.
  </acceptance_criteria>
  <done>pyobjc 12.1 deps wired as runtime per D-05, lockfile refreshed and committed, all five Wave-0 spikes recorded in `02-01-SPIKES.md` with locked tactical decisions for Tasks 2/3 + Plan 02-03 to read.</done>
</task>

<task type="auto">
  <name>Task 2: Exception hierarchy extension + deep-link + osascript_send wrappers</name>
  <files>src/whatsapp_desktop_mcp/exceptions.py, src/whatsapp_desktop_mcp/sender/deeplink.py, src/whatsapp_desktop_mcp/sender/osascript_send.py</files>
  <read_first>
    - src/whatsapp_desktop_mcp/exceptions.py (the frozen Phase 0/1 surface — append, do not modify existing classes)
    - src/whatsapp_desktop_mcp/permissions/osascript.py (the `run_osascript` wrapper this task reuses verbatim)
    - .planning/phases/02-send-ui-automation-guardrails/02-RESEARCH.md §"Deep-Link Send Path (D-01)" and §"Example 2: Deep-link URL builder"
    - .planning/phases/02-send-ui-automation-guardrails/02-CONTEXT.md D-01 (deep-link primary), D-04 (no AX-set-value in v0.1 — keystroke only)
    - .planning/phases/02-send-ui-automation-guardrails/02-01-SPIKES.md (results of SP-2 — whether `-g` is kept or dropped)
    - CLAUDE.md §"Hard architectural rules" (stdout=JSON-RPC, no HTTP, REL-05)
  </read_first>
  <action>
Append five exception classes to `src/whatsapp_desktop_mcp/exceptions.py` BELOW the existing `ReadOnlyMode` class — do not modify any existing class definition; the Phase 1 contract is frozen per Plan 01-03's SUMMARY. Required additions, all inheriting from `WhatsAppMCPError`: `ChatHeaderMismatch(WhatsAppMCPError)`, `AccessibilityAPIUnavailable(WhatsAppMCPError)`, `OsascriptError(WhatsAppMCPError)`, `SendTimeout(WhatsAppMCPError)`, `AutomationRevoked(WhatsAppMCPError)`. None of them need keyword-only constructors — they inherit `Exception(message)` bare. Add module-level docstring sentences for each explaining the failure semantics (mirror the Plan 01-03 prose style for `ReadOnlyMode`). DO NOT export them via a frontmatter `__all__` — `exceptions.py` does not currently have one; preserve that.

Create `src/whatsapp_desktop_mcp/sender/deeplink.py`. Module docstring explains D-01 deep-link semantics, the `open -g` flag rationale (cite 02-01-SPIKES.md SP-2 conclusion), and the 1.5s settle-poll budget. Public surface — two callables:

- `build_send_url(phone_e164: str, body: str) -> str`. Strip leading `+`, all spaces, and all `-` from phone; assert the remainder via `.isdigit()` and raise `ValueError(f"phone must be E.164 digits-only after stripping +/-: got {phone_e164!r}")` if not. URL-encode body via `urllib.parse.quote(body, safe='')` (note: `quote` not `quote_plus` — `+` must stay literal in body per RFC 3986). Return `f"whatsapp://send?phone={cleaned}&text={quoted}"`.
- `async def send_deeplink(phone_e164: str, body: str) -> None`. Build URL via `build_send_url`. Spawn `/usr/bin/open` with arguments per the SP-2 locked decision: if SP-2 confirmed `-g` brings WhatsApp foreground within 1.5s, use `["-g", url]`; if SP-2 showed `-g` keeps WhatsApp suppressed, use `[url]` only. Use `asyncio.create_subprocess_exec` + `asyncio.wait_for(proc.communicate(), timeout=5.0)`. After `open` returns, run a settle-poll: up to 30 iterations of `await asyncio.sleep(0.05)` + a `run_osascript('tell application "System Events" to tell process "WhatsApp" to get name of front window', timeout=1.0)` call; break when the stdout contains `"WhatsApp"` (substring match per the RESEARCH note on U+200E LRM prefix — do NOT use equality). If the loop exhausts, raise `SendTimeout("WhatsApp window did not settle within 1.5s after deep-link open")`.

CRITICAL: per CLAUDE.md hard rule #5, `deeplink.py` opens NO network sockets. It only spawns `/usr/bin/open` (a local URL-scheme handler) and the existing osascript wrapper. Re-state this in the module docstring.

Create `src/whatsapp_desktop_mcp/sender/osascript_send.py`. Module docstring explains the "keystroke return = send" pattern (D-01 step 3 / D-02 step 8), the `-1743 → AutomationRevoked` mapping (T-6 mid-send revocation), and the BMP-only constraint on `keystroke <string>` (research P12 — surrogate pairs truncated). Public surface — two async callables:

- `async def press_return(timeout: float = 3.0) -> None`. Call `run_osascript('tell application "System Events" to keystroke return', timeout=timeout)`. On `exit_code == 0`: return. On `error_code == -1743`: raise `AutomationRevoked(f"Automation TCC revoked mid-keystroke; grant Automation in System Settings → Privacy & Security → Automation")`. On any other non-zero exit: raise `OsascriptError(f"keystroke return failed: exit={result.exit_code} stderr={result.stderr!r}")`.
- `async def type_string(text: str, timeout: float = 3.0) -> None`. Reject any character outside the BMP (codepoint > 0xFFFF) with `OsascriptError(f"Body contains non-BMP character U+{ord(c):X}; AppleScript keystroke truncates non-BMP — group send body is BMP-only in v0.1")` so the group-send fallback in 02-03 surfaces a clean error rather than silently truncating. Escape double-quote and backslash via `.replace("\\", "\\\\").replace('"', '\\"')` (NOTE the backslash-replace MUST be first so already-escaped backslashes don't double-escape). Call `run_osascript(f'tell application "System Events" to keystroke "{escaped}"', timeout=timeout)`. Same `-1743 → AutomationRevoked` and other-non-zero → `OsascriptError` mapping as `press_return`.

Both `deeplink.py` and `osascript_send.py` import `run_osascript` from `whatsapp_desktop_mcp.permissions.osascript` (Phase 0 surface, reuse verbatim). Neither imports anything from `whatsapp_desktop_mcp.reader.*` (REL-05 — verified by the isolation test in 02-05). Both files include a `from __future__ import annotations` line per project style.
  </action>
  <verify>
    <automated>cd /Users/jlqueguiner/dev/whatsapp-desktop-mcp &amp;&amp; uv run ruff check src/whatsapp_desktop_mcp/exceptions.py src/whatsapp_desktop_mcp/sender/deeplink.py src/whatsapp_desktop_mcp/sender/osascript_send.py &amp;&amp; uv run mypy src/whatsapp_desktop_mcp/exceptions.py src/whatsapp_desktop_mcp/sender/deeplink.py src/whatsapp_desktop_mcp/sender/osascript_send.py &amp;&amp; uv run python - &lt;&lt;'PY'
from whatsapp_desktop_mcp.exceptions import (
    ChatHeaderMismatch, AccessibilityAPIUnavailable, OsascriptError,
    SendTimeout, AutomationRevoked, WhatsAppMCPError,
)
for c in (ChatHeaderMismatch, AccessibilityAPIUnavailable, OsascriptError, SendTimeout, AutomationRevoked):
    assert issubclass(c, WhatsAppMCPError), c
from whatsapp_desktop_mcp.sender.deeplink import build_send_url
assert build_send_url("+33 612-345-678", "Hello!") == "whatsapp://send?phone=33612345678&amp;text=Hello%21", build_send_url("+33 612-345-678", "Hello!")
try:
    build_send_url("not-a-number", "hi")
except ValueError:
    pass
else:
    raise AssertionError("expected ValueError on non-digit phone")
print("OK")
PY</automated>
  </verify>
  <acceptance_criteria>
    - File `src/whatsapp_desktop_mcp/sender/deeplink.py` exists; `grep -cE '^def build_send_url' src/whatsapp_desktop_mcp/sender/deeplink.py` returns 1; `grep -cE '^async def send_deeplink' src/whatsapp_desktop_mcp/sender/deeplink.py` returns 1.
    - File `src/whatsapp_desktop_mcp/sender/osascript_send.py` exists; `grep -cE '^async def press_return' src/whatsapp_desktop_mcp/sender/osascript_send.py` returns 1; `grep -cE '^async def type_string' src/whatsapp_desktop_mcp/sender/osascript_send.py` returns 1.
    - `grep -cE '^class (ChatHeaderMismatch|AccessibilityAPIUnavailable|OsascriptError|SendTimeout|AutomationRevoked)\(' src/whatsapp_desktop_mcp/exceptions.py` returns 5.
    - REL-05 D-24 enforcement: `grep -rE 'whatsapp_desktop_mcp\.reader' src/whatsapp_desktop_mcp/sender/deeplink.py src/whatsapp_desktop_mcp/sender/osascript_send.py` returns no lines (exit code 1 from grep). Sender → reader.connection edge is allowed ONLY from `sender/verify.py` (Plan 02-03), not these primitives.
    - CLAUDE.md hard rule #5 (no HTTP/TCP/UDP) holds: `grep -rE '(socket|http\.client|httpx|urllib\.request|aiohttp|requests)' src/whatsapp_desktop_mcp/sender/deeplink.py src/whatsapp_desktop_mcp/sender/osascript_send.py | grep -v '^#'` returns no lines. (Note `urllib.parse` is fine — it's pure-string URL encoding, not networking.)
    - `uv run ruff check src/whatsapp_desktop_mcp/exceptions.py src/whatsapp_desktop_mcp/sender/` passes with exit code 0.
    - `uv run mypy src/whatsapp_desktop_mcp/exceptions.py src/whatsapp_desktop_mcp/sender/deeplink.py src/whatsapp_desktop_mcp/sender/osascript_send.py` passes with exit code 0 under `--strict` (configured in `[tool.mypy]`).
    - The above heredoc inline test prints `OK` and exits 0.
  </acceptance_criteria>
  <done>5 new exception classes shipped, `sender/deeplink.py` + `sender/osascript_send.py` ship per D-01 / D-02 with SP-2-driven `-g` flag decision pinned, REL-05 sender→reader isolation preserved on these two files, mypy --strict clean.</done>
</task>

<task type="auto">
  <name>Task 3: AX preflight (`sender/ax_assert.py`) — D-06 pyobjc-optional + bidi-strip + bounded walk + group-fallback `_assert_first_search_result_matches`</name>
  <files>src/whatsapp_desktop_mcp/sender/ax_assert.py</files>
  <read_first>
    - .planning/phases/02-send-ui-automation-guardrails/02-RESEARCH.md §"AX-API State Assertion (D-03) — verified live on user's Mac" (Pattern 2 — verbatim implementation outline) + §"Example 3: Bidi-strip for AX header comparison"
    - .planning/phases/02-send-ui-automation-guardrails/02-CONTEXT.md D-03 (load-bearing P5 mitigation), D-06 (try/except ImportError fallback)
    - .planning/phases/02-send-ui-automation-guardrails/02-01-SPIKES.md SP-3 (AXHeading-vs-AXStaticText when chat is open) + SP-4 (pyobjc 12.1 AXUIElementCopyAttributeValue exact return shape) + SP-5 (sidebar-search first-result AX role + attribute pair — locks `_assert_first_search_result_matches`)
    - src/whatsapp_desktop_mcp/exceptions.py (the just-added `AccessibilityAPIUnavailable` + `ChatHeaderMismatch` classes from Task 2)
  </read_first>
  <action>
Create `src/whatsapp_desktop_mcp/sender/ax_assert.py`. Module docstring (≥20 lines): why D-03 is load-bearing (P5 mitigation — wrong-chat-fuzzy-send class), why pyobjc is wrapped in `try/except ImportError` (D-06 — broken user install must NOT crash the whole server / read tools still work), the three bidi invisibles (U+200E LRM, U+2068 FSI, U+2069 PDI) verified live on WhatsApp 26.16.74 2026-05-13, the bounded depth-first walk rationale (chat header sits at variable depth — hardcoding a path is brittler than a 200-node-bounded walk), the casefold-substring match rationale (chat header may carry locale-dependent "online / Last seen" suffix), and the SP-3 conclusion (whether `_walk_for_heading` collects only `AXHeading` or also `AXStaticText`).

Top-of-module: try/except ImportError block per D-06 / 02-RESEARCH.md Pattern 2 — import `AXUIElementCreateApplication`, `AXUIElementCopyAttributeValue`, `kAXFocusedWindowAttribute`, `kAXChildrenAttribute`, `kAXRoleAttribute`, `kAXTitleAttribute`, `kAXDescriptionAttribute` from `ApplicationServices`; import `NSWorkspace` from `Cocoa`. Each symbol gets `# type: ignore[import-not-found]`. On success, set `_PYOBJC_AVAILABLE = True`; on `ImportError`, set `_PYOBJC_AVAILABLE = False`.

Module constant `_INVISIBLE_BIDI: frozenset[str] = frozenset({"‎", "⁨", "⁩"})` — use the `\uNNNN` literal escape form, NOT the raw characters, so the file is grep-stable (the literal raw characters would render as zero-width invisibles in source viewers and pollute downstream `grep`s). Module constant `_WHATSAPP_BUNDLE_ID = "net.whatsapp.WhatsApp"`. Module constant `_MAX_WALK_NODES = 200` (cap on the bounded DFS).

Private helpers:
- `def _strip_bidi(s: str) -> str`: returns `"".join(c for c in s if c not in _INVISIBLE_BIDI).strip()`. Plus `casefold()` is NOT applied here — apply it at the call site so other callers (e.g. Plan 02-03's group fallback) can keep case for display while still comparing case-insensitively.
- `def _resolve_whatsapp_pid() -> int | None`: returns the PID if `_PYOBJC_AVAILABLE` and `NSWorkspace.sharedWorkspace().runningApplications()` contains an app whose `bundleIdentifier() == _WHATSAPP_BUNDLE_ID`; returns None otherwise (including when pyobjc is missing OR WhatsApp not running).
- `def _walk_for_heading(elem) -> list[str]`: bounded DFS using a `list[Any]` queue (initialised `[elem]`) and a `visited` counter capped at `_MAX_WALK_NODES`. For each node, call `AXUIElementCopyAttributeValue(node, kAXRoleAttribute, None)` per the SP-4-locked return shape (tuple `(err, role)` per pyobjc convention, OR adjusted to whatever SP-4 confirmed). Collect both `kAXDescriptionAttribute` AND `kAXTitleAttribute` for nodes whose role is `"AXHeading"` (and, if SP-3 showed open-chat name appears only as `AXStaticText`, ALSO for nodes whose role is `"AXStaticText"` — locked per SP-3). For each node, enqueue `kAXChildrenAttribute` children iff the err is 0 and the value is non-empty.

Public surface — two callables:
- `def assert_focused_chat_matches(expected_chat_name: str) -> None`. Raises `AccessibilityAPIUnavailable("pyobjc not available; cannot perform AX preflight — reinstall pyobjc-core, pyobjc-framework-Cocoa, and pyobjc-framework-ApplicationServices to enable wrong-chat protection")` if `not _PYOBJC_AVAILABLE`. Resolves WhatsApp's PID via `_resolve_whatsapp_pid`; if None, raise `ChatHeaderMismatch("WhatsApp.app is not running; cannot read focused-window header — start WhatsApp Desktop and retry")`. Build `AXUIElementCreateApplication(pid)`, copy `kAXFocusedWindowAttribute`; on err != 0 or None window, raise `ChatHeaderMismatch(f"AXFocusedWindow lookup failed (err={err}); cannot verify chat header — bring WhatsApp Desktop to foreground and retry")`. Run `_walk_for_heading(window)`; compute `expected = _strip_bidi(expected_chat_name).casefold()`. Iterate headings: if any `expected in _strip_bidi(h).casefold()`, return cleanly. Otherwise raise `ChatHeaderMismatch(f"Focused chat header does not match expected={expected_chat_name!r}; observed AXHeading values (stripped) = {[_strip_bidi(h) for h in headings]}")`. The substring-on-stripped-casefolded form is the verified-correct comparison per Pattern 2.

- `def _assert_first_search_result_matches(chat_name: str) -> None`. Companion helper used by Plan 02-03's group-send fallback (per CONTEXT.md D-02 search-and-click flow). Same `_PYOBJC_AVAILABLE` / `_resolve_whatsapp_pid` preflight as `assert_focused_chat_matches`. Differences: (a) the AX target is the SIDEBAR list (not the focused chat header) — walks AXButton (and per SP-5, possibly AXStaticText / AXCell) nodes inside the topmost AXScrollArea or AXList element of the focused window; (b) compares the FIRST sidebar result's stripped-bidi-casefold-substring name against `chat_name`. If `_PYOBJC_AVAILABLE` is False, raise `AccessibilityAPIUnavailable` (same message format). If WhatsApp not running, raise `ChatHeaderMismatch("WhatsApp.app is not running")`. If no sidebar elements were found (search returned no results) OR the topmost result's stripped-casefolded name does NOT contain `_strip_bidi(chat_name).casefold()` as a substring, raise `ChatHeaderMismatch(f"Group search topmost result does not match expected chat_name={chat_name!r}; observed first-result name(s) (stripped) = {[...]}")`. Same `_MAX_WALK_NODES` cap. Implementation reuses `_walk_for_heading` if SP-5 confirms sidebar entries surface usable AXTitle / AXDescription; otherwise add a parallel `_walk_for_first_button(elem) -> list[str]` (same DFS shape, different role filter) and call it instead. This helper is load-bearing for the P5 mitigation on the group-send branch (RESEARCH §"Common Pitfall 4: Group send keystroke before AX-list-result preflight").

Synchronous function (not async): the pyobjc AX-API calls are CPU-bound C extensions; wrapping in `asyncio.to_thread` adds latency without benefit. Plan 02-03's orchestrator will call this from inside its own `asyncio.to_thread` if needed; for now keep it sync so unit tests can call it directly with patched pyobjc.

REL-05 D-24 invariant: `ax_assert.py` imports NOTHING from `whatsapp_desktop_mcp.reader.*`. Allowed `whatsapp_desktop_mcp.*` imports: `whatsapp_desktop_mcp.exceptions` only.
  </action>
  <verify>
    <automated>cd /Users/jlqueguiner/dev/whatsapp-desktop-mcp &amp;&amp; uv run ruff check src/whatsapp_desktop_mcp/sender/ax_assert.py &amp;&amp; uv run mypy src/whatsapp_desktop_mcp/sender/ax_assert.py &amp;&amp; uv run python - &lt;&lt;'PY'
from whatsapp_desktop_mcp.sender import ax_assert
from whatsapp_desktop_mcp.exceptions import AccessibilityAPIUnavailable, ChatHeaderMismatch, WhatsAppMCPError
# Bidi strip regression — verbatim live-verified codepoints
sample = "‎⁨Olivier Giffard⁩"
assert ax_assert._strip_bidi(sample) == "Olivier Giffard", repr(ax_assert._strip_bidi(sample))
# Module imports cleanly even if pyobjc absent (D-06): on this Mac it IS installed,
# so just verify the flag exists and matches the import outcome.
assert hasattr(ax_assert, "_PYOBJC_AVAILABLE")
# Public function exists with the locked signature
import inspect
sig = inspect.signature(ax_assert.assert_focused_chat_matches)
params = list(sig.parameters)
assert params == ["expected_chat_name"], params
print("OK")
PY</automated>
  </verify>
  <acceptance_criteria>
    - File `src/whatsapp_desktop_mcp/sender/ax_assert.py` exists with `from __future__ import annotations` as the first non-docstring line.
    - `grep -cE '^def assert_focused_chat_matches\(expected_chat_name: str\) -> None:' src/whatsapp_desktop_mcp/sender/ax_assert.py` returns 1.
    - `grep -cE '^def _assert_first_search_result_matches\(chat_name: str\) -> None:' src/whatsapp_desktop_mcp/sender/ax_assert.py` returns 1 (group-fallback first-result preflight helper added per W-5 / Plan 02-03's group-send dependency).
    - `grep -cE 'try:\s*$' src/whatsapp_desktop_mcp/sender/ax_assert.py` returns ≥1 (the D-06 pyobjc try/except ImportError block).
    - `grep -cE '_PYOBJC_AVAILABLE = (True|False)' src/whatsapp_desktop_mcp/sender/ax_assert.py` returns 2 (one for the try-branch True, one for the except-branch False).
    - `grep -cE '\\\\u200E|\\\\u2068|\\\\u2069' src/whatsapp_desktop_mcp/sender/ax_assert.py` returns ≥3 (all three bidi codepoints declared via escape literal, not raw chars — keeps the file grep-stable).
    - REL-05 D-24 enforcement: `grep -E 'whatsapp_desktop_mcp\.reader' src/whatsapp_desktop_mcp/sender/ax_assert.py` returns no lines (only allowed in `sender/verify.py` which lands in 02-03).
    - The heredoc inline test prints `OK` and exits 0 (verifies bidi strip + public signature + pyobjc-available flag).
    - `uv run ruff check src/whatsapp_desktop_mcp/sender/ax_assert.py` passes exit 0.
    - `uv run mypy src/whatsapp_desktop_mcp/sender/ax_assert.py` passes exit 0 (the `type: ignore[import-not-found]` comments on each pyobjc import are accepted under mypy --strict).
  </acceptance_criteria>
  <done>`sender/ax_assert.py` ships D-03 load-bearing P5 mitigation: D-06 try/except ImportError fallback, verified-live bidi strip via `\uNNNN` escape literals, SP-3/SP-4/SP-5-locked AX walk and return-tuple handling, public surface `{assert_focused_chat_matches, _assert_first_search_result_matches}` for the focused-chat (D-03) AND the group-search-first-result (P5 group-fallback) preflights, REL-05 sender-isolation preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM client → tool layer | Body string + chat_id arrive as untrusted LLM-generated content; Plan 02-01 ships only the primitives downstream of the tool layer — no LLM-input handling here, but the URL builder MUST reject non-digit phones and `keystroke` MUST reject non-BMP code points so the upstream tool surface (Plan 02-03) inherits a fail-loud baseline. |
| Sender process → macOS osascript subprocess | Every `osascript -e` invocation crosses a TCC boundary (Automation/Apple Events bucket); error_code parsing is locale-blind (Phase 0 invariant); -1743 mid-keystroke maps to `AutomationRevoked` (T-6 surface). |
| Sender process → WhatsApp.app via AX-API | pyobjc AX-API reads from a SIP-protected app; permission lives in the Accessibility TCC bucket. D-06 try/except ImportError is the failure-mode boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | `deeplink.py:build_send_url` — caller passes phone string containing shell metachars / URL injection (e.g. `+33-1; rm -rf /`) | mitigate | `cleaned.isdigit()` assertion rejects ANY non-digit after stripping `+`, ` `, `-`. Body goes through `urllib.parse.quote(safe='')` — every non-alphanumeric byte percent-escaped. `/usr/bin/open` invoked via `asyncio.create_subprocess_exec` with explicit argv list (no shell) so even a hypothetical bypass cannot reach a shell parse. |
| T-02-01-02 | Spoofing / Wrong-Recipient | `ax_assert.py` — focused chat is NOT the resolved chat at send time (user manually switched chats between resolve and send, OR WhatsApp opened a different chat due to deep-link side-effect) | mitigate | Load-bearing D-03 / P5 mitigation: `assert_focused_chat_matches(expected_chat_name)` enforces stripped-bidi casefold-substring match before any keystroke fires. `ChatHeaderMismatch` raised on any drift — Plan 02-03 orchestrator surfaces this as structured tool error and audit-logs `outcome="error"`. Three bidi invisibles (U+200E, U+2068, U+2069) stripped via `_strip_bidi` per verified-live trap. |
| T-02-01-03 | Denial-of-service | `deeplink.py:send_deeplink` settle-poll hangs forever if WhatsApp never foregrounds | mitigate | Bounded loop: max 30 iterations × 50ms = 1.5s wall-clock. After exhaustion → `SendTimeout`. The outer `@timeout(seconds=15)` decorator at the tool layer (Plan 02-03) is the second wall. |
| T-02-01-04 | Denial-of-service | `ax_assert.py:_walk_for_heading` walks an unbounded AX tree (could OOM on a pathological window with millions of nodes) | mitigate | `_MAX_WALK_NODES = 200` cap on the visited counter. Aborts walk and falls through to `ChatHeaderMismatch` on overflow (safer than reporting false-positive match). |
| T-02-01-05 | Information Disclosure | Body string ends up in audit log via accidental logging in primitives | accept | Plan 02-01 primitives never call `logger.info` with the body string (verified: `osascript_send.py:type_string` logs only `len(text)` and BMP-violation codepoint, never the body itself; `deeplink.py:send_deeplink` logs only the URL with body already URL-encoded). Final audit log writer ships in Plan 02-02 with D-13 SHA-256-only enforcement. |
| T-02-01-06 | Elevation of Privilege | TCC Automation revoked silently between server start and a keystroke | mitigate (partial) | `osascript_send.py` maps `-1743` → `AutomationRevoked` so the upstream tool can return a structured error AND audit log the event. The full T-6 pre-send re-check (D-09 PATCHED probe) is Plan 02-03's responsibility; this plan provides the failure-detection on the keystroke path itself. |
| T-02-01-07 | Tampering (anti-pattern) | pyobjc ImportError surfaces as a raw Python traceback to the MCP client | mitigate | D-06 try/except ImportError block at top of `ax_assert.py` flips `_PYOBJC_AVAILABLE = False`; `assert_focused_chat_matches` raises structured `AccessibilityAPIUnavailable` (a `WhatsAppMCPError` subclass) which Plan 02-03 maps to a `ValueError` → MCP error response per the existing pattern. |
| T-02-01-08 | Tampering | Non-BMP body silently truncated by AppleScript `keystroke` → group send delivers partial body | mitigate | `osascript_send.py:type_string` rejects non-BMP characters BEFORE handing to osascript, raising `OsascriptError` with the offending codepoint. Documented v0.1 constraint: group-send body restricted to BMP. The 1:1 deep-link path doesn't have this constraint — `build_send_url` URL-encodes the full Unicode body. |
</threat_model>

<verification>
- All three sender primitive files exist at the specified paths under `src/whatsapp_desktop_mcp/sender/` with the locked public surface.
- `pyproject.toml` lists pyobjc-core / pyobjc-framework-Cocoa / pyobjc-framework-ApplicationServices in `[project] dependencies` (NOT `[project.optional-dependencies] dev`).
- `uv.lock` regenerated and committed with the new pyobjc entries.
- `02-01-SPIKES.md` records empirical results for all 4 Wave-0 spikes with locked tactical decisions cited.
- Five new exception classes in `whatsapp_desktop_mcp.exceptions` — all subclassing `WhatsAppMCPError`.
- REL-05 D-24 invariant holds for Tasks 2/3: zero `whatsapp_desktop_mcp.reader.*` imports in `deeplink.py`, `osascript_send.py`, or `ax_assert.py` (the `verify.py` reader-connection edge lands in Plan 02-03 only).
- `uv run ruff check src/whatsapp_desktop_mcp/sender/` exits 0; `uv run mypy src/whatsapp_desktop_mcp/sender/` exits 0 under `--strict`.
- `uv run pytest -m "not live"` still passes (Phase 0 + Phase 1 test suite intact — Plan 02-01 modifies only `pyproject.toml`, `exceptions.py` (append-only), and creates new sender files; nothing existing should break).
</verification>

<success_criteria>
1. `from whatsapp_desktop_mcp.sender.deeplink import build_send_url, send_deeplink` resolves without ImportError.
2. `from whatsapp_desktop_mcp.sender.osascript_send import press_return, type_string` resolves.
3. `from whatsapp_desktop_mcp.sender.ax_assert import assert_focused_chat_matches, _assert_first_search_result_matches, _strip_bidi` resolves.
4. `from whatsapp_desktop_mcp.exceptions import ChatHeaderMismatch, AccessibilityAPIUnavailable, OsascriptError, SendTimeout, AutomationRevoked` resolves.
5. `_strip_bidi("‎⁨Olivier Giffard⁩") == "Olivier Giffard"` is True.
6. `build_send_url("+33 612-345-678", "Hello!") == "whatsapp://send?phone=33612345678&text=Hello%21"` is True.
7. `build_send_url("not-a-number", "x")` raises `ValueError`.
8. All four Wave-0 spike outcomes recorded in `02-01-SPIKES.md` with explicit "locked" decisions for Task 2/3 + downstream plans.
9. `uv build` produces a wheel whose `dist-info/METADATA` lists pyobjc-core / pyobjc-framework-Cocoa / pyobjc-framework-ApplicationServices as runtime `Requires-Dist:` lines (NOT extras).
10. Phase 0 + Phase 1 tests (148 non-live) still green: `uv run pytest -m "not live"` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/02-send-ui-automation-guardrails/02-01-SUMMARY.md`. Include:
- The 4 Wave-0 spike outcomes (one paragraph each, with locked decisions).
- The exact pyobjc versions resolved in `uv.lock`.
- Any Rule-1 deviations (e.g. literal-token grep-gate near-misses analogous to Phase 0/1 patterns).
- File-by-file diff summary (paths + line counts).
- Confirmation that `sender/__init__.py` is STILL empty (it stays empty until Plan 02-03 adds re-exports).
</output>
