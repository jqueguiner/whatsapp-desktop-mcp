---
phase: 02-send-ui-automation-guardrails
plan: 05
title: Tests — sender unit suite, 4 mandatory regression tests, RUN_LIVE=1 send smoke, send_message tool tests
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
requirements: [SEND-01, SEND-02, SEND-03, SEND-04, SEND-05, SEND-06, SEND-07, SEND-08]
files_modified:
  - tests/unit/test_sender/__init__.py
  - tests/unit/test_sender/test_deeplink.py
  - tests/unit/test_sender/test_osascript_send.py
  - tests/unit/test_sender/test_ax_assert.py
  - tests/unit/test_sender/test_rate_limit.py
  - tests/unit/test_sender/test_audit.py
  - tests/unit/test_sender/test_cross_chat_quote.py
  - tests/unit/test_sender/test_verify.py
  - tests/unit/test_sender/test_ui_send.py
  - tests/unit/test_tools/test_send_message.py
  - tests/integration/test_live_send.py
  - tests/unit/conftest.py
autonomous: true
must_haves:
  truths:
    - "The 4 mandatory regression tests from CONTEXT.md §Specifics ship and PASS: `test_send_message_refuses_string_chat_id` (SEND-01), `test_send_message_aborts_on_chat_header_mismatch` (D-03/SEND-04/P5), `test_send_message_rate_limit_persists_across_restart` (D-11/SEND-05), `test_send_message_appends_audit_log_with_body_sha256_not_body` (D-13/SEND-06)."
    - "Every Phase 2 source module under `src/whatsapp_mcp/sender/` has a corresponding `tests/unit/test_sender/test_<module>.py` test file covering its public surface."
    - "`tests/unit/test_tools/test_send_message.py` exercises each branch of the D-25 11-step orchestration with mocked sender + reader collaborators — at least one test per outcome enum value (sent / sent_unverified / cancelled / rate_limited / error)."
    - "Integration smoke `tests/integration/test_live_send.py` is `@pytest.mark.live` + `RUN_LIVE=1` gated; sends to the maintainer's self-chat (discovered via env-var-configurable display name); asserts audit log entry with non-error outcome. Skipped by default. **B-2:** rate_limit._DB_PATH + audit._LOG_PATH + audit._LOG_DIR are monkey-patched to `tmp_path` via an autouse fixture so live tests do NOT burn the maintainer's daily WhatsApp budget or pollute the real audit log; the WhatsApp send itself drives the maintainer's real account (the sandbox covers guardrail persistence only)."
    - "Full non-live suite passes: `uv run pytest -m \"not live\"` exits 0. Phase 0 (28) + Phase 1 (120) tests still all pass. Phase 2 adds ~80-110 new tests."
    - "pyobjc / AX-API tests are mockable on non-mac CI: tests patch `ApplicationServices` symbols via `monkeypatch.setattr` so AX-walk logic runs without a live macOS GUI."
    - "stdout-purity invariant preserved: `tests/unit/test_stdout_purity.py` (Phase 0) still passes; importing `whatsapp_mcp.tools.send_message` emits zero stdout bytes."
    - "D-13 STRUCTURAL invariant codified in TWO places: schema-level (test_audit.py asserts `body` not in AuditEntry.model_fields) AND runtime-write contract (test_send_message.py asserts no `body` key in the actual audit.log JSON line)."
  artifacts:
    - path: "tests/unit/test_sender/__init__.py"
      provides: "Empty package marker for the test_sender subpackage"
    - path: "tests/unit/test_sender/test_deeplink.py"
      provides: "Unit tests for build_send_url (E.164 normalization, non-digit rejection, urllib.parse.quote behavior, emoji body, U+200E settle-poll substring match)"
    - path: "tests/unit/test_sender/test_osascript_send.py"
      provides: "Unit tests for press_return / type_string with mocked run_osascript: -1743→AutomationRevoked mapping, non-BMP rejection, escape-character handling"
    - path: "tests/unit/test_sender/test_ax_assert.py"
      provides: "Unit tests for _strip_bidi + assert_focused_chat_matches with mocked pyobjc + ImportError fallback path; bidi-strip regression on verified-live codepoint sequence"
    - path: "tests/unit/test_sender/test_rate_limit.py"
      provides: "_resolve_limits env-var bounds tests; peek-and-raise check_and_reserve contract; record_outcome counters; MANDATORY test_send_message_rate_limit_persists_across_restart"
    - path: "tests/unit/test_sender/test_audit.py"
      provides: "AuditEntry schema-level D-13 STRUCTURAL test (no body field); append round-trip with mode 0600 + buffering=1; body_sha256 helper"
    - path: "tests/unit/test_sender/test_cross_chat_quote.py"
      provides: "record_bodies / check / OffendingSource: 40-char threshold, 30-min window, same-chat-not-cross-chat invariant, LRU eviction at 1000 entries"
    - path: "tests/unit/test_sender/test_verify.py"
      provides: "poll_for_outgoing with fixture SQLite DB matching ZWAMESSAGE schema subset; 10s timeout → None mapping; first-poll-hit; SQL WHERE filter regressions"
    - path: "tests/unit/test_sender/test_ui_send.py"
      provides: "send_text orchestration: direct vs group dispatch; AX assertion fires BEFORE press_return on both paths (mock call-order assertion)"
    - path: "tests/unit/test_tools/test_send_message.py"
      provides: "Tool-tier tests of send_message — D-25 11-step orchestration branch coverage with mocks; MANDATORY test_send_message_refuses_string_chat_id + test_send_message_aborts_on_chat_header_mismatch + test_send_message_appends_audit_log_with_body_sha256_not_body"
    - path: "tests/integration/test_live_send.py"
      provides: "@pytest.mark.live + RUN_LIVE=1 gated. Sends to self-chat (auto-discovered via WHATSAPP_MCP_LIVE_TEST_SELF_NAME env var); asserts audit + rate-limit + non-error outcome. Skipped by default."
    - path: "tests/unit/conftest.py"
      provides: "New fixtures: tmp_rate_limit_db, tmp_audit_log, reset_xcq_lru, mock_pyobjc (preserves all existing Phase 1 fixtures verbatim)"
  key_links:
    - from: "tests/unit/test_sender/test_audit.py"
      to: "src/whatsapp_mcp/sender/audit.py:AuditEntry"
      via: "Pydantic model_fields reflection — 'body' MUST NOT appear"
      pattern: "AuditEntry\\.model_fields"
    - from: "tests/unit/test_sender/test_rate_limit.py"
      to: "src/whatsapp_mcp/sender/rate_limit.py"
      via: "Persistence across simulated server restart"
      pattern: "persists_across_restart"
    - from: "tests/integration/test_live_send.py"
      to: "real WhatsApp.app on user's Mac"
      via: "send_text to self-chat under RUN_LIVE=1 opt-in"
      pattern: "@pytest\\.mark\\.live"
---

<objective>
Land the test suite that proves Phase 2's 5 ROADMAP success criteria. This plan is the verification artifact for the entire phase — without it, Phase 2 ships with no automated proof that SEND-01..08 work.

Three test layers (mirrors Phase 1 Plan 01-06):
1. **Sender unit tests** — one `tests/unit/test_sender/test_<module>.py` per Phase 2 sender module.
2. **Tool-tier tests** — `tests/unit/test_tools/test_send_message.py` for D-25 11-step orchestration branch coverage.
3. **Integration smoke** — `tests/integration/test_live_send.py` (`@pytest.mark.live` + `RUN_LIVE=1`).

The **4 mandatory regression tests from CONTEXT.md §Specifics** are explicitly placed and named — verification gates for the phase.

Output: 10 new test modules + 1 conftest extension. ~80-110 new tests. No source-code changes — Phase 2's source surface is FROZEN at this point.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-send-ui-automation-guardrails/02-CONTEXT.md
@.planning/phases/02-send-ui-automation-guardrails/02-RESEARCH.md
@CLAUDE.md
@src/whatsapp_mcp/sender/deeplink.py
@src/whatsapp_mcp/sender/osascript_send.py
@src/whatsapp_mcp/sender/ax_assert.py
@src/whatsapp_mcp/sender/rate_limit.py
@src/whatsapp_mcp/sender/audit.py
@src/whatsapp_mcp/sender/cross_chat_quote.py
@src/whatsapp_mcp/sender/verify.py
@src/whatsapp_mcp/sender/ui_send.py
@src/whatsapp_mcp/sender/__init__.py
@src/whatsapp_mcp/tools/send_message.py
@src/whatsapp_mcp/server.py
@tests/unit/conftest.py
@tests/unit/test_isolation.py

<interfaces>
<!-- Surfaces under test — sender modules + send_message tool + server module-attribute read_only_mode. -->

`sender/deeplink.py`: build_send_url(phone_e164, body); async send_deeplink(phone_e164, body)
`sender/osascript_send.py`: async press_return(timeout=3.0); async type_string(text, timeout=3.0)
`sender/ax_assert.py`: _strip_bidi(s); _PYOBJC_AVAILABLE; def assert_focused_chat_matches(expected_chat_name)
`sender/rate_limit.py`: _resolve_limits; _DB_PATH (monkey-patchable); async check_and_reserve; async record_outcome
`sender/audit.py`: AuditEntry (Pydantic); _LOG_PATH / _LOG_DIR (monkey-patchable); async append; def body_sha256
`sender/cross_chat_quote.py`: def record_bodies; def check; def _reset_for_test; constants _MIN_SUBSTRING/_WINDOW_SECONDS/_MAX_ENTRIES
`sender/verify.py`: async poll_for_outgoing(chat_id, body, send_started_unix)
`sender/ui_send.py`: async send_text(chat_id, body, chat_name, recipient_phone_e164, kind); async send_group_via_search(chat_name, body)
`tools/send_message.py`: @mcp.tool async send_message(chat_id: int, body: str, ctx: Context) -> SendResult
`server.py`: module attribute `read_only_mode: bool` (default True)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: conftest extensions + sender unit tests for 5 stateless/lightweight modules (deeplink, osascript_send, ax_assert, cross_chat_quote, audit)</name>
  <files>tests/unit/conftest.py, tests/unit/test_sender/__init__.py, tests/unit/test_sender/test_deeplink.py, tests/unit/test_sender/test_osascript_send.py, tests/unit/test_sender/test_ax_assert.py, tests/unit/test_sender/test_cross_chat_quote.py, tests/unit/test_sender/test_audit.py</files>
  <read_first>
    - tests/unit/conftest.py (Phase 1 baseline — preserve existing fixtures; extend with new ones)
    - .planning/phases/02-send-ui-automation-guardrails/02-RESEARCH.md (Patterns 2-7 + Common Pitfalls 1, 2, 3, 5, 7, 8 — failure modes the tests must cover)
    - src/whatsapp_mcp/sender/deeplink.py, osascript_send.py, ax_assert.py, cross_chat_quote.py, audit.py (the actual implementations from Plans 02-01 / 02-02)
    - .planning/phases/02-send-ui-automation-guardrails/02-01-SPIKES.md SP-3, SP-4 (locks AX-walk node-type collection + pyobjc return shape)
  </read_first>
  <action>
**Subtask 1a — extend `tests/unit/conftest.py`** with 4 new fixtures AFTER the Phase 1 fixtures (preserve every existing fixture verbatim): `tmp_rate_limit_db` (monkey-patches `sender.rate_limit._DB_PATH` to `tmp_path / "rate-limit.db"`); `tmp_audit_log` (monkey-patches `sender.audit._LOG_DIR` + `_LOG_PATH` to tmp); `reset_xcq_lru` (calls `cross_chat_quote._reset_for_test` before AND after each test for isolation); `mock_pyobjc` (provides a `types.SimpleNamespace` fake `ApplicationServices` + `Cocoa` surface; sets `ax._PYOBJC_AVAILABLE = True` + monkeypatches each AX-API symbol to the fake; lets tests configure return tuples on a per-test basis).

**Subtask 1b — `tests/unit/test_sender/__init__.py`** — empty marker (mirrors `tests/unit/test_reader/__init__.py`).

**Subtask 1c — `tests/unit/test_sender/test_deeplink.py`** — ≥8 tests covering:
- `test_build_send_url_strips_plus_and_spaces_and_hyphens` (`"+33 612-345-678"` → 33612345678 digits-only)
- `test_build_send_url_rejects_non_digits` (raises ValueError)
- `test_build_send_url_url_encodes_body_with_quote_not_quote_plus` (`"Hello+World!"` → `Hello%2BWorld%21`; `+` encoded, NOT spaced)
- `test_build_send_url_url_encodes_emoji` (`"🎉 hi"` percent-encoded byte sequence verified)
- `test_build_send_url_empty_body_produces_empty_text_param`
- `test_send_deeplink_settle_poll_substring_matches_LRM_prefixed_window_name` (mock run_osascript returning `"‎WhatsApp"` U+200E-prefixed; does NOT raise SendTimeout — Pattern 3 verified-live detail)
- `test_send_deeplink_raises_send_timeout_on_settle_exhaustion` (mock returns no "WhatsApp"; SendTimeout raised after 30 polls)
- `test_send_deeplink_subprocess_uses_argv_not_shell` (T-02-01-01 regression — patch `asyncio.create_subprocess_exec`, assert URL passed as argv element, not concatenated into shell string)

**Subtask 1d — `tests/unit/test_sender/test_osascript_send.py`** — ≥6 tests covering:
- `test_press_return_clean_exit` (mock returns exit 0; press_return returns None)
- `test_press_return_raises_automation_revoked_on_1743` (error_code=-1743 → AutomationRevoked)
- `test_press_return_raises_osascript_error_on_other_nonzero` (any other non-zero → OsascriptError with stderr)
- `test_type_string_rejects_non_bmp` (😀 = U+1F600 → OsascriptError with codepoint in message)
- `test_type_string_escapes_double_quote_and_backslash` (verify escapes via captured-argv inspection)
- `test_type_string_clean_exit_with_bmp_unicode` (`"héllo"` BMP — succeeds)

**Subtask 1e — `tests/unit/test_sender/test_ax_assert.py`** — ≥9 tests (use `mock_pyobjc` fixture except for the no-pyobjc case):
- `test_strip_bidi_removes_lrm_fsi_pdi` (verbatim verified-live regression: `_strip_bidi("‎⁨Olivier Giffard⁩") == "Olivier Giffard"`)
- `test_strip_bidi_preserves_normal_text`
- `test_pyobjc_unavailable_raises_accessibility_api_unavailable` (set _PYOBJC_AVAILABLE=False; AccessibilityAPIUnavailable raised)
- `test_whatsapp_not_running_raises_chat_header_mismatch` (NSWorkspace.runningApplications=[]; ChatHeaderMismatch with "not running")
- `test_focused_window_lookup_fail_raises_chat_header_mismatch` (mock AX call returns (err=-25212, value=None); ChatHeaderMismatch)
- `test_matching_heading_returns_cleanly` (mock walk returns ["‎⁨Olivier Giffard⁩"]; assert_focused_chat_matches("Olivier Giffard") returns None)
- `test_non_matching_heading_raises_chat_header_mismatch` (heading is "Mom", expected is "Momentum project" — substring direction matters per implementation; ChatHeaderMismatch raised with observed strings in message)
- `test_walk_caps_at_200_nodes` (pathological 1000-deep AX tree; verify counter ≤200)
- `test_strip_bidi_casefold_substring_match` (lowercase expected matches mixed-case heading)

**Subtask 1f — `tests/unit/test_sender/test_cross_chat_quote.py`** — ≥10 tests (use `reset_xcq_lru` on each):
- `test_constants` (_MIN_SUBSTRING==40, _WINDOW_SECONDS==1800, _MAX_ENTRIES==1000)
- `test_record_bodies_skips_short_bodies`
- `test_record_bodies_skips_none_and_empty`
- `test_record_then_check_finds_40_char_substring` (different chats — returns one OffendingSource)
- `test_same_chat_id_does_not_trigger`
- `test_window_30_min_eviction` (recorded_at >30 min ago — entry skipped; manipulate via direct _Entry push or time.time monkey-patch)
- `test_lru_eviction_at_1000_entries` (record 1100; assert deque maxlen caps at 1000)
- `test_check_below_threshold_outgoing_returns_empty` (outgoing < 40 chars → [])
- `test_snippet_capped_at_100_chars`
- `test_offending_source_dataclass_frozen` (FrozenInstanceError on mutation)

**Subtask 1g — `tests/unit/test_sender/test_audit.py`** — ≥7 tests (use `tmp_audit_log`):
- `test_audit_entry_schema_has_no_plaintext_body_field` (D-13 SCHEMA-LEVEL STRUCTURAL — assert `"body" not in AuditEntry.model_fields and "body_text" not in AuditEntry.model_fields and "body_preview" not in AuditEntry.model_fields`)
- `test_audit_entry_outcome_literal` (`outcome="bogus"` raises ValidationError)
- `test_append_roundtrip_to_tmp_path` (one append; readable JSONL; correct keys; no `body` key)
- `test_append_sets_mode_0600_on_first_create` (file mode == 0o600 after first append)
- `test_append_does_not_re_chmod_existing` (pre-create with mode 0644; append; mode stays 0644 — chmod runs only on first create per Pattern 6)
- `test_append_line_buffered` (capture open()'s buffering kwarg; assert buffering=1)
- `test_body_sha256_helper_returns_64_char_lowercase_hex` (matches hashlib.sha256("hello".encode()).hexdigest())
- `test_outcome_literal_values` (`typing.get_args(Outcome) == ("sent","sent_unverified","cancelled","rate_limited","error")`)
  </action>
  <verify>
    <automated>cd /Users/jlqueguiner/dev/whatsapp-mcp &amp;&amp; uv run ruff check tests/unit/conftest.py tests/unit/test_sender/ &amp;&amp; uv run mypy tests/unit/conftest.py tests/unit/test_sender/ &amp;&amp; uv run pytest tests/unit/test_sender/test_deeplink.py tests/unit/test_sender/test_osascript_send.py tests/unit/test_sender/test_ax_assert.py tests/unit/test_sender/test_cross_chat_quote.py tests/unit/test_sender/test_audit.py -v</automated>
  </verify>
  <acceptance_criteria>
    - 5 sender test files created: deeplink, osascript_send, ax_assert, cross_chat_quote, audit (verify via `ls tests/unit/test_sender/test_{deeplink,osascript_send,ax_assert,cross_chat_quote,audit}.py` returning 5 paths).
    - Each test file declares ≥6 tests: per-file `grep -cE '^def test_' tests/unit/test_sender/test_<name>.py` returns ≥6.
    - Total ≥40 new tests across these 5 files.
    - D-13 SCHEMA-LEVEL regression present: `grep -cE 'def test_audit_entry_schema_has_no_plaintext_body_field' tests/unit/test_sender/test_audit.py` returns 1.
    - Bidi-strip verified-live regression present: `grep -cE 'Olivier Giffard' tests/unit/test_sender/test_ax_assert.py` returns ≥1 (the literal regression string from the verified-live codepoint sequence).
    - `tests/unit/conftest.py` extended with 4 new fixtures: `grep -cE 'def (tmp_rate_limit_db|tmp_audit_log|reset_xcq_lru|mock_pyobjc)\(' tests/unit/conftest.py` returns 4.
    - All 5 test files pass: `uv run pytest tests/unit/test_sender/test_deeplink.py tests/unit/test_sender/test_osascript_send.py tests/unit/test_sender/test_ax_assert.py tests/unit/test_sender/test_cross_chat_quote.py tests/unit/test_sender/test_audit.py` exits 0.
    - Phase 1's 148 tests STILL pass (existing conftest fixtures preserved): `uv run pytest -m "not live" --ignore=tests/unit/test_sender --ignore=tests/unit/test_tools/test_send_message.py --ignore=tests/integration/test_live_send.py` exits 0.
    - `uv run ruff check tests/unit/conftest.py tests/unit/test_sender/` exits 0; `uv run mypy tests/unit/conftest.py tests/unit/test_sender/` exits 0 under `--strict`.
  </acceptance_criteria>
  <done>Conftest extended with 4 fixtures; 5 sender-module test files shipped covering stateless/lightweight modules; D-13 schema-level structural regression in place; bidi-strip verified-live regression in place; all ≥40 new tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Stateful sender tests (rate_limit + verify + ui_send) — includes 1 mandatory regression test</name>
  <files>tests/unit/test_sender/test_rate_limit.py, tests/unit/test_sender/test_verify.py, tests/unit/test_sender/test_ui_send.py</files>
  <read_first>
    - src/whatsapp_mcp/sender/rate_limit.py (DDL + check_and_reserve + record_outcome impls)
    - src/whatsapp_mcp/sender/verify.py (250ms × 40 poll loop + ZWAMESSAGE schema fields)
    - src/whatsapp_mcp/sender/ui_send.py (kind-based dispatch + AX-preflight-before-keystroke invariant)
    - .planning/phases/02-send-ui-automation-guardrails/02-CONTEXT.md §"Specifics" — this task ships `test_send_message_rate_limit_persists_across_restart`
    - .planning/phases/02-send-ui-automation-guardrails/02-RESEARCH.md §"Pattern 5" / §"Pattern 8" / §"Patterns 3 + 4"
    - tests/unit/test_reader/test_concurrency.py (Phase 1's stress test — borrow the fixture pattern)
  </read_first>
  <action>
**Subtask 2a — `tests/unit/test_sender/test_rate_limit.py`** — ≥8 tests including MANDATORY regression. All use `tmp_rate_limit_db`:
- `test_resolve_limits_defaults` ((5, 30) when env unset)
- `test_resolve_limits_env_override_within_bounds` (PER_MIN=10, PER_DAY=100 → (10, 100))
- `test_resolve_limits_rejects_per_min_above_hard_max` (=21 → ValueError "exceeds hard max")
- `test_resolve_limits_rejects_per_day_above_hard_max` (=201 → ValueError)
- `test_check_and_reserve_does_not_insert` (peek-only contract — Plan 02-02 Task 1)
- `test_record_outcome_inserts_one_row`
- `test_rate_limit_minute_window_trips_at_per_min` (record 5 "sent" → next check_and_reserve raises RateLimitExceeded)
- `test_rate_limit_day_window_counts_only_sent_outcomes` (record 30 "cancelled" → check still returns (5, 30) — SQL CHECK in DDL excludes non-send outcomes)
- **MANDATORY: `test_send_message_rate_limit_persists_across_restart`** — exact CONTEXT.md name. Implementation: (a) record 5× `outcome="sent"` against `tmp_rate_limit_db`; (b) simulate restart: `del sys.modules["whatsapp_mcp.sender.rate_limit"]`, re-import, re-monkey-patch `_DB_PATH` to the SAME tmp file (the file persists across the simulated restart); (c) call `check_and_reserve` and assert RateLimitExceeded raised. This proves T-5 mitigation.

**Subtask 2b — `tests/unit/test_sender/test_verify.py`** — ≥6 tests. Build fixture SQLite with the minimal ZWAMESSAGE subset (ZSTANZAID TEXT, ZCHATSESSION INTEGER, ZISFROMME INTEGER, ZTEXT TEXT, ZMESSAGEDATE REAL, ZSORT REAL). Add fixture `verify_db(tmp_path)` that creates this schema in a tmp SQLite. Monkey-patch `resolve_chatstorage_path` to return the fixture path:
- `test_poll_finds_existing_outgoing_message_immediately` (pre-inserted row matches; returns ZSTANZAID on first poll)
- `test_poll_returns_none_on_timeout` (no matching row; monkey-patch `_POLL_INTERVAL_SECONDS=0.01` + `_MAX_POLLS=4` to keep wall-clock ~40ms; returns None)
- `test_poll_ignores_old_messages_before_send_started` (ZMESSAGEDATE < since_cocoa → not matched)
- `test_poll_ignores_incoming_messages` (ZISFROMME=0 → not matched)
- `test_poll_ignores_other_chats` (different ZCHATSESSION → not matched)
- `test_poll_exact_match_on_ZTEXT_no_normalization` (ZTEXT="Hello world" but body="Hello world " trailing space → returns None per D-22 soft-fail)

**Subtask 2c — `tests/unit/test_sender/test_ui_send.py`** — ≥7 tests. Patch send_deeplink, press_return, type_string, assert_focused_chat_matches with call-order recording mocks:
- `test_send_text_kind_direct_calls_deeplink_then_ax_then_press_return` (load-bearing P5 ordering — call_log == ["send_deeplink", "assert_focused_chat_matches", "press_return"])
- `test_send_text_kind_direct_returns_send_started_unix_before_subprocess` (captured timestamp ≤ time at subprocess entry)
- `test_send_text_kind_direct_returns_is_experimental_false`
- `test_send_text_kind_group_calls_search_and_click_with_ax_before_keystroke` (AX preflight BEFORE every press_return in the group flow)
- `test_send_text_kind_group_returns_is_experimental_true`
- `test_send_text_unsupported_kind_raises_not_implemented` (kind="broadcast" / "community" / "other" → NotImplementedError)
- `test_send_text_direct_with_no_phone_raises` (recipient_phone_e164=None on direct kind → ValueError)
- `test_send_group_via_search_AX_preflight_on_search_results` (assert _assert_first_search_result_matches called BEFORE press_return; if it raises ChatHeaderMismatch, no subsequent press_return runs)
  </action>
  <verify>
    <automated>cd /Users/jlqueguiner/dev/whatsapp-mcp &amp;&amp; uv run ruff check tests/unit/test_sender/test_rate_limit.py tests/unit/test_sender/test_verify.py tests/unit/test_sender/test_ui_send.py &amp;&amp; uv run mypy tests/unit/test_sender/test_rate_limit.py tests/unit/test_sender/test_verify.py tests/unit/test_sender/test_ui_send.py &amp;&amp; uv run pytest tests/unit/test_sender/test_rate_limit.py tests/unit/test_sender/test_verify.py tests/unit/test_sender/test_ui_send.py -v</automated>
  </verify>
  <acceptance_criteria>
    - 3 test files created at the expected paths.
    - **MANDATORY regression test present verbatim:** `grep -cE 'def test_send_message_rate_limit_persists_across_restart' tests/unit/test_sender/test_rate_limit.py` returns 1.
    - Per-file test counts: test_rate_limit.py ≥8; test_verify.py ≥6; test_ui_send.py ≥7.
    - Total ≥20 new tests across these 3 files.
    - D-11 / T-5 verified: the mandatory test simulates a module reload preserving `_DB_PATH`, then asserts RateLimitExceeded raised.
    - D-22 soft-fail verified: `test_poll_exact_match_on_ZTEXT_no_normalization` exercises the documented limitation.
    - D-03 load-bearing P5 ordering verified: `test_send_text_kind_direct_calls_deeplink_then_ax_then_press_return` asserts call order.
    - All 3 test files pass: `uv run pytest tests/unit/test_sender/test_rate_limit.py tests/unit/test_sender/test_verify.py tests/unit/test_sender/test_ui_send.py` exits 0.
    - `uv run ruff check tests/unit/test_sender/` exits 0; `uv run mypy tests/unit/test_sender/` exits 0 under `--strict`.
  </acceptance_criteria>
  <done>3 stateful sender test files shipped covering rate_limit (MANDATORY persistence regression), verify (10s poll + SQL filters), ui_send (D-03 P5 ordering invariant); ≥20 new tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Tool-tier test for `send_message` (D-25 branch coverage + 2 mandatory regression tests) + live integration smoke</name>
  <files>tests/unit/test_tools/test_send_message.py, tests/integration/test_live_send.py</files>
  <read_first>
    - src/whatsapp_mcp/tools/send_message.py (the D-25 11-step orchestration to exercise)
    - .planning/phases/02-send-ui-automation-guardrails/02-CONTEXT.md §"Specifics" — `test_send_message_refuses_string_chat_id`, `test_send_message_aborts_on_chat_header_mismatch`, `test_send_message_appends_audit_log_with_body_sha256_not_body`
    - tests/unit/test_tools/test_doctor_phase1.py (Phase 1 tool-tier test pattern — `mcp.list_tools()` introspection + tool-body invocation)
    - tests/integration/test_live_reader.py (Phase 1 `@pytest.mark.live` + `RUN_LIVE=1` pattern to mirror)
  </read_first>
  <action>
**Subtask 3a — `tests/unit/test_tools/test_send_message.py`** — ≥14 tests. Mock heavily (`reader.find_chat_by_id`, `permissions.automation.check_whatsapp`, `sender.cross_chat_quote.check`, `sender.rate_limit.check_and_reserve` + `record_outcome`, `sender.ui_send.send_text`, `sender.verify.poll_for_outgoing`, `sender.audit.append`, `mcp.server.Context.elicit`). Provide a `Context` test double matching the FastMCP-injected shape.

Server-state fixture: `monkeypatch.setattr("whatsapp_mcp.server.read_only_mode", False)` to enable the send tool body. The `test_send_message_raises_in_read_only_mode` test leaves it True.

Tests:
- **MANDATORY: `test_send_message_refuses_string_chat_id`** — exact CONTEXT.md name. Invoke `mcp.call_tool(name="send_message", arguments={"chat_id": "not-an-int", "body": "hi"})`. Assert FastMCP's Pydantic int-coercion failure surfaces as a structured tool error with `chat_id` validation message.
- **MANDATORY: `test_send_message_aborts_on_chat_header_mismatch`** — exact name. Mock `ui_send.send_text` to raise `ChatHeaderMismatch("expected=X observed=Y")`. Call send_message. Assert ValueError raised (FastMCP layer maps WhatsAppMCPError subclasses to ValueError). Assert exactly ONE audit entry with `outcome="error"`, `error="ChatHeaderMismatch"` — verifies the try/finally branch.
- **MANDATORY: `test_send_message_appends_audit_log_with_body_sha256_not_body`** — exact name. Uses `tmp_audit_log`. Mock all collaborators for a successful send (outcome="sent"). Read audit.log content. Assert: (a) line is valid JSON, (b) `body_sha256` key present + 64-char hex matching `hashlib.sha256(body.encode()).hexdigest()`, (c) `body` key DOES NOT appear, (d) the literal body string DOES NOT appear anywhere in the raw line (covers BOTH schema-level + runtime-write contract D-13).
- `test_send_message_raises_read_only_mode_when_flag_true` (read_only_mode=True → ValueError; audit error="ReadOnlyMode")
- `test_send_message_raises_automation_revoked_when_tcc_denied` (mock check_whatsapp returns state="denied" → ValueError; audit error="AutomationRevoked")
- `test_send_message_raises_invalid_chat_id_when_reader_returns_none` (find_chat_by_id returns None → ValueError; audit error="InvalidChatId")
- `test_send_message_raises_invalid_chat_id_when_direct_chat_has_no_phone` (Chat(kind="direct", jid.phone=None) → ValueError with "@lid identifier")
- `test_send_message_calls_cross_chat_quote_check_with_chat_id` (cross_chat_quote.check called with (42, "hi"))
- `test_send_message_raises_rate_limit_exceeded_when_at_budget` (check_and_reserve raises RateLimitExceeded → ValueError; audit outcome="rate_limited")
- `test_send_message_returns_cancelled_on_decline` (ctx.elicit → DeclinedElicitation → SendResult.status="cancelled"; audit outcome="cancelled")
- `test_send_message_returns_cancelled_on_elicit_cancel` (CancelledElicitation → status="cancelled")
- `test_send_message_returns_cancelled_on_confirm_false` (AcceptedElicitation(data=ConfirmationSchema(confirm=False)) → status="cancelled")
- `test_send_message_skips_elicit_when_env_var_set` (WHATSAPP_MCP_SKIP_CONFIRM=1 → no elicit call; SendResult.confirm_skipped=True; audit confirm_skipped=True)
- `test_send_message_returns_sent_on_successful_verify` (happy path → status="sent", message_id="STANZA-123", audit outcome="sent")
- `test_send_message_returns_sent_unverified_on_verify_timeout` (verify returns None → status="sent_unverified", message_id=None, verification_note non-None, audit outcome="sent_unverified")
- `test_send_message_records_outcome_in_rate_limit_db_on_every_branch` (parametrized BEHAVIORAL contract per W-7 — does NOT pin to the `finally:` implementation detail; a defensible refactor that moves the record_outcome call elsewhere within the function body must still pass this test). Parametrize over the 5 outcome enum values: `("sent", "sent_unverified", "cancelled", "rate_limited", "error")`. For each parametrization, mock the appropriate collaborator to force that branch:
  - `"sent"`: happy path — verify.poll_for_outgoing returns "STANZA-1"; rate_limit.record_outcome called with outcome="sent".
  - `"sent_unverified"`: verify.poll_for_outgoing returns None; rate_limit.record_outcome called with outcome="sent_unverified".
  - `"cancelled"`: ctx.elicit returns DeclinedElicitation (OR AcceptedElicitation(data=ConfirmationSchema(confirm=False))); rate_limit.record_outcome called with outcome="cancelled". (Does NOT count against budget per SQL CHECK in rate_limit DDL — but the INSERT still happens.)
  - `"rate_limited"`: rate_limit.check_and_reserve raises RateLimitExceeded; rate_limit.record_outcome called with outcome="rate_limited" (via the `except RateLimitExceeded` branch's outcome assignment + finally).
  - `"error"`: ui_send.send_text raises ChatHeaderMismatch (or any other mapped exception); rate_limit.record_outcome called with outcome="error".
  Each case asserts on `rate_limit.record_outcome` mock's call_args — its `outcome` kwarg (or positional) matches the expected enum value. This pins the BEHAVIORAL contract ("on every branch, the outcome is recorded with the correct enum value"), not the IMPLEMENTATION DETAIL ("the record happens in the finally block").
- `test_send_message_tool_annotations_match_D20` (tools = mcp.list_tools(); annotations.readOnlyHint=False / destructiveHint=True / openWorldHint=True; meta["anthropic/maxResultSizeChars"]==60000)
- `test_send_message_excluded_from_tools_list_when_read_only` (read_only_mode=True at server import → mcp.list_tools() has only 8 tools, no send_message)
- `test_send_message_15s_timeout_decorator_present` (introspect __wrapped__ chain OR monkey-patch asyncio.wait_for and verify timeout=15)

**Subtask 3b — `tests/integration/test_live_send.py`** — ≥3 tests under `@pytest.mark.live`, with `RUN_LIVE=1` opt-in. The maintainer's WhatsApp must be running with TCC granted. Skipped silently otherwise. Module structure mirrors `tests/integration/test_live_reader.py`: top-of-file `pytestmark = [pytest.mark.live]` + `RUN_LIVE = os.environ.get("RUN_LIVE") == "1"` + `@pytest.mark.skipif(not RUN_LIVE, reason=...)` decorator.

**B-2 (mandatory) — production-state hygiene for live tests:** every live test in this module MUST monkey-patch `whatsapp_mcp.sender.rate_limit._DB_PATH` AND `whatsapp_mcp.sender.audit._LOG_PATH` (+ `_LOG_DIR`) to `tmp_path` BEFORE invoking `send_message` (or any downstream send pipeline), and restore them on teardown. Without this guard, live tests would (a) burn the maintainer's REAL daily rate-limit budget, polluting future production sends, and (b) write to the maintainer's REAL audit log, polluting investigation state. The recommended pattern is an autouse function-scope fixture inside this test module:

```python
@pytest.fixture(autouse=True)
def _isolate_live_state(tmp_path, monkeypatch):
    """B-2: redirect rate-limit DB + audit log to tmp_path for every live test.

    Live tests still drive the REAL WhatsApp UI (deeplink + keystroke + AX-API
    + post-hoc DB read of the real ChatStorage.sqlite), but the guardrail
    persistence is sandboxed. The maintainer's daily budget is preserved;
    the maintainer's audit log is preserved.
    """
    from whatsapp_mcp.sender import rate_limit, audit
    monkeypatch.setattr(rate_limit, "_DB_PATH", tmp_path / "rate-limit.db")
    monkeypatch.setattr(audit, "_LOG_DIR", tmp_path)
    monkeypatch.setattr(audit, "_LOG_PATH", tmp_path / "audit.log")
    yield
```

The autouse fixture covers ALL tests in this file uniformly — no per-test boilerplate. Each test's docstring states "Live test — rate-limit + audit state is sandboxed to tmp_path; the WhatsApp send itself drives the maintainer's real account." Document the cost in the test module docstring: a successful live test still places a real message in the maintainer's self-chat, so the test is observable in WhatsApp UI; what is NOT observable post-test is any persistent state delta in the rate-limit DB or audit log.

- `test_live_send_to_self_chat_smoke` — discover self-chat via `reader.list_chats(limit=50)` filtering by `chat.kind == "direct"` AND `chat.display_name == os.environ.get("WHATSAPP_MCP_LIVE_TEST_SELF_NAME")`. If env var unset or chat not found, `pytest.skip(...)`. Send fixture body `"[whatsapp-mcp live test {ISO-8601-timestamp}]"`. Assert SendResult.status in `("sent", "sent_unverified")`; assert the SANDBOXED `tmp_path / "audit.log"` has a matching body_sha256 line (NOT the maintainer's real audit log).
- `test_live_send_observes_post_hoc_verify` — same path; assert SendResult.message_id is not None (verifies the DB poll against the user's real WhatsApp DB writes). The DB poll still reads from the maintainer's REAL ChatStorage.sqlite — the sandbox covers rate_limit + audit only.
- `test_live_send_respects_rate_limit` — under additional `RUN_LIVE_BURN_BUDGET=1` env gate (double opt-in). With the B-2 sandbox in place, this test now does NOT burn the maintainer's REAL daily budget — the 5/min trip happens against the `tmp_path` rate-limit DB. Sends 5 unique bodies in tight loop; 6th send raises RateLimitExceeded. The maintainer still observes 5 fresh messages in the self-chat (REAL WhatsApp sends happen) but production state is preserved. Document this in the test docstring.
  </action>
  <verify>
    <automated>cd /Users/jlqueguiner/dev/whatsapp-mcp &amp;&amp; uv run ruff check tests/unit/test_tools/test_send_message.py tests/integration/test_live_send.py &amp;&amp; uv run mypy tests/unit/test_tools/test_send_message.py tests/integration/test_live_send.py &amp;&amp; uv run pytest tests/unit/test_tools/test_send_message.py -v &amp;&amp; uv run pytest -m "not live" -q</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/unit/test_tools/test_send_message.py` exists with ≥14 tests: `grep -cE '^def test_|^async def test_' tests/unit/test_tools/test_send_message.py` returns ≥14.
    - **All 3 MANDATORY regression test names present verbatim:** `grep -cE 'def test_send_message_refuses_string_chat_id' tests/unit/test_tools/test_send_message.py` returns 1; `grep -cE 'def test_send_message_aborts_on_chat_header_mismatch' tests/unit/test_tools/test_send_message.py` returns 1; `grep -cE 'def test_send_message_appends_audit_log_with_body_sha256_not_body' tests/unit/test_tools/test_send_message.py` returns 1.
    - File `tests/integration/test_live_send.py` exists with live marker + RUN_LIVE gate: `grep -cE 'pytestmark = \[pytest\.mark\.live\]|@pytest\.mark\.live' tests/integration/test_live_send.py` returns ≥1; `grep -cE 'RUN_LIVE' tests/integration/test_live_send.py` returns ≥1; `grep -cE 'WHATSAPP_MCP_LIVE_TEST_SELF_NAME' tests/integration/test_live_send.py` returns ≥1.
    - **B-2 production-state sandbox**: `grep -cE 'def _isolate_live_state' tests/integration/test_live_send.py` returns 1; `grep -cE 'autouse=True' tests/integration/test_live_send.py` returns ≥1; `grep -cE 'rate_limit.*_DB_PATH|_DB_PATH.*tmp_path' tests/integration/test_live_send.py` returns ≥1; `grep -cE 'audit.*_LOG_PATH|_LOG_PATH.*tmp_path' tests/integration/test_live_send.py` returns ≥1. Documented cost: live tests still drive the maintainer's real WhatsApp account (a real message lands in the self-chat) but consume ZERO production rate-limit/audit-log state.
    - Tool-tier test file passes: `uv run pytest tests/unit/test_tools/test_send_message.py -v` exits 0 — all ≥14 tests green.
    - Full non-live suite passes: `uv run pytest -m "not live" -q` exits 0. Phase 0/1/2 unit tests all pass.
    - Live integration smoke is SKIPPED by default: `uv run pytest -m "not live" tests/integration/` exits 0 with live tests reported as skipped/deselected (not failed).
    - `uv run ruff check tests/unit/test_tools/test_send_message.py tests/integration/test_live_send.py` exits 0; `uv run mypy tests/unit/test_tools/test_send_message.py tests/integration/test_live_send.py` exits 0 under `--strict`.
  </acceptance_criteria>
  <done>Tool-tier test for send_message ships with full D-25 branch coverage; all 3 mandatory regression tests landed (refuses_string_chat_id, aborts_on_chat_header_mismatch, appends_audit_log_with_body_sha256_not_body); RUN_LIVE=1-gated integration smoke ships skipped by default; total Phase 2 test count ~80-110 new tests; full Phase 0+1+2 non-live suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runtime → tmp filesystem | Tests use `tmp_path` fixtures so the maintainer's real `~/Library/Application Support/whatsapp-mcp/` and `~/Library/Logs/whatsapp-mcp/` are NEVER touched by unit tests. |
| Live test → real WhatsApp.app | `@pytest.mark.live` + `RUN_LIVE=1` gate; sends to user-configured self-chat only; budget-burning test under additional `RUN_LIVE_BURN_BUDGET=1` opt-in. |
| Tests → mocked pyobjc | `mock_pyobjc` fixture patches AX-API symbols so non-mac CI can exercise AX-walk logic without a real GUI. |
| Tests → mocked osascript / subprocess | `pytest_subprocess` fixture (Phase 1) stubs osascript invocations in deeplink + osascript_send tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Information disclosure | Live test exposes maintainer's actual chat / phone number in source | mitigate | Self-chat discovery uses `WHATSAPP_MCP_LIVE_TEST_SELF_NAME` env var; no actual chat / phone string in source. Test body is generic "[whatsapp-mcp live test ...]" prefix — non-identifying. |
| T-02-05-02 | Denial of service | Live test exhausts maintainer's daily WhatsApp budget | mitigate | Rate-limit budget-burning test (5 sends + 1 expected-fail) double-gated: `RUN_LIVE=1` AND `RUN_LIVE_BURN_BUDGET=1`. Maintainer opts INTO budget-burning separately from regular live smoke. |
| T-02-05-03 | Tampering | Test pollutes maintainer's audit.log / rate-limit DB / cross_chat_quote LRU across runs | mitigate | `tmp_audit_log`, `tmp_rate_limit_db`, `reset_xcq_lru` fixtures redirect state to `tmp_path` (auto-cleaned per pytest). Live tests touch real maintainer paths — accepted as "live test" semantics, documented. |
| T-02-05-04 | Repudiation | Mandatory regression tests are present but DON'T actually exercise the invariant (false-pass) | mitigate | Each mandatory test has BOTH source-level grep gates (name present) AND runtime gates (assertions on actual invariant). The body-sha256-not-body regression uses BOTH schema-level (no `body` key in JSON) AND string-search (no body plaintext substring) assertions — defense-in-depth. |
| T-02-05-05 | Spoofing | Mocked Context returns wrong `elicit` shape, hiding real bugs | accept | The Context test double mirrors what FastMCP injects; elicit-return-shape tests cover all 3 union variants explicitly. If real Context API changes between mcp==1.27.1 and a future version, mocks lag — pyproject.toml pins mcp version, so acceptable for v0.1. |
| T-02-05-06 | Denial of service | Unit test for verify.poll_for_outgoing's timeout takes 10s wall-clock | mitigate | Monkey-patch `_POLL_INTERVAL_SECONDS = 0.01` + `_MAX_POLLS = 4` for the timeout test; full 10s only exercised by live test. Documented in test docstring. |
</threat_model>

<verification>
- `uv run pytest -m "not live"` exits 0 — full Phase 0+1+2 non-live suite passes.
- Test count: ≥240 non-live tests total (148 from Phase 0+1, ≥90 new from Phase 2).
- All 4 mandatory regression tests pass with verbatim CONTEXT.md names:
  - `test_send_message_refuses_string_chat_id` (in `test_send_message.py`)
  - `test_send_message_aborts_on_chat_header_mismatch` (in `test_send_message.py`)
  - `test_send_message_rate_limit_persists_across_restart` (in `test_rate_limit.py`)
  - `test_send_message_appends_audit_log_with_body_sha256_not_body` (in `test_send_message.py`)
- Live integration smoke skipped by default; opt-in via `RUN_LIVE=1` + (for budget-burning) `RUN_LIVE_BURN_BUDGET=1`.
- `uv run ruff check src/ tests/` exits 0; `uv run mypy src/ tests/` exits 0 under `--strict`.
- Phase 0 stdout-purity invariant holds: `tests/unit/test_stdout_purity.py` passes (verifies the 9-tool server emits zero stdout bytes — but only with read_only_mode=False; under default read_only_mode=True it's 8 tools).
- D-13 STRUCTURAL invariant codified in TWO places: `test_audit_entry_schema_has_no_plaintext_body_field` (schema-level — Task 1) + `test_send_message_appends_audit_log_with_body_sha256_not_body` (runtime-write contract — Task 3).
- REL-05 D-24 narrow edge still asserted (Plan 02-04's `test_sender_to_reader_edge_is_exactly_one_file` test — unchanged by this plan).
</verification>

<success_criteria>
1. `uv run pytest -m "not live"` exits 0 with ≥240 tests passing.
2. The 4 mandatory regression tests pass under their exact CONTEXT.md-specified names.
3. Plan 02-04's isolation tests still pass (no new sender→reader edges introduced).
4. `uv run pytest tests/integration/test_live_send.py -m live --collect-only` lists ≥3 tests (verifies they're discoverable).
5. `uv run pytest -m "not live"` does NOT execute any test in `tests/integration/test_live_send.py` (live marker gating works).
6. `RUN_LIVE=1 WHATSAPP_MCP_LIVE_TEST_SELF_NAME="<name>" uv run pytest -m live tests/integration/test_live_send.py::test_live_send_to_self_chat_smoke` succeeds on the maintainer's machine and consumes ZERO of the maintainer's real daily rate-limit budget AND writes ZERO bytes to the maintainer's real audit.log (B-2 sandbox via autouse fixture monkey-patches `_DB_PATH` + `_LOG_PATH` to `tmp_path`). This is the Phase 2 verifier-level end-to-end proof.
7. D-13 STRUCTURAL regression has both representations: schema-level on `AuditEntry.model_fields` AND runtime-content on the audit.log JSON line.
8. Rate-limit persistence test (`test_send_message_rate_limit_persists_across_restart`) explicitly simulates module reload preserving SQLite file at same path → proves T-5 mitigation.
9. Chat-header-mismatch regression (`test_send_message_aborts_on_chat_header_mismatch`) asserts audit log gets `outcome="error", error="ChatHeaderMismatch"` entry → proves try/finally branch on load-bearing P5 mitigation.
10. Tool-annotation assertions verify D-20: readOnlyHint=False, destructiveHint=True, openWorldHint=True, meta budget 60000.
</success_criteria>

<output>
After completion, create `.planning/phases/02-send-ui-automation-guardrails/02-05-SUMMARY.md`. Include:
- File-by-file test count summary (per new test file: number of tests).
- Total new test count vs Phase 1 baseline (e.g., "Phase 1: 148 non-live + 9 live; Phase 2: + 95 non-live + 3 live = 243 non-live + 12 live total").
- Live test smoke result (if RUN_LIVE=1 was exercised at SUMMARY-write time, include actual sent message_id + audit log path + outcome).
- Confirmation that all 4 mandatory regression tests pass with verbatim names.
- Confirmation that D-13 STRUCTURAL invariant is codified in BOTH test_audit.py AND test_send_message.py.
- Any Rule-1 deviations (literal-token grep-gate near-misses).
- Phase 2 transitions to "5/5 plans complete; ready for /gsd-verify-work."
</output>
