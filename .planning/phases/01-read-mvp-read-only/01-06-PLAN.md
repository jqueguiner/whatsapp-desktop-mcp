---
phase: 01-read-mvp-read-only
plan: 6
title: "Tests — unit (models, reader, tools), concurrency stress, REL-05 isolation re-test, --read-only mode, live integration"
type: execute
wave: 4
depends_on: [1, 2, 3, 4, 5]
requirements: [REL-05]
files_modified:
  - tests/unit/test_models_phase1.py
  - tests/unit/test_time.py
  - tests/unit/test_paths_phase1.py
  - tests/unit/test_cursor.py
  - tests/unit/test_reader/__init__.py
  - tests/unit/test_reader/conftest.py
  - tests/unit/test_reader/test_connection.py
  - tests/unit/test_reader/test_schema_v1.py
  - tests/unit/test_reader/test_tombstones.py
  - tests/unit/test_reader/test_media.py
  - tests/unit/test_reader/test_chats_messages.py
  - tests/unit/test_reader/test_contacts.py
  - tests/unit/test_reader/test_concurrency.py
  - tests/unit/test_isolation.py
  - tests/unit/test_tools/__init__.py
  - tests/unit/test_tools/test_decorators.py
  - tests/unit/test_tools/test_read_tools_registration.py
  - tests/unit/test_tools/test_doctor_phase1.py
  - tests/unit/test_read_only_mode.py
  - tests/integration/test_live_reader.py
autonomous: true
must_haves:
  truths:
    - "Phase 0 baseline of 28 tests still passes after Plan 06 — no regression"
    - "REL-05 isolation re-test (`tests/unit/test_isolation.py`) is updated to assert the now-NON-empty reader package imports zero symbols from `whatsapp_mcp.sender` via AST walk; the Phase 0 vacuous-true assertion becomes load-bearing in Phase 1"
    - "Concurrency stress test (`test_reader/test_concurrency.py`): 10 concurrent reader coroutines against a fixture WAL sqlite while a separate thread writes; assert zero `database is locked` errors over 100 iterations"
    - "`tools/list` after a `--read-only` startup returns exactly 8 tools (doctor + 7 read tools), every one `readOnlyHint=True`; verified via subprocess JSON-RPC handshake AND via the in-process `mcp.list_tools()` introspection"
    - "DIAG-02 codified: a test that mocks `fda.check` to return `state=\"denied\"` calls `doctor()` and asserts the report still returns successfully with `schema_fingerprint.state == \"unreachable\"`"
    - "Cursor round-trip + tampering test: `decode_cursor(\"junk\")` raises `CursorError`; mismatched chat_id in cursor is rejected by `read_chat`"
    - "Tombstone predicate exercised against fixture rows that match all four observed `ZFLAGS` patterns (0x01000000, 0x05000000, 0x05008000, 0x05000180); only the high-bit pattern + null text is filtered"
    - "Char-cap test: synthetic 5000-message chat fixture; `read_chat` returns ≤ 60_000 chars + a decodable `next_cursor`"
    - "Live integration tests gated by `RUN_LIVE=1` exercise `list_chats`, `read_chat`, `extract_recent`, `search_messages`, `search_contacts`, `get_chat_metadata`, `get_message_context`, `doctor` against the user's real WhatsApp on the maintainer's Mac (skipped in CI; runs in pre-release smoke)"
  artifacts:
    - path: "tests/unit/test_models_phase1.py"
      provides: "Pydantic round-trip + Literal enforcement for all 8 Plan 01 models"
      contains: "test_chat_round_trip"
    - path: "tests/unit/test_time.py"
      provides: "Cocoa↔Unix conversion boundary + round-trip + live anchor"
      contains: "test_cocoa_epoch_offset"
    - path: "tests/unit/test_cursor.py"
      provides: "encode/decode round-trip + tampering raises CursorError"
      contains: "test_decode_cursor_invalid"
    - path: "tests/unit/test_reader/test_connection.py"
      provides: "open_ro succeeds RO; refuses writes; busy_timeout set"
      contains: "test_open_ro"
    - path: "tests/unit/test_reader/test_concurrency.py"
      provides: "10 concurrent readers + 1 writer thread, no `database is locked` errors over 100 iterations"
      contains: "test_concurrent_reads_with_writer"
    - path: "tests/unit/test_isolation.py"
      provides: "REL-05 — AST walk of every reader/*.py asserts zero whatsapp_mcp.sender imports (now load-bearing)"
      contains: "test_reader_does_not_import_sender"
    - path: "tests/unit/test_read_only_mode.py"
      provides: "subprocess --read-only smoke; tools/list shape; readOnlyHint annotations"
      contains: "test_read_only_lists_only_read_tools"
    - path: "tests/unit/test_tools/test_doctor_phase1.py"
      provides: "DIAG-01 expanded shape + DIAG-02 defensive probing"
      contains: "test_doctor_diag02_fda_denied"
    - path: "tests/integration/test_live_reader.py"
      provides: "RUN_LIVE=1 integration smoke against user's real WhatsApp DB"
      contains: "@pytest.mark.live"
  key_links:
    - from: "tests/unit/test_reader/conftest.py"
      to: "ChatStorage-fixture-shaped sqlite (in-process tempfile)"
      via: "synthetic fixture matching ZWACHATSESSION/ZWAMESSAGE/ZWAGROUPINFO/ZWAGROUPMEMBER/ZWAMEDIAITEM live schema column shapes"
      pattern: "CREATE TABLE ZWACHATSESSION"
    - from: "tests/integration/test_live_reader.py"
      to: "real ChatStorage.sqlite via paths.resolve_chatstorage_path()"
      via: "@pytest.mark.live + RUN_LIVE env-var skip"
      pattern: "@pytest.mark.live"
---

<objective>
Codify every behavioral invariant Plans 01-05 ship via fast deterministic
unit tests against synthetic fixtures, plus a `RUN_LIVE=1`-gated integration
smoke against the maintainer's real WhatsApp install. Re-run the Phase 0
test_isolation REL-05 gate now that the reader package is non-empty (the
test gains teeth: the AST walk really does have something to refuse).

Purpose: Phase 1 verification (`/gsd-verify-work` after Plans 01-06) reads
this test suite as the empirical proof that the 5 ROADMAP §"Phase 1"
success criteria are met. The CI pipeline (Plan 0-05's ci.yml) will run
`uv run pytest -m "not live"` on every push from Phase 1 onward; the live
integration is the maintainer-side smoke before tagging v0.1.0.

Output: ~14 new test files (4 model/helper tests + 8 reader-tier tests
including conftest fixture, plus reader concurrency stress + REL-05
isolation update + 3 tool-tier tests + read-only mode subprocess test +
live integration). Phase 0 baseline of 28 tests preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-read-mvp-read-only/01-RESEARCH.md
@.planning/phases/01-read-mvp-read-only/01-01-SUMMARY.md
@.planning/phases/01-read-mvp-read-only/01-02-SUMMARY.md
@.planning/phases/01-read-mvp-read-only/01-03-SUMMARY.md
@.planning/phases/01-read-mvp-read-only/01-04-SUMMARY.md
@.planning/phases/01-read-mvp-read-only/01-05-SUMMARY.md
@CLAUDE.md
@tests/unit/test_isolation.py
@tests/conftest.py

<interfaces>
<!-- Existing Phase 0 test surface this plan extends. -->

From tests/conftest.py (Phase 0 — near-empty marker module; pytest-asyncio's asyncio_mode=auto is configured in pyproject.toml; `fp` fixture from pytest-subprocess is auto-loaded).

From pyproject.toml [tool.pytest.ini_options]:
```
asyncio_mode = "auto"
markers = ["live: requires a live macOS environment with WhatsApp installed (RUN_LIVE=1)"]
```
The `@pytest.mark.live` marker + `RUN_LIVE` env-var gate is the established Phase 0 pattern for the integration smoke. Plan 06 reuses verbatim.

From tests/unit/test_isolation.py (Phase 0 — currently asserts `whatsapp_mcp.reader` and `whatsapp_mcp.sender` import independently AND that AST-walk of each package shows no cross-imports). Phase 0's assertion is "vacuous true" because `reader/` was empty. Plan 06 keeps the test name and structure but the AST walk now has 10 reader files to check — the test gains teeth.

Plan-by-plan public surface tests target:

Plan 01 → tests/unit/test_models_phase1.py + test_time.py + test_paths_phase1.py + test_cursor.py
Plan 02 → tests/unit/test_reader/* (8 files)
Plan 03 → tests/unit/test_read_only_mode.py + extends tests/unit/test_exceptions.py via a new test (or its own file)
Plan 04 → tests/unit/test_tools/test_decorators.py + test_read_tools_registration.py
Plan 05 → tests/unit/test_tools/test_doctor_phase1.py
REL-05 → tests/unit/test_isolation.py (UPDATED, not new)
Live → tests/integration/test_live_reader.py
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Model + time + cursor + paths + REL-05 isolation update tests (Plans 01 + 03 coverage)</name>
  <files>
    tests/unit/test_models_phase1.py,
    tests/unit/test_time.py,
    tests/unit/test_paths_phase1.py,
    tests/unit/test_cursor.py,
    tests/unit/test_isolation.py,
    tests/unit/test_read_only_mode.py
  </files>
  <read_first>
    .planning/phases/01-read-mvp-read-only/01-01-SUMMARY.md (Plan 01 model field maps);
    .planning/phases/01-read-mvp-read-only/01-03-SUMMARY.md (--read-only flag wiring);
    tests/unit/test_isolation.py (existing Phase 0 — extend the AST walk; preserve the import-independence asserts);
    tests/unit/test_stdout_purity.py (existing Phase 0 — pattern for subprocess JSON-RPC handshake; the read-only test follows the same shape);
    src/whatsapp_mcp/models/__init__.py (Plan 01 + Plan 05 re-exports — what the round-trip tests target)
  </read_first>
  <action>
    `tests/unit/test_models_phase1.py` — write ~10 tests covering the locked Pydantic surface from Plan 01:
    - `test_chat_round_trip()` — `Chat(...).model_dump_json()` parses back via `Chat.model_validate_json(...)` losslessly.
    - Same shape for `Message`, `Contact`, `GroupInfo`, `GroupMember`, `MediaRef`, `Coverage`, `Jid`.
    - `test_chat_kind_literal_rejects_unknown()` — `Chat(chat_id=1, kind="banana", ...)` raises `pydantic.ValidationError`.
    - `test_message_kind_literal_includes_other()` — `Message(..., kind="other", ...)` constructs (the catch-all bucket Plan 02 uses for unknown ZMESSAGETYPE values).
    - `test_jid_kind_phone_default_resolution_fields_optional()` — `Jid(kind="phone", raw="33612345678@s.whatsapp.net")` constructs with `phone=None, lid=None`.
    - `test_media_ref_no_bytes_field()` — `MediaRef.model_fields` does NOT contain any field hinting at bytes/base64/data inlining; only path metadata. (DATA-03 enforcement at the schema level.)

    `tests/unit/test_time.py` — boundary + round-trip + live anchor tests per RESEARCH §"Cocoa Epoch ↔ Unix Conversion → Test fixtures":
    - `test_cocoa_epoch_offset()` — `cocoa_to_unix(0) == 978_307_200`.
    - `test_unix_to_cocoa_inverse()` — `unix_to_cocoa(978_307_200) == 0.0`.
    - `test_round_trip_arbitrary_unix()` — `cocoa_to_unix(unix_to_cocoa(1_747_140_000)) == 1_747_140_000` for several Unix values including 0, current-ish, and a 2050-era timestamp.
    - `test_live_anchor_from_user_mac()` — comment-only assertion that ZMESSAGEDATE=800352916 → 2026-05-13 (regenerable; documents the verified-live anchor; uses `datetime.fromtimestamp(cocoa_to_unix(800352916), tz=UTC).date()` to keep it deterministic).

    `tests/unit/test_paths_phase1.py` — extend the existing path test surface (Phase 0's `permissions/test_fda.py` covers `resolve_chatstorage_path`):
    - `test_lid_path_shape()` — `resolve_lid_path()` returns absolute path ending in `/group.net.whatsapp.WhatsApp.shared/LID.sqlite`.
    - `test_contactsv2_path_shape()` — same for `ContactsV2.sqlite`.
    - `test_media_root_shape()` — same for `Message` directory (no trailing slash).
    - `test_phase0_resolve_chatstorage_unchanged()` — assert `resolve_chatstorage_path()` returns the Phase 0-shaped path; the test exists to catch any regression that accidentally changed the Phase 0 frozen contract.

    `tests/unit/test_cursor.py` — round-trip + tampering tests:
    - `test_round_trip_z_sort()` — `decode(encode(42, 1.5e18, 'z_sort')) == (42, 1.5e18, 'z_sort')` (W2 widened codec).
    - `test_round_trip_cocoa_ts()` — `decode(encode(7, 1_747_140_000.0, 'cocoa_ts')) == (7, 1_747_140_000.0, 'cocoa_ts')`.
    - `test_round_trip_zero_and_max()` — boundary values for both `anchor_kind` discriminator values.
    - `test_decode_invalid_base64_raises_cursor_error()` — `decode_cursor("not-base64-!!!")` raises `CursorError`.
    - `test_decode_invalid_json_raises_cursor_error()` — pass a base64-encoded but non-JSON payload.
    - `test_decode_missing_keys_raises_cursor_error()` — `base64(json.dumps({"foo": "bar"}))` raises; also a payload missing only `anchor_kind` raises (W2 — the discriminator is mandatory).
    - `test_decode_unknown_anchor_kind_raises_cursor_error()` — `base64(json.dumps({"chat_id": 1, "anchor": 1.0, "anchor_kind": "made_up"}))` raises (Literal enforcement at decode time).
    - `test_decode_wrong_types_raises_cursor_error()` — `base64(json.dumps({"chat_id": "not-int", "anchor": 1.0, "anchor_kind": "z_sort"}))` raises.

    `tests/unit/test_isolation.py` — UPDATE the Phase 0 file. Preserve the existing import-independence assertions (separate `import whatsapp_mcp.reader` and `import whatsapp_mcp.sender` succeed independently). UPDATE the AST walk: now that `reader/` has 10 modules (Plan 02), the walk MUST iterate every `.py` file under `src/whatsapp_mcp/reader/` and assert no `Import` or `ImportFrom` node references `whatsapp_mcp.sender`. Mirror the same check for sender (still empty in Phase 1, so it remains vacuous-true on the sender side until Phase 2). Add a new test `test_reader_imports_models_paths_time_only()` that asserts every `whatsapp_mcp.*` import in `reader/*.py` is one of `{models, paths, time, exceptions, reader}` — a positive whitelist that catches accidental drift into `tools/`, `permissions/`, or `sender/`. The Phase 0 test_isolation tests that already exist must STILL PASS after this update — extend, do not rewrite.

    `tests/unit/test_read_only_mode.py` — SETUP-06 + Plan 03 + Plan 04 verification. Two test shapes:
    1. **In-process** (fast): `test_default_flag_is_true()` asserts `from whatsapp_mcp import server; server.read_only_mode is True` at import time. `test_setting_flag_observable()` assigns `server.read_only_mode = False`, asserts the new value persists; resets at teardown.
    2. **Subprocess JSON-RPC handshake** (mirroring Phase 0's `test_stdout_purity.py` pattern): `test_read_only_lists_only_read_tools()` spawns `python -m whatsapp_mcp --read-only` via `asyncio.create_subprocess_exec`, drives the full `initialize → notifications/initialized → tools/list` JSON-RPC sequence, parses the `tools/list` response, asserts the names equal `{doctor, list_chats, read_chat, extract_recent, search_messages, search_contacts, get_chat_metadata, get_message_context}` (8 names total) AND that every tool has `annotations.readOnlyHint == True`. Pattern reused from `tests/unit/test_stdout_purity.py` — see that file for the JSON-RPC handshake helper functions.
    3. `test_no_read_only_flag_parses_without_error()` — invoke `python -m whatsapp_mcp --no-read-only --help` (combines `--no-read-only` flag with `--help` to exit before FastMCP loads); assert exit code 0 AND that the help output contains `--no-read-only` in the rendered usage.

    Every test file imports `from __future__ import annotations` for mypy strict; uses `import pytest` for fixtures and parametrization; mypy strict + ruff pass (the per-file ignore for `T201` in tests/ from Phase 0 pyproject.toml means `print` is allowed in tests for debugging — but new tests should not rely on it).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_models_phase1.py tests/unit/test_time.py tests/unit/test_paths_phase1.py tests/unit/test_cursor.py tests/unit/test_isolation.py tests/unit/test_read_only_mode.py -v &amp;&amp; uv run ruff check tests/unit/test_models_phase1.py tests/unit/test_time.py tests/unit/test_paths_phase1.py tests/unit/test_cursor.py tests/unit/test_isolation.py tests/unit/test_read_only_mode.py &amp;&amp; uv run ruff format --check tests/unit/test_models_phase1.py tests/unit/test_time.py tests/unit/test_paths_phase1.py tests/unit/test_cursor.py tests/unit/test_isolation.py tests/unit/test_read_only_mode.py &amp;&amp; uv run mypy tests/unit/test_models_phase1.py tests/unit/test_time.py tests/unit/test_paths_phase1.py tests/unit/test_cursor.py tests/unit/test_isolation.py tests/unit/test_read_only_mode.py</automated>
  </verify>
  <acceptance_criteria>
    - All 6 new/modified test files exist and pass under `pytest -m "not live"` (live integration is Task 3).
    - `tests/unit/test_isolation.py` STILL passes the Phase 0 4 isolation tests AND the new positive-whitelist test added in this task.
    - `tests/unit/test_read_only_mode.py` subprocess test asserts exactly 8 tool names returned by `tools/list` after `--read-only` startup; every tool's `annotations.readOnlyHint is True`.
    - Cursor tampering: `decode_cursor("junk")` raises `CursorError` (not raw ValueError; Plan 01 wraps).
    - Time round-trip: `cocoa_to_unix(0) == 978_307_200` AND `cocoa_to_unix(unix_to_cocoa(N)) == N` for all sampled N.
    - Paths: all four `resolve_*` paths absolute (no `~`).
    - Lint/type: ruff (full ruleset) + ruff format + mypy --strict green on all 6 files.
    - Phase 0 baseline regression: total test count is now ≥ 28 + (new tests in this task), all passing.
  </acceptance_criteria>
  <done>Plans 01 + 03 covered by unit tests; Phase 0 isolation test extended with load-bearing AST walk over the now-non-empty reader/; subprocess --read-only smoke confirms the 8-tool / readOnlyHint=True surface end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Reader unit tests + concurrency stress + tool tests (Plans 02, 04, 05 coverage)</name>
  <files>
    tests/unit/test_reader/__init__.py,
    tests/unit/test_reader/conftest.py,
    tests/unit/test_reader/test_connection.py,
    tests/unit/test_reader/test_schema_v1.py,
    tests/unit/test_reader/test_tombstones.py,
    tests/unit/test_reader/test_media.py,
    tests/unit/test_reader/test_chats_messages.py,
    tests/unit/test_reader/test_contacts.py,
    tests/unit/test_reader/test_concurrency.py,
    tests/unit/test_tools/__init__.py,
    tests/unit/test_tools/test_decorators.py,
    tests/unit/test_tools/test_read_tools_registration.py,
    tests/unit/test_tools/test_doctor_phase1.py
  </files>
  <read_first>
    .planning/phases/01-read-mvp-read-only/01-02-SUMMARY.md (Plan 02 reader API + the open-question findings about ZWAGROUPINFO description and is_muted columns);
    .planning/phases/01-read-mvp-read-only/01-04-SUMMARY.md (Plan 04 tool surface + cursor semantics);
    .planning/phases/01-read-mvp-read-only/01-05-SUMMARY.md (Plan 05 doctor expansion);
    .planning/phases/01-read-mvp-read-only/01-RESEARCH.md (sections: "Pitfall P1: Cache vs. truth → Verification"; "Pitfall P3 → Verification: Concurrency stress test"; "Pitfall P9 → Verification: char-cap test"; "Pitfall P10 → Verification: tombstone fixture"; "Pitfall P11 → Verification: JID/LID dedup");
    .planning/research/ARCHITECTURE.md (Verified ChatStorage.sqlite Schema essentials — column shapes for the synthetic fixture)
  </read_first>
  <action>
    Build the reader test infrastructure first, then the tool tests on top.

    `tests/unit/test_reader/__init__.py` — empty marker file (the package needs to exist).

    `tests/unit/test_reader/conftest.py` — pytest fixtures factory for synthetic ChatStorage-shaped sqlite. Provide:
    - `chatstorage_fixture` (function-scoped) — creates a tempfile.sqlite in WAL mode with `Z_METADATA(Z_VERSION=1)`, `ZWACHATSESSION` (matching live columns + indexes from RESEARCH §"Core Data Schema Essentials → ZWACHATSESSION"), `ZWAMESSAGE` (compound index `(ZCHATSESSION, ZSORT)` per RESEARCH), `ZWAGROUPINFO`, `ZWAGROUPMEMBER`, `ZWAMEDIAITEM`. Seeds:
      - 3 chats: 1 direct (`ZSESSIONTYPE=0`), 1 group (`ZSESSIONTYPE=1`), 1 broadcast (`ZSESSIONTYPE=3`).
      - 50 messages on the direct chat across the last 30 days (Cocoa epoch); mix of types (text=0, image=1, system=6, revoked=14, ephemeral=15).
      - 5 deletions: 2 with `ZMESSAGETYPE=14`, 2 with `ZTEXT IS NULL` + `ZFLAGS=0x05000000`, 1 normal (control — should NOT be filtered).
      - 1 quote-reply (`ZPARENTMESSAGE` set on a recent message pointing to an earlier one).
      - 1 media message (`ZMEDIAITEM` FK populated; `ZMEDIALOCALPATH` set to `images/abc/def.jpg`).
      - 5 group members on the group chat (mix admin / non-admin / active / inactive).
      - 5000-message synthetic chat (separate fixture `large_chat_fixture`) for the char-cap test — same schema, just bulk INSERTs.
    - `lid_fixture` — separate tempfile sqlite with `ZWAPHONENUMBERLIDPAIR` (matching live schema), seeded with 3 phone↔lid mappings.
    - `contactsv2_fixture` — `ZWAADDRESSBOOKCONTACT` shape, seeded with 5 contacts.
    - `monkeypatch_paths` (autouse fixture optional) — monkeypatches `whatsapp_mcp.paths.resolve_chatstorage_path` / `resolve_lid_path` / `resolve_contactsv2_path` / `resolve_media_root` to return the fixture paths. Use `pytest.MonkeyPatch.context` so each test gets isolation.

    `tests/unit/test_reader/test_connection.py` — REL-01 + P3 mitigation tests:
    - `test_open_ro_succeeds_against_fixture()` — open the chatstorage_fixture, run `SELECT 1`, close.
    - `test_open_ro_refuses_writes()` — attempt `INSERT INTO ZWACHATSESSION VALUES (...)` inside `with open_ro(p)`; assert `sqlite3.OperationalError` with "readonly" or "attempt to write" in the message.
    - `test_open_ro_no_immutable_flag_in_uri()` — assert the URI string format used by open_ro contains `mode=ro` and NOT `immutable`.
    - `test_busy_timeout_set()` — open the connection, `cursor.execute("PRAGMA busy_timeout").fetchone()` returns `(5000,)`.

    `tests/unit/test_reader/test_schema_v1.py` — REL-04 tests:
    - `test_supported_versions_includes_1()` — `1 in SUPPORTED_VERSIONS`.
    - `test_probe_z_version_returns_int()` — against the chatstorage_fixture (which seeds `Z_METADATA.Z_VERSION=1`), returns `1`.
    - `test_probe_z_version_raises_on_empty_z_metadata()` — fixture variant with empty Z_METADATA; raises RuntimeError.
    - `test_is_supported_predicate()` — `is_supported(1) is True`, `is_supported(99) is False`.

    `tests/unit/test_reader/test_tombstones.py` — READ-08 + P10 tests per RESEARCH §"Pitfall P10 → Verification":
    - `test_tombstone_message_type_14()` — `is_tombstone(14, 0x01000000, "still-here") is True` (type 14 always tombstone regardless of flags or text).
    - `test_tombstone_high_bit_with_null_text()` — `is_tombstone(0, 0x05000000, None) is True`; `is_tombstone(0, 0x05008000, None) is True`; `is_tombstone(0, 0x05000180, None) is True`.
    - `test_not_tombstone_normal_text()` — `is_tombstone(0, 0x01000000, "hi") is False`.
    - `test_not_tombstone_high_bit_with_text()` — `is_tombstone(0, 0x05000000, "still text") is False` (the high-bit pattern only filters when text is also null).
    - `test_TOMBSTONE_SQL_WHERE_constant_present()` — assert the constant matches the expected `"ZMESSAGETYPE != 14 AND NOT (ZTEXT IS NULL AND (ZFLAGS & 0xFF000000) = 0x05000000)"` — locked SQL filter shape.

    `tests/unit/test_reader/test_media.py` — DATA-03 + path-traversal defense:
    - `test_resolve_media_ref_returns_none_for_empty_path()`.
    - `test_resolve_media_ref_returns_absolute_path()` — fake row with `ZMEDIALOCALPATH="images/abc/photo.jpg"`, fake media_root via tempdir; assert returned `MediaRef.local_path` is `<tempdir>/images/abc/photo.jpg` (absolute).
    - `test_resolve_media_ref_refuses_traversal()` — fake row with `ZMEDIALOCALPATH="../../../etc/passwd"`; assert returned value is `None` (path-traversal defense).
    - `test_resolve_media_ref_mime_guess()` — `.jpg` → `image/jpeg`, `.mp4` → `video/mp4`, unknown extension → `application/octet-stream`.

    `tests/unit/test_reader/test_chats_messages.py` — P1 cache + READ-08 default + READ-02/03/07 behavior tests against the chatstorage_fixture. Use `monkeypatch_paths` to redirect:
    - `test_list_chats_returns_seeded_three()` — `await reader.list_chats()` returns 3 Chat instances (1 direct + 1 group + 1 broadcast).
    - `test_list_chats_kind_mapping()` — verify direct → "direct", group → "group", broadcast → "broadcast".
    - `test_window_default_filters_tombstones()` — `await reader.window(chat_id=1, before_z_sort=None, limit=200)` returns the seeded normal messages (no type-14, no high-bit + null-text rows).
    - `test_window_include_deleted_returns_tombstones()` — same call with `include_deleted=True` returns the additional 4 tombstones.
    - `test_window_orders_by_zsort_desc()` — first message in the result has the highest ZSORT.
    - `test_since_returns_messages_in_window()` — `await reader.since(chat_id=1, cutoff_unix_ts=now-7days)` returns last week's messages only.
    - `test_context_around_stanza_returns_target_plus_window()` — `await reader.context_around_stanza(known_stanza_id, before=2, after=2)` returns 5 messages centered on target.
    - `test_parent_of_stanza()` — for a quote-reply message, returns the parent Message; for a non-quote, returns None.
    - `test_latest_timestamp()` — returns `max(timestamps)` Unix-converted from the fixture.

    `tests/unit/test_reader/test_contacts.py` — P11 JID/LID dedup test per RESEARCH §"Pitfall P11 → Verification". Use chatstorage + lid + contactsv2 fixtures together:
    - `test_search_contacts_finds_by_name()` — fixture seeds "Alice" with both `33612345678@s.whatsapp.net` (in chatstorage) AND `99887766554433@lid` (in chatstorage + LID.sqlite mapping). `await reader.search_contacts("Alice")` returns ONE Contact whose `known_identifiers` contains BOTH JIDs (deduplicated by phone).
    - `test_search_contacts_lid_only_disambiguation_required()` — fixture with a contact whose only JID is `@lid` and whose LID is NOT in LID.sqlite; assert `disambiguation_required=True` and `phone is None`.
    - `test_resolve_lid_to_phone()` — direct lookup against the fixture LID.sqlite.

    `tests/unit/test_reader/test_concurrency.py` — P3 + P8 stress test per RESEARCH §"Pitfall P3 → Verification":
    - `test_concurrent_reads_with_writer()` — synthetic test sqlite (NOT the user's WhatsApp DB; absolute REL-01 invariant: never write to WhatsApp's DB even in tests). Open a separate writer thread that INSERTs into a tempfile WAL sqlite once every 10 ms for 1 second (~100 writes). Concurrently spawn 10 reader coroutines that each call `await asyncio.to_thread(read_loop)` running `SELECT * FROM test_table LIMIT 100` 10 times. Assert ZERO `database is locked` exceptions across all 100 read operations. Pass criteria: 100% of read attempts succeed; the writer thread reports its 100 successful INSERTs.

    `tests/unit/test_tools/__init__.py` — empty marker.

    `tests/unit/test_tools/test_decorators.py` — Plan 04 timeout decorator:
    - `test_timeout_returns_value_when_under_budget()`.
    - `test_timeout_raises_value_error_on_overrun()` — fast `asyncio.sleep(1)` wrapped in `@timeout(seconds=0.01)` raises `ValueError` (NOT raw `TimeoutError`).
    - `test_timeout_preserves_function_signature()` — the decorated function's `__name__` and `__wrapped__` are preserved (functools.wraps invariant — FastMCP relies on this).

    `tests/unit/test_tools/test_read_tools_registration.py` — Plan 04 wired-up tools:
    - `test_eight_tools_registered()` — `from whatsapp_mcp.server import mcp; await mcp.list_tools()` returns exactly 8 names: `{doctor, list_chats, read_chat, extract_recent, search_messages, search_contacts, get_chat_metadata, get_message_context}`.
    - `test_every_tool_is_read_only_hint()` — every tool's `annotations.readOnlyHint is True`.
    - `test_every_tool_has_max_result_size_meta()` — every tool (W1 — INCLUDING `doctor` after the Plan 05 W1 fix) carries `meta["anthropic/maxResultSizeChars"] == 60_000`. Iterate `mcp.list_tools()` with no carve-out; assert the meta is present on all 8 tools. This codifies the no-carve-out rule from READ-09.
    - `test_search_messages_has_10s_timeout()` — introspect the wrapped function (or document the expectation in a comment if the decorator doesn't expose the budget; in the latter case verify by triggering a forced timeout in a separate slow-path test).
    - `test_read_chat_cursor_chat_id_mismatch_rejected()` — call `read_chat(chat_id=1, cursor=encode_cursor(99, 1.0, 'z_sort'))`; assert `ValueError` with "Cursor does not match chat_id" (T-04-01 mitigation).
    - `test_read_chat_cursor_wrong_anchor_kind_rejected()` — call `read_chat(chat_id=1, cursor=encode_cursor(1, 1_747_140_000.0, 'cocoa_ts'))`; assert `ValueError` with "anchor_kind" in the message (W2 cross-tool cursor reuse guard — a search_messages cursor must not be valid input to read_chat).
    - `test_search_messages_cursor_anchor_kind()` — `search_messages` returns a `next_cursor` whose decoded `anchor_kind == 'cocoa_ts'` (W2 — the codec discriminator distinguishes the two tools' pagination semantics).
    - `test_read_chat_invalid_cursor_rejected()` — call `read_chat(chat_id=1, cursor="junk")`; assert structured `ValueError` (CursorError converted).
    - `test_read_chat_char_cap()` (against the `large_chat_fixture` from conftest with 5000 messages) — call `read_chat(chat_id=<large>, limit=200)`; assert response JSON length ≤ 60_000 chars; assert `next_cursor` is non-None and decodable to `(chat_id, valid_z_sort_float, 'z_sort')` (W2 — third tuple element is the anchor_kind discriminator).

    `tests/unit/test_tools/test_doctor_phase1.py` — Plan 05 DIAG-01 + DIAG-02:
    - `test_doctor_returns_8_field_report()` — `report = await doctor(); assert {f for f in DoctorReport.model_fields} >= {"full_disk_access", "automation_whatsapp", "accessibility", "db_path", "schema_fingerprint", "whatsapp_app_version", "last_message_ts", "coverage_summary"}`.
    - `test_doctor_diag02_fda_denied()` — monkeypatch `whatsapp_mcp.permissions.fda.check` to return `PermissionStatus(bucket="fda", state="denied", binary_path="/x", db_path="/y", system_settings_url="x", remediation="grant")`; call `doctor()`; assert it returns successfully (NO exception); `report.schema_fingerprint.state == "unreachable"`; `report.last_message_ts is None`; `report.coverage_summary.from_ts is None`.
    - `test_doctor_diag02_db_open_fails()` — monkeypatch `_probe_db_safely` (or `open_ro`) to raise `sqlite3.OperationalError("simulated")`; assert `doctor()` returns successfully with `schema_fingerprint.state == "unreachable"`.
    - `test_doctor_no_whatsapp_app()` — monkeypatch `_probe_whatsapp_version` to simulate `FileNotFoundError`; assert `report.whatsapp_app_version is None` AND `doctor()` does not raise.
    - `test_phase0_doctor_tool_invariant_preserved()` — `mcp.list_tools()` still returns ONE tool named `doctor` (D-08). The Phase 0 `tests/unit/test_doctor_tool.py` already asserts this; this test is a Phase 1 belt-and-braces.
    - `test_doctor_does_not_have_tool_level_timeout()` — introspect `doctor`'s wrapper chain; assert no `@timeout` wrapper present (Plan 05 explicitly forbids tool-level timeout on doctor; DIAG-02 partial-result risk).

    All tests use `pytest.mark.asyncio` implicitly (via `asyncio_mode=auto` in pyproject.toml) for the async ones. mypy strict + ruff full ruleset green.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_reader/ tests/unit/test_tools/ -v --tb=short &amp;&amp; uv run pytest -m "not live" 2&gt;&amp;1 | tail -5 &amp;&amp; uv run ruff check tests/unit/test_reader/ tests/unit/test_tools/ &amp;&amp; uv run ruff format --check tests/unit/test_reader/ tests/unit/test_tools/ &amp;&amp; uv run mypy tests/unit/test_reader/ tests/unit/test_tools/</automated>
  </verify>
  <acceptance_criteria>
    - All reader tests pass: `pytest tests/unit/test_reader/ -v` exits 0 with every test green.
    - All tool tests pass: `pytest tests/unit/test_tools/ -v` exits 0.
    - Concurrency stress test (`test_concurrent_reads_with_writer`) reports 100/100 successful reads concurrent with ~100 writer INSERTs; zero `database is locked` exceptions.
    - Tombstone predicate test exercises the four observed `ZFLAGS` patterns (0x01000000 normal; 0x05000000 / 0x05008000 / 0x05000180 with null text → tombstone) AND `ZMESSAGETYPE=14` → tombstone.
    - JID/LID dedup test demonstrates one logical contact returned even when seeded with two JID forms (phone + lid).
    - Path-traversal defense test asserts crafted `../../../etc/passwd` MEDIALOCALPATH returns `None`.
    - Char-cap test against 5000-msg fixture asserts response ≤ 60_000 chars + decodable `next_cursor`.
    - DIAG-02 test with mocked FDA-denied returns successful DoctorReport with `schema_fingerprint.state == "unreachable"`.
    - Total `pytest -m "not live"` count is 28 (Phase 0) + N (Plan 06's new tests) — all passing. Specific count documented in the SUMMARY but the gate is "all green", not a specific number.
    - Lint/type: ruff (full ruleset) + ruff format + mypy --strict green on all of `tests/unit/test_reader/` and `tests/unit/test_tools/`.
  </acceptance_criteria>
  <done>The reader and tool surfaces are codified by deterministic unit tests against synthetic fixtures; the P1/P3/P8/P9/P10/P11 mitigations are each verified; concurrency stress test passes; DIAG-02 defensive probing is mock-tested.</done>
</task>

<task type="auto">
  <name>Task 3: Live integration tests (RUN_LIVE=1 gated, maintainer-side smoke)</name>
  <files>tests/integration/test_live_reader.py</files>
  <read_first>
    .planning/phases/01-read-mvp-read-only/01-RESEARCH.md (sections: "Environment Availability" — what's available on the user's Mac; "Open Questions" — what to investigate against live data);
    tests/integration/test_live_doctor.py (Phase 0 — pattern to mirror: `@pytest.mark.live` + `RUN_LIVE` env-var skip; shape-correct asserts that stay green across content changes);
    .planning/phases/01-read-mvp-read-only/01-04-SUMMARY.md (which tools to smoke-test);
    .planning/phases/01-read-mvp-read-only/01-05-SUMMARY.md (expanded doctor shape)
  </read_first>
  <action>
    `tests/integration/test_live_reader.py` — mirror the Phase 0 `test_live_doctor.py` shape:
    - Module-level `@pytest.mark.live` decorator on every test (the `live` marker is configured in pyproject.toml).
    - Belt-and-braces `RUN_LIVE` env-var check at module scope: `pytestmark = [pytest.mark.live, pytest.mark.skipif(not os.environ.get("RUN_LIVE"), reason="set RUN_LIVE=1 to enable")]`.
    - Shape-correct asserts only — never value-correct (the user's data changes between runs).

    Tests:
    - `test_live_list_chats()` — `result = await tools.list_chats(limit=10)`; assert it returns a dict with `chats` key, `count` key; `len(result["chats"]) > 0` (the maintainer's Mac has hundreds of chats per Plan 02 reader's verified-live count); each chat has `chat_id`, `kind`, `display_name`, `coverage`.
    - `test_live_read_chat()` — call `list_chats(limit=5)` first, pick the most recent chat, call `read_chat(chat_id=that, limit=10)`; assert response shape; assert response JSON length ≤ 60_000 chars.
    - `test_live_extract_recent()` — call `extract_recent(chat_id=<recent>, hours=24)`; assert the human-readable `"asked Xh, have Yh"` summary is present per RESEARCH §"Pattern 4 → For extract_recent".
    - `test_live_search_messages()` — call `search_messages(query="hi", limit=5)`; assert shape (the LIKE search will likely return some results on a long history).
    - `test_live_search_contacts()` — call `search_contacts(query="a", limit=5)`; assert deduplication works on real data (no two Contacts in result share the same `phone` value).
    - `test_live_get_chat_metadata_for_group()` — find a group chat from `list_chats()`, call `get_chat_metadata(chat_id=that)`; assert `members` list non-empty.
    - `test_live_get_message_context()` — `read_chat(limit=5)` to get a stanza_id, call `get_message_context(message_id=that, before=2, after=2)`; assert window length ≤ 5.
    - `test_live_doctor_full_payload()` — `report = await doctor()`; assert (a) `report.full_disk_access.state == "granted"` (the maintainer's Mac has FDA), (b) `report.schema_fingerprint.state == "supported"` AND `observed_version == 1`, (c) `report.whatsapp_app_version` matches a `r"^\d+\.\d+\.\d+"` regex, (d) `report.last_message_ts` is non-None and within the last 7 days (sanity gate), (e) `report.coverage_summary.from_ts` is non-None and predates `to_ts`.
    - `test_live_concurrent_with_real_writer()` (OPTIONAL — only runs if WhatsApp.app is foregrounded by the test setup; mark `@pytest.mark.skipif(not is_whatsapp_foregrounded())`) — run 5 concurrent `await read_chat()` calls; assert all succeed without `database is locked`. This is the strongest evidence for REL-01 against the real writer; it is documented but does NOT block CI (it requires a running WhatsApp).

    Live test runtime budget: each test ≤ 5 seconds (per-tool timeout from REL-03). Full live suite ≤ 30 seconds on the maintainer's Mac.

    Document in the module docstring that:
    - Live tests are intentionally non-deterministic in content (the user's data changes); they assert shape and bounds, not values.
    - CI does NOT run live tests (`pytest -m "not live"` excludes them per Phase 0 `ci.yml`).
    - Maintainer runs `RUN_LIVE=1 uv run pytest -m live` before tagging a release.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_live_reader.py --collect-only 2&gt;&amp;1 | grep -E 'test_live' &amp;&amp; uv run pytest -m "not live" tests/integration/test_live_reader.py 2&gt;&amp;1 | grep -E 'deselected|skipped' &amp;&amp; uv run ruff check tests/integration/test_live_reader.py &amp;&amp; uv run ruff format --check tests/integration/test_live_reader.py &amp;&amp; uv run mypy tests/integration/test_live_reader.py &amp;&amp; (RUN_LIVE=1 uv run pytest tests/integration/test_live_reader.py -v --tb=short || echo 'live smoke MAY skip if WhatsApp.app absent or FDA denied — manual verification required')</automated>
  </verify>
  <acceptance_criteria>
    - File exists with ≥ 7 live tests (one per read tool + doctor; concurrent test optional).
    - `pytest --collect-only` lists all the new live tests.
    - `pytest -m "not live"` deselects all of them (CI safety).
    - `RUN_LIVE=1 pytest -m live` succeeds on the maintainer's Mac (skipped tests are acceptable for the optional concurrent test if WhatsApp.app is not foregrounded; the 7 base tests must pass when FDA is granted).
    - Every test uses shape-correct assertions only (no hardcoded values for chat counts / message counts / dates).
    - Lint/type: ruff (full ruleset) + ruff format + mypy --strict green.
    - The Phase 0 `tests/integration/test_live_doctor.py` still passes under `RUN_LIVE=1` — Plan 06 does not regress it.
  </acceptance_criteria>
  <done>The live integration smoke covers all 7 read tools + the expanded doctor; all tests are RUN_LIVE-gated; CI is unaffected (deselect on `-m "not live"`); maintainer can run `RUN_LIVE=1 uv run pytest -m live` before tagging v0.1.0 to validate the full Phase 1 user story end-to-end on real WhatsApp data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures ↔ tempfile sqlite | Tests create + write + delete tempfile DBs in `pytest`'s tmp_path; never touch the user's `~/Library/Group Containers/...ChatStorage.sqlite`. CLAUDE.md hard rule #3 enforced even in tests — `monkeypatch_paths` redirects every `resolve_*` to fixture paths. |
| `RUN_LIVE=1` integration tests ↔ user's real ChatStorage.sqlite | Read-only via the same `open_ro` Plan 02 ships; tests assert shape-correctness only, never write, never log full message bodies. The integration suite is opt-in (CI excludes it via `-m "not live"`). |
| Concurrency stress test ↔ tempfile sqlite | The writer thread writes to a TEST sqlite, NEVER to the user's WhatsApp DB. The test exercises sqlite's WAL concurrency primitives in isolation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | A test accidentally writes to the user's real ChatStorage.sqlite via misuse of `open_ro` | mitigate | `open_ro` opens with `?mode=ro` URI; SQLite refuses writes structurally. The unit test `test_open_ro_refuses_writes` codifies this; live tests use `monkeypatch_paths` for unit paths but the live suite explicitly uses the real path AND only invokes reader async functions (which never write). |
| T-06-02 | Information Disclosure | Live test prints full message bodies / JIDs to CI logs | mitigate | Live tests assert shape only (e.g. `len(messages) > 0`); never print or log message bodies / JIDs. The pyproject.toml per-file ignore allows `print` in tests but Plan 06's tests do not use it for payload data. |
| T-06-03 | Tampering | Concurrency test's writer thread crashes mid-test, leaving a half-written sqlite that masks `database is locked` errors | accept | The test uses `pytest.fixture(autouse=False)` per-test scoped tempfile; cleanup is automatic; even if the writer thread crashes, the next test starts with a fresh fixture. Rare flakiness is acceptable for a 100-iteration stress test; failures are surfaced via the `assert no_database_locked_errors` gate. |
| T-06-04 | Denial of Service | Live test suite hangs the CI runner if accidentally enabled | mitigate | `pytestmark = [pytest.mark.live, pytest.mark.skipif(not os.environ.get("RUN_LIVE"), reason="...")]` is module-scoped — every test is skipped under default CI. The `ci.yml` from Plan 0-05 invokes `pytest -m "not live"` which excludes the entire live module. |
| T-06-05 | Spoofing | Test fixture seeds rows that don't actually match the live WhatsApp schema, giving false-green test results | mitigate | The `chatstorage_fixture` schema is derived directly from RESEARCH §"Core Data Schema Essentials" (which is verified-live against the user's actual DB on 2026-05-13). The live integration suite (Task 3) is the second-line defense — if the fixture diverges from reality, the live tests fail on the maintainer's Mac before release. |
| T-06-06 | Repudiation | A test fails intermittently in CI and contributors can't reproduce it locally | mitigate | All non-live tests are deterministic (synthetic fixtures, no time-of-day dependencies, no random seeds outside test scope). The concurrency test uses fixed iteration counts (100) and a deterministic writer interval (10ms). Live tests are explicitly maintainer-only and don't run in CI. |
</threat_model>

<verification>
- All ~14 new test files exist; all pass under `pytest -m "not live"`.
- Phase 0 baseline of 28 tests still passes; Plan 06 adds new tests on top, never regresses existing.
- Concurrency stress test reports zero `database is locked` errors over 100 iterations.
- REL-05 isolation re-test (`tests/unit/test_isolation.py`) is now load-bearing — the AST walk has 10 reader files to inspect AND the new positive-whitelist test catches drift to non-allowed import targets.
- DIAG-02 codified via mocked FDA-denied / DB-open-failure tests on `doctor()`.
- `--read-only` subprocess smoke test confirms 8 tools (doctor + 7 reads) all `readOnlyHint=True` after a real JSON-RPC handshake.
- Live integration suite (`RUN_LIVE=1 pytest -m live`) passes on the maintainer's Mac; `pytest -m "not live"` deselects the entire live module on CI.
- ruff full ruleset + ruff format --check + mypy --strict green on all of `tests/`.
</verification>

<success_criteria>
- The 5 ROADMAP §"Phase 1" success criteria are now empirically backed by tests:
  1. `--read-only` lists exactly the 8 read tools with `readOnlyHint=True` → `tests/unit/test_read_only_mode.py::test_read_only_lists_only_read_tools`.
  2. `read_chat` on a busy chat returns paginated ≤ 60k JSON within 5s with `next_cursor` and `coverage` → `tests/unit/test_tools/test_read_tools_registration.py::test_read_chat_char_cap` + the `@timeout(seconds=5)` decorator wrapping the body.
  3. `extract_recent` against a group returns deterministic, JID/LID-deduped messages with Cocoa→Unix timestamps, defaults `include_deleted=False`, surfaces media as `MediaRef` → `tests/unit/test_reader/test_chats_messages.py::test_window_default_filters_tombstones` + `test_resolve_media_ref_returns_absolute_path` + the live `test_live_extract_recent` smoke.
  4. `doctor` returns the structured preflight (DB path / FDA / Automation / Accessibility / schema fingerprint OK / WhatsApp.app version / last-message timestamp / coverage summary) and remains callable when permissions are missing → `tests/unit/test_tools/test_doctor_phase1.py::test_doctor_returns_8_field_report` + `test_doctor_diag02_fda_denied` + the live `test_live_doctor_full_payload`.
  5. Reader and Sender modules MUST NOT import each other; concurrent reads succeed without `database is locked` → `tests/unit/test_isolation.py` (load-bearing now) + `tests/unit/test_reader/test_concurrency.py::test_concurrent_reads_with_writer`.
- Phase 1 transitions from "planned" to "ready for /gsd-verify-work" — every locked invariant from Plans 01-05 is exercised; the live smoke is the maintainer's pre-tag gate.
</success_criteria>

<output>
After completion, create `.planning/phases/01-read-mvp-read-only/01-06-SUMMARY.md` documenting (a) the new test count and per-area breakdown (model / time / cursor / paths / reader / tool / read-only / live), (b) the concurrency stress test's empirical result (writes/sec achieved + reads/sec sustained without lock errors), (c) the JID/LID dedup test's empirical result on the synthetic fixture, (d) the live smoke's last successful invocation timestamp on the maintainer's Mac, (e) any tests that the executor had to mark `@pytest.mark.xfail` (with documented reason — should be zero in a clean Phase 1 ship). Phase 1 is now ready for `/gsd-verify-work` to validate the full ROADMAP §"Phase 1" success criteria end-to-end before transitioning STATE.md / ROADMAP.md to "Phase 1: complete".
</output>
</content>
</invoke>