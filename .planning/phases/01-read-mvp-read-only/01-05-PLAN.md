---
phase: 01-read-mvp-read-only
plan: 5
title: "Doctor expansion: DB path + schema fingerprint + WhatsApp.app version + last-message ts + coverage summary"
type: execute
wave: 3
depends_on: [1, 2]
requirements: [DIAG-01, DIAG-02]
files_modified:
  - src/whatsapp_desktop_mcp/models/doctor.py
  - src/whatsapp_desktop_mcp/tools/doctor.py
autonomous: true
must_haves:
  truths:
    - "`doctor()` returns a `DoctorReport` whose three Phase-0 PermissionStatus fields (full_disk_access, automation_whatsapp, accessibility) are byte-stable with Phase 0 — same field names, same Literal enums, same payload shape"
    - "`doctor()` returns five new fields (DIAG-01): `db_path`, `schema_fingerprint`, `whatsapp_app_version`, `last_message_ts`, `coverage_summary`"
    - "`doctor()` remains callable when FDA is denied (DIAG-02): the schema/last-message/coverage probes are wrapped in try/except; failures degrade their fields to `null` / `state=\"unreachable\"` annotations rather than raising"
    - "`schema_fingerprint.state == \"supported\"` when the live `Z_VERSION` is in `SUPPORTED_VERSIONS = {1}` per Plan 02; `\"unsupported\"` when out of range with `observed_version` populated and runbook remediation; `\"unreachable\"` when FDA denied or DB absent"
    - "`whatsapp_app_version` reads `/Applications/WhatsApp.app/Contents/Info.plist[CFBundleShortVersionString]` via `plistlib.load` dispatched through `asyncio.to_thread` (REL-02); returns `None` when WhatsApp.app is not installed"
    - "`last_message_ts` calls `reader.latest_timestamp()` and surfaces the Unix timestamp of the most recent message across all chats; `None` when DB unreadable or empty"
    - "`coverage_summary` is a global `Coverage(from_ts=earliest message ts across all chats, to_ts=latest, asked_window_seconds=None, have_window_seconds=to_ts-from_ts, is_full=False)` — the cache-vs-truth disclosure at the global scope"
    - "Phase 0 tool annotations preserved: `@mcp.tool(name=\"doctor\", title=..., description=..., annotations=ToolAnnotations(readOnlyHint=True, destructiveHint=False, idempotentHint=True, openWorldHint=False))` — the Phase 0 D-08 invariant that exactly one tool registration named `doctor` exists in `tools/doctor.py` is preserved"
    - "Doctor decorator gains `meta={\"anthropic/maxResultSizeChars\": 60000}` per W1 (READ-09 applies to every tool with no carve-outs); doctor remains the ONLY tool with no outer `@timeout` wrapper per W3 + DIAG-02 (per-probe defenses; outer wrapper would mask partial-result risk)"
  artifacts:
    - path: "src/whatsapp_desktop_mcp/models/doctor.py"
      provides: "Extended DoctorReport with 5 new fields + new SchemaFingerprint Pydantic model"
      contains: "class SchemaFingerprint"
    - path: "src/whatsapp_desktop_mcp/tools/doctor.py"
      provides: "Expanded doctor tool with defensive probing for DIAG-02"
      contains: "_probe_db_safely"
  key_links:
    - from: "tools/doctor.py"
      to: "reader.latest_timestamp + reader.open_ro + reader.probe_z_version + paths.resolve_chatstorage_path"
      via: "defensive db probe (only when FDA granted)"
      pattern: "from whatsapp_desktop_mcp\\.reader"
    - from: "tools/doctor.py"
      to: "stdlib plistlib (WhatsApp.app version probe)"
      via: "asyncio.to_thread(_probe_whatsapp_version_blocking)"
      pattern: "plistlib"
---

<objective>
Expand Phase 0's `doctor` tool from a 3-field permission report to a full
preflight per DIAG-01: add DB path resolved + schema fingerprint OK +
WhatsApp.app version detected + last-message timestamp + coverage summary,
while preserving the Phase 0 byte-stable `PermissionStatus` fields and the
single-tool-named-`doctor` invariant (D-08).

Critical DIAG-02 invariant: `doctor` MUST remain callable even when other
tools would fail. Each new probe is wrapped in its own try/except; a failed
probe sets the related field to `null` / `state="unreachable"` annotation,
NEVER raises. This is the diagnostic path — it must never crash.

Purpose: A user with a freshly-installed `whatsapp-desktop-mcp` and any combination
of permission failures should be able to call `doctor` and get a single
JSON payload that names every actionable next step (which permissions to
grant, which binary to grant them to, what schema version was observed,
what the local DB's coverage window is).

Output: 5 new fields on `DoctorReport` + 1 new `SchemaFingerprint` Pydantic
model in `models/doctor.py`; expanded async body in `tools/doctor.py`. The
3 Phase-0 permission probes (`fda.check`, `automation.check_whatsapp`,
`accessibility.check`) and the existing `@mcp.tool(...)` registration block
are preserved verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-read-mvp-read-only/01-RESEARCH.md
@.planning/phases/01-read-mvp-read-only/01-01-SUMMARY.md
@.planning/phases/01-read-mvp-read-only/01-02-SUMMARY.md
@.planning/phases/00-setup-and-permissions-skeleton/00-CONTEXT.md
@CLAUDE.md
@src/whatsapp_desktop_mcp/tools/doctor.py
@src/whatsapp_desktop_mcp/models/doctor.py
@src/whatsapp_desktop_mcp/permissions/fda.py
@src/whatsapp_desktop_mcp/permissions/automation.py
@src/whatsapp_desktop_mcp/permissions/accessibility.py

<interfaces>
<!-- Existing Phase 0 surface this plan EXTENDS (does NOT rewrite). -->

From src/whatsapp_desktop_mcp/models/doctor.py (Phase 0 — byte-stable; Plan 01 did NOT touch this):
```python
PermissionState = Literal["granted", "denied", "whatsapp_not_installed"]
PermissionBucket = Literal["fda", "automation", "accessibility"]
class PermissionStatus(BaseModel):
    bucket: PermissionBucket
    state: PermissionState
    binary_path: str
    db_path: str | None = None
    system_settings_url: str
    remediation: str = ""
class DoctorReport(BaseModel):
    full_disk_access: PermissionStatus
    automation_whatsapp: PermissionStatus
    accessibility: PermissionStatus
    @property
    def all_granted(self) -> bool: ...
```
This plan APPENDS 5 fields to `DoctorReport` and adds a new `SchemaFingerprint` model. The 3 existing fields and the `all_granted` property are preserved EXACTLY (D-07 freeze + the Phase 0 test_doctor_tool.py expectation).

From src/whatsapp_desktop_mcp/tools/doctor.py (Phase 0):
```python
@mcp.tool(
    name="doctor",
    title="Doctor — preflight permission check",
    description="...",
    annotations=ToolAnnotations(readOnlyHint=True, destructiveHint=False, idempotentHint=True, openWorldHint=False),
)
async def doctor() -> DoctorReport:
    return DoctorReport(
        full_disk_access=await fda.check(),
        automation_whatsapp=await automation.check_whatsapp(),
        accessibility=await accessibility.check(),
    )
```
This plan EXTENDS the body. The `@mcp.tool(...)` registration block is preserved exactly. The existing 3-probe sequence is preserved exactly (only the constructor argument set grows). Plan 03 already confirmed `mcp.list_tools()` returns exactly the existing tools — Plan 04 added 7 read tools — Plan 05 must NOT add a second tool registration; it expands the EXISTING doctor tool.

From src/whatsapp_desktop_mcp/reader/ (Plan 02):
```python
async def latest_timestamp() -> int | None
def open_ro(db_path) -> contextmanager  # synchronous context manager (used inside _blocking helpers via to_thread)
def probe_z_version(conn) -> int
SUPPORTED_VERSIONS: frozenset[int] = frozenset({1})
def is_supported(version: int) -> bool
async def list_chats(...) -> list[Chat]   # carries per-chat coverage; doctor aggregates to global
```

From src/whatsapp_desktop_mcp/paths.py (Phase 0 + Plan 01):
```python
def resolve_chatstorage_path() -> str
def resolve_lid_path() -> str
def resolve_contactsv2_path() -> str
def resolve_media_root() -> str
```

From src/whatsapp_desktop_mcp/models/coverage.py (Plan 01):
```python
class Coverage(BaseModel):
    from_ts: int | None
    to_ts: int | None
    asked_window_seconds: int | None = None
    have_window_seconds: int | None = None
    is_full: bool
```

From src/whatsapp_desktop_mcp/permissions/ (Phase 0):
```python
async def fda.check() -> PermissionStatus
async def automation.check_whatsapp() -> PermissionStatus
async def accessibility.check() -> PermissionStatus
```

External: `plistlib` (stdlib) for `/Applications/WhatsApp.app/Contents/Info.plist[CFBundleShortVersionString]` — verified live returns `"26.16.74"` on the user's Mac per RESEARCH §"Doctor Expansion → _probe_whatsapp_version".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend DoctorReport with 5 new fields + SchemaFingerprint model</name>
  <files>src/whatsapp_desktop_mcp/models/doctor.py</files>
  <read_first>
    .planning/phases/01-read-mvp-read-only/01-RESEARCH.md (section: "Doctor Expansion (DIAG-01, DIAG-02) — extended DoctorReport block + SchemaFingerprint block");
    src/whatsapp_desktop_mcp/models/doctor.py (existing — preserve byte-stable Phase 0 fields);
    src/whatsapp_desktop_mcp/models/coverage.py (Plan 01 — Coverage shape)
  </read_first>
  <action>
    Modify `src/whatsapp_desktop_mcp/models/doctor.py`. Two additions:

    1. **Add `SchemaFingerprint` Pydantic model** above `DoctorReport`. Per RESEARCH §"Doctor Expansion → SchemaFingerprint block":
       ```
       SchemaState = Literal["supported", "unsupported", "unreachable"]

       class SchemaFingerprint(BaseModel):
           """Schema-version probe result (REL-04 + DIAG-01).

           - state="supported": observed_version in SUPPORTED_VERSIONS set per reader.schema_v1
           - state="unsupported": observed_version is set but outside the supported range; remediation field carries the upgrade runbook hint
           - state="unreachable": DB couldn't be opened (FDA denied, file missing, or schema query failed); observed_version is None
           """
           state: SchemaState
           observed_version: int | None
           supported_versions: list[int]   # sorted snapshot of SUPPORTED_VERSIONS (frozenset is not JSON-serializable; convert to sorted list)
           remediation: str = ""
       ```

    2. **Extend `DoctorReport` with 5 new fields** AFTER the existing 3 PermissionStatus fields and BEFORE the `all_granted` property. Per RESEARCH §"Doctor Expansion → extended DoctorReport block":
       ```
       # Phase 1 ADDITIONS (DIAG-01)
       db_path: str = Field(
           description="Resolved absolute path to ChatStorage.sqlite. May not exist on disk if FDA denied; doctor still returns it so the user knows what to grant access to.",
       )
       schema_fingerprint: SchemaFingerprint = Field(
           description="Z_VERSION probe result + supported-versions snapshot + degraded-mode remediation (REL-04).",
       )
       whatsapp_app_version: str | None = Field(
           default=None,
           description="CFBundleShortVersionString from /Applications/WhatsApp.app/Contents/Info.plist; null when WhatsApp.app is not installed.",
       )
       last_message_ts: int | None = Field(
           default=None,
           description="Unix timestamp of the latest message across all chats. null when DB unreadable or empty.",
       )
       coverage_summary: "Coverage" = Field(
           description="Global cache coverage across all chats (P1 cache-vs-truth disclosure at the doctor level).",
       )
       ```
       Use `from whatsapp_desktop_mcp.models.coverage import Coverage` at the top of the module.

    Preserve EXACTLY:
    - The 3 existing fields `full_disk_access`, `automation_whatsapp`, `accessibility` (D-07, byte-stable).
    - The `@property def all_granted(self) -> bool: ...` (Phase 0 test asserts this exact behavior; computed across the 3 PermissionStatus fields, NOT across the new schema/version fields — DIAG-02 mandates `all_granted` reflects only TCC permissions, not schema reachability).
    - The module docstring's "frozen public surface" framing — extend it to mention the Phase 1 additions (and that Phase 1 additions are also now frozen for downstream callers).
    - The `PermissionState` and `PermissionBucket` Literal aliases (Phase 0 — Phase 1 imports them).

    Add the new `SchemaState` Literal alias and re-export it via `models/__init__.py` (Plan 01 set up the re-exports — extend the `__all__` list to include `SchemaFingerprint, SchemaState`).

    The new field types use modern Python 3.12 syntax (`int | None`, `str | None`, no `from typing import Optional`). mypy --strict + ruff full ruleset must pass.
  </action>
  <verify>
    <automated>uv run python -c "from whatsapp_desktop_mcp.models.doctor import DoctorReport, PermissionStatus, SchemaFingerprint, SchemaState, PermissionState, PermissionBucket; from whatsapp_desktop_mcp.models.coverage import Coverage; ps = PermissionStatus(bucket='fda', state='granted', binary_path='/x', system_settings_url='x-apple:'); cov = Coverage(from_ts=1, to_ts=2, is_full=False); sf = SchemaFingerprint(state='supported', observed_version=1, supported_versions=[1]); r = DoctorReport(full_disk_access=ps, automation_whatsapp=ps, accessibility=ps, db_path='/x/ChatStorage.sqlite', schema_fingerprint=sf, whatsapp_app_version='26.16.74', last_message_ts=1747140000, coverage_summary=cov); assert r.all_granted is True; assert r.schema_fingerprint.state == 'supported'; print('OK', r.model_dump_json()[:200])" &amp;&amp; uv run python -c "from whatsapp_desktop_mcp.models import SchemaFingerprint, SchemaState, DoctorReport, PermissionStatus, Coverage; print('re-exports OK')" &amp;&amp; uv run ruff check src/whatsapp_desktop_mcp/models/doctor.py src/whatsapp_desktop_mcp/models/__init__.py &amp;&amp; uv run ruff format --check src/whatsapp_desktop_mcp/models/doctor.py src/whatsapp_desktop_mcp/models/__init__.py &amp;&amp; uv run mypy src/whatsapp_desktop_mcp/models/ &amp;&amp; uv run pytest tests/unit/test_doctor_tool.py tests/unit/test_exceptions.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -cE '^class SchemaFingerprint\(BaseModel\):' src/whatsapp_desktop_mcp/models/doctor.py` returns exactly 1.
    - Source: `grep -cE '^SchemaState\s*=\s*Literal\[' src/whatsapp_desktop_mcp/models/doctor.py` returns 1.
    - Source: `grep -cE 'db_path:\s*str' src/whatsapp_desktop_mcp/models/doctor.py` returns ≥ 2 (one on PermissionStatus from Phase 0, one on DoctorReport from this plan).
    - Source: `grep -cE 'schema_fingerprint:\s*SchemaFingerprint' src/whatsapp_desktop_mcp/models/doctor.py` returns 1.
    - Source: `grep -cE 'whatsapp_app_version:\s*str\s*\|\s*None' src/whatsapp_desktop_mcp/models/doctor.py` returns 1.
    - Source: `grep -cE 'last_message_ts:\s*int\s*\|\s*None' src/whatsapp_desktop_mcp/models/doctor.py` returns 1.
    - Source: `grep -cE 'coverage_summary:' src/whatsapp_desktop_mcp/models/doctor.py` returns 1.
    - Source: `grep -cE '@property\s*$' src/whatsapp_desktop_mcp/models/doctor.py` returns ≥ 1 (Phase 0 `all_granted` property preserved); `grep -cE 'def all_granted\(self\)' src/whatsapp_desktop_mcp/models/doctor.py` returns 1.
    - Source: `grep -cE 'PermissionState\s*=\s*Literal\[' src/whatsapp_desktop_mcp/models/doctor.py` returns 1 (Phase 0 alias preserved).
    - Behavior: instantiating `DoctorReport(...)` with all 8 fields succeeds; `model_dump_json()` produces valid JSON; `all_granted` is True iff all 3 PermissionStatus.state are "granted" (Phase 0 invariant — does NOT consider the new schema/version fields).
    - Behavior: `from whatsapp_desktop_mcp.models import SchemaFingerprint, SchemaState` works (Plan 01's `__init__.py` extended by this task).
    - Phase 0 regression: `tests/unit/test_doctor_tool.py` and `tests/unit/test_exceptions.py` still pass (those tests assert the 3 existing fields + the `all_granted` property — adding fields does not break them).
    - Lint/type: ruff (full ruleset) + ruff format + mypy --strict green.
  </acceptance_criteria>
  <done>`DoctorReport` carries 5 new fields per DIAG-01; `SchemaFingerprint` model defined with the 3-state Literal; `models/__init__.py` re-exports the new names; Phase 0 byte-stable surface preserved; lint/type green.</done>
</task>

<task type="auto">
  <name>Task 2: Extend tools/doctor.py with defensive probes (DB path, schema, WA version, last ts, coverage)</name>
  <files>src/whatsapp_desktop_mcp/tools/doctor.py</files>
  <read_first>
    .planning/phases/01-read-mvp-read-only/01-RESEARCH.md (sections: "Doctor Expansion (DIAG-01, DIAG-02)" — full sketch + defensive probing rationale; "Pattern 1: Short-lived RO WAL Connection"; "Pattern 3: Schema Fingerprint Probe");
    src/whatsapp_desktop_mcp/tools/doctor.py (existing — preserve `@mcp.tool(...)` block AND the existing 3-probe sequence inside the body);
    src/whatsapp_desktop_mcp/models/doctor.py (Task 1 — extended DoctorReport surface);
    .planning/phases/01-read-mvp-read-only/01-02-SUMMARY.md (Plan 02's reader API contract for `latest_timestamp`, `open_ro`, `probe_z_version`, `SUPPORTED_VERSIONS`)
  </read_first>
  <action>
    Modify `src/whatsapp_desktop_mcp/tools/doctor.py`. Three additions, one preservation:

    1. **Preserve** the existing `@mcp.tool(...)` decorator block exactly (D-08 invariant: exactly one tool named `doctor` registered; `readOnlyHint=True`, `destructiveHint=False`, `idempotentHint=True`, `openWorldHint=False`). **ADD** the `meta={"anthropic/maxResultSizeChars": 60000}` argument to the `@mcp.tool(...)` decorator (W1 fix per checker — READ-09 says "every read tool" with no carve-out; doctor is a read tool). The doctor payload is small (~1 KB) so honoring the cap is a no-op in practice, but the annotation must be uniform across all 8 tools for the Plan 06 "every tool" assertion to pass. **Do NOT add a `@timeout(seconds=N)` decorator from Plan 04** — RESEARCH §"Per-Tool Timeouts (REL-03)" (post-W3 revision) explicitly removes `doctor`'s outer timeout row and substitutes a per-probe defense rationale: the existing Phase 0 probes carry their own 3-second osascript timeouts (D-10), the new `_probe_db_safely` and `_probe_whatsapp_version` helpers each own their try/except + bounded I/O, and wrapping the whole doctor body in a tool-level timeout would fire mid-probe and surface a partial DoctorReport that violates DIAG-02 ("`doctor` MUST remain callable when other tools would fail"). The deviation from the original "every tool gets `@timeout`" framing is therefore intentional and codified in RESEARCH.

    2. **Add helper functions** as private module-level coroutines (NOT decorators, NOT registered as separate tools):
       - `async def _probe_whatsapp_version() -> str | None` — uses `asyncio.to_thread(_probe_whatsapp_version_blocking)`. The blocking helper opens `/Applications/WhatsApp.app/Contents/Info.plist` via `plistlib.load(path.open("rb"))` and returns `data.get("CFBundleShortVersionString")` as `str | None`. Wraps the open in try/except `(FileNotFoundError, KeyError, plistlib.InvalidFileException)` and returns `None` on any failure (DIAG-02 — never raise from doctor).
       - `async def _probe_db_safely(db_path: str) -> tuple[SchemaFingerprint, int | None, Coverage]` — runs the schema fingerprint probe + last-message-ts probe + global coverage aggregation in a single short-lived RO connection. Wrapped in try/except `(sqlite3.OperationalError, sqlite3.DatabaseError, FileNotFoundError, PermissionError)`; on any failure, returns the "unreachable" fingerprint + None + empty coverage shape per RESEARCH §"Doctor Expansion → tools/doctor.py sketch":
         ```
         (SchemaFingerprint(state="unreachable", observed_version=None, supported_versions=sorted(SUPPORTED_VERSIONS),
                            remediation="Cannot read DB until Full Disk Access is granted to the running binary."),
          None,
          Coverage(from_ts=None, to_ts=None, is_full=False))
         ```
         On success: opens the RO connection via `await asyncio.to_thread(_probe_db_blocking, db_path)` which inside one `with open_ro(db_path) as conn:` block runs (a) `probe_z_version(conn)`, (b) `SELECT MIN(ZMESSAGEDATE), MAX(ZMESSAGEDATE) FROM ZWAMESSAGE`, (c) maps to `Coverage(from_ts=cocoa_to_unix(min) if min else None, to_ts=cocoa_to_unix(max) if max else None, ...)`. The Z_VERSION value populates `SchemaFingerprint`: if `is_supported(v)` → `state="supported"`, else `state="unsupported"` with `remediation="Schema version {v} not in supported set {sorted(SUPPORTED_VERSIONS)}. Open a bug report with the doctor output."`.
         The `last_message_ts` is just `to_ts` from the coverage probe (returned as the second tuple element).

    3. **Modify the `doctor()` body** to call all three Phase 0 probes (preserved exactly) AND the two new probes, then construct `DoctorReport` with all 8 fields:
       ```
       async def doctor() -> DoctorReport:
           # Phase 0 probes (PRESERVED EXACTLY — order + concurrency unchanged)
           fda_status = await fda.check()
           automation_status = await automation.check_whatsapp()
           accessibility_status = await accessibility.check()

           # Phase 1 additions (DIAG-01 + DIAG-02 defensive probing)
           db_path = resolve_chatstorage_path()
           if fda_status.state == "granted":
               schema_fp, last_ts, coverage = await _probe_db_safely(db_path)
           else:
               schema_fp = SchemaFingerprint(state="unreachable", observed_version=None,
                                             supported_versions=sorted(SUPPORTED_VERSIONS),
                                             remediation="Grant Full Disk Access to read the WhatsApp DB; until then schema/coverage cannot be probed.")
               last_ts = None
               coverage = Coverage(from_ts=None, to_ts=None, is_full=False)

           wa_version = await _probe_whatsapp_version()  # always tries — Info.plist read does NOT need FDA

           return DoctorReport(
               full_disk_access=fda_status,
               automation_whatsapp=automation_status,
               accessibility=accessibility_status,
               db_path=db_path,
               schema_fingerprint=schema_fp,
               whatsapp_app_version=wa_version,
               last_message_ts=last_ts,
               coverage_summary=coverage,
           )
       ```

    DIAG-02 compliance: every new probe is wrapped in try/except inside its `_probe_*` helper; the doctor() body NEVER does an unwrapped network/file/sqlite call that could raise. The osascript probes are already DIAG-02-safe per Phase 0's D-10 (3-second timeouts + structured PermissionStatus on every error). The `_probe_whatsapp_version` is conservative — runs even when FDA is denied because reading `/Applications/WhatsApp.app/Contents/Info.plist` does NOT require FDA (it's a public app bundle resource). The `_probe_db_safely` is the only probe gated on FDA, because its first action is opening the user's WA SQLite store.

    Update the existing module docstring to describe the Phase 1 expansion (the 3 probes are now 5 probes; DIAG-02 invariant is enforced by per-probe try/except; the `@mcp.tool` block is unchanged from Phase 0). Preserve the Phase 0 P-PHASE0-06 import-order invariant comment (the `from whatsapp_desktop_mcp.server import mcp` line and its rationale).

    Add the new imports: `import plistlib`, `import sqlite3` (for the OperationalError/DatabaseError catch), `from whatsapp_desktop_mcp import reader` OR explicit imports of the names used (`from whatsapp_desktop_mcp.reader.connection import open_ro`, `from whatsapp_desktop_mcp.reader.schema_v1 import probe_z_version, SUPPORTED_VERSIONS, is_supported`), `from whatsapp_desktop_mcp.paths import resolve_chatstorage_path`, `from whatsapp_desktop_mcp.models.doctor import DoctorReport, SchemaFingerprint`, `from whatsapp_desktop_mcp.models.coverage import Coverage`, `from whatsapp_desktop_mcp.time import cocoa_to_unix`.

    REL-05: ensure no `from whatsapp_desktop_mcp.sender ...` import sneaks in. Tools/doctor.py is in the `tools/` package, not `reader/`, so it CAN import from reader/ — but it MUST NOT import from sender/ (Phase 1 sender is empty; the rule preserves Phase 0 isolation).
  </action>
  <verify>
    <automated>uv run python -c "import asyncio; from whatsapp_desktop_mcp.tools.doctor import doctor; from whatsapp_desktop_mcp.models.doctor import DoctorReport, SchemaFingerprint; r = asyncio.run(doctor()); assert isinstance(r, DoctorReport); assert isinstance(r.schema_fingerprint, SchemaFingerprint); assert r.db_path.endswith('/ChatStorage.sqlite'); assert r.schema_fingerprint.state in {'supported', 'unsupported', 'unreachable'}; print('doctor live OK', r.schema_fingerprint.state, 'wa_version', r.whatsapp_app_version, 'last_ts', r.last_message_ts)" &amp;&amp; uv run python -c "import asyncio; from whatsapp_desktop_mcp.server import mcp; tools = asyncio.run(mcp.list_tools()); doctors = [t for t in tools if t.name == 'doctor']; assert len(doctors) == 1, f'expected exactly 1 doctor tool, got {len(doctors)}'; assert doctors[0].annotations.readOnlyHint is True; print('one doctor tool OK', doctors[0].annotations)" &amp;&amp; bash -c '! grep -nE "from whatsapp_desktop_mcp\.sender|import whatsapp_desktop_mcp\.sender" src/whatsapp_desktop_mcp/tools/doctor.py' &amp;&amp; uv run ruff check src/whatsapp_desktop_mcp/tools/doctor.py &amp;&amp; uv run ruff format --check src/whatsapp_desktop_mcp/tools/doctor.py &amp;&amp; uv run mypy src/whatsapp_desktop_mcp/tools/doctor.py &amp;&amp; uv run pytest -m "not live"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -cE '^@mcp\.tool\(' src/whatsapp_desktop_mcp/tools/doctor.py` returns exactly 1 (D-08: still exactly one tool registration).
    - Source: `grep -cE '^async def doctor\(\)\s*-&gt;\s*DoctorReport' src/whatsapp_desktop_mcp/tools/doctor.py` returns exactly 1.
    - Source: `grep -cE 'async def _probe_whatsapp_version\(' src/whatsapp_desktop_mcp/tools/doctor.py` returns 1.
    - Source: `grep -cE 'async def _probe_db_safely\(' src/whatsapp_desktop_mcp/tools/doctor.py` returns 1.
    - Source: `grep -cE 'plistlib' src/whatsapp_desktop_mcp/tools/doctor.py` returns ≥ 1.
    - Source: `grep -cE 'asyncio\.to_thread' src/whatsapp_desktop_mcp/tools/doctor.py` returns ≥ 2 (one for plistlib, one for sqlite probe).
    - Source: `! grep -nE 'from whatsapp_desktop_mcp\.sender|import whatsapp_desktop_mcp\.sender' src/whatsapp_desktop_mcp/tools/doctor.py` succeeds (REL-05).
    - Source: `grep -cE 'try:' src/whatsapp_desktop_mcp/tools/doctor.py` returns ≥ 2 (defensive try/except in each new probe — DIAG-02).
    - Source: `grep -cE 'readOnlyHint=True' src/whatsapp_desktop_mcp/tools/doctor.py` returns 1 (Phase 0 annotation preserved).
    - Source (W1): `grep -cE 'anthropic/maxResultSizeChars' src/whatsapp_desktop_mcp/tools/doctor.py` returns 1 (doctor now carries the meta cap; READ-09 uniform across all 8 tools).
    - Source (W3): `! grep -nE '@timeout\(' src/whatsapp_desktop_mcp/tools/doctor.py` succeeds (doctor deliberately has no outer @timeout — DIAG-02 partial-result invariant).
    - Behavior: `await doctor()` returns a `DoctorReport` with 8 populated fields; on the user's Mac (FDA granted) `schema_fingerprint.state == "supported"` and `whatsapp_app_version == "26.16.74"` (verified live RESEARCH §"Doctor Expansion → _probe_whatsapp_version").
    - Behavior: `mcp.list_tools()` STILL returns exactly the same 8 tool names as after Plan 04 (doctor + 7 reads); no second `doctor` registration accidentally created.
    - Behavior: with FDA simulated as denied (mock fda.check), `await doctor()` STILL returns successfully — `schema_fingerprint.state == "unreachable"`, `last_message_ts is None`, `coverage_summary` has all-None timestamps. NO exception propagates (DIAG-02 invariant — Plan 06's test_doctor_diag02 codifies this with a mock).
    - Phase 0 regression: full `uv run pytest -m "not live"` returns 28 passed (Plan 06 will add new tests; Plan 05 itself adds none).
    - Lint/type: ruff (full ruleset) + ruff format + mypy --strict green on the file.
  </acceptance_criteria>
  <done>doctor returns the full DIAG-01 payload; defensive probing per DIAG-02 ensures the tool remains callable when FDA / WhatsApp.app / DB schema are unavailable; Phase 0 single-tool invariant preserved; live invocation on user's Mac returns the expected schema_fingerprint=supported + wa_version=26.16.74 shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `tools/doctor.py` ↔ `/Applications/WhatsApp.app/Contents/Info.plist` | Read-only file open via stdlib `plistlib`; the file is owned by the WhatsApp installer and signed by team `57T9237FN3` (verified live). No write, no execution. |
| `tools/doctor.py` ↔ `ChatStorage.sqlite` (only when FDA granted) | Same boundary as Plan 02 reader; uses the same `open_ro` short-lived RO connection per Pattern 1. |
| `doctor()` response → MCP client | Discloses `db_path`, `whatsapp_app_version`, `last_message_ts`, `coverage_summary` (timestamps), `schema_fingerprint.observed_version`. None of these are PII beyond what the user already shares with Claude Desktop by installing the MCP. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | A schema-version probe failure crashes `doctor` mid-call, blinding the user's diagnostic path | mitigate (DIAG-02) | Each new probe in `_probe_db_safely` and `_probe_whatsapp_version` is wrapped in its own try/except; failures degrade the field to `null` / `state="unreachable"` instead of raising. Plan 06's `test_doctor_diag02` codifies this with mocked failures. |
| T-05-02 | Tampering | Crafted `Info.plist` with malicious payload (e.g. plist bomb) crashes `plistlib.load` | mitigate | `plistlib.load` is stdlib and accepts only well-formed plists; the worst case is `plistlib.InvalidFileException` which the wrapper catches and returns `None`. The `/Applications/WhatsApp.app` directory is system-protected; only an attacker with root could plant a malicious plist there, in which case the user has bigger problems than `doctor` returning a wrong version string. |
| T-05-03 | Information Disclosure | `doctor` reveals exact `db_path` to an LLM that could exfiltrate it | accept | The path is `~/Library/Group Containers/group.net.whatsapp.WhatsApp.shared/ChatStorage.sqlite` — well-known per WhatsApp forensics literature; not secret. The user gave the LLM file-system access by granting FDA to the binary. |
| T-05-04 | Spoofing | An attacker installs a fake WhatsApp.app at `/Applications/WhatsApp.app` to spoof the version | accept | Out of scope — placing files in `/Applications` requires admin privileges; if achieved, every other defense is moot. `doctor`'s job is informational, not authentication. |
| T-05-05 | Tampering | Future Plan 04's `@timeout` decorator is accidentally added to `doctor()`, breaking DIAG-02 by introducing a tool-level timeout that could partial-result | mitigate | This plan explicitly does NOT add `@timeout` to doctor (W3 codified — RESEARCH §"Per-Tool Timeouts" no longer lists a doctor row). The rationale is documented inline in the docstring; Plan 06's `test_doctor_does_not_have_tool_level_timeout` asserts `doctor.__wrapped__` lacks a `_timeout_seconds` attribute (or the equivalent introspection on the decorator chain). The W1 `meta` annotation does NOT introduce timeout semantics — it is purely a client-side cap hint. |
| T-05-06 | Elevation of Privilege | `_probe_db_safely` accidentally writes to ChatStorage.sqlite by reusing the open_ro connection in the wrong mode | mitigate | `open_ro` opens with `?mode=ro` URI flag — SQLite refuses writes structurally. Plan 02's grep gate `grep -cE 'immutable' src/whatsapp_desktop_mcp/reader/connection.py == 0` ensures this remains true. |
| T-05-07 | Repudiation | A schema mismatch in production silently degrades doctor without surfacing to the user | mitigate | `SchemaFingerprint.remediation` carries an actionable message including the observed version and the supported set; the LLM passes this to the user. Phase 1 SUMMARY documents the upgrade runbook for "schema version not in supported set" reports. |
</threat_model>

<verification>
- `models/doctor.py` carries 8 DoctorReport fields total (3 PermissionStatus from Phase 0 + 5 new); `SchemaFingerprint` model defined; Phase 0 byte-stable surface preserved.
- `tools/doctor.py` registers exactly one tool named `doctor` (D-08 invariant); body returns the full DIAG-01 payload; defensive try/except around every new probe (DIAG-02 invariant).
- Live invocation on the user's Mac returns `schema_fingerprint.state="supported"`, `whatsapp_app_version="26.16.74"`, populated `last_message_ts` and `coverage_summary`.
- `mcp.list_tools()` returns exactly 8 tools (doctor + 7 read tools) — Plan 05 does NOT add new tools.
- Phase 0 baseline tests (28) still pass.
- ruff full ruleset + ruff format --check + mypy --strict green.
- REL-05 invariant maintained: no `whatsapp_desktop_mcp.sender` imports in `tools/doctor.py`.
</verification>

<success_criteria>
- DIAG-01 satisfied: `doctor` returns DB path resolved + FDA / Automation / Accessibility status (Phase 0 — preserved) + schema fingerprint + WhatsApp.app version + last-message timestamp + coverage summary.
- DIAG-02 satisfied: `doctor` remains callable when FDA is denied / WhatsApp.app missing / schema unsupported — each probe degrades to `null` / `state="unreachable"` with actionable remediation, never raises.
- Phase 0 D-08 invariant preserved: exactly one tool registration named `doctor` in `tools/doctor.py`.
- Phase 0 D-07 invariant honored: the 3 PermissionStatus fields are byte-stable; new fields are additive.
</success_criteria>

<output>
After completion, create `.planning/phases/01-read-mvp-read-only/01-05-SUMMARY.md` documenting the expanded `DoctorReport` shape (the 8 fields), the `SchemaFingerprint.state` semantics (supported / unsupported / unreachable) and their `remediation` strings, the `_probe_*` helper signatures (NOT public — but Plan 06 mocks them in the DIAG-02 test), the W1 invariant (doctor decorator now carries `meta={"anthropic/maxResultSizeChars": 60000}` uniform with the 7 read tools), the W3 invariant (doctor deliberately has no outer `@timeout` — RESEARCH §"Per-Tool Timeouts" codifies the deviation), and a one-paragraph runbook for "what to do when a user reports `schema_fingerprint.state == 'unsupported'`" — the upgrade path from RESEARCH §"Pattern 3 → Upgrade runbook".
</output>
</content>
</invoke>