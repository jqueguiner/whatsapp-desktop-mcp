---
phase: 01-read-mvp-read-only
plan: 3
title: "--read-only flag mechanics: CLI flag, ReadOnlyMode exception, server state"
type: execute
wave: 1
depends_on: []
requirements: [SETUP-06]
files_modified:
  - src/whatsapp_mcp/cli.py
  - src/whatsapp_mcp/exceptions.py
  - src/whatsapp_mcp/server.py
autonomous: true
must_haves:
  truths:
    - "`whatsapp-mcp --read-only` and `whatsapp-mcp --no-read-only` both parse without error; default is `--read-only` (= True) per the v0.1 carry-over decision"
    - "`whatsapp_mcp.server.read_only_mode` is a module-level `bool` set by `cli.main()` before the FastMCP tool registration imports trigger; reading the flag from inside a tool returns the user's CLI choice"
    - "`ReadOnlyMode` exception class exists, is importable, subclasses `WhatsAppMCPError`, and Phase 2's `send_message` will raise it when `server.read_only_mode is True`"
    - "Phase 1 ships zero send tools, so the registration-gate impact in Phase 1 is structural (Plan 04 imports 7 read tools unconditionally; Phase 2 will wrap its send tool import in `if not server.read_only_mode:`)"
    - "Existing Phase 0 server behavior is preserved: `python -m whatsapp_mcp --version` and `--help` exit before FastMCP loads; stdio JSON-RPC handshake unchanged; the existing `doctor` tool import still runs at module import time"
  artifacts:
    - path: "src/whatsapp_mcp/cli.py"
      provides: "--read-only / --no-read-only argparse flag wired to server.read_only_mode"
      contains: "BooleanOptionalAction"
    - path: "src/whatsapp_mcp/exceptions.py"
      provides: "ReadOnlyMode exception class minted (Phase 2 raises)"
      contains: "class ReadOnlyMode"
    - path: "src/whatsapp_mcp/server.py"
      provides: "read_only_mode module-level state set by cli.main() before tool imports"
      contains: "read_only_mode"
  key_links:
    - from: "cli.py"
      to: "server.read_only_mode"
      via: "module-level assignment before lazy server.run import"
      pattern: "server.read_only_mode = args.read_only"
    - from: "exceptions.py"
      to: "WhatsAppMCPError base class (Phase 0)"
      via: "ReadOnlyMode subclass"
      pattern: "class ReadOnlyMode\\(WhatsAppMCPError\\)"
---

<objective>
Wire the `--read-only` flag (SETUP-06) end-to-end without disturbing the
Phase 0 stdio / handshake / doctor surface. Three small edits:

1. Add the argparse flag to `cli.py` with `BooleanOptionalAction` so both
   `--read-only` and `--no-read-only` work; default is True for v0.1 (per
   STATE.md §"Todos / Carry-overs" decision recorded in 01-RESEARCH.md
   §"User Constraints" → A5).
2. Mint the `ReadOnlyMode` exception in `exceptions.py` so Phase 2's send
   tool can import it by name; Phase 1 ships the class but does NOT raise it
   (Phase 1 ships zero send tools — REL-05 invariant maintained).
3. Add `read_only_mode: bool = True` module-level state in `server.py`,
   assigned by `cli.main()` before the lazy `server.run()` import. Phase 2
   will wrap its send tool import in `if not server.read_only_mode:`; Phase 1
   does NOT add the wrap (no send tools exist yet to wrap).

Purpose: SETUP-06 acceptance requires that with `--read-only` set, every send
tool is unregistered AND every remaining tool is annotated `readOnlyHint=True`.
Phase 1's read tools (Plan 04) all carry `readOnlyHint=True` regardless of
flag — the annotation is always-on per RESEARCH §"Pattern 8". The flag's
real effect is structural for Phase 2.

Output: 1 new exception class + 1 new module-level flag + 1 argparse
addition. All other Phase 0 invariants (stdio purity, doctor tool, JSON-RPC
handshake) preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-read-mvp-read-only/01-RESEARCH.md
@.planning/phases/00-setup-and-permissions-skeleton/00-CONTEXT.md
@CLAUDE.md
@src/whatsapp_mcp/cli.py
@src/whatsapp_mcp/server.py
@src/whatsapp_mcp/exceptions.py
@src/whatsapp_mcp/__init__.py

<interfaces>
<!-- Existing Phase 0 surface this plan extends (NOT rewrites). -->

From src/whatsapp_mcp/cli.py (Phase 0):
```python
def main(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="whatsapp-mcp", description=...)
    parser.add_argument("--version", action="version", version=f"whatsapp-mcp {__version__}")
    parser.parse_args(argv)
    # Lazy import — argparse exits before FastMCP loads
    from whatsapp_mcp.server import run
    run()
    return 0
```
Preserve the lazy-import ordering: argparse FIRST, server import AFTER.

From src/whatsapp_mcp/server.py (Phase 0):
```python
# logging.basicConfig(stream=sys.stderr, ...)  FIRST
from mcp.server.fastmcp import FastMCP  # noqa: E402
mcp: FastMCP = FastMCP("whatsapp-mcp")
from whatsapp_mcp.tools import doctor as _doctor  # noqa: E402, F401
def run() -> None:
    mcp.run()
```
The `mcp = FastMCP(...)` line + the `tools import doctor` import side-effect must remain functional.

From src/whatsapp_mcp/exceptions.py (Phase 0 — frozen surface):
```python
class WhatsAppMCPError(Exception): ...
class PermissionRequired(WhatsAppMCPError): ...
class FullDiskAccessRequired(PermissionRequired): ...
class AutomationPermissionRequired(PermissionRequired): ...
class AccessibilityPermissionRequired(PermissionRequired): ...
```
APPEND `ReadOnlyMode(WhatsAppMCPError)` to this module — do NOT rename or modify the existing classes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReadOnlyMode exception (exceptions.py append)</name>
  <files>src/whatsapp_mcp/exceptions.py</files>
  <read_first>
    .planning/phases/01-read-mvp-read-only/01-RESEARCH.md (section: "Pattern 8: --read-only Flag Mechanics → ReadOnlyMode exception block");
    src/whatsapp_mcp/exceptions.py (Phase 0 — preserve frozen surface);
    .planning/phases/00-setup-and-permissions-skeleton/00-CONTEXT.md (D-12 — frozen exception hierarchy contract)
  </read_first>
  <action>
    Append a `ReadOnlyMode` class to `src/whatsapp_mcp/exceptions.py`. Place it after `AccessibilityPermissionRequired` at the end of the module. Class shape per 01-RESEARCH.md §"Pattern 8 → ReadOnlyMode block":

    ```
    class ReadOnlyMode(WhatsAppMCPError):
        """Raised by a send tool when the server was started with --read-only.

        Phase 1 mints this class so Phase 2's send_message can import it by
        name without a circular dependency on a Phase 2-only module. Phase 1
        ships zero send tools (REL-05 sender/ is empty), so nothing in
        Phase 1 ever raises this — but the contract surface is fixed now.
        """
    ```

    Do NOT modify any other class in the file. Do NOT add new fields to `PermissionRequired` or its subclasses (D-12 freezes them for Phase 1). Preserve the module-level docstring's "frozen public surface" framing — extend it in passing if you want to call out `ReadOnlyMode` as a Phase 1 addition, but do not rewrite the existing paragraphs.

    The class takes no `__init__` override — it inherits the bare `Exception(message)` from `WhatsAppMCPError`. Phase 2's send tool will raise it via `raise ReadOnlyMode("send_message is disabled because server is in --read-only mode")`.
  </action>
  <verify>
    <automated>uv run python -c "from whatsapp_mcp.exceptions import WhatsAppMCPError, PermissionRequired, FullDiskAccessRequired, AutomationPermissionRequired, AccessibilityPermissionRequired, ReadOnlyMode; assert issubclass(ReadOnlyMode, WhatsAppMCPError); assert not issubclass(ReadOnlyMode, PermissionRequired); e = ReadOnlyMode('test'); assert str(e) == 'test'; print('OK')" &amp;&amp; uv run ruff check src/whatsapp_mcp/exceptions.py &amp;&amp; uv run ruff format --check src/whatsapp_mcp/exceptions.py &amp;&amp; uv run mypy src/whatsapp_mcp/exceptions.py &amp;&amp; uv run pytest tests/unit/test_exceptions.py</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -cE '^class ReadOnlyMode\(WhatsAppMCPError\):' src/whatsapp_mcp/exceptions.py` returns exactly 1.
    - Source: `grep -cE '^class FullDiskAccessRequired\(PermissionRequired\):' src/whatsapp_mcp/exceptions.py` returns exactly 1 (Phase 0 class preserved unchanged).
    - Source: `grep -cE '^class AutomationPermissionRequired\(PermissionRequired\):' src/whatsapp_mcp/exceptions.py` returns 1 (preserved).
    - Source: `grep -cE '^class AccessibilityPermissionRequired\(PermissionRequired\):' src/whatsapp_mcp/exceptions.py` returns 1 (preserved).
    - Behavior: `ReadOnlyMode("x")` instantiates; `issubclass(ReadOnlyMode, WhatsAppMCPError)` is True; `issubclass(ReadOnlyMode, PermissionRequired)` is False (it's a sibling of `PermissionRequired`, not a child).
    - Phase 0 regression: `uv run pytest tests/unit/test_exceptions.py` still passes — the Phase 0 hierarchy tests must still report green (they assert the existing classes; adding a new sibling class does not break them).
    - Lint/type: ruff (full ruleset) + ruff format + mypy --strict green on the file.
  </acceptance_criteria>
  <done>`ReadOnlyMode` exception exists; subclasses `WhatsAppMCPError`; Phase 0 exception hierarchy and tests unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: server.py module-level read_only_mode flag</name>
  <files>src/whatsapp_mcp/server.py</files>
  <read_first>
    .planning/phases/01-read-mvp-read-only/01-RESEARCH.md (sections: "Pattern 8: --read-only Flag Mechanics" — full server.py edit block; "Pattern 9: FastMCP Tool Registration in Phase 1" — why server.py owns the tool imports);
    src/whatsapp_mcp/server.py (existing — preserve the docstring + the `logging.basicConfig` first-line invariant + the `mcp = FastMCP(...)` + the `from whatsapp_mcp.tools import doctor as _doctor` import line);
    .planning/phases/00-setup-and-permissions-skeleton/00-CONTEXT.md (D-04 — stdio only; D-05 — logging to stderr first)
  </read_first>
  <action>
    Modify `src/whatsapp_mcp/server.py` to add the `read_only_mode` module-level flag. Insert ONE new line:

    `read_only_mode: bool = True`

    The line goes immediately AFTER `mcp: FastMCP = FastMCP("whatsapp-mcp")` and BEFORE the existing `from whatsapp_mcp.tools import doctor as _doctor` line.

    Update the module docstring to add one paragraph at the bottom explaining the flag's role:

    > `read_only_mode` is a module-level boolean set by `cli.main()` BEFORE
    > the lazy `from whatsapp_mcp.server import run` import resolves —
    > because the tool-registration side-effect imports execute at server
    > module import time, `cli.main` must assign the flag prior to that
    > import (Plan 03 wires this; Phase 2 will gate its send tool import on
    > `if not read_only_mode:`). Phase 1 ships zero send tools, so the flag
    > is consulted only by Phase 2's `send_message` registration and by
    > unit tests that assert tool-listing behavior. Read tools always carry
    > `readOnlyHint=True` regardless of the flag.

    Do NOT add any send-tool import. Do NOT add the 7 Plan-04 read-tool imports here (Plan 04 will append them; this plan only adds the flag). Do NOT change the `logging.basicConfig(stream=sys.stderr, ...)` block or the `from mcp.server.fastmcp import FastMCP` line. Preserve the existing `from whatsapp_mcp.tools import doctor as _doctor # noqa: E402, F401` line exactly — the SETUP-02 / Plan 04 stdout-purity smoke test asserts the doctor tool is registered.

    The Phase 0 test `tests/unit/test_doctor_tool.py` asserts `mcp.list_tools()` returns exactly one tool named `doctor`; after this plan it should still pass (Plan 04 adds 7 more tools, but Plan 03 alone adds zero tools). The Phase 0 `test_stdout_purity.py` test must also still pass — adding a module-level `bool` assignment does not produce stdout.
  </action>
  <verify>
    <automated>uv run python -c "from whatsapp_mcp import server; assert hasattr(server, 'read_only_mode'); assert isinstance(server.read_only_mode, bool); assert server.read_only_mode is True; print('OK default', server.read_only_mode)" &amp;&amp; uv run python -c "from whatsapp_mcp.server import mcp; import asyncio; tools = asyncio.run(mcp.list_tools()); names = [t.name for t in tools]; assert names == ['doctor'], f'expected only doctor, got {names}'; print('OK', names)" &amp;&amp; uv run ruff check src/whatsapp_mcp/server.py &amp;&amp; uv run ruff format --check src/whatsapp_mcp/server.py &amp;&amp; uv run mypy src/whatsapp_mcp/server.py &amp;&amp; uv run pytest -m "not live"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -cE '^read_only_mode\s*:\s*bool\s*=\s*True' src/whatsapp_mcp/server.py` returns exactly 1.
    - Source: `grep -cE '^from whatsapp_mcp\.tools import doctor as _doctor' src/whatsapp_mcp/server.py` returns 1 (Phase 0 import preserved exactly once).
    - Source: `grep -cE '^mcp\s*:\s*FastMCP\s*=' src/whatsapp_mcp/server.py` returns 1 (Phase 0 instance preserved).
    - Source: `grep -cE 'logging\.basicConfig\(stream=sys\.stderr' src/whatsapp_mcp/server.py` returns 1 (Phase 0 stdout-purity invariant preserved).
    - Behavior: `from whatsapp_mcp import server; server.read_only_mode is True` (default at import time).
    - Behavior: `mcp.list_tools()` still returns exactly `["doctor"]` after this plan (no new tools registered yet — Plan 04 adds them).
    - Phase 0 regression: full `uv run pytest -m "not live"` returns 28 passed (no test regresses; this plan adds no new tests).
    - Lint/type: ruff (full ruleset) + ruff format + mypy --strict green on the file.
    - Threat: the only mutation in this plan is a module-level `bool` assignment + a docstring edit; no new I/O, no new tool, no new dependency.
  </acceptance_criteria>
  <done>`server.read_only_mode` exists at module scope, default True; Phase 0 doctor tool still registers exactly once; stdout-purity test still green.</done>
</task>

<task type="auto">
  <name>Task 3: cli.py --read-only / --no-read-only argparse flag</name>
  <files>src/whatsapp_mcp/cli.py</files>
  <read_first>
    .planning/phases/01-read-mvp-read-only/01-RESEARCH.md (section: "Pattern 8: --read-only Flag Mechanics" — cli.py edit block; "User Constraints → Inherited Locked Decisions" — A5 default-on for v0.1);
    src/whatsapp_mcp/cli.py (existing — preserve the lazy server import + the argparse pattern);
    .planning/phases/00-setup-and-permissions-skeleton/00-CONTEXT.md (Claude's discretion → version/help flags)
  </read_first>
  <action>
    Modify `src/whatsapp_mcp/cli.py` to add the `--read-only` / `--no-read-only` flag. Three edits:

    1. After the existing `parser.add_argument("--version", ...)` line, ADD:
       ```
       parser.add_argument(
           "--read-only",
           action=argparse.BooleanOptionalAction,
           default=True,
           help="Disable every send tool; tools/list returns read tools + doctor only. "
                "Default is on for v0.1 (no send tools exist yet — Phase 2 adds them and the "
                "flag gates their registration). Pass --no-read-only on Phase 2+ servers to enable sends.",
       )
       ```
       `BooleanOptionalAction` is the modern argparse pattern that gives `--read-only` (True) and `--no-read-only` (False) for free in a single declaration. Available in stdlib argparse since Python 3.9; the project's `requires-python = ">=3.12"` makes it always available.

    2. After `args = parser.parse_args(argv)`, BEFORE the existing `from whatsapp_mcp.server import run` line, ADD:
       ```
       # Set the read_only_mode flag BEFORE importing server.run so that the
       # FastMCP tool-registration side-effect imports in server.py observe
       # the user's choice. Phase 1 ships zero send tools, so the flag is
       # structural — Phase 2's send_message import will be gated by
       # `if not server.read_only_mode:` and will observe THIS assignment.
       from whatsapp_mcp import server
       server.read_only_mode = args.read_only
       ```

    3. Keep the existing `from whatsapp_mcp.server import run` and `run()` lines exactly as they are. The two-step import (`from whatsapp_mcp import server` to set the flag, then `from whatsapp_mcp.server import run` for the entry function) is intentional: importing the `server` module triggers the same top-down module load whether you access `server.read_only_mode` first or `server.run` first — the second line is a no-op import (already cached) but reads clearly.

    Update the module docstring's "lazy import" paragraph to mention that the new flag assignment also happens before `run()` is called and that this ordering is what makes the flag observable to subsequent tool imports — even though in Phase 1 every Plan 04 import runs unconditionally (no send tools).

    The function signature `def main(argv: list[str] | None = None) -> int` is preserved.

    Behavior gates:
    - `whatsapp-mcp --version` STILL exits before FastMCP loads (argparse handles `--version` before `parse_args` returns).
    - `whatsapp-mcp --help` STILL exits with usage including the new `--read-only / --no-read-only` line.
    - `whatsapp-mcp --read-only` and `whatsapp-mcp --no-read-only` both parse without error; the latter sets `server.read_only_mode = False`.
    - Default (no flag) sets `server.read_only_mode = True` per A5.
  </action>
  <verify>
    <automated>uv run whatsapp-mcp --help 2&gt;&amp;1 | grep -E -- '--read-only.*--no-read-only|--no-read-only.*' &amp;&amp; uv run python &lt;&lt;'PY' &amp;&amp; uv run python &lt;&lt;'PY2' &amp;&amp; uv run ruff check src/whatsapp_mcp/cli.py &amp;&amp; uv run ruff format --check src/whatsapp_mcp/cli.py &amp;&amp; uv run mypy src/whatsapp_mcp/cli.py &amp;&amp; uv run pytest -m "not live"
import sys
from whatsapp_mcp.cli import main
rc = None
try:
    main(['--version'])
except SystemExit as e:
    rc = e.code
assert rc == 0, f'expected --version to exit 0, got {rc}'
print('VERSION exits 0')
PY
import argparse
# Simulate full parse path WITHOUT running server.run()
parser = argparse.ArgumentParser(prog='whatsapp-mcp')
parser.add_argument('--read-only', action=argparse.BooleanOptionalAction, default=True)
a = parser.parse_args([]); assert a.read_only is True
a = parser.parse_args(['--read-only']); assert a.read_only is True
a = parser.parse_args(['--no-read-only']); assert a.read_only is False
print('flag parsing OK')
PY2</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -cE 'action=argparse\.BooleanOptionalAction' src/whatsapp_mcp/cli.py` returns 1.
    - Source: `grep -cE '"--read-only"' src/whatsapp_mcp/cli.py` returns 1.
    - Source: `grep -cE 'server\.read_only_mode\s*=\s*args\.read_only' src/whatsapp_mcp/cli.py` returns 1.
    - Source: `grep -cE 'from whatsapp_mcp\.server import run' src/whatsapp_mcp/cli.py` returns 1 (Phase 0 lazy import preserved).
    - Source: `grep -cE '^\s*run\(\)\s*$' src/whatsapp_mcp/cli.py` returns 1 (Phase 0 entry-point invocation preserved).
    - Behavior: `whatsapp-mcp --help` exits 0 and prints usage containing `--no-read-only` (verified above).
    - Behavior: `whatsapp-mcp --version` exits 0 (Phase 0 invariant preserved — argparse exits before server import).
    - Behavior: parsing `[]`, `["--read-only"]`, `["--no-read-only"]` produces `args.read_only == True, True, False` respectively (verified above).
    - Phase 0 regression: full `uv run pytest -m "not live"` returns 28 passed.
    - Lint/type: ruff (full ruleset) + ruff format + mypy --strict green on the file.
  </acceptance_criteria>
  <done>`--read-only` / `--no-read-only` argparse flag wired to `server.read_only_mode`; default is True (v0.1 carry-over); Phase 0 stdout purity + version/help behavior preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → `server.read_only_mode` module state | argparse parses untrusted argv into a bounded boolean; `BooleanOptionalAction` rejects any non-boolean spelling (e.g. `--read-only=maybe`). No string concatenation, no shell interpretation. |
| `server.read_only_mode` → tool registration | Module-level boolean read by future Phase 2 send-tool imports. Phase 1 ships zero send tools, so the flag is structural — verified by Plan 06's `test_read_only_mode.py` asserting tools/list returns exactly the 8 read tools + `doctor` (all `readOnlyHint=True`) on every flag setting. |
| `ReadOnlyMode` exception → MCP tool framework | Phase 2's send tool will catch `ReadOnlyMode` and convert it to a structured tool error response; Phase 1 never raises, so the boundary is dormant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | LLM caller bypasses `--read-only` by re-invoking the server with `--no-read-only` via a separate process | accept | Out of scope for Phase 1: a process that can spawn arbitrary `whatsapp-mcp --no-read-only` invocations already has shell access on the user's Mac. The flag defends against the LLM-driven misuse pattern (P6) within a single server lifetime, not against escalated local-attacker scenarios. Phase 2's elicitation confirmation + rate limit + audit log are the layered defenses. |
| T-03-02 | Elevation of Privilege | Future Phase 2 send tool imports unconditionally despite `--read-only` flag being True | mitigate (Phase 2) | Plan 03 ships only the flag plumbing; Phase 2's send-tool registration will be wrapped in `if not server.read_only_mode:` per RESEARCH §"Pattern 8". Plan 06's `test_read_only_mode.py` will assert the negative case (no send tool advertised when flag is True) by spawning a `--read-only` subprocess and inspecting `tools/list`. |
| T-03-03 | Information Disclosure | Argparse `--help` output discloses internal Phase 2 plans by mentioning send tools | accept | The help text references the future Phase 2 surface to disclose intent honestly to users — there is no secret being protected. Same disclosure already exists in README's quickstart section. |
| T-03-04 | Tampering | argparse interprets a maliciously crafted argv ("--read-only=$(rm -rf /)") | mitigate | argparse `BooleanOptionalAction` only accepts the exact form `--read-only` or `--no-read-only` — any other token raises `SystemExit(2)` before reaching any side-effecting code. No shell expansion is performed by Python; argv is a C-level `char**` array, not a shell string. |
| T-03-05 | Denial of Service | `ReadOnlyMode` exception class import is part of every server startup; if its definition triggered I/O it would block | mitigate | The class is a 2-line subclass with no `__init__` override — zero I/O at import time. Verified by `grep -cE 'open\(|subprocess|requests' src/whatsapp_mcp/exceptions.py` returning 0. |
</threat_model>

<verification>
- All three files modified; existing Phase 0 surface (logging, FastMCP instance, doctor import, lazy run import) preserved byte-for-functional-byte.
- `whatsapp-mcp --version` and `whatsapp-mcp --help` still exit before FastMCP loads.
- `whatsapp-mcp --no-read-only` parses without error and would set `server.read_only_mode = False` if `run()` were invoked (the verify command parses the args dict without running the server).
- Full Phase 0 test suite (`uv run pytest -m "not live"`) returns 28 passed.
- `tools/list` (via `mcp.list_tools()`) still returns exactly `["doctor"]` after this plan — Plan 04 adds the 7 read tools.
- ruff full ruleset + ruff format + mypy --strict green on all three files.
- `from whatsapp_mcp.exceptions import ReadOnlyMode` succeeds; `issubclass(ReadOnlyMode, WhatsAppMCPError)` is True.
</verification>

<success_criteria>
- SETUP-06 partially satisfied: the `--read-only` / `--no-read-only` flag exists with `BooleanOptionalAction`, default True; `server.read_only_mode` is set by `cli.main()` before server startup. The remainder of SETUP-06 ("every send tool is unregistered when --read-only is set") is structurally satisfied in Phase 1 (no send tools exist) and will be re-verified in Phase 2 when send tools land.
- `ReadOnlyMode` exception class minted, importable, subclasses `WhatsAppMCPError`, ready for Phase 2 to raise.
- Phase 0 invariants preserved: stdout purity, doctor-only tools/list, version/help-before-FastMCP-import ordering.
- Plan 04 can depend on this plan's `server.read_only_mode` being True when it appends its 7 read-tool imports (the imports run regardless of flag, per the always-on `readOnlyHint=True` annotation rationale).
</success_criteria>

<output>
After completion, create `.planning/phases/01-read-mvp-read-only/01-03-SUMMARY.md` documenting the locked `--read-only` flag default (True for v0.1), the `ReadOnlyMode` exception class shape, the `server.read_only_mode` module-level state, and a one-paragraph note for Phase 2's planner: "Phase 2's send tool registration MUST be gated on `if not server.read_only_mode:` and should raise `ReadOnlyMode(...)` at tool call time when the flag flips after startup (defensive belt-and-braces, since the flag is set at startup only in Phase 1)."
</output>
</content>
</invoke>