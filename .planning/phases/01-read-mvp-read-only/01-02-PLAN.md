---
phase: 01-read-mvp-read-only
plan: 2
title: "Reader internals: RO-WAL connection, schema probe, queries, tombstones, media"
type: execute
wave: 2
depends_on: [1]
requirements: [REL-01, REL-02, REL-04, READ-08, DATA-03, DATA-04]
files_modified:
  - src/whatsapp_mcp/reader/__init__.py
  - src/whatsapp_mcp/reader/connection.py
  - src/whatsapp_mcp/reader/schema_v1.py
  - src/whatsapp_mcp/reader/tombstones.py
  - src/whatsapp_mcp/reader/media.py
  - src/whatsapp_mcp/reader/chats.py
  - src/whatsapp_mcp/reader/messages.py
  - src/whatsapp_mcp/reader/groups.py
  - src/whatsapp_mcp/reader/contacts.py
  - src/whatsapp_mcp/reader/search.py
autonomous: true
must_haves:
  truths:
    - "`reader.connection.open_ro(path)` opens a read-only WAL connection that succeeds concurrent with WhatsApp's writer (REL-01); never `immutable=1`"
    - "Every reader public function is `async` and dispatches its blocking work via `asyncio.to_thread` (REL-02); reader code does NO `await` inside the sqlite block other than via `to_thread`"
    - "`reader.schema_v1.probe_z_version(conn)` returns the integer Z_VERSION; `SUPPORTED_VERSIONS = frozenset({1})` matches the verified-live value (REL-04)"
    - "`reader.tombstones.is_tombstone(message_type, flags, text)` returns True for `ZMESSAGETYPE=14` AND for `ZTEXT IS NULL` rows whose `ZFLAGS & 0xFF000000 == 0x05000000` (READ-08)"
    - "`reader.media.resolve_media_ref(row, db_root)` returns None for empty paths and refuses any path that resolves outside the WhatsApp media root (DATA-03 + path-traversal defense)"
    - "Reader package does NOT import from `whatsapp_mcp.sender` (REL-05 invariant — Plan 06 re-tests)"
    - "Reader queries use parameterized SQL exclusively (no f-string SQL); the `Z_WAMessage_compoundIndex (ZCHATSESSION, ZSORT)` is the index every chat-window query hits"
    - "`reader.window` returns `tuple[list[Message], float | None]` where the float is the last-seen ZSORT (B2 — feeds Plan 04's `next_cursor` without leaking ZSORT into the public `Message` schema)"
    - "`_row_to_message` lives in `reader/messages.py` and is imported by `reader/search.py` (W4 — one-direction edge; no circular risk; no speculative `_row_mapping.py` module)"
    - "`GroupInfo.description = None` and `GroupInfo.is_muted = False` for v0.1 (W5 — locked by RESEARCH Q2/Q3 RESOLVED; no live PRAGMA probe at execute time)"
  artifacts:
    - path: "src/whatsapp_mcp/reader/connection.py"
      provides: "open_ro context manager + async helper"
      contains: "def open_ro"
    - path: "src/whatsapp_mcp/reader/schema_v1.py"
      provides: "Z_VERSION probe + SQL templates registry + SUPPORTED_VERSIONS"
      contains: "SUPPORTED_VERSIONS"
    - path: "src/whatsapp_mcp/reader/tombstones.py"
      provides: "is_tombstone predicate (Python + SQL filter snippet)"
      contains: "def is_tombstone"
    - path: "src/whatsapp_mcp/reader/media.py"
      provides: "resolve_media_ref with path-traversal defense"
      contains: "def resolve_media_ref"
    - path: "src/whatsapp_mcp/reader/chats.py"
      provides: "list_chats + find_chat_by_id async functions"
      contains: "async def list_chats"
    - path: "src/whatsapp_mcp/reader/messages.py"
      provides: "window (returns tuple[list[Message], float | None]) / since / context_around_stanza / _row_to_message helper"
      contains: "async def window"
    - path: "src/whatsapp_mcp/reader/groups.py"
      provides: "get_group_info / get_members async functions"
      contains: "async def get_group_info"
    - path: "src/whatsapp_mcp/reader/contacts.py"
      provides: "search_contacts + resolve_lid_to_phone async functions"
      contains: "async def search_contacts"
    - path: "src/whatsapp_mcp/reader/search.py"
      provides: "like_search async function (LIKE for v0.1; FTS5 deferred to Phase 3)"
      contains: "async def like_search"
  key_links:
    - from: "reader/messages.py"
      to: "reader/connection.py"
      via: "open_ro context manager"
      pattern: "with open_ro\\("
    - from: "reader/messages.py"
      to: "models.Message + time.cocoa_to_unix"
      via: "row → Message mapping"
      pattern: "from whatsapp_mcp.time import cocoa_to_unix"
    - from: "reader/contacts.py"
      to: "paths.resolve_lid_path + paths.resolve_contactsv2_path"
      via: "sibling DB resolution"
      pattern: "from whatsapp_mcp.paths import"
    - from: "reader/media.py"
      to: "paths.resolve_media_root"
      via: "path-traversal prefix check"
      pattern: "resolve_media_root"
---

<objective>
Land the entire `reader/` package — the data tier that owns ALL SQL knowledge
in the codebase. Short-lived RO-WAL connections (REL-01), `asyncio.to_thread`
wrappers (REL-02), Z_VERSION probe with degraded-mode signaling (REL-04),
tombstone predicate (READ-08), MediaRef resolver with path-traversal defense
(DATA-03), and the eight reader-side async functions the Plan 04 tools call.

Purpose: This is the single point of contact with `ChatStorage.sqlite`,
`LID.sqlite`, and `ContactsV2.sqlite`. Every read tool in Plan 04 calls into
this layer; every concurrency / SQLite-locked / schema-drift bug class
(P3 / P8 / P2) is mitigated here, not in the tool layer.

Output: 10 new files under `src/whatsapp_mcp/reader/` (the existing
`__init__.py` becomes an explicit re-export module). REL-05 invariant
preserved: the entire reader/ tree imports zero symbols from
`whatsapp_mcp.sender`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-read-mvp-read-only/01-RESEARCH.md
@.planning/phases/01-read-mvp-read-only/01-01-SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@src/whatsapp_mcp/paths.py
@src/whatsapp_mcp/time.py
@src/whatsapp_mcp/exceptions.py

<interfaces>
<!-- From Plan 01 — the data tier the reader produces and consumes. -->

From src/whatsapp_mcp/models/ (Plan 01):
```python
from whatsapp_mcp.models import (
    Chat, ChatKind,                      # chats.py returns list[Chat]
    Message, MessageKind,                # messages.py returns list[Message]
    Contact, Jid, JidKind,               # contacts.py returns list[Contact]
    GroupInfo, GroupMember,              # groups.py returns GroupInfo
    MediaRef,                            # media.py returns MediaRef | None
    Coverage,                            # not constructed in reader; tools build it
)
```

From src/whatsapp_mcp/paths.py (Plan 01-extended):
```python
def resolve_chatstorage_path() -> str: ...
def resolve_lid_path() -> str: ...
def resolve_contactsv2_path() -> str: ...
def resolve_media_root() -> str: ...
```

From src/whatsapp_mcp/time.py (Plan 01):
```python
COCOA_EPOCH_OFFSET: int = 978_307_200
def cocoa_to_unix(cocoa_seconds: float) -> int: ...
def unix_to_cocoa(unix_seconds: int) -> float: ...
```

From src/whatsapp_mcp/exceptions.py (Phase 0 — frozen surface):
```python
class WhatsAppMCPError(Exception): ...
class PermissionRequired(WhatsAppMCPError):
    bucket: str
    system_settings_url: str
    def __init__(self, message, *, binary_path, db_path=None, remediation="")
class FullDiskAccessRequired(PermissionRequired): bucket = "fda"
class AutomationPermissionRequired(PermissionRequired): bucket = "automation"
class AccessibilityPermissionRequired(PermissionRequired): bucket = "accessibility"
```

REL-05 invariant (CLAUDE.md hard rule #1): reader/ MUST NOT import from
`whatsapp_mcp.sender`. Plan 06 re-tests this with an AST walk of every
reader/*.py module.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: connection.py + schema_v1.py + tombstones.py + media.py</name>
  <files>
    src/whatsapp_mcp/reader/connection.py,
    src/whatsapp_mcp/reader/schema_v1.py,
    src/whatsapp_mcp/reader/tombstones.py,
    src/whatsapp_mcp/reader/media.py
  </files>
  <read_first>
    .planning/phases/01-read-mvp-read-only/01-RESEARCH.md (sections: "Pattern 1: Short-lived Read-Only WAL Connection (REL-01) [VERIFIED LIVE]" — full code; "Pattern 3: Schema Fingerprint Probe (REL-04) [VERIFIED LIVE]" — full code + upgrade runbook; "Pattern 6: Tombstone Filter (READ-08, P10 mitigation)" — full code + SQL filter; "Core Data Schema Essentials → ZWAMEDIAITEM" — resolve_media_ref code block);
    .planning/research/PITFALLS.md (P3 — SQLite locked; P10 — soft-deleted; P9 — char-cap context);
    src/whatsapp_mcp/paths.py (resolve_media_root for the path-traversal prefix check);
    .planning/phases/01-read-mvp-read-only/01-01-SUMMARY.md (locked MediaRef shape from Plan 01)
  </read_first>
  <action>
    `src/whatsapp_mcp/reader/connection.py` — implement the short-lived RO-WAL connection per 01-RESEARCH.md §"Pattern 1" verbatim. Required surface:
    - `@contextmanager def open_ro(db_path: str | Path) -> Iterator[sqlite3.Connection]` with the EXACT shape from RESEARCH §"Pattern 1": `uri = f"file:{db_path}?mode=ro"`, `sqlite3.connect(uri, uri=True, isolation_level=None, check_same_thread=False, timeout=5.0)`, `conn.row_factory = sqlite3.Row`, `PRAGMA busy_timeout = 5000`, single `BEGIN`...`COMMIT` block, `try`/`finally` close.
    - The docstring MUST quote RESEARCH's "Why this exact shape" rationale (mode=ro vs immutable=1; busy_timeout=5000; check_same_thread=False; Row factory; BEGIN/COMMIT).
    - VERIFIED LIVE 2026-05-13 against the user's 89 MB ChatStorage.sqlite per RESEARCH § "Pattern 1" — preserve that callout in the docstring.

    `src/whatsapp_mcp/reader/schema_v1.py` — implement per 01-RESEARCH.md §"Pattern 3" verbatim. Required surface:
    - `SUPPORTED_VERSIONS: frozenset[int] = frozenset({1})` — the only value verified live; broaden in Phase 3 per the upgrade runbook.
    - `def probe_z_version(conn: sqlite3.Connection) -> int` — runs `SELECT Z_VERSION FROM Z_METADATA LIMIT 1`, raises `RuntimeError` on empty result.
    - `def is_supported(version: int) -> bool` — checks `version in SUPPORTED_VERSIONS`.
    - Module-level constants for the SQL templates the messages/chats/groups modules will reuse: `_SQL_LIST_CHATS`, `_SQL_WINDOW`, `_SQL_SINCE`, `_SQL_CONTEXT_AROUND_STANZA`, `_SQL_PARENT_BY_STANZA`, `_SQL_GROUP_INFO`, `_SQL_GROUP_MEMBERS`, `_SQL_LIKE_SEARCH`, `_SQL_LAST_MESSAGE_TS`. Take the verbatim SQL from 01-RESEARCH.md §"Core Data Schema Essentials" → query shape blocks (list_chats, read_chat, get_message_context, like_search). Include the tombstone WHERE clause inline (`AND ZMESSAGETYPE != 14 AND NOT (ZTEXT IS NULL AND (ZFLAGS & 0xFF000000) = 0x05000000)`) — wrap each SQL constant with a comment naming the index it hits (e.g. `# Uses Z_WAMessage_compoundIndex (ZCHATSESSION, ZSORT)`).
    - The upgrade runbook from RESEARCH §"Pattern 3" lives as a module-level docstring so a future maintainer can find it without grepping the planning dir.

    `src/whatsapp_mcp/reader/tombstones.py` — implement per 01-RESEARCH.md §"Pattern 6" verbatim. Required surface:
    - `_TOMBSTONE_HIGH_BITS_MASK = 0x05000000` constant.
    - `def is_tombstone(message_type: int, flags: int, text: str | None) -> bool` returning `True` if `message_type == 14` OR (`(flags & 0xFF000000) == _TOMBSTONE_HIGH_BITS_MASK and text is None`).
    - Module docstring quoting the verified-live row counts (532 type-14 rows, the 0x05000000 high-bit distribution) as the empirical justification per RESEARCH §"Pattern 6".
    - A constant `TOMBSTONE_SQL_WHERE: str = "ZMESSAGETYPE != 14 AND NOT (ZTEXT IS NULL AND (ZFLAGS & 0xFF000000) = 0x05000000)"` so SQL templates in `schema_v1.py` can reference one source of truth.

    `src/whatsapp_mcp/reader/media.py` — implement `resolve_media_ref(row, db_root)` per 01-RESEARCH.md §"Core Data Schema Essentials → ZWAMEDIAITEM → resolve_media_ref code block" + §"MediaRef Resolution". Required surface:
    - `def resolve_media_ref(row: sqlite3.Row, media_root: str | Path) -> MediaRef | None` (NOT async — pure path computation; reader/messages.py joins with ZWAMEDIAITEM and feeds rows in).
    - Returns None when `ZMEDIALOCALPATH` is NULL or empty.
    - Builds absolute path via `Path(media_root) / rel`, calls `.resolve()`, then `startswith()` checks against `Path(media_root).resolve()` to refuse anything escaping the media root (defends against the `lharries#241` path-traversal class — RESEARCH §"Security Domain → Path traversal in MediaRef.local_path").
    - Mime guess via stdlib `mimetypes.guess_type(absolute.name)[0] or "application/octet-stream"`.
    - Surfaces `duration_seconds` from `ZMOVIEDURATION` (REAL → float | None), `latitude` / `longitude` from the matching columns when present.
    - DATA-04 invariant: NEVER touches `ZMEDIAKEY` or `ZMETADATA` — silently omits them. Document in module docstring.

    All four modules: `from __future__ import annotations`, parameterized SQL only (no f-strings), mypy --strict + ruff full ruleset green. None of them import from `whatsapp_mcp.sender` (REL-05).
  </action>
  <verify>
    <automated>uv run python -c "from whatsapp_mcp.reader.connection import open_ro; from whatsapp_mcp.reader.schema_v1 import SUPPORTED_VERSIONS, is_supported, probe_z_version; from whatsapp_mcp.reader.tombstones import is_tombstone, TOMBSTONE_SQL_WHERE; from whatsapp_mcp.reader.media import resolve_media_ref; assert SUPPORTED_VERSIONS == frozenset({1}); assert is_supported(1) and not is_supported(99); assert is_tombstone(14, 0x01000000, 'hi'); assert is_tombstone(0, 0x05000000, None); assert not is_tombstone(0, 0x01000000, 'hi'); assert not is_tombstone(0, 0x05000000, 'still has text'); print('OK')" &amp;&amp; uv run python -c "import sqlite3, tempfile, os; fd, p = tempfile.mkstemp(suffix='.sqlite'); os.close(fd); c = sqlite3.connect(p); c.execute('CREATE TABLE Z_METADATA (Z_VERSION INTEGER)'); c.execute('INSERT INTO Z_METADATA VALUES (1)'); c.commit(); c.close(); from whatsapp_mcp.reader.connection import open_ro; from whatsapp_mcp.reader.schema_v1 import probe_z_version; ctx = open_ro(p); conn = ctx.__enter__(); v = probe_z_version(conn); ctx.__exit__(None,None,None); os.unlink(p); assert v == 1; print('RO OK', v)" &amp;&amp; uv run ruff check src/whatsapp_mcp/reader/connection.py src/whatsapp_mcp/reader/schema_v1.py src/whatsapp_mcp/reader/tombstones.py src/whatsapp_mcp/reader/media.py &amp;&amp; uv run ruff format --check src/whatsapp_mcp/reader/connection.py src/whatsapp_mcp/reader/schema_v1.py src/whatsapp_mcp/reader/tombstones.py src/whatsapp_mcp/reader/media.py &amp;&amp; uv run mypy src/whatsapp_mcp/reader/</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -cE 'def open_ro\(' src/whatsapp_mcp/reader/connection.py` returns 1.
    - Source: `grep -cE 'mode=ro' src/whatsapp_mcp/reader/connection.py` returns ≥ 1; `grep -cE 'immutable' src/whatsapp_mcp/reader/connection.py` returns 0 (NEVER `immutable=1` per CLAUDE.md hard rule #3 + RESEARCH §"Pattern 1").
    - Source: `grep -cE 'PRAGMA busy_timeout' src/whatsapp_mcp/reader/connection.py` returns ≥ 1.
    - Source: `grep -cE 'SUPPORTED_VERSIONS\s*:\s*frozenset' src/whatsapp_mcp/reader/schema_v1.py` returns 1; the value is `frozenset({1})` per the verified-live probe.
    - Source: `grep -cE 'def is_tombstone\(' src/whatsapp_mcp/reader/tombstones.py` returns 1.
    - Source: `grep -cE 'TOMBSTONE_SQL_WHERE' src/whatsapp_mcp/reader/tombstones.py` returns ≥ 1.
    - Source: `grep -cE 'def resolve_media_ref\(' src/whatsapp_mcp/reader/media.py` returns 1; `grep -cE 'ZMEDIAKEY|ZMETADATA' src/whatsapp_mcp/reader/media.py` returns 0 (DATA-04 — never reads encrypted columns).
    - REL-05: `python -c "import ast, pathlib; [ast.parse(p.read_text()) for p in pathlib.Path('src/whatsapp_mcp/reader').glob('*.py')]"` succeeds AND no `whatsapp_mcp.sender` substring appears in any reader/*.py: `! grep -r 'whatsapp_mcp.sender' src/whatsapp_mcp/reader/`.
    - Behavior: `is_tombstone(14, 0, 'x')` is True; `is_tombstone(0, 0x05000000, None)` is True; `is_tombstone(0, 0x05000000, 'present')` is False; `is_tombstone(0, 0x01000000, 'normal')` is False.
    - Behavior: `open_ro` against a fixture sqlite reads `Z_VERSION = 1` round-trip without raising.
    - Lint/type: ruff full ruleset + ruff format + mypy --strict green on all of `src/whatsapp_mcp/reader/`.
  </acceptance_criteria>
  <done>The four foundational reader modules ship with verified-live patterns, parameterized SQL templates, the tombstone predicate matching empirical row counts, and a path-traversal-safe MediaRef resolver. REL-05 isolation invariant intact.</done>
</task>

<task type="auto">
  <name>Task 2: chats.py + messages.py + groups.py + contacts.py + search.py + reader/__init__.py</name>
  <files>
    src/whatsapp_mcp/reader/chats.py,
    src/whatsapp_mcp/reader/messages.py,
    src/whatsapp_mcp/reader/groups.py,
    src/whatsapp_mcp/reader/contacts.py,
    src/whatsapp_mcp/reader/search.py,
    src/whatsapp_mcp/reader/__init__.py
  </files>
  <read_first>
    .planning/phases/01-read-mvp-read-only/01-RESEARCH.md (sections: "Pattern 2: Async Wrapping (REL-02)" — async wrapper pattern; "Core Data Schema Essentials → ZWACHATSESSION" + "→ ZWAMESSAGE" + "→ ZWAGROUPINFO" + "→ ZWAGROUPMEMBER" — column tables + query shapes; "Pattern 7: JID/LID Model and Dedup" — search_contacts dedup recipe; "Search: LIKE Strategy (READ-04 v0.1)" — LIKE SQL; "Code Examples → Read a chat window using the killer compound index");
    src/whatsapp_mcp/reader/connection.py (Task 1 — open_ro);
    src/whatsapp_mcp/reader/schema_v1.py (Task 1 — SQL constants);
    src/whatsapp_mcp/reader/tombstones.py (Task 1 — is_tombstone, TOMBSTONE_SQL_WHERE);
    src/whatsapp_mcp/reader/media.py (Task 1 — resolve_media_ref);
    src/whatsapp_mcp/time.py (Plan 01 — cocoa_to_unix);
    .planning/phases/01-read-mvp-read-only/01-01-SUMMARY.md (Plan 01 model field maps)
  </read_first>
  <action>
    Implement the five reader modules that produce Pydantic models from sqlite rows. Every public function is `async def` and follows the Pattern 2 shape: `async def f(...)` resolves the path, then `return await asyncio.to_thread(_blocking_f, ...)`; `_blocking_f` is a plain `def` containing the `with open_ro(...) as conn:` block. NEVER call `cursor.execute` from a coroutine directly (REL-02).

    `src/whatsapp_mcp/reader/chats.py` — per RESEARCH §"Core Data Schema Essentials → ZWACHATSESSION → Query shape for list_chats". Public surface:
    - `async def list_chats(limit: int = 200) -> list[Chat]` — runs `_SQL_LIST_CHATS` (from `schema_v1`), maps rows to `Chat` instances, populates `Chat.kind` via the `ZSESSIONTYPE` mapping (0→"direct", 1→"group", 3→"broadcast", 4→"community", anything else→"other" — covers the unknown `ZSESSIONTYPE = 2` row per RESEARCH §"Open Questions Q1"), parses `ZCONTACTJID` into a `Jid` via the kind-suffix rule (`@s.whatsapp.net` → phone, `@lid` → lid, `@g.us` → group, `0@status` → status, anything else → broadcast). The `Chat.coverage` field is populated as `Coverage(from_ts=<earliest msg ts in this chat>, to_ts=<ZLASTMESSAGEDATE>, asked_window_seconds=None, have_window_seconds=<to_ts - from_ts>, is_full=False)` — earliest msg ts comes from a single grouped query: `SELECT ZCHATSESSION, MIN(ZMESSAGEDATE) FROM ZWAMESSAGE GROUP BY ZCHATSESSION` (Plan 04 may move this into a per-chat lazy probe if perf demands; for now, the join is fine for the verified-live 988-chat scale).
    - `async def find_chat_by_id(chat_id: int) -> Chat | None` — single-row variant.
    - `async def find_chat_by_jid(jid_raw: str) -> Chat | None` — uses `Z_WAChatSession_byContactJIDIndex`.

    `src/whatsapp_mcp/reader/messages.py` — per RESEARCH §"Code Examples → Read a chat window" + §"Core Data Schema Essentials → ZWAMESSAGE → query shapes". Public surface:
    - `async def window(chat_id: int, before_z_sort: float | None, limit: int, include_deleted: bool = False) -> tuple[list[Message], float | None]` — **return shape locked per checker B2:** returns `(messages, last_seen_z_sort)` where `last_seen_z_sort` is the `ZSORT` value of the final row in `messages` (or `None` if `messages` is empty). This lets Plan 04's `read_chat` build its `next_cursor` without exposing `ZSORT` on the public `Message` model. Runs `_SQL_WINDOW` from `schema_v1` (which already inlines the `TOMBSTONE_SQL_WHERE` filter when `include_deleted=False`; Plan 02 implementation may switch between two SQL templates based on the flag). The SQL MUST `SELECT ... ZSORT, ZMESSAGEDATE, ...` so the helper can capture both the cursor anchor (ZSORT) and the timestamp. Uses `1e18` as the sentinel for "no cursor" per RESEARCH §"Code Examples". Maps each row to a `Message` via the private `_row_to_message(row, media_root)` helper (locked to `reader/messages.py` per W4 — see below) that:
      - converts `ZMESSAGEDATE` via `cocoa_to_unix`,
      - parses `ZFROMJID` (or `ZTOJID` when `ZISFROMME=1`) into a `Jid`,
      - maps `ZMESSAGETYPE` to `MessageKind` via the verified-live distribution table in RESEARCH §"Core Data Schema Essentials → ZWAMESSAGE → ZMESSAGETYPE row" (0→text, 1→image, 2→video, 3→audio, 6→system, 7→location, 8→contact, 10→sticker, 11→call, 14→revoked, 15→ephemeral, 59→poll, 66→reaction, anything else → "other"),
      - calls `resolve_media_ref(row, media_root)` when `ZMEDIAITEM` is non-NULL (joining with ZWAMEDIAITEM in the SQL — extend `_SQL_WINDOW` with a `LEFT JOIN ZWAMEDIAITEM mi ON mi.ZMESSAGE = m.Z_PK` and the relevant `mi.*` columns),
      - resolves `quoted_message_id` via a follow-up `SELECT ZSTANZAID FROM ZWAMESSAGE WHERE Z_PK = ?` for non-NULL `ZPARENTMESSAGE` (Plan 04 may batch this; correctness first, then perf).
    - `async def since(chat_id: int, cutoff_unix_ts: int, include_deleted: bool = False) -> list[Message]` — `extract_recent` calls this. Uses `_SQL_SINCE` with `ZMESSAGEDATE >= unix_to_cocoa(cutoff_unix_ts)`.
    - `async def context_around_stanza(message_id: str, before: int, after: int, include_deleted: bool = False) -> list[Message]` — `get_message_context` calls this. Implements RESEARCH §"Core Data Schema Essentials → ZWAMESSAGE → Query shape for get_message_context" (CTE on target by `ZSTANZAID`, then window by `ZSORT BETWEEN target.ZSORT - before AND target.ZSORT + after`).
    - `async def parent_of_stanza(message_id: str) -> Message | None` — second part of get_message_context.
    - `async def latest_timestamp() -> int | None` — runs `SELECT MAX(ZMESSAGEDATE) FROM ZWAMESSAGE`, returns Unix int or None. `doctor` (Plan 05) uses this for `last_message_ts`.

    `src/whatsapp_mcp/reader/groups.py` — per RESEARCH §"Core Data Schema Essentials → ZWAGROUPINFO" + "→ ZWAGROUPMEMBER". Public surface:
    - `async def get_group_info(chat_id: int) -> GroupInfo | None` — joins `ZWACHATSESSION` (for `subject = ZPARTNERNAME`) with `ZWAGROUPINFO` (creator/owner/creation date) and queries `ZWAGROUPMEMBER` for the member list. **Locked per W5 + RESEARCH §"Open Questions (RESOLVED)" Q2/Q3:** sets `description=None` always (the verified-live Catalyst `ZWAGROUPINFO` schema has no description column on the user's Mac) and sets `is_muted=False` always (no authoritative source confirmed cross-machine — defer to v2 once a muted group is observed in test data). Do NOT execute a `PRAGMA table_info(ZWAGROUPINFO)` probe at execute time. Do NOT add `# TODO: confirm during execution` comments — these are locked, not pending. Document the v0.1 limitation in `01-02-SUMMARY.md` so consumers know `description` and `is_muted` are placeholder fields until v2.
    - `async def get_members(chat_id: int) -> list[GroupMember]` — separate accessor for callers that only want the member list.

    `src/whatsapp_mcp/reader/contacts.py` — per RESEARCH §"Pattern 7: JID/LID Model and Dedup" + the dedup recipe. Public surface:
    - `async def search_contacts(query: str, limit: int = 20) -> list[Contact]` — implements the 6-step recipe verbatim from RESEARCH §"Pattern 7" → "Dedup recipe for `search_contacts`": LIKE on `ZWACHATSESSION.ZPARTNERNAME` + `ZWAADDRESSBOOKCONTACT.ZFULLNAME` (latter from `ContactsV2.sqlite` opened via `resolve_contactsv2_path()` — second short-lived RO connection); for each match, parse `ZCONTACTJID` → `Jid` (kind via suffix); for `lid`-kind: lookup `ZWAPHONENUMBERLIDPAIR.ZPHONENUMBER` (third short-lived RO connection against `resolve_lid_path()`); for `phone`-kind: reverse lookup the LID. Dedup by `(phone or lid)`. Set `disambiguation_required=True` if only `@lid` known with no phone resolution.
    - `async def resolve_lid_to_phone(lid: str) -> str | None` — exposed helper for messages.py to enrich `sender_jid` for lid-kind senders. Cached per-process is OK (Plan 04 may add an `lru_cache` wrapper); for now, one query per call.
    - `async def resolve_phone_to_lid(phone: str) -> str | None` — reverse direction.

    `src/whatsapp_mcp/reader/search.py` — per RESEARCH §"Search: LIKE Strategy (READ-04 v0.1)". Public surface:
    - `async def like_search(query: str, chat_id: int | None, sender_jid: str | None, before: int | None, after: int | None, limit: int = 50, include_deleted: bool = False) -> list[Message]` — runs the parameterized LIKE SQL from RESEARCH §"Search: LIKE Strategy". Optional filters use the SQL `(? IS NULL OR col = ?)` pattern (single placeholder bound twice) per RESEARCH. Returns Messages, NOT raw rows — imports `_row_to_message` from `reader.messages` directly (one-direction edge: `search.py` depends on `messages.py`; `messages.py` does NOT depend on `search.py`, so no circular risk). **Locked per W4: do NOT extract into a shared `_row_mapping.py`; do NOT speculatively duplicate the helper.**
    - LIKE matches are case-insensitive (`LOWER(ZTEXT) LIKE LOWER(?)`).

    `src/whatsapp_mcp/reader/__init__.py` — replace the empty file with explicit re-exports of the public async API. Use `__all__ = ["list_chats", "find_chat_by_id", "find_chat_by_jid", "window", "since", "context_around_stanza", "parent_of_stanza", "latest_timestamp", "get_group_info", "get_members", "search_contacts", "resolve_lid_to_phone", "resolve_phone_to_lid", "like_search", "open_ro", "probe_z_version", "SUPPORTED_VERSIONS", "is_supported", "is_tombstone", "resolve_media_ref"]`.

    All five modules: every public function is `async def`; every blocking sqlite work happens inside `await asyncio.to_thread(_blocking_*, ...)`; parameterized SQL only; mypy --strict + ruff full ruleset green; NO imports from `whatsapp_mcp.sender` (REL-05 — Plan 06 re-tests).

    **Open Questions** are RESOLVED in `01-RESEARCH.md` §"Open Questions (RESOLVED)" — the planner has locked the answers; the executor MUST NOT re-investigate at execute time:
    - Q1 RESOLVED: `ZSESSIONTYPE = 2` → `ChatKind="other"` unconditionally (already wired in `chats.py` mapping above).
    - Q2 RESOLVED (W5): `GroupInfo.description = None` always for v0.1; no live PRAGMA probe.
    - Q3 RESOLVED (W5): `GroupInfo.is_muted = False` always for v0.1; no live PRAGMA probe.
    - Q5 RESOLVED: `?mode=ro` opens a WAL DB even with `.sqlite-wal` absent; no special doctor probe required for Phase 1.
    The plan SUMMARY restates these locks for downstream consumers (Plans 04/05/06).
  </action>
  <verify>
    <automated>uv run python -c "import asyncio, inspect; from whatsapp_mcp.reader import list_chats, window, since, context_around_stanza, get_group_info, get_members, search_contacts, like_search, latest_timestamp, resolve_lid_to_phone, resolve_phone_to_lid, parent_of_stanza, find_chat_by_id, find_chat_by_jid; assert all(inspect.iscoroutinefunction(f) for f in [list_chats, window, since, context_around_stanza, get_group_info, get_members, search_contacts, like_search, latest_timestamp, resolve_lid_to_phone, resolve_phone_to_lid, parent_of_stanza, find_chat_by_id, find_chat_by_jid]); print('all-async OK')" &amp;&amp; bash -c '! grep -rE "(?:from|import)\s+whatsapp_mcp\.sender" src/whatsapp_mcp/reader/' &amp;&amp; bash -c '! grep -nE "f\"SELECT|f\".*FROM ZWA" src/whatsapp_mcp/reader/' &amp;&amp; uv run ruff check src/whatsapp_mcp/reader/ &amp;&amp; uv run ruff format --check src/whatsapp_mcp/reader/ &amp;&amp; uv run mypy src/whatsapp_mcp/reader/ &amp;&amp; uv run pytest -m "not live"</automated>
  </verify>
  <acceptance_criteria>
    - Source: every public function declared in `reader/__init__.py`'s `__all__` is `async def` (verified by the inline introspection assertion above).
    - Source: `! grep -rE "(?:from|import)\s+whatsapp_mcp\.sender" src/whatsapp_mcp/reader/` succeeds (REL-05 — no sender imports anywhere in reader/).
    - Source: `! grep -nE 'f"SELECT|f".*FROM ZWA' src/whatsapp_mcp/reader/` succeeds (no f-string SQL — parameterized queries only; defends against SQL injection class).
    - Source: `grep -rE "asyncio\.to_thread" src/whatsapp_mcp/reader/` returns ≥ 8 (one per public async function dispatching to its `_blocking` impl).
    - Source: `grep -cE 'async def list_chats\(' src/whatsapp_mcp/reader/chats.py` returns 1; same shape for `window` in messages.py, `get_group_info` in groups.py, `search_contacts` in contacts.py, `like_search` in search.py.
    - Behavior: importing every public name in `__all__` succeeds (verified above).
    - Behavior: Phase 0 baseline of 28 tests still passes (`uv run pytest -m "not live"` exits 0).
    - REL-05: AST-walk no `whatsapp_mcp.sender` substring (verified above).
    - Lint/type: ruff full ruleset + ruff format + mypy --strict green on all of `src/whatsapp_mcp/reader/`.
    - Open-question artefacts: Plan SUMMARY restates the RESOLVED locks from RESEARCH §"Open Questions (RESOLVED)" — Q1 ChatKind='other' for ZSESSIONTYPE=2; Q2 description=None for v0.1; Q3 is_muted=False for v0.1. No live PRAGMA probe artifacts are produced (W5 lock).
  </acceptance_criteria>
  <done>The reader package is fully wired: 9 public async functions producing locked Plan 01 Pydantic models from parameterized SQL against ChatStorage / LID / ContactsV2 SQLite stores. REL-05 isolation maintained; Phase 0 baseline tests still green; SQL is parameterized; the three open questions from RESEARCH are investigated and documented in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `reader/` ↔ `ChatStorage.sqlite` (file I/O) | The only file-I/O surface in Phase 1; reader holds the user's WhatsApp data plaintext during a query. Read-only by construction (`?mode=ro`); no writes (CLAUDE.md hard rule #3). |
| `reader/` ↔ `LID.sqlite` and `ContactsV2.sqlite` | Same shape as the primary ChatStorage boundary; sibling DBs opened RO via separate `open_ro` calls per RESEARCH §"Pattern 7". |
| `reader.media.resolve_media_ref` ↔ filesystem | Returns absolute path strings that the LLM may forward; path-traversal attack class (lharries#241) defeated by `Path.resolve()` + prefix-check against `paths.resolve_media_root()`. |
| `reader/` ↔ `whatsapp_mcp.sender` | NO BOUNDARY — REL-05 invariant: zero imports between reader/ and sender/. Plan 06 re-tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | SQL injection via reader query parameters (chat_id, sender_jid, query string) | mitigate | Parameterized queries everywhere — `?` placeholders only, never f-string SQL. Verified by `! grep -nE 'f"SELECT|f".*FROM ZWA' src/whatsapp_mcp/reader/` gate in this plan. mypy `--strict` blocks `Any`-typed inputs from reaching the cursor. |
| T-02-02 | Tampering / Information Disclosure | `MediaRef.local_path` escapes the WhatsApp media root via crafted `ZMEDIALOCALPATH` (`../../../etc/passwd`) | mitigate | `reader.media.resolve_media_ref` calls `Path.resolve()` then `startswith(media_root.resolve())` prefix check; returns `None` on escape (RESEARCH §"MediaRef Resolution"). Same threat class as `lharries/whatsapp-mcp#241`. |
| T-02-03 | Denial of Service | `database is locked` storms when WhatsApp checkpoints WAL mid-query (P3) | mitigate | `?mode=ro` URI + `PRAGMA busy_timeout = 5000` + connection-level `timeout=5.0` per RESEARCH §"Pattern 1" (verified live concurrent with WhatsApp's writer 2026-05-13). Plan 04 adds per-tool `wait_for(timeout=N)` on top. |
| T-02-04 | Tampering | Reader writes to `ChatStorage.sqlite` and corrupts WhatsApp's writer | mitigate | Connection opened with `?mode=ro` URI flag — SQLite refuses writes structurally. CLAUDE.md hard rule #3 enforced. No `BEGIN IMMEDIATE`, no `INSERT`, no `UPDATE`, no `DELETE`, no `VACUUM` — only `BEGIN`...`COMMIT` deferred-read txns. |
| T-02-05 | Information Disclosure | Reader parses encrypted/protobuf BLOBs (`ZMEDIAKEY`, `ZMETADATA`, `ZRECEIPTINFO`) | mitigate (DATA-04) | These columns are silently NEVER selected by any reader SQL template. Verified by `grep -cE 'ZMEDIAKEY|ZMETADATA|ZRECEIPTINFO' src/whatsapp_mcp/reader/` returning 0. |
| T-02-06 | Availability | Future WhatsApp Catalyst version ships a column rename; reader crashes with `OperationalError` | mitigate (REL-04) | `Z_VERSION` probe + `SUPPORTED_VERSIONS` set + degraded-mode signal in `doctor` (Plan 05). Read tools (Plan 04) catch `sqlite3.OperationalError` and surface as a structured error directing to `doctor` — not a Python traceback. |
| T-02-07 | Tampering | Reader imports from `whatsapp_mcp.sender` opening a UI-attack vector inside the read path | mitigate (REL-05) | Static gate: `! grep -rE "(?:from|import)\s+whatsapp_mcp\.sender" src/whatsapp_mcp/reader/` succeeds in this plan AND Plan 06's `test_isolation.py` AST-walks every reader/*.py to assert no cross-imports. |
| T-02-08 | Information Disclosure | Reader logs full JIDs (phone numbers / LIDs) at INFO level, leaking PII to stderr / external log aggregators | accept (Phase 1) → mitigate (Phase 3) | Phase 1 reader uses `logger.debug` for verbose query info; INFO and above never log JID values. Phase 3 may add a structured-log redactor when distribution surface widens. |
</threat_model>

<verification>
- All 10 reader files exist; `from whatsapp_mcp.reader import ...` for the 19 names in `__all__` succeeds.
- Every public function is `async def` (introspection-verified).
- No reader file imports from `whatsapp_mcp.sender` (grep gate).
- No reader file uses f-string SQL (grep gate).
- ruff (full ruleset) + ruff format --check + mypy --strict green on all of `src/whatsapp_mcp/reader/`.
- Phase 0 baseline `uv run pytest -m "not live"` still reports 28 passed.
- Open-questions findings (Q1/Q2/Q3) documented in `01-02-SUMMARY.md`.
</verification>

<success_criteria>
- REL-01 satisfied: `open_ro` opens `?mode=ro` URI connections with `busy_timeout=5000`; verified-live concurrent-with-writer behavior.
- REL-02 satisfied at the reader tier: every public function is async and dispatches blocking work via `asyncio.to_thread`.
- REL-04 satisfied: `Z_VERSION` probe + `SUPPORTED_VERSIONS = frozenset({1})` + `is_supported(version)` predicate; Plan 05's doctor surfaces unsupported versions as degraded-mode without crashing reader calls.
- READ-08 satisfied at the data tier: `is_tombstone` predicate matches the verified-live row pattern; `TOMBSTONE_SQL_WHERE` constant inlined into every messages SQL template; default `include_deleted=False` filters tombstones.
- DATA-03 satisfied at the resolution tier: `resolve_media_ref` returns absolute paths inside the media root, refuses traversal, never inlines bytes.
- DATA-04 satisfied at the SQL tier: encrypted/protobuf BLOB columns are never selected; verified by grep gate.
- REL-05 invariant maintained: zero `whatsapp_mcp.sender` imports anywhere in `reader/`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-read-mvp-read-only/01-02-SUMMARY.md` documenting (a) the 19-name reader public surface (note `reader.window` returns `tuple[list[Message], float | None]` per B2), (b) the SQL templates registered in `schema_v1.py` and which index each hits, (c) the locked-by-research answers to the open questions (Q1 → ChatKind='other'; Q2 → description=None for v0.1; Q3 → is_muted=False for v0.1; Q5 → no special WAL-sidecar probe), (d) the W4 invariant that `_row_to_message` lives in `reader/messages.py` and is imported by `reader/search.py` (one-direction edge). Plan 04 (tools) and Plan 05 (doctor) consume this SUMMARY as their reader API contract.
</output>
</content>
</invoke>