---
phase: 04-rust-port-parallel-binary
plan: 01
title: Rust workspace skeleton (no behavior, structure only)
type: execute
wave: 1
depends_on: []
requirements: []   # Phase 4 has no v1 REQ-IDs; mapped to ROADMAP §"Phase 4" SC1 via must_haves
files_modified:
  - rs/Cargo.toml
  - rs/Cargo.lock
  - rs/rustfmt.toml
  - rs/clippy.toml
  - rs/.gitignore
  - rs/.cargo/config.toml
  - rs/deny.toml
  - rs/README.md
  - rs/whatsapp-desktop-mcp-rs/Cargo.toml
  - rs/whatsapp-desktop-mcp-rs/src/main.rs
  - rs/crates/wamcp-models/Cargo.toml
  - rs/crates/wamcp-models/src/lib.rs
  - rs/crates/wamcp-permissions/Cargo.toml
  - rs/crates/wamcp-permissions/src/lib.rs
  - rs/crates/wamcp-paths/Cargo.toml
  - rs/crates/wamcp-paths/src/lib.rs
  - rs/crates/wamcp-tools/Cargo.toml
  - rs/crates/wamcp-tools/src/lib.rs
  - rs/crates/wamcp-reader/Cargo.toml
  - rs/crates/wamcp-reader/src/lib.rs
  - rs/crates/wamcp-sender/Cargo.toml
  - rs/crates/wamcp-sender/src/lib.rs
  - rs/tests/isolation.rs
autonomous: true
must_haves:
  truths:
    - "Top-level rs/ directory exists alongside src/whatsapp_desktop_mcp/ with zero shared source files (SC1)"
    - "cd rs && cargo build --workspace --locked succeeds against a workspace with 1 binary crate + 6 lib crate stubs (SC1)"
    - "cd rs && cargo test --workspace --locked passes the dep-graph isolation test (SC1, REL-05 D-24 carry)"
    - "Running uv run pytest -m 'not live' on the Python suite still passes — no Python source byte-changes, no Rust toolchain required (SC1)"
    - "wamcp-reader crate's Cargo.toml does NOT list wamcp-sender in [dependencies] and vice versa (REL-05 D-24 enforced structurally from day one)"
  artifacts:
    - path: "rs/Cargo.toml"
      provides: "Cargo workspace root (members + [workspace.package] + [workspace.dependencies] + [workspace.lints])"
      contains: "resolver = \"2\""
    - path: "rs/Cargo.lock"
      provides: "Pinned dep versions, committed for reproducible builds (CONTEXT D-26)"
    - path: "rs/whatsapp-desktop-mcp-rs/src/main.rs"
      provides: "Binary crate entry — compiles, prints tracing line to stderr, exits cleanly (no rmcp wiring yet — that's 04-02)"
    - path: "rs/crates/wamcp-reader/src/lib.rs"
      provides: "Empty Phase 4.x reservation crate enforcing REL-05 by being empty"
      contains: "Phase 4.x: reserved"
    - path: "rs/crates/wamcp-sender/src/lib.rs"
      provides: "Empty Phase 4.x reservation crate enforcing REL-05 by being empty"
      contains: "Phase 4.x: reserved"
    - path: "rs/tests/isolation.rs"
      provides: "Integration test asserting wamcp-reader does NOT depend on wamcp-sender via cargo metadata JSON walk"
    - path: "rs/.gitignore"
      provides: "Excludes rs/target/; INCLUDES rs/Cargo.lock"
      contains: "target/"
    - path: "rs/README.md"
      provides: "Build / test / parity instructions for the Rust workspace (CONTEXT D-30)"
  key_links:
    - from: "rs/Cargo.toml"
      to: "rs/whatsapp-desktop-mcp-rs/Cargo.toml + rs/crates/wamcp-*/Cargo.toml"
      via: "[workspace] members = [...]"
      pattern: "members\\s*=\\s*\\["
    - from: "rs/whatsapp-desktop-mcp-rs/Cargo.toml"
      to: "rs/Cargo.toml [workspace.dependencies]"
      via: "<dep>.workspace = true"
      pattern: "workspace\\s*=\\s*true"
    - from: "rs/tests/isolation.rs"
      to: "cargo metadata JSON output for wamcp-reader / wamcp-sender"
      via: "subprocess invocation of cargo metadata --format-version 1"
      pattern: "cargo.*metadata"
---

<objective>
Stand up the Rust Cargo workspace under `rs/` so that `cd rs && cargo build --workspace --locked` and `cd rs && cargo test --workspace --locked` are both green against a workspace containing one binary crate (`whatsapp-desktop-mcp-rs`) and six lib crate stubs (`wamcp-models`, `wamcp-permissions`, `wamcp-paths`, `wamcp-tools`, `wamcp-reader`, `wamcp-sender`). No real behavior lands in this plan — Plan 04-02 fills in the doctor tool and probes; this plan establishes the load-bearing scaffolding (workspace TOML, lints, formatting, dep-graph isolation test) that everything else assumes.

Purpose: realize ROADMAP §"Phase 4" SC1 ("New top-level directory `rs/` (Cargo workspace) ships alongside `src/whatsapp_desktop_mcp/` (Python). Building Rust does NOT touch Python; running Python tests does NOT require Rust toolchain. Both binaries co-exist in the repo root with zero shared source files.") and bake REL-05 D-24 into the Cargo dep graph from day one so that no future commit can accidentally introduce a `wamcp-sender → wamcp-reader` edge.

Output: a buildable, testable, lint-clean Rust workspace skeleton with stub crates, ready to host the doctor tool implementation in Plan 04-02. Python source files (`src/whatsapp_desktop_mcp/**/*.py`) are byte-stable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rust-port-parallel-binary/04-CONTEXT.md
@.planning/phases/04-rust-port-parallel-binary/04-RESEARCH.md
</context>

<interfaces>
<!-- The workspace skeleton creates NEW Rust source surfaces from scratch — there is no existing Rust code to integrate against. -->
<!-- Python sources to MIRROR (do NOT import; do NOT modify; structure only — actual port lands in 04-02): -->

From `src/whatsapp_desktop_mcp/__init__.py` and tree layout:
- Package root: `src/whatsapp_desktop_mcp/`
- Subpackages mirrored as Cargo lib crates: `models/` → `wamcp-models`, `permissions/` → `wamcp-permissions`, `tools/` → `wamcp-tools`, `paths.py` → `wamcp-paths`, `reader/` (deferred) → `wamcp-reader`, `sender/` (deferred) → `wamcp-sender`
- Server entry point `whatsapp_desktop_mcp/cli.py` + `whatsapp_desktop_mcp/server.py` → Rust binary `whatsapp-desktop-mcp-rs/src/main.rs`

From `pyproject.toml` (build/lint floors carried to Rust workspace conventions):
- Python ruff `line-length = 100` → Rust rustfmt `max_width = 100` (D-27)
- Python mypy strict → Rust `clippy::correctness = "deny"` + `clippy::suspicious = "deny"` (D-28)
- Python `T201` (no print) → Rust `clippy::print_stdout = "deny"` (D-08)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Workspace root TOML + formatter/lint configs + .cargo/config.toml + deny.toml + .gitignore + rs/README.md</name>
  <files>
    rs/Cargo.toml,
    rs/rustfmt.toml,
    rs/clippy.toml,
    rs/.gitignore,
    rs/.cargo/config.toml,
    rs/deny.toml,
    rs/README.md
  </files>
  <read_first>
    .planning/phases/04-rust-port-parallel-binary/04-RESEARCH.md (§"Pattern 1: Workspace root `Cargo.toml` (complete file recommendation)" lines ~313-358 — verbatim TOML to lift),
    .planning/phases/04-rust-port-parallel-binary/04-RESEARCH.md (§"Recommended Project Structure" lines ~262-311 for tree layout),
    .planning/phases/04-rust-port-parallel-binary/04-RESEARCH.md (§"Open Questions" items 2 + 3 for cargo-deny + .cargo/config rationale),
    .planning/phases/04-rust-port-parallel-binary/04-RESEARCH.md (§"Pitfall 9" — workspace lints requires resolver=2 + member [lints] workspace=true),
    .planning/phases/04-rust-port-parallel-binary/04-CONTEXT.md (D-04 [workspace.package], D-26 lockfile, D-27 rustfmt, D-28 clippy, D-08 stdout-purity)
  </read_first>
  <action>
    Create the workspace root and supporting config files.

    `rs/Cargo.toml` — workspace root TOML lifted verbatim from 04-RESEARCH.md §"Pattern 1: Workspace root `Cargo.toml` (complete file recommendation)". Honor:
    - `[workspace] resolver = "2"` (Pitfall 9 — required for [workspace.lints] propagation)
    - `members = ["whatsapp-desktop-mcp-rs", "crates/wamcp-models", "crates/wamcp-permissions", "crates/wamcp-paths", "crates/wamcp-tools", "crates/wamcp-reader", "crates/wamcp-sender"]` per CONTEXT D-02
    - `[workspace.package]` per CONTEXT D-04: `version = "0.0.0"`, `edition = "2021"`, `rust-version = "1.75"`, `license = "MIT"`, `repository = "https://github.com/jqueguiner/whatsapp-desktop-mcp"`, `homepage = "https://github.com/jqueguiner/whatsapp-desktop-mcp"`
    - `[workspace.dependencies]` block per 04-RESEARCH.md §"Concrete `[workspace.dependencies]` block (recommendation)" — pin every crate per the research-locked tactical adjustments: `rmcp = { version = "1.7", features = ["server", "macros", "transport-io"] }`, `tokio = { version = "1", features = ["macros", "rt-multi-thread", "io-std", "process"] }`, `serde = { version = "1", features = ["derive"] }`, `serde_json = "1"`, `anyhow = "1"`, `thiserror = "2"`, `tracing = "0.1"`, `tracing-subscriber = { version = "0.3", features = ["fmt"] }`, `regex = "1"`, `clap = { version = "4.5", features = ["derive"] }` (NOT 4.6 — MSRV trap per Pitfall 2), `plist = "1.7"` (NOT 1.9 — MSRV trap per Pitfall 2), `objc2 = "0.6"`, `objc2-foundation = { version = "0.3", features = ["std"] }`, `objc2-application-services = { version = "0.3", features = ["std"] }`, `rusqlite = { version = "0.31", features = ["bundled"] }` (CONTEXT D-17 explicit lock; deferred USE to Phase 4.x).
    - `[workspace.lints.rust]` per 04-RESEARCH.md §"Pattern 1": `unsafe_op_in_unsafe_fn = "deny"`, `unused_must_use = "deny"`.
    - `[workspace.lints.clippy]` per CONTEXT D-08 + D-28: `print_stdout = "deny"` (load-bearing for stdout-purity), `print_stderr = "warn"`, `correctness = { level = "deny", priority = -1 }`, `suspicious = { level = "deny", priority = -1 }`, `style = { level = "warn", priority = -1 }`, `unwrap_used = "warn"`, `expect_used = "warn"`. Do NOT enable `pedantic = "warn"` (D-28 explicit reject — too noisy).
    - `[profile.release]`: `strip = true`, `lto = "thin"`, `codegen-units = 1` per 04-RESEARCH.md §"Pattern 1".

    `rs/rustfmt.toml` per CONTEXT D-27: `edition = "2021"`, `max_width = 100`, `imports_granularity = "Module"`, `group_imports = "StdExternalCrate"`.

    `rs/clippy.toml` per CONTEXT D-28: `msrv = "1.75"`. (Lint LEVELS live in Cargo.toml [workspace.lints.clippy]; clippy.toml is reserved for non-level config like MSRV.)

    `rs/.gitignore` per CONTEXT D-26: single entry `target/` (relative to `rs/` — do NOT add `Cargo.lock`; lockfile MUST be committed for reproducible builds).

    `rs/.cargo/config.toml` per 04-RESEARCH.md §"Open Questions" item 3 (Claude discretion → recommend YES) — `[target.aarch64-apple-darwin] rustflags = ["-C", "target-cpu=apple-m1"]` for ~5–15% perf wins on async + JSON paths on Apple Silicon. Apple-Silicon-only scope per CONTEXT D-19; Phase 4.x cross-compile would add an `[target.x86_64-apple-darwin]` block without conflict.

    `rs/deny.toml` per 04-RESEARCH.md §"Open Questions" item 2 (Claude discretion → recommend YES) — produce by running `cargo deny init` semantics: minimal config covering `[advisories] db-urls = ["https://github.com/rustsec/advisory-db"]`, `[licenses] allow = ["MIT", "Apache-2.0", "Apache-2.0 WITH LLVM-exception", "BSD-2-Clause", "BSD-3-Clause", "ISC", "Unicode-DFS-2016", "Unicode-3.0", "CC0-1.0", "Zlib", "MPL-2.0"]`, `[bans] multiple-versions = "warn"`. No custom rules; default config is fine.

    `rs/README.md` per CONTEXT D-30 — Rust-specific README inside the workspace. Sections:
    - "Rust port — what this is" (1 paragraph: parallel binary, Phase 4 v0 = doctor only, Python is load-bearing v1.0)
    - "Build" (`cargo build --release --locked`)
    - "Test" (`cargo test --workspace --locked`)
    - "Install" (the cargo install --git invocation per CONTEXT D-29 + Pitfall 8 correct form: `cargo install --git https://github.com/jqueguiner/whatsapp-desktop-mcp --root /usr/local whatsapp-desktop-mcp-rs` — workspace member NAME, not path)
    - "Parity test" (placeholder: forward-references `tests/integration/test_rust_python_parity.py` from Plan 04-03; document `RUN_LIVE_RUST=1` env var)
    - "TCC churn note" carry-over per 04-RESEARCH.md §"Runtime State Inventory": each cargo install creates a new binary path; stick to one --root for grant stability
    - Cross-link to project root README and CLAUDE.md
  </action>
  <verify>
    <automated>
      cd /Users/jlqueguiner/dev/whatsapp-mcp/rs && \
      grep -q 'resolver = "2"' Cargo.toml && \
      grep -q 'rust-version = "1.75"' Cargo.toml && \
      grep -q 'rmcp = { version = "1.7"' Cargo.toml && \
      grep -q 'clap = { version = "4.5"' Cargo.toml && \
      grep -q 'plist = "1.7"' Cargo.toml && \
      grep -q 'objc2 = "0.6"' Cargo.toml && \
      grep -q 'objc2-foundation = { version = "0.3"' Cargo.toml && \
      grep -q 'objc2-application-services = { version = "0.3"' Cargo.toml && \
      grep -q 'rusqlite = { version = "0.31"' Cargo.toml && \
      grep -q 'print_stdout = "deny"' Cargo.toml && \
      grep -q 'max_width = 100' rustfmt.toml && \
      grep -q 'msrv = "1.75"' clippy.toml && \
      grep -qx 'target/' .gitignore && \
      grep -q 'target-cpu=apple-m1' .cargo/config.toml && \
      grep -q 'cargo install --git' README.md && \
      grep -q 'whatsapp-desktop-mcp-rs' README.md && \
      ! grep -q 'pedantic' Cargo.toml
    </automated>
  </verify>
  <done>
    Workspace root TOML, rustfmt.toml, clippy.toml, .gitignore, .cargo/config.toml, deny.toml, README.md all exist under rs/ with the values specified. No member-crate Cargo.toml files yet (Task 2). cargo build NOT yet runnable (depends on member crates existing — Task 2 + 3).
  </done>
</task>

<task type="auto">
  <name>Task 2: Member crate stubs (binary + 6 lib crates) + workspace lints inheritance</name>
  <files>
    rs/whatsapp-desktop-mcp-rs/Cargo.toml,
    rs/whatsapp-desktop-mcp-rs/src/main.rs,
    rs/crates/wamcp-models/Cargo.toml,
    rs/crates/wamcp-models/src/lib.rs,
    rs/crates/wamcp-permissions/Cargo.toml,
    rs/crates/wamcp-permissions/src/lib.rs,
    rs/crates/wamcp-paths/Cargo.toml,
    rs/crates/wamcp-paths/src/lib.rs,
    rs/crates/wamcp-tools/Cargo.toml,
    rs/crates/wamcp-tools/src/lib.rs,
    rs/crates/wamcp-reader/Cargo.toml,
    rs/crates/wamcp-reader/src/lib.rs,
    rs/crates/wamcp-sender/Cargo.toml,
    rs/crates/wamcp-sender/src/lib.rs
  </files>
  <read_first>
    .planning/phases/04-rust-port-parallel-binary/04-RESEARCH.md (§"Pattern 2: Binary crate `Cargo.toml`" lines ~360-388 — verbatim binary TOML),
    .planning/phases/04-rust-port-parallel-binary/04-RESEARCH.md (§"Recommended Project Structure" lines ~262-311 — full tree),
    .planning/phases/04-rust-port-parallel-binary/04-RESEARCH.md (§"Pitfall 9" — every member needs [lints] workspace = true to inherit workspace lints),
    .planning/phases/04-rust-port-parallel-binary/04-CONTEXT.md (D-02 member layout, D-03 REL-05 D-24 isolation rule, D-22 binary version 0.0.0)
  </read_first>
  <action>
    Create the binary crate + 6 lib crate stubs. Each crate's `Cargo.toml` MUST end with `[lints]\nworkspace = true` per Pitfall 9 (workspace lints do NOT propagate without this in every member).

    `rs/whatsapp-desktop-mcp-rs/Cargo.toml` per 04-RESEARCH.md §"Pattern 2": `[package]` with `name = "whatsapp-desktop-mcp-rs"`, all metadata fields inherited via `<field>.workspace = true` (version, edition, rust-version, license, repository, homepage), plus `description = "Rust port of the macOS WhatsApp Desktop MCP server (experimental)"`. `[[bin]] name = "whatsapp-desktop-mcp-rs", path = "src/main.rs"`. `[dependencies]` includes ONLY what the skeleton main.rs needs in 04-01: `anyhow = { workspace = true }`, `tracing = { workspace = true }`, `tracing-subscriber = { workspace = true }`, `tokio = { workspace = true }`, `clap = { workspace = true }`. Do NOT depend on rmcp or wamcp-tools yet — Plan 04-02 wires those in. Append `[lints] workspace = true`.

    `rs/whatsapp-desktop-mcp-rs/src/main.rs` (skeleton — replaced by Plan 04-02 with the full ~50-line server entry):
    - Module docstring: "Skeleton — full rmcp + tracing + DoctorTool wiring lands in Plan 04-02."
    - `use anyhow::Result;`, `use clap::Parser;`, `use tracing_subscriber::EnvFilter;`
    - `#[derive(Parser, Debug)] #[command(name = "whatsapp-desktop-mcp-rs", version)] struct Cli {}` (no flags v0; --version/--help auto-provided by clap derive)
    - `#[tokio::main] async fn main() -> Result<()> { let _cli = Cli::parse(); tracing_subscriber::fmt().with_env_filter(EnvFilter::try_from_default_env().unwrap_or_else(|_| EnvFilter::new("info"))).with_writer(std::io::stderr).with_ansi(false).init(); tracing::info!(version = env!("CARGO_PKG_VERSION"), "whatsapp-desktop-mcp-rs skeleton starting (no MCP server yet — Plan 04-02 wires rmcp)"); Ok(()) }`
    - The binary MUST exit 0 immediately. No rmcp; no stdio loop; no DoctorTool; no MCP handshake. The point is: a runnable, tracing-to-stderr binary that compiles against the locked workspace.

    For each lib crate, create `Cargo.toml` with `[package] name = "wamcp-<name>"`, all metadata `<field>.workspace = true`, NO `[dependencies]` block (stubs depend on nothing), and `[lints] workspace = true`. Then create `src/lib.rs` with a single doc comment and `// Phase 4 v0: stub` or `// Phase 4.x: reserved` per the table below.

    | Crate | lib.rs comment | Notes |
    |-------|----------------|-------|
    | `wamcp-models` | `// Phase 4 v0: stub — DoctorReport / PermissionCheck land in Plan 04-02.` | No deps. |
    | `wamcp-permissions` | `// Phase 4 v0: stub — fda/automation/accessibility probes land in Plan 04-02.` | No deps. |
    | `wamcp-paths` | `// Phase 4 v0: stub — resolve_chatstorage_path lands in Plan 04-02 (mirror src/whatsapp_desktop_mcp/paths.py).` | No deps. |
    | `wamcp-tools` | `// Phase 4 v0: stub — DoctorTool with #[tool_router] lands in Plan 04-02.` | No deps. |
    | `wamcp-reader` | `// Phase 4.x: reserved. MUST NOT depend on wamcp-sender (CONTEXT D-03 / REL-05 D-24).` | No deps. EMPTY in v0. |
    | `wamcp-sender` | `// Phase 4.x: reserved. MAY depend ONLY on a future wamcp-reader-connection thin sub-crate, NEVER on wamcp-reader proper (CONTEXT D-03 / REL-05 D-24).` | No deps. EMPTY in v0. |

    CRITICAL: do NOT add `wamcp-reader` to `wamcp-sender`'s [dependencies] (or vice versa) — REL-05 D-24 is enforced structurally by the empty stubs and by the dep-graph isolation test in Task 3. The stubs MUST stay empty in v0 so the test in Task 3 cannot break by accident.

    Run `cargo build --workspace --locked` once after creating all files to generate `rs/Cargo.lock` (will be committed). The build MUST succeed with zero warnings (workspace lints are now active on the skeletons).

    Note: do NOT modify any file under src/whatsapp_desktop_mcp/. Python source byte-stable invariant per ROADMAP §"Phase 4" SC1 — Plan 04-01's files_modified contains zero src/whatsapp_desktop_mcp/* paths by design.
  </action>
  <verify>
    <automated>
      cd /Users/jlqueguiner/dev/whatsapp-mcp/rs && \
      cargo build --workspace --locked 2>&1 | tee /tmp/04-01-build.log && \
      ! grep -E '^(warning|error)' /tmp/04-01-build.log && \
      test -f Cargo.lock && \
      grep -lq 'workspace = true' whatsapp-desktop-mcp-rs/Cargo.toml crates/wamcp-models/Cargo.toml crates/wamcp-permissions/Cargo.toml crates/wamcp-paths/Cargo.toml crates/wamcp-tools/Cargo.toml crates/wamcp-reader/Cargo.toml crates/wamcp-sender/Cargo.toml && \
      for c in wamcp-models wamcp-permissions wamcp-paths wamcp-tools wamcp-reader wamcp-sender; do \
        grep -A1 '^\[lints\]' "crates/$c/Cargo.toml" | grep -q 'workspace = true' || { echo "MISSING [lints] workspace=true in $c"; exit 1; }; \
      done && \
      grep -A1 '^\[lints\]' whatsapp-desktop-mcp-rs/Cargo.toml | grep -q 'workspace = true' && \
      ! grep -E 'wamcp-(reader|sender)' crates/wamcp-reader/Cargo.toml | grep -v 'name =' && \
      ! grep -E 'wamcp-reader' crates/wamcp-sender/Cargo.toml | grep -v 'name =' && \
      ./target/debug/whatsapp-desktop-mcp-rs --version 2>&1 | grep -q '0.0.0'
    </automated>
  </verify>
  <done>
    Workspace builds cleanly with zero warnings. Cargo.lock committed. All 7 member crates have `[lints] workspace = true` so workspace lints (print_stdout=deny, etc.) are active on every member. The binary `whatsapp-desktop-mcp-rs` runs, prints a tracing line to stderr, and exits 0; `--version` reports `0.0.0` per CONTEXT D-22. No file under src/whatsapp_desktop_mcp/ has been modified.
  </done>
</task>

<task type="auto">
  <name>Task 3: REL-05 D-24 dep-graph isolation integration test + Python-byte-stable invariant guard</name>
  <files>
    rs/tests/isolation.rs
  </files>
  <read_first>
    .planning/phases/04-rust-port-parallel-binary/04-CONTEXT.md (D-03 — REL-05 D-24 evolution mirrored in Cargo; rs/tests/isolation.rs parses Cargo.lock graph),
    .planning/phases/04-rust-port-parallel-binary/04-RESEARCH.md (§"Don't Hand-Roll" row "Cargo dep-graph isolation check" — recommends `cargo metadata` JSON walk over text-grep),
    src/whatsapp_desktop_mcp/tools/doctor.py (top-level docstring referencing the original Python REL-05 isolation test for parity in spirit),
    CLAUDE.md (hard rule #1: Reader and Sender MUST NOT import each other)
  </read_first>
  <action>
    Create `rs/tests/isolation.rs` — a Cargo integration test (lives at workspace root under `tests/`) that asserts REL-05 D-24 in the Cargo dep graph.

    Implementation approach (per 04-RESEARCH.md §"Don't Hand-Roll"): use `std::process::Command` to invoke `cargo metadata --format-version 1 --no-deps` from the test, parse the JSON via `serde_json` (or `String` substring matching against the well-defined JSON shape), walk the `packages[]` array to find `wamcp-reader` and `wamcp-sender`, inspect each's `dependencies[]` list, and assert NEITHER lists the other.

    Test functions to include:

    1. `#[test] fn wamcp_reader_does_not_depend_on_wamcp_sender()` — fails with a clear message if `wamcp-reader`'s dependencies include `wamcp-sender`. Message: `"REL-05 D-24 violation: wamcp-reader MUST NOT depend on wamcp-sender. See CONTEXT.md D-03 + CLAUDE.md hard rule #1."`

    2. `#[test] fn wamcp_sender_does_not_depend_on_wamcp_reader()` — symmetric guard. Message: `"REL-05 D-24 violation: wamcp-sender MUST NOT depend on wamcp-reader (only the future wamcp-reader-connection thin sub-crate is permitted). See CONTEXT.md D-03."`

    3. `#[test] fn wamcp_reader_and_sender_are_empty_v0_stubs()` — opens `crates/wamcp-reader/src/lib.rs` and `crates/wamcp-sender/src/lib.rs` via `std::fs::read_to_string`, asserts each contains the literal string `"Phase 4.x: reserved"` and contains zero `pub fn` / `pub struct` / `pub enum` declarations (use simple substring checks: assert `!contents.contains("pub fn ")`, etc.). The point is to make accidental code-addition to these stubs visible at test time during Phase 4 v0.

    Test dependencies (`[dev-dependencies]` in `rs/Cargo.toml` if needed): the integration test under `rs/tests/` is part of the workspace's virtual manifest — it does NOT have a `Cargo.toml` of its own. To keep this simple and avoid pulling serde_json into the workspace dev deps, parse the `cargo metadata` JSON output via straightforward `String::contains` / `String::find` substring checks scoped to per-package blocks (the JSON shape is stable and well-known: each package object contains `"name": "<crate>"` and a `"dependencies": [{"name": "<dep>", ...}]` array). Document this choice with a top-of-file comment: `// Substring-based parsing avoids adding serde_json as a dev-dep at the workspace root just for one integration test. The JSON shape from `cargo metadata --format-version 1` is stable per <https://doc.rust-lang.org/cargo/commands/cargo-metadata.html>.`

    The test invokes `Command::new("cargo").args(["metadata", "--format-version", "1", "--no-deps"]).current_dir(env!("CARGO_MANIFEST_DIR")).output()` — `CARGO_MANIFEST_DIR` resolves to the workspace root for top-level `tests/` files.

    The Python-byte-stable invariant is enforced procedurally (no test) by the fact that `files_modified` for plan 04-01 lists ZERO paths under `src/whatsapp_desktop_mcp/`. Document this in the file header comment of `rs/tests/isolation.rs`: `// Companion invariant: Plan 04-01's files_modified contains zero src/whatsapp_desktop_mcp/* paths. Verified by inspecting 04-01-PLAN.md frontmatter.`
  </action>
  <verify>
    <automated>
      cd /Users/jlqueguiner/dev/whatsapp-mcp/rs && \
      cargo test --workspace --locked --test isolation 2>&1 | tee /tmp/04-01-isolation.log && \
      grep -q 'test wamcp_reader_does_not_depend_on_wamcp_sender ... ok' /tmp/04-01-isolation.log && \
      grep -q 'test wamcp_sender_does_not_depend_on_wamcp_reader ... ok' /tmp/04-01-isolation.log && \
      grep -q 'test wamcp_reader_and_sender_are_empty_v0_stubs ... ok' /tmp/04-01-isolation.log && \
      cd /Users/jlqueguiner/dev/whatsapp-mcp && \
      git diff --name-only HEAD -- src/whatsapp_desktop_mcp/ | tee /tmp/04-01-python-diff.log && \
      test ! -s /tmp/04-01-python-diff.log && \
      uv run pytest -m 'not live' tests/unit/test_stdout_purity.py -x -q
    </automated>
  </verify>
  <done>
    `cargo test --workspace --locked` passes 3 isolation tests asserting wamcp-reader ⊥ wamcp-sender. Python suite (`uv run pytest -m 'not live' tests/unit/test_stdout_purity.py`) still passes — the Rust workspace addition has not broken Python. `git diff --name-only HEAD -- src/whatsapp_desktop_mcp/` is EMPTY (Python source byte-stable invariant SC1 satisfied). No member of `src/whatsapp_desktop_mcp/**/*.py` has been touched in this plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cargo dep graph | The wamcp-reader / wamcp-sender package boundary — REL-05 D-24 isolation enforced via Cargo metadata, not just convention. |
| Python ↔ Rust source trees | `src/whatsapp_desktop_mcp/` (Python, untouched) and `rs/` (Rust, new) — zero shared files; no imports across the boundary; CI pipelines run independently. |
| `cargo install --git` source delivery | Maintainer publishes via git tag; user runs cargo install; Cargo build happens on user's machine (no pre-built binary attack surface in v0). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Cargo dep graph (a future commit accidentally adds `wamcp-sender` to `wamcp-reader`'s [dependencies] or vice versa) | mitigate | `rs/tests/isolation.rs` parses `cargo metadata --format-version 1 --no-deps` JSON and fails the build with a clear REL-05 message if the dep edge appears. Runs in CI on every PR (Plan 04-03 wires the rust-lint-test job). |
| T-04-02 | Tampering | Python source (a future Rust-side commit accidentally edits a Python file) | mitigate | Plan 04-01's `files_modified` lists ZERO paths under `src/whatsapp_desktop_mcp/`. Verify command in Task 3 asserts `git diff --name-only HEAD -- src/whatsapp_desktop_mcp/` is empty before commit. |
| T-04-03 | Tampering | Cargo supply chain (yanked crate or known-CVE crate slipping into Cargo.lock) | mitigate | `rs/deny.toml` (this plan) + `cargo deny check` step (Plan 04-03 ci.yml job) catches advisories. Cargo.lock committed (CONTEXT D-26) ensures reproducible builds. |
| T-04-04 | Tampering | objc2 family version skew breaking the workspace (Pitfall 6) | mitigate | All `objc2-*` crates pinned to compatible major lines (`0.6` / `0.3` / `0.3` per research-locked override of CONTEXT D-09's stale `0.5` / `0.2`). Workspace deps single-source-of-truth (no per-crate duplication). |
| T-04-05 | Information Disclosure | `rs/.cargo/config.toml` accidentally committed with maintainer-specific paths | accept | Config contains only `target-cpu=apple-m1` rustflags — no paths, no secrets, no machine-specific identifiers. Public rust target tuning. |
| T-04-06 | Denial of Service | MSRV drift (`clap 4.6` / `plist 1.9` requiring Rust 1.85+ would break the locked 1.75 floor — Pitfall 2) | mitigate | Pin minor versions (`clap = "4.5"`, `plist = "1.7"`) in [workspace.dependencies]. CI enforces with `--locked`. |
</threat_model>

<verification>
- `cd rs && cargo build --workspace --locked` exits 0 with no warnings.
- `cd rs && cargo test --workspace --locked` exits 0 with all 3 isolation tests passing.
- `cd rs && cargo fmt --all -- --check` exits 0 (rustfmt config honored).
- `cd rs && cargo clippy --workspace --all-targets --locked -- -D warnings` exits 0 (workspace lints active).
- `git diff --name-only HEAD -- src/whatsapp_desktop_mcp/` is empty (Python source byte-stable).
- `uv run pytest -m 'not live'` still passes (no Rust toolchain dependency on the Python suite).
- `rs/Cargo.lock` exists and is committed (NOT in .gitignore).
- `rs/target/` is in `rs/.gitignore`.
</verification>

<success_criteria>
- ROADMAP §"Phase 4" SC1 satisfied: `rs/` exists alongside `src/whatsapp_desktop_mcp/`; building Rust does NOT touch Python; running Python tests does NOT require Rust toolchain; both binaries co-exist with zero shared source files.
- REL-05 D-24 (CONTEXT D-03) enforced structurally via Cargo dep graph, not convention.
- Workspace lints (`print_stdout = "deny"`, etc.) propagate to every member crate per Pitfall 9.
- Python source `src/whatsapp_desktop_mcp/**/*.py` is byte-stable (RUST-NOOVERRIDE invariant).
- Plan 04-02 has a buildable, lint-clean workspace skeleton to fill in.
</success_criteria>

<output>
After completion, create `.planning/phases/04-rust-port-parallel-binary/04-01-SUMMARY.md` per `templates/summary.md` capturing:
- Crate versions actually pinned in Cargo.toml (verify against research-locked tactical adjustments)
- Cargo.lock total line count (informational; Pitfall 1 fingerprint for future drift checks)
- Whether `cargo deny check` was run as part of Task 1 verification (recommended yes per Open Question 2)
- Any deviation from the research-locked tactical adjustments (rmcp 1.7, objc2 0.6/0.3/0.3, clap 4.5, plist 1.7) with rationale
</output>
