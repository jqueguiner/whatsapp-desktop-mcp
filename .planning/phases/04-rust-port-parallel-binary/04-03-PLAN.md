---
phase: 04-rust-port-parallel-binary
plan: 03
title: CI rust-lint-test job + release.yml rust-build job + cross-binary parity test + README install row + rs/README.md parity instructions
type: execute
wave: 3
depends_on: [04-01, 04-02]
requirements: []   # Phase 4 has no v1 REQ-IDs; mapped to ROADMAP §"Phase 4" SC3 + SC4 + SC5 via must_haves
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
  - tests/integration/test_rust_python_parity.py
  - README.md
  - rs/README.md
autonomous: true
must_haves:
  truths:
    - "On every PR + push to main, .github/workflows/ci.yml runs the new `rust-lint-test` job in PARALLEL with the existing `lint-type-test` job (Python); a failure in one does NOT block the other (SC1 isolation + SC3 enforcement) (CONTEXT D-25)"
    - "`RUN_LIVE_RUST=1 uv run pytest tests/integration/test_rust_python_parity.py -v` on a Mac with WhatsApp installed + 3 TCC perms granted + Rust release binary built, passes — both binaries' doctor output agrees on shape AND per-bucket TCC state (SC3, CONTEXT D-23)"
    - "On `git tag v*` push, .github/workflows/release.yml runs the new `rust-build` job downstream of `publish` and attaches `whatsapp-desktop-mcp-rs-{version}-aarch64-apple-darwin.tar.gz` to the GitHub release; the existing PyPI publish + pkg-build + tap-update jobs are untouched (SC5, CONTEXT D-19 / D-20)"
    - "README.md gains a 4th install row 'Rust port (experimental)' under the existing install matrix from Phase 3 D-31, with the cargo install --git invocation and a stark experimental-scope warning (SC4, CONTEXT D-29)"
    - "rs/README.md (created in Plan 04-01) gains the parity-test invocation + RUN_LIVE_RUST=1 doc + TCC churn note (CONTEXT D-30 — full Rust-specific README inside the workspace)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Adds `rust-lint-test` job (cargo fmt --check + cargo clippy + cargo test --workspace + stdout-purity smoke) parallel to existing lint-type-test"
      contains: "rust-lint-test"
    - path: ".github/workflows/release.yml"
      provides: "Adds `rust-build` job downstream of `publish` that builds + strips + tarballs + attaches the Rust binary to the GitHub release; macos-14 only (Apple Silicon)"
      contains: "rust-build"
    - path: "tests/integration/test_rust_python_parity.py"
      provides: "Cross-binary parity test invoking both Python + Rust binaries via subprocess.Popen and asserting doctor output shape + state parity"
      contains: "RUN_LIVE_RUST"
    - path: "README.md"
      provides: "4th install row 'Rust port (experimental)' with cargo install --git invocation + experimental warning"
      contains: "Rust port (experimental)"
    - path: "rs/README.md"
      provides: "Updated with full parity-test invocation + RUN_LIVE_RUST env var doc + TCC churn note"
      contains: "RUN_LIVE_RUST"
  key_links:
    - from: ".github/workflows/ci.yml::rust-lint-test"
      to: ".github/workflows/ci.yml::lint-type-test"
      via: "parallel jobs (no needs: dependency between them)"
      pattern: "rust-lint-test"
    - from: ".github/workflows/release.yml::rust-build"
      to: ".github/workflows/release.yml::publish"
      via: "needs: [publish]"
      pattern: "needs:\\s*\\[publish\\]"
    - from: "tests/integration/test_rust_python_parity.py"
      to: "rs/target/release/whatsapp-desktop-mcp-rs (Rust binary) AND `python -m whatsapp_desktop_mcp` (Python binary)"
      via: "subprocess.Popen for each, identical JSON-RPC sequence"
      pattern: "subprocess.Popen"
    - from: "README.md::Install (4th row)"
      to: "rs/README.md (full Rust workspace docs)"
      via: "markdown link"
      pattern: "rs/README"
    - from: "rs/README.md"
      to: "tests/integration/test_rust_python_parity.py"
      via: "documented invocation `RUN_LIVE_RUST=1 uv run pytest ...`"
      pattern: "RUN_LIVE_RUST=1"
---

<objective>
Wire the Rust workspace into CI, release automation, and project docs, and ship the cross-binary parity test that proves Python + Rust agree on doctor output shape + TCC state. After this plan: every PR exercises the Rust workspace; every release tag attaches a Rust tarball to the GitHub release; users can find the experimental install path in README.md and the parity-test recipe in rs/README.md.

Purpose: realize ROADMAP §"Phase 4" SC3 ("Cross-binary parity test (`tests/integration/test_rust_python_parity.py`, RUN_LIVE_RUST=1) calls the same MCP tool against both binaries against the same WhatsApp Desktop install and asserts response shapes match"), SC4 ("README adds a 'Rust port (experimental)' subsection under Install with a `cargo install --git` invocation; existing Python install paths byte-stable"), and SC5 ("The Rust port does NOT add to release.yml's PyPI publish job; a separate GitHub release asset attached via a Phase-4 `cargo-build` job is acceptable").

Output: green CI parallel matrix entry; release.yml additive `rust-build` job; runnable parity test; user-discoverable install instructions in README.md + comprehensive rs/README.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rust-port-parallel-binary/04-CONTEXT.md
@.planning/phases/04-rust-port-parallel-binary/04-RESEARCH.md
@.planning/phases/04-rust-port-parallel-binary/04-01-PLAN.md
@.planning/phases/04-rust-port-parallel-binary/04-02-PLAN.md
@.github/workflows/ci.yml
@.github/workflows/release.yml
@README.md
@rs/README.md
</context>

<interfaces>
<!-- Existing CI / release.yml integration points (lifted from .github/workflows/ci.yml + release.yml head-of-file context). -->

From `.github/workflows/ci.yml`:
- Workflow name: `CI`
- Single job: `lint-type-test` runs on `macos-14` (Apple Silicon)
- Concurrency group cancels in-progress runs
- `workflow_call` enabled — release.yml reuses ci.yml as a gate via `uses: ./.github/workflows/ci.yml`

From `.github/workflows/release.yml`:
- Trigger: `git tag v*` push
- Job order: `ci` (reusable) → `preflight` (gates downstream on secret presence) → `publish` (PyPI, OIDC) → `pkg-build` (Phase 3 — gated by APPLE_INSTALLER_CERT_P12) + `tap-update` (Phase 3 — gated by BREW_TAP_DEPLOY_KEY)
- New `rust-build` job inserts AFTER `publish` (per CONTEXT D-19: "downstream of publish PyPI job"); independent of pkg-build / tap-update — Rust artifact attaches even if Python pkg/tap jobs fail or skip
- `softprops/action-gh-release@v2` already used by Phase 3 pkg-build (assumption A6 verified) — same usage pattern works for the Rust tarball

From `rs/target/release/whatsapp-desktop-mcp-rs` (built in Plan 04-02):
- The artifact the parity test invokes
- The artifact the release.yml `rust-build` job tarballs and attaches

From `python -m whatsapp_desktop_mcp` (Python entry, established by Phase 0):
- The Python binary the parity test invokes for comparison
- Shipped via `whatsapp-desktop-mcp` PyPI package (Phase 3 publish job)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Cross-binary parity test (tests/integration/test_rust_python_parity.py) — both binaries doctor output shape + state parity</name>
  <files>
    tests/integration/test_rust_python_parity.py
  </files>
  <read_first>
    .planning/phases/04-rust-port-parallel-binary/04-RESEARCH.md (§"Cross-binary parity test (`tests/integration/test_rust_python_parity.py`)" lines ~1036-1179 — verbatim Python source to lift),
    .planning/phases/04-rust-port-parallel-binary/04-CONTEXT.md (D-23 parity test 6-step assertion sequence; D-24 sandbox carry; T-5 TCC race mitigation),
    .planning/phases/04-rust-port-parallel-binary/04-RESEARCH.md (§"Pitfall 5: Cross-binary parity test races on TCC state changes" — tight-sequence mitigation),
    src/whatsapp_desktop_mcp/__main__.py (verify the `python -m whatsapp_desktop_mcp` invocation works as expected — should be the same as phase 0 ships),
    .planning/phases/04-rust-port-parallel-binary/04-02-PLAN.md (the Rust binary path the parity test invokes — `rs/target/release/whatsapp-desktop-mcp-rs`)
  </read_first>
  <action>
    Create `tests/integration/test_rust_python_parity.py` per 04-RESEARCH.md §"Cross-binary parity test" (lines 1036-1179) verbatim. The research code is exhaustive — lift it as-is.

    Module-level constants:
    - `_REPO_ROOT = Path(__file__).resolve().parents[2]`
    - `_RUST_BIN = _REPO_ROOT / "rs" / "target" / "release" / "whatsapp-desktop-mcp-rs"`
    - `_PYTHON_INVOCATION = [sys.executable, "-m", "whatsapp_desktop_mcp"]`

    Helper `_send_jsonrpc_sequence(cmd: list[str | os.PathLike]) -> dict`:
    - Spawns subprocess via `subprocess.Popen(cmd, stdin=subprocess.PIPE, stdout=subprocess.PIPE, stderr=subprocess.PIPE, text=True)`
    - Sends `initialize` request (protocolVersion 2025-11-25), reads + discards initialize response
    - Sends `notifications/initialized` notification
    - Sends `tools/call` for `doctor` with empty arguments
    - Reads result line, parses JSON, returns `result["result"]`
    - finally: closes stdin, terminates, waits with 2s timeout, kills if needed

    Test function `test_rust_python_doctor_parity()`:
    - Skip if `RUN_LIVE_RUST != "1"` (clear pytest.skip message)
    - Skip if `_RUST_BIN.exists()` is False (informative message: "run `cd rs && cargo build --release --locked` first")
    - Invoke Rust binary first, then `time.sleep(0.1)` (tight sequence per Pitfall 5 / CONTEXT T-5), then invoke Python binary
    - Extract structured doctor reports: try `result.get("structuredContent")` first, fall back to `json.loads(result["content"][0]["text"])` for protocol-version variation tolerance (Assumption A3)
    - Per CONTEXT D-23 6-step assertion sequence:
      1. Top-level field shape parity — compare against the Rust v0 populated subset `{"full_disk_access", "automation_whatsapp", "accessibility", "db_path", "whatsapp_app_version"}` because v0 Rust intentionally returns None for `schema_fingerprint` / `last_message_ts` / `coverage_summary` per CONTEXT D-13
      2. Per-bucket state parity — assert `rust_doctor[bucket]["state"] == python_doctor[bucket]["state"]` for each of the 3 PermissionChecks; failure here means a probe logic bug in one binary (the load-bearing parity claim)
      3. binary_path differs (sanity: assert two distinct binaries actually ran)
      4. db_path matches across binaries (both probe the same WhatsApp install)
      5. system_settings_url matches across binaries for the FDA bucket (URLs are constants; mismatch indicates a constant-drift bug)

    Test function MUST be marked `@pytest.mark.live` so the existing `pytest -m "not live"` invocations (CI default) skip it. Only `RUN_LIVE_RUST=1` + manual maintainer execution runs it. Document this in the docstring + add to `pyproject.toml` `[tool.pytest.ini_options]` markers list if not already present (check first; the `live` marker is already established by Phase 1/3 from the rest of the suite — verify and re-use).

    Document at the top of the file:
    - Sandbox carry note (CONTEXT D-24): "Reuses Phase 2 B-2 `_isolate_live_state` autouse fixture extended in Phase 3 D-24 — but only the rate-limit + audit + FTS sandbox parts apply (Rust v0 has none of those subsystems). Documented as a known v0 limitation; full sandbox extension is Phase 4.x."
    - TCC race note (CONTEXT T-5 / Pitfall 5): "Test runs both binaries in tight (<1s) sequence to minimize the TCC-toggle race window. If the test fails intermittently, ensure no permissions were toggled mid-run."
    - Protocol-version note (Assumption A3): "Handles both `result.structuredContent` and `result.content[0].text` JSON wire-shape variation; FastMCP 1.27.1 and rmcp 1.7.0 may negotiate different protocol versions but both should produce equivalent doctor output for the comparison subset."

    Verify the Python invocation `python -m whatsapp_desktop_mcp` works by grepping for `__main__.py` under `src/whatsapp_desktop_mcp/` — if Phase 0 used a console script entry point only (not `__main__.py`), substitute the entry-point invocation. The research example assumes `python -m whatsapp_desktop_mcp` — adjust if the actual repo layout differs.
  </action>
  <verify>
    <automated>
      cd /Users/jlqueguiner/dev/whatsapp-mcp && \
      uv run pytest --collect-only tests/integration/test_rust_python_parity.py 2>&1 | tee /tmp/04-03-task1-collect.log && \
      grep -q 'test_rust_python_doctor_parity' /tmp/04-03-task1-collect.log && \
      uv run pytest tests/integration/test_rust_python_parity.py -v 2>&1 | tee /tmp/04-03-task1-skip.log && \
      grep -E '(SKIPPED|skipped)' /tmp/04-03-task1-skip.log && \
      grep -q 'RUN_LIVE_RUST' tests/integration/test_rust_python_parity.py && \
      grep -q 'subprocess.Popen' tests/integration/test_rust_python_parity.py && \
      grep -q 'time.sleep' tests/integration/test_rust_python_parity.py && \
      grep -q 'structuredContent' tests/integration/test_rust_python_parity.py && \
      grep -q 'binary_path' tests/integration/test_rust_python_parity.py && \
      uv run ruff check tests/integration/test_rust_python_parity.py && \
      uv run mypy tests/integration/test_rust_python_parity.py
    </automated>
  </verify>
  <done>
    Test file collects + skips cleanly without RUN_LIVE_RUST=1 (default CI behavior). With RUN_LIVE_RUST=1 + a built Rust binary + WhatsApp installed + 3 perms granted on the maintainer's Mac, the test passes — verified manually on the maintainer's Mac and documented in 04-03-SUMMARY.md (manual smoke step, not in CI). Ruff + mypy pass. The 6-step assertion sequence from CONTEXT D-23 is encoded.
  </done>
</task>

<task type="auto">
  <name>Task 2: CI workflow extension — rust-lint-test job parallel to lint-type-test (per CONTEXT D-25)</name>
  <files>
    .github/workflows/ci.yml
  </files>
  <read_first>
    .github/workflows/ci.yml (the existing single-job workflow being EXTENDED — current head-of-file documents the workflow's role as the SETUP-03 stdout-purity gate),
    .planning/phases/04-rust-port-parallel-binary/04-RESEARCH.md (§"CI integration (`.github/workflows/ci.yml` extension)" lines ~1181-1235 — verbatim YAML to lift),
    .planning/phases/04-rust-port-parallel-binary/04-CONTEXT.md (D-25 — rust-lint-test runs PARALLEL to existing lint-type-test job; macos-14; pinned setup-rust-toolchain version),
    .planning/phases/04-rust-port-parallel-binary/04-RESEARCH.md (§"Pitfall 1" — pinned action version v1.16.1; §"Assumption A4" — toolchain parameter honored)
  </read_first>
  <action>
    Append a new `rust-lint-test` job to `.github/workflows/ci.yml` per 04-RESEARCH.md §"CI integration" (lines 1181-1228) verbatim, with adaptations.

    Critical placement rules (CONTEXT D-25):
    - Add the new job UNDER `jobs:` AFTER the existing `lint-type-test` job, NOT inside it.
    - The new job has NO `needs:` clause — it runs IN PARALLEL with `lint-type-test`. A failure in one does NOT block the other (separate matrix entries).
    - Both jobs share the workflow-level `concurrency` group (already configured) so an in-progress PR's older runs are cancelled together.

    Job spec for `rust-lint-test` (write the YAML literally — research §"CI integration" lines 1181-1228 has the verbatim source to lift; deviations spelled out below):

    Top-level job key `rust-lint-test:`. Required keys:
    - `runs-on: macos-14` — objc2 + plist + AX-API require macOS; CONTEXT D-25 mandates the Apple Silicon runner.
    - `steps:` — ordered list of 5 steps (no `needs:` clause, no environment, no permissions).

    Step 1 — checkout. Use `actions/checkout@v4` (no `with:` block — defaults).

    Step 2 — Install Rust toolchain. `uses: actions-rust-lang/setup-rust-toolchain@v1.16.1` (PINNED — CONTEXT D-25 + Pitfall 1). `with:` block sets `toolchain: "1.75"` (honors CONTEXT D-04 MSRV) and `components: rustfmt,clippy`. Step name: "Install Rust toolchain (reads rust-version from rs/Cargo.toml)".

    Step 3 — cargo fmt. Step name `cargo fmt --check`. `working-directory: rs`. `run: cargo fmt --all -- --check`.

    Step 4 — cargo clippy. Step name `cargo clippy (workspace, deny warnings)`. `working-directory: rs`. `run: cargo clippy --workspace --all-targets --locked -- -D warnings`.

    Step 5 — cargo test. Step name `cargo test (workspace, including isolation test)`. `working-directory: rs`. `run: cargo test --workspace --locked` (this picks up the REL-05 D-24 isolation test from Plan 04-01).

    Step 6 — Stdout-purity smoke. Step name `Stdout-purity smoke (mirrors Python tests/unit/test_stdout_purity.py)`. `working-directory: rs`. The `run:` block is a multi-line shell script: first invokes `cargo build --release --locked` (necessary because earlier steps only built debug), then invokes a `python3 <<'PY' ... PY` heredoc (NOT the `python3 -c "..."` escaped-newline form — the deep-work rules forbid that). Inside the heredoc:
      - import json + subprocess
      - subprocess.Popen the release binary at `./target/release/whatsapp-desktop-mcp-rs` with stdin/stdout/stderr piped, `text=True`
      - construct a list of 4 JSON-RPC frames as Python dicts: (a) initialize with protocolVersion `2025-11-25`, capabilities `{}`, clientInfo `{name:"ci",version:"0"}`, id 1; (b) initialized notification (no id); (c) tools/list with empty params, id 2; (d) tools/call doctor with empty arguments, id 3
      - json.dumps each frame, write to stdin with trailing newline, flush, close stdin
      - call `p.communicate(timeout=15)` to read all stdout/stderr (more robust than multiple readline() calls)
      - in finally: if process still alive, terminate + wait(2) + kill if needed
      - iterate over `out.splitlines()`: skip empty, json.loads each line (raises if not valid JSON), assert `obj.get("jsonrpc") == "2.0"` with diagnostic message
      - print "OK: stdout-purity verified across initialize+tools/list+tools/call doctor"

    The 6-step structure mirrors Python's tests/unit/test_stdout_purity.py — every byte on stdout MUST parse as a JSON-RPC frame across the full handshake + tool call sequence.

    Key adaptations vs the research pattern:
    - Use a `python3 <<'PY' ... PY` heredoc (NOT the `python3 -c "...\n..."` form — the project's deep-work rules forbid escaped-newline -c invocations).
    - Use `p.communicate(timeout=15)` instead of multiple `readline()` calls — more robust for the 4-frame batch sequence; matches how the parity test handles process lifecycle.
    - The `stdout-purity smoke` step BUILDS the release binary (`cargo build --release --locked`) before invoking it — necessary because earlier steps only ran cargo test (debug builds).

    Update the workflow file's head-of-file comment block to mention the new `rust-lint-test` job's role: "Phase 4 v0 adds a parallel `rust-lint-test` job that runs cargo fmt/clippy/test against the rs/ workspace and verifies the Rust binary's stdout-purity invariant. Runs in parallel to lint-type-test; failure in one does not block the other."

    Do NOT modify the existing `lint-type-test` job. Do NOT add `needs:` between the two jobs. Do NOT bump the `actions/checkout@v4` reference. Python-side CI must remain byte-stable beyond the comment update.
  </action>
  <verify>
    <automated>
      cd /Users/jlqueguiner/dev/whatsapp-mcp && \
      python3 <<'PY'
import yaml, sys
with open('.github/workflows/ci.yml') as f:
    wf = yaml.safe_load(f)
jobs = wf.get('jobs', {})
assert 'lint-type-test' in jobs, "existing lint-type-test job missing"
assert 'rust-lint-test' in jobs, "new rust-lint-test job missing"
rj = jobs['rust-lint-test']
assert rj.get('runs-on') == 'macos-14', f"rust-lint-test must run on macos-14, got {rj.get('runs-on')!r}"
assert 'needs' not in rj, "rust-lint-test must NOT have needs: — must run in parallel with lint-type-test (CONTEXT D-25)"
step_names = [s.get('name', s.get('uses', '')) for s in rj.get('steps', [])]
assert any('Install Rust toolchain' in n for n in step_names), f"missing Install Rust toolchain step; got {step_names}"
assert any('cargo fmt' in n for n in step_names), f"missing cargo fmt step; got {step_names}"
assert any('cargo clippy' in n for n in step_names), f"missing cargo clippy step; got {step_names}"
assert any('cargo test' in n for n in step_names), f"missing cargo test step; got {step_names}"
assert any('Stdout-purity' in n for n in step_names), f"missing stdout-purity smoke step; got {step_names}"
# Verify pinned setup-rust-toolchain version
toolchain_step = next(s for s in rj['steps'] if 'setup-rust-toolchain' in s.get('uses', ''))
assert '@v1.16.1' in toolchain_step['uses'], f"setup-rust-toolchain must be pinned to v1.16.1 per CONTEXT D-25; got {toolchain_step['uses']!r}"
assert toolchain_step.get('with', {}).get('toolchain') == '1.75', f"toolchain must be 1.75 per CONTEXT D-04; got {toolchain_step.get('with')}"
# Verify lint-type-test job is byte-stable (no needs: added).
ltt = jobs['lint-type-test']
assert 'needs' not in ltt, "lint-type-test must NOT have needs: rust-lint-test — they run in parallel"
print("OK: ci.yml extension validates per CONTEXT D-25")
PY
    </automated>
  </verify>
  <done>
    `.github/workflows/ci.yml` parses as valid YAML, contains both `lint-type-test` (unchanged structure) and `rust-lint-test` (new) at the top level of `jobs:`, with no `needs:` between them. The `rust-lint-test` job pins `actions-rust-lang/setup-rust-toolchain@v1.16.1` and `toolchain: "1.75"` per CONTEXT D-25 + D-04. Steps include cargo fmt --check, cargo clippy with -D warnings, cargo test --workspace --locked, and the stdout-purity smoke (using a python3 heredoc — NOT the forbidden `-c "..."` form). Existing Python lint-type-test job byte-stable beyond the head-of-file comment update.
  </done>
</task>

<task type="auto">
  <name>Task 3: release.yml rust-build job + README.md 4th install row + rs/README.md parity-test additions</name>
  <files>
    .github/workflows/release.yml,
    README.md,
    rs/README.md
  </files>
  <read_first>
    .github/workflows/release.yml (existing job graph: ci → preflight → publish → pkg-build + tap-update; rust-build inserts after publish per CONTEXT D-19),
    .planning/phases/04-rust-port-parallel-binary/04-RESEARCH.md (§"Release.yml `rust-build` job" lines ~1237-1303 — verbatim YAML to lift),
    .planning/phases/04-rust-port-parallel-binary/04-RESEARCH.md (§"Open Questions" item 5 — tarball arch identifier `aarch64-apple-darwin`),
    .planning/phases/04-rust-port-parallel-binary/04-CONTEXT.md (D-19 GitHub release artifact only; D-20 NO PyPI publish for Rust; D-21 NO brew formula bottle for Rust v0; D-22 binary version 0.0.0 independent; D-29 README 4th install row; D-30 rs/README.md),
    README.md (the existing install-matrix from Phase 3 D-31 — find the install section and append the 4th row),
    rs/README.md (the placeholder created in Plan 04-01 — flesh out with parity test recipe)
  </read_first>
  <action>
    **Sub-task 3a — `.github/workflows/release.yml` `rust-build` job:**

    Append a new `rust-build` job AFTER the existing `publish` job per 04-RESEARCH.md §"Release.yml `rust-build` job" (lines 1237-1297) verbatim. Key elements:
    - `needs: [publish]` — downstream of PyPI publish (CONTEXT D-19)
    - `runs-on: macos-14` — Apple Silicon only for v0 (CONTEXT D-19)
    - `permissions: contents: write` — required for softprops/action-gh-release@v2 release upload (Assumption A6)
    - Steps:
      1. `actions/checkout@v4`
      2. `actions-rust-lang/setup-rust-toolchain@v1.16.1` with `toolchain: "1.75"`
      3. `cargo build --release --locked` in `rs/` working directory
      4. `strip target/release/whatsapp-desktop-mcp-rs`
      5. Compute Rust binary version per CONTEXT D-22: read `version` from `rs/Cargo.toml` `[workspace.package]` (which is "0.0.0" v0); if placeholder, fall back to `${GITHUB_REF#refs/tags/v}-rust0` to keep the file name informative
      6. Tarball: `whatsapp-desktop-mcp-rs-${VERSION}-aarch64-apple-darwin.tar.gz` per Open Question 5 recommendation (Rust target triple, NOT `arm64`). Use `tar -czf "$TARBALL" -C rs/target/release whatsapp-desktop-mcp-rs`.
      7. `softprops/action-gh-release@v2` with `files: ${{ env.TARBALL }}` to attach to the same release the publish job creates

    Critical placement rules:
    - The new `rust-build` job is INDEPENDENT of `pkg-build` and `tap-update` — Rust artifact attaches even if those Phase 3 jobs fail or skip (CONTEXT D-19 + D-20).
    - DO NOT add `rust-build` to PyPI publish job's logic. DO NOT add a Rust step to the existing `publish` job. The `rust-build` job is purely additive.
    - Update the file's head-of-file comment block to document the new job: "Phase 4 v0 adds a `rust-build` job that builds + strips + tarballs the Rust port binary and attaches it to the same GitHub release as the Python .pkg. Independent of pkg-build / tap-update; no PyPI publish change for the Rust binary (CONTEXT D-19 / D-20)."

    Use the verified `aarch64-apple-darwin` arch identifier in the tarball name per Open Question 5 (Rust target triple is the canonical Cargo identifier and survives cross-compile expansion in Phase 4.x).

    **Sub-task 3b — `README.md` 4th install row:**

    Open `README.md`, locate the install matrix established in Phase 3 D-31 (search for `## Install` or the existing 3-row install table). Append a 4th row "Rust port (experimental)" AFTER the existing 3 rows (uvx / brew / .pkg). Per CONTEXT D-29:

    Row content:
    - Title: "Rust port (experimental)"
    - Stark warning callout above the install command: "**Experimental — v0 ships only the `doctor` tool, not yet at parity with the Python implementation. For early adopters interested in the Rust port. Use the Python install paths above for production use.**"
    - Install command: `cargo install --git https://github.com/jqueguiner/whatsapp-desktop-mcp --root /usr/local whatsapp-desktop-mcp-rs` (per Pitfall 8 — workspace member NAME, NOT path; `--root /usr/local` for stable TCC grant path per CONTEXT T-2)
    - claude_desktop_config.json snippet showing how to register the Rust binary alongside the Python one (e.g. as a SECOND entry named `whatsapp-rs` with command `/usr/local/bin/whatsapp-desktop-mcp-rs`)
    - Forward-link to `rs/README.md` for build/test/parity instructions
    - Note: Each binary needs SEPARATE TCC grants (per CONTEXT T-2 + RESEARCH §"Runtime State Inventory") — point user to call `whatsapp-rs:doctor` to get the binary path that needs FDA + Accessibility + Automation

    DO NOT modify the existing 3 install rows (uvx / brew / .pkg). DO NOT change any content above the install matrix. Python install paths byte-stable per ROADMAP §"Phase 4" SC4.

    **Sub-task 3c — `rs/README.md` parity-test additions:**

    The skeleton `rs/README.md` was created in Plan 04-01. This task expands it with the now-actual content per CONTEXT D-30. Sections to ensure are present (add or expand):

    - "Build" — `cd rs && cargo build --release --locked` (already present from 04-01; verify)
    - "Test" — `cd rs && cargo test --workspace --locked` (already present from 04-01; verify)
    - "Install (developer)" — `cargo install --git https://github.com/jqueguiner/whatsapp-desktop-mcp --root /usr/local whatsapp-desktop-mcp-rs` per Pitfall 8 + CONTEXT D-29
    - "Parity test (manual)" — NEW SECTION:
      - Pre-conditions: WhatsApp Desktop installed; 3 TCC permissions granted to BOTH the Python binary AND the Rust binary; both binaries built (`cd rs && cargo build --release --locked` for Rust; `uv sync` + `uv pip install -e .` for Python)
      - Invocation: `RUN_LIVE_RUST=1 uv run pytest tests/integration/test_rust_python_parity.py -v`
      - Expected: 1 test passes (or skips with informative message if the Rust binary is not built)
      - Failure interpretation per CONTEXT T-5 / Pitfall 5: intermittent failures usually indicate TCC state changed mid-run (one binary saw `granted` while the other saw `denied` for the same probe); re-run, do not toggle permissions during the test window
    - "TCC churn note" (carry-over from Plan 04-01 stub): each `cargo install` to a new `--root` orphans TCC grants; recommend sticking to one `--root` (e.g. `/usr/local`) for stability
    - "Phase 4.x roadmap" — short note that v0 is doctor-only; read tools / send_message / FTS5 / brew bottle / Intel cross-compile are deferred per CONTEXT §"Deferred Ideas"

    Cross-link from the project root README's new install row TO `rs/README.md` (relative link `rs/README.md`).

    Do NOT modify any existing Python install path documentation in README.md (Phase 3 D-31 sections stay byte-stable per SC4).
  </action>
  <verify>
    <automated>
      cd /Users/jlqueguiner/dev/whatsapp-mcp && \
      python3 <<'PY'
import yaml
with open('.github/workflows/release.yml') as f:
    wf = yaml.safe_load(f)
jobs = wf.get('jobs', {})
# Existing jobs byte-stable
for j in ('ci', 'preflight', 'publish'):
    assert j in jobs, f"existing job {j} missing"
# pkg-build / tap-update are Phase 3 — verify still present
assert 'pkg-build' in jobs, "Phase 3 pkg-build missing — release.yml regression"
assert 'tap-update' in jobs, "Phase 3 tap-update missing — release.yml regression"
# New rust-build job
assert 'rust-build' in jobs, "rust-build job missing"
rb = jobs['rust-build']
assert rb.get('runs-on') == 'macos-14', f"rust-build must run on macos-14; got {rb.get('runs-on')!r}"
needs = rb.get('needs')
needs_list = [needs] if isinstance(needs, str) else needs
assert needs_list == ['publish'], f"rust-build must have needs: [publish] only per CONTEXT D-19; got {needs_list!r}"
# rust-build should NOT be a needs: of any other job (independent)
for jn, jc in jobs.items():
    if jn == 'rust-build': continue
    deps = jc.get('needs', [])
    if isinstance(deps, str): deps = [deps]
    assert 'rust-build' not in deps, f"job {jn} unexpectedly depends on rust-build (must be independent)"
# Verify softprops/action-gh-release@v2 used
step_uses = [s.get('uses', '') for s in rb.get('steps', [])]
assert any('softprops/action-gh-release@v2' in u for u in step_uses), f"missing softprops/action-gh-release@v2 step; got {step_uses}"
assert any('actions-rust-lang/setup-rust-toolchain@v1.16.1' in u for u in step_uses), f"setup-rust-toolchain not pinned to v1.16.1; got {step_uses}"
print("OK: release.yml rust-build job validates")
PY
      cd /Users/jlqueguiner/dev/whatsapp-mcp && \
      grep -q 'Rust port (experimental)' README.md && \
      grep -q 'cargo install --git' README.md && \
      grep -q '/usr/local/bin/whatsapp-desktop-mcp-rs' README.md && \
      grep -qE '(rs/README|rs/README\.md)' README.md && \
      grep -q 'RUN_LIVE_RUST=1' rs/README.md && \
      grep -q 'cargo install --git' rs/README.md && \
      grep -q 'Parity test' rs/README.md && \
      grep -q 'TCC churn' rs/README.md && \
      grep -q 'cargo build --release --locked' rs/README.md && \
      grep -q 'aarch64-apple-darwin' .github/workflows/release.yml && \
      grep -q 'whatsapp-desktop-mcp-rs' .github/workflows/release.yml && \
      ! diff <(git show HEAD:src/whatsapp_desktop_mcp/__init__.py) src/whatsapp_desktop_mcp/__init__.py
    </automated>
  </verify>
  <done>
    `release.yml` contains the new `rust-build` job downstream of `publish` only, independent of `pkg-build` / `tap-update`, on macos-14, attaching `whatsapp-desktop-mcp-rs-{version}-aarch64-apple-darwin.tar.gz` via `softprops/action-gh-release@v2`. The existing PyPI publish + pkg-build + tap-update jobs are byte-stable. README.md has a 4th install row "Rust port (experimental)" with the correct cargo install --git invocation per Pitfall 8 + the experimental warning + cross-link to rs/README.md; the existing 3 Python install rows are byte-stable per SC4. rs/README.md (created in Plan 04-01) now contains a "Parity test (manual)" section with the RUN_LIVE_RUST=1 invocation and TCC race interpretation. `src/whatsapp_desktop_mcp/__init__.py` (and by extension all Python source) is byte-stable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner ↔ crates.io | `cargo build --locked` in CI / release respects Cargo.lock — no transitive version drift. cargo-deny (workspace dep from Plan 04-01) catches advisories at lint time. |
| GitHub Actions runner ↔ GitHub release storage | `softprops/action-gh-release@v2` requires `permissions: contents: write` job-level; minimal blast radius (NOT workflow-level). Same pattern as Phase 3 pkg-build. |
| Parity test ↔ subprocess.Popen of two binaries | Test runs both as MCP stdio servers in tight sequence; no untrusted input flows in (probe scripts hard-coded; tool args empty). |
| Parity test ↔ TCC state | Test reads OS state via the binaries' probes; no mutation. Race window per CONTEXT T-5 documented in rs/README.md. |
| README install instructions ↔ user | Public documentation for the cargo install command — user explicitly opts into running the Rust binary; experimental warning sets expectations per CONTEXT D-29. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-15 | Tampering | Cargo supply chain — yanked or known-CVE crate slipping into the release tarball | mitigate | `cargo build --locked` in both CI rust-lint-test and release.yml rust-build jobs ensures Cargo.lock is honored. cargo-deny config (Plan 04-01 deny.toml) catches advisories. Consider adding a `cargo deny check` step to the rust-lint-test job in Phase 4.x. |
| T-04-16 | Tampering | release.yml regression — a Rust step accidentally added to the Python publish job, breaking PyPI release | mitigate | Sub-task 3a verify command parses release.yml YAML and asserts `publish` job's structure unchanged (no Rust deps); rust-build is purely additive with `needs: [publish]` only. CONTEXT D-20 + D-21 explicitly forbid PyPI / brew bottle changes for Rust v0. |
| T-04-17 | Information Disclosure | release tarball naming leaking machine-specific info | accept | Tarball name uses CARGO_PKG_VERSION (workspace.package.version = "0.0.0") + git tag suffix + Rust target triple `aarch64-apple-darwin`. No machine identifiers. |
| T-04-18 | Spoofing | parity test false-positive due to TCC race (CONTEXT T-5 / Pitfall 5) | accept (documented) | Test runs both binaries in tight (<1s) sequence. rs/README.md documents the race interpretation: intermittent failures = re-run; do not toggle TCC during test window. Not a code bug — a test design quirk. |
| T-04-19 | Tampering | Future contributor accidentally adds rust-build to pkg-build needs: chain (would block Rust release on Apple cert presence) | mitigate | Sub-task 3a verify asserts no other job lists rust-build in its needs:. Documented in release.yml head-of-file comment that rust-build is independent. |
| T-04-20 | Repudiation | Stdout pollution regression in CI not caught by unit tests | mitigate | rust-lint-test job's "Stdout-purity smoke" step (Sub-task 2) builds the release binary and asserts every stdout line parses as JSON-RPC across initialize+tools/list+tools/call doctor — same coverage as Python `tests/unit/test_stdout_purity.py`. |
</threat_model>

<verification>
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0; `rust-lint-test` job present with no `needs:`.
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"` exits 0; `rust-build` job present with `needs: [publish]` only.
- `uv run pytest --collect-only tests/integration/test_rust_python_parity.py` collects 1 test (`test_rust_python_doctor_parity`).
- `uv run pytest tests/integration/test_rust_python_parity.py -v` skips cleanly without RUN_LIVE_RUST=1 (default).
- `RUN_LIVE_RUST=1 uv run pytest tests/integration/test_rust_python_parity.py -v` passes on the maintainer's Mac with both binaries built + WhatsApp installed + 3 TCC perms granted (manual smoke; documented in 04-03-SUMMARY.md).
- `grep -c 'Rust port (experimental)' README.md` returns 1.
- `git diff --name-only HEAD -- src/whatsapp_desktop_mcp/` is empty (Python source byte-stable).
- `git diff --stat HEAD -- README.md` shows additions only — no deletions in the existing 3 install rows.
- The `lint-type-test` Python job in `.github/workflows/ci.yml` is structurally unchanged (only the head-of-file comment block expands).
</verification>

<success_criteria>
- ROADMAP §"Phase 4" SC3 satisfied: `tests/integration/test_rust_python_parity.py` exists, gated by RUN_LIVE_RUST=1, asserts response shape parity + per-bucket TCC state parity per CONTEXT D-23 6-step sequence.
- ROADMAP §"Phase 4" SC4 satisfied: README.md gains "Rust port (experimental)" 4th install row with cargo install --git invocation; existing Python install paths byte-stable.
- ROADMAP §"Phase 4" SC5 satisfied: release.yml `rust-build` job downstream of `publish`, no PyPI publish change for Rust; tarball attached to GitHub release via `softprops/action-gh-release@v2`.
- CI rust-lint-test job runs PARALLEL to lint-type-test per CONTEXT D-25; failure isolation enforced (no `needs:` between them).
- All locked CONTEXT decisions D-19, D-20, D-21, D-23, D-24, D-25, D-29, D-30 honored.
- Python source byte-stable invariant maintained (no edits under `src/whatsapp_desktop_mcp/`).
</success_criteria>

<output>
After completion, create `.planning/phases/04-rust-port-parallel-binary/04-03-SUMMARY.md` per `templates/summary.md` capturing:
- Maintainer-Mac parity test output: paste the pytest -v output from `RUN_LIVE_RUST=1 uv run pytest tests/integration/test_rust_python_parity.py -v` showing the test passing — this is the SC3 acceptance evidence
- A side-by-side diff of the Rust vs Python doctor JSON outputs (the v0-populated subset) demonstrating shape + state parity
- CI link to a green PR run showing both `lint-type-test` and `rust-lint-test` jobs passing in parallel (manual maintainer follow-up after merge)
- Whether `cargo deny check` was added to the rust-lint-test job (recommended deferral to Phase 4.x per Open Question 2 — capture decision)
- Any deviation from the verbatim research patterns (CI YAML, release.yml YAML, parity test) and rationale
</output>
