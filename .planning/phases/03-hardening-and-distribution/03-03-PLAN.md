---
phase: 03-hardening-and-distribution
plan: 3
title: Hardening — tested_versions parser + doctor degraded warning + audit rotation + dev subcommand
wave: 1
depends_on: []
requirements: []
files_modified:
  - docs/tested_versions.md
  - src/whatsapp_desktop_mcp/reader/tested_versions.py
  - src/whatsapp_desktop_mcp/models/doctor.py
  - src/whatsapp_desktop_mcp/tools/doctor.py
  - src/whatsapp_desktop_mcp/sender/audit.py
  - src/whatsapp_desktop_mcp/cli.py
  - src/whatsapp_desktop_mcp/dev/__init__.py
  - src/whatsapp_desktop_mcp/dev/reset_rate_limit.py
  - pyproject.toml
  - tests/unit/test_tested_versions_parser.py
  - tests/unit/test_doctor_degraded_warning.py
  - tests/unit/test_audit_rotation.py
  - tests/unit/test_dev_subcommand.py
autonomous: true
must_haves:
  truths:
    - "docs/tested_versions.md exists with one initial row (WA 26.16.74 / macOS 26.4 / Z_VERSION 1 / maintainer / 2026-05-13)"
    - "reader/tested_versions.py parses the markdown table at module-load and exposes SUPPORTED_VERSION_RANGE: tuple[int,int] (defaulting to (1,1) on missing/empty/malformed file — fault-tolerant per DIAG-02)"
    - "SchemaFingerprint Pydantic model gains supported_version_range: tuple[int,int] and degraded_mode_warning: str | None fields (D-20)"
    - "doctor populates degraded_mode_warning when observed Z_VERSION is in-range BUT WhatsApp.app version is outside the tested-versions matrix (structured for LLM consumption)"
    - "sender/audit.py rotates audit.log at 10 MB (configurable via WHATSAPP_DESKTOP_MCP_AUDIT_LOG_MAX_BYTES env var) keeping last 5 archives audit.log.1..audit.log.5; D-13 plaintext-body invariant preserved (rotation moves complete JSONL lines verbatim — body_sha256 stays the only body-derived field)"
    - "whatsapp-desktop-mcp dev reset-rate-limit subcommand prompts for tty-confirmation; non-tty refuses; on 'y' unlinks ~/Library/Application Support/whatsapp-desktop-mcp/rate-limit.db"
    - "--audit-log-max-bytes CLI arg overrides the default 10485760 byte threshold (D-28)"
  artifacts:
    - path: "docs/tested_versions.md"
      provides: "Manual markdown table of known-good WhatsApp Desktop versions; parsed by reader/tested_versions.py at module load"
      contains: "Z_VERSION"
    - path: "src/whatsapp_desktop_mcp/reader/tested_versions.py"
      provides: "load_tested_z_versions() -> tuple[int,int]; SUPPORTED_VERSION_RANGE module constant; _load_tested_wa_versions() helper for doctor"
      exports: ["load_tested_z_versions", "SUPPORTED_VERSION_RANGE"]
    - path: "src/whatsapp_desktop_mcp/models/doctor.py"
      provides: "SchemaFingerprint extended with supported_version_range + degraded_mode_warning fields (D-20); existing 3 PermissionStatus fields and all_granted accessor byte-stable per Phase 0 D-07"
      contains: "supported_version_range"
    - path: "src/whatsapp_desktop_mcp/tools/doctor.py"
      provides: "Doctor populates supported_version_range + degraded_mode_warning when wa_version not in tested-versions matrix"
    - path: "src/whatsapp_desktop_mcp/sender/audit.py"
      provides: "_blocking_append extended with size-based rotation; _resolve_max_bytes + _rotate_in_place helpers"
      contains: "rotate"
    - path: "src/whatsapp_desktop_mcp/cli.py"
      provides: "argparse subparser dispatch (top-level OR 'dev' subcommand); --audit-log-max-bytes arg; dev reset-rate-limit subcommand wiring"
      contains: "dev"
    - path: "src/whatsapp_desktop_mcp/dev/__init__.py"
      provides: "Empty package marker for the dev subcommand surface"
    - path: "src/whatsapp_desktop_mcp/dev/reset_rate_limit.py"
      provides: "run() -> int: confirmation prompt + DB unlink; non-tty refuse"
      exports: ["run"]
    - path: "pyproject.toml"
      provides: "[tool.ruff.lint.per-file-ignores] entry exempting src/whatsapp_desktop_mcp/dev/*.py from T201 (these CLI utilities print to stdout intentionally — NOT the stdio server)"
      contains: "src/whatsapp_desktop_mcp/dev"
  key_links:
    - from: "src/whatsapp_desktop_mcp/tools/doctor.py"
      to: "src/whatsapp_desktop_mcp/reader/tested_versions.py"
      via: "from whatsapp_desktop_mcp.reader.tested_versions import SUPPORTED_VERSION_RANGE, _load_tested_wa_versions"
      pattern: "from whatsapp_desktop_mcp\\.reader\\.tested_versions import"
    - from: "src/whatsapp_desktop_mcp/cli.py"
      to: "src/whatsapp_desktop_mcp/dev/reset_rate_limit.py"
      via: "argparse subcommand dispatch on args.cmd == 'dev' AND args.dev_cmd == 'reset-rate-limit'"
      pattern: "from whatsapp_desktop_mcp\\.dev\\.reset_rate_limit import run"
    - from: "src/whatsapp_desktop_mcp/sender/audit.py"
      to: "WHATSAPP_DESKTOP_MCP_AUDIT_LOG_MAX_BYTES env var"
      via: "_resolve_max_bytes() reads os.environ; cli.py sets it from --audit-log-max-bytes BEFORE server import"
      pattern: "WHATSAPP_DESKTOP_MCP_AUDIT_LOG_MAX_BYTES"
---

<objective>
Land four hardening surfaces that tighten Phase 0/1/2 invariants for v1.0 ship: (1) a `tested_versions.md` matrix + parser that drives a doctor `degraded_mode_warning` for out-of-tested-range WhatsApp Desktop versions; (2) size-based rotation of the JSONL audit log preserving the D-13 plaintext-body invariant; (3) a `whatsapp-desktop-mcp dev reset-rate-limit` subcommand for budget recovery; (4) an `--audit-log-max-bytes` CLI override.

Purpose: Closes Phase 2 D-14 audit log rotation deferred-to-Phase-3 carry-over. Closes ROADMAP §"Phase 3" Success Criterion 3 (tested_versions.md matrix). Reinforces Phase 1 P2 (schema/path drift) via doctor degraded-mode warning. Provides the live-test budget recovery path called out in CONTEXT.md D-27 for Phase 2 verification continuity.

Output: 1 new markdown doc + 1 new reader parser module + extended SchemaFingerprint model + extended doctor tool + extended sender/audit rotation + new dev subcommand + extended CLI argparse subparsers + 4 new unit-test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hardening-and-distribution/03-CONTEXT.md
@.planning/phases/03-hardening-and-distribution/03-RESEARCH.md
@CLAUDE.md

# Phase 1/2 surfaces extended in place
@src/whatsapp_desktop_mcp/models/doctor.py
@src/whatsapp_desktop_mcp/tools/doctor.py
@src/whatsapp_desktop_mcp/reader/schema_v1.py
@src/whatsapp_desktop_mcp/sender/audit.py
@src/whatsapp_desktop_mcp/sender/rate_limit.py
@src/whatsapp_desktop_mcp/cli.py
@src/whatsapp_desktop_mcp/permissions/whatsapp_app.py
@pyproject.toml

<interfaces>
<!-- Phase 0/1/2 interfaces this plan extends. -->

From src/whatsapp_desktop_mcp/models/doctor.py (Phase 1 shape, Phase 3 extends):
```python
class SchemaFingerprint(BaseModel):
    state: SchemaState  # "supported" | "unsupported" | "unreachable"
    observed_version: int | None
    supported_versions: list[int]
    remediation: str
    # Phase 3 ADDS:
    #   supported_version_range: tuple[int, int] = (1, 1)  # D-20
    #   degraded_mode_warning: str | None = None           # D-20
```

From src/whatsapp_desktop_mcp/sender/audit.py (Phase 2 shape):
```python
_LOG_DIR = Path.home() / "Library" / "Logs" / "whatsapp-desktop-mcp"
_LOG_PATH = _LOG_DIR / "audit.log"

class AuditEntry(BaseModel): ...
def body_sha256(body: str) -> str: ...
def _blocking_append(entry_json: str) -> None  # PHASE 3 EXTENDS WITH ROTATION
async def append(entry: AuditEntry) -> None
```

From src/whatsapp_desktop_mcp/sender/rate_limit.py (Phase 2 shape — referenced, not modified):
```python
_DB_PATH = Path.home() / "Library" / "Application Support" / "whatsapp-desktop-mcp" / "rate-limit.db"
# dev/reset_rate_limit.py imports rate_limit._DB_PATH for the unlink target.
```

From src/whatsapp_desktop_mcp/cli.py (Phase 1 shape):
```python
def main(argv: list[str] | None = None) -> int
# argparse with --version + --read-only/--no-read-only (Phase 1 D-19).
# Phase 3 extends with: --fts5-mode (Plan 03-01), --audit-log-max-bytes (this plan), AND
# argparse subparser dispatch for `dev reset-rate-limit` subcommand (this plan).
```

From src/whatsapp_desktop_mcp/permissions/whatsapp_app.py (Phase 1 — used to fetch wa_version):
```python
def detect_whatsapp_version() -> str | None
# Returns CFBundleShortVersionString from /Applications/WhatsApp.app/Contents/Info.plist.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: docs/tested_versions.md + reader/tested_versions.py parser + SchemaFingerprint extension + doctor degraded warning</name>
  <read_first>
    - .planning/phases/03-hardening-and-distribution/03-RESEARCH.md §"Pattern 5: tested_versions.md Parser + SchemaFingerprint Extension" (lines 1018-1161) — verbatim-liftable parser + model + doctor patches
    - .planning/phases/03-hardening-and-distribution/03-RESEARCH.md §"Common Pitfalls" Pitfall 4 (parser fault-tolerance — must not crash on malformed row)
    - .planning/phases/03-hardening-and-distribution/03-CONTEXT.md D-19, D-20, D-21
    - src/whatsapp_desktop_mcp/models/doctor.py — Phase 1 SchemaFingerprint + DoctorReport shape; Phase 0 D-07 byte-stability invariant on the 3 PermissionStatus fields and all_granted accessor (NEW fields are appended; existing fields/order are preserved)
    - src/whatsapp_desktop_mcp/tools/doctor.py — Phase 1 _probe_db_safely + _probe_whatsapp_version call sites; Phase 3 augments AFTER they execute
    - src/whatsapp_desktop_mcp/permissions/whatsapp_app.py — `detect_whatsapp_version()` signature
    - tests/unit/test_doctor*.py — Phase 1 doctor unit-test patterns
    - tests/unit/test_tested_versions_parser.py + tests/unit/test_doctor_degraded_warning.py — write FAILING tests first (RED)
  </read_first>
  <behavior>
    - Test 1 (parser default on missing file): `load_tested_z_versions()` returns `(1, 1)` when `docs/tested_versions.md` is absent.
    - Test 2 (parser extracts integers): given a temp file with three valid data rows (Z_VERSION 1, 1, 2), returns `(1, 2)`.
    - Test 3 (parser fault-tolerant on malformed row): given a row with Z_VERSION="unknown" mixed with two integer rows (1 and 2), the parser logs a WARNING (captured via caplog) AND returns `(1, 2)` — does NOT raise (Pitfall 4 — DIAG-02 mandates doctor stays callable).
    - Test 4 (parser skips header + separator rows): a typical markdown table with header `| WhatsApp Desktop |...` and separator `|------|...` rows produces zero spurious entries.
    - Test 5 (SchemaFingerprint default): a fresh `SchemaFingerprint(state="supported", observed_version=1, supported_versions=[1])` has `supported_version_range == (1, 1)` AND `degraded_mode_warning is None`.
    - Test 6 (SchemaFingerprint accepts non-defaults): can be constructed with `supported_version_range=(1, 3)` and `degraded_mode_warning="WhatsApp.app v26.99.0 not in tested-versions.md (last tested: 26.16.74); reads may degrade silently."`.
    - Test 7 (doctor populates supported_version_range): mock `_probe_db_safely` to return a SchemaFingerprint with state="supported"; mock `detect_whatsapp_version` to return "26.16.74" (matching the initial row); the resulting `DoctorReport.schema_fingerprint.supported_version_range` equals `SUPPORTED_VERSION_RANGE` from the parser.
    - Test 8 (doctor sets degraded_mode_warning when WA version OOR): mock `detect_whatsapp_version` to return "26.99.0"; doctor's `schema_fingerprint.degraded_mode_warning` is the structured string `"WhatsApp.app v26.99.0 not in tested-versions.md (last tested: 26.16.74); reads may degrade silently."`.
    - Test 9 (doctor leaves degraded_mode_warning=None when WA version in matrix): mock `detect_whatsapp_version` to return "26.16.74"; the warning is `None`.
    - Test 10 (Phase 0 D-07 byte-stability preserved): `DoctorReport.model_fields` lists `full_disk_access`, `automation_whatsapp`, `accessibility` BEFORE the Phase 1 additions (`db_path`, `schema_fingerprint`, ...) — the new SchemaFingerprint fields are appended INSIDE SchemaFingerprint, NOT at DoctorReport top level, so DoctorReport's field order is unchanged.
  </behavior>
  <action>
    Five coordinated edits.

    1. `docs/tested_versions.md` — RESEARCH.md §"Pattern 5" provides the verbatim initial content. Lift it. Single data row per CONTEXT.md D-21:
       ```
       | 26.16.74 | 26.4 | 1 | FDA/Auto/Acc all granted | maintainer | 2026-05-13 | Phase 1+2 live-verified |
       ```
       Plus the surrounding markdown header that explains the file's purpose and where the parser lives.

    2. `src/whatsapp_desktop_mcp/reader/tested_versions.py` (NEW) — RESEARCH.md §"Pattern 5" provides the verbatim parser. Lift it. Module shape:
       - `_TESTED_VERSIONS_PATH = Path(__file__).resolve().parent.parent.parent.parent / "docs" / "tested_versions.md"` (4 parents up: reader/ → whatsapp_desktop_mcp/ → src/ → repo root, then `/docs/tested_versions.md`).
       - `_ROW_RE = re.compile(r"^\|\s*([^|]+?)\s*\|\s*([^|]+?)\s*\|\s*(\d+)\s*\|")` — header rows have a non-digit Z_VERSION column so they don't match; separator rows start with `|---` and don't match either.
       - `_parse_z_versions(text: str) -> list[int]` — iterate lines; on regex match, `try: int(group(3)); except ValueError: logger.warning(...); continue`.
       - `load_tested_z_versions() -> tuple[int, int]` — `try: text = _TESTED_VERSIONS_PATH.read_text(encoding="utf-8") except (FileNotFoundError, PermissionError): return (1, 1)`. If `_parse_z_versions` returns empty list, return `(1, 1)`. Else `return (min(versions), max(versions))`.
       - `SUPPORTED_VERSION_RANGE: tuple[int, int] = load_tested_z_versions()` — module-load constant per RESEARCH.md ("module-load parse is the right cost trade").
       - `_load_tested_wa_versions() -> set[str]` — sibling helper for the doctor extension; parses column 1 (WhatsApp Desktop version string) from each data row using a similar regex `r"^\|\s*([^|]+?)\s*\|"` plus the same fault-tolerance discipline. Returns set of strings (e.g. `{"26.16.74"}`).

    3. `src/whatsapp_desktop_mcp/models/doctor.py` — APPEND two fields to `SchemaFingerprint` (RESEARCH.md §"Pattern 5"). Place them AFTER the existing `remediation` field so existing serialization order on the leading fields is byte-stable:
       - `supported_version_range: tuple[int, int] = Field(default=(1, 1), description=...)`.
       - `degraded_mode_warning: str | None = Field(default=None, description="WhatsApp.app v{x} not in tested-versions.md ...")`.
       Description strings: lift from RESEARCH.md §"Pattern 5" verbatim (they're carefully worded for LLM consumption per D-20).

    4. `src/whatsapp_desktop_mcp/tools/doctor.py` — augment the existing report-assembly logic to populate the two new fields. Per RESEARCH.md §"Pattern 5":
       - After `schema_fp` and `wa_version` are computed, import `SUPPORTED_VERSION_RANGE` and `_load_tested_wa_versions` from `whatsapp_desktop_mcp.reader.tested_versions`.
       - `schema_fp = schema_fp.model_copy(update={"supported_version_range": SUPPORTED_VERSION_RANGE})` — pure derivation, NOT a new probe.
       - If `wa_version is not None and schema_fp.state == "supported"`: load `tested_wa = _load_tested_wa_versions()`. If `wa_version not in tested_wa`: compute `latest = max(tested_wa, default="(none)")` (lexical max is fine for SemVer-shaped strings; documented as a v1.0 simplification — actual SemVer comparison is a v1.1 candidate). `schema_fp = schema_fp.model_copy(update={"degraded_mode_warning": f"WhatsApp.app v{wa_version} not in tested-versions.md (last tested: {latest}); reads may degrade silently."})`.
       - Use the patched `schema_fp` when constructing the final `DoctorReport`.

    5. Tests (NEW): `tests/unit/test_tested_versions_parser.py` covers Tests 1-4 (use `tmp_path` + `monkeypatch.setattr("whatsapp_desktop_mcp.reader.tested_versions._TESTED_VERSIONS_PATH", tmp_path/"tv.md")` plus a `monkeypatch.setattr("whatsapp_desktop_mcp.reader.tested_versions.load_tested_z_versions", ...)` reload trick or just call the function with arg-injection). `tests/unit/test_doctor_degraded_warning.py` covers Tests 5-10 (mocks `_probe_db_safely` + `detect_whatsapp_version` + monkey-patches `_TESTED_VERSIONS_PATH`).

    Write tests FIRST (RED), then implement (GREEN).

    The doctor extension is a pure derivation — no new probe, no new external surface. DIAG-02 invariant preserved: doctor stays callable when other surfaces fail (the parser's `(1,1)` fallback ensures even a corrupted/missing tested_versions.md never crashes doctor).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_tested_versions_parser.py tests/unit/test_doctor_degraded_warning.py -x -q</automated>
    Existing Phase 1 doctor tests must still pass (D-07 byte-stability + DIAG-02 carry):
    <automated>uv run pytest tests/unit/test_doctor.py -x -q 2>/dev/null || uv run pytest -k 'doctor' tests/unit/ -x -q</automated>
    Parser fault-tolerance grep (must NOT raise on int conversion — try/except in source):
    <automated>grep -cE 'except ValueError' src/whatsapp_desktop_mcp/reader/tested_versions.py | xargs -I{} test {} -ge 1</automated>
    Initial tested_versions.md row present:
    <automated>grep -cE '\|\s*26\.16\.74\s*\|\s*26\.4\s*\|\s*1\s*\|' docs/tested_versions.md | xargs -I{} test {} -ge 1</automated>
    SchemaFingerprint has the two new fields:
    <automated>python <<'PY'
from whatsapp_desktop_mcp.models.doctor import SchemaFingerprint
fields = SchemaFingerprint.model_fields
assert "supported_version_range" in fields, list(fields)
assert "degraded_mode_warning" in fields, list(fields)
print("ok")
PY</automated>
  </verify>
  <done>
    `docs/tested_versions.md` exists with the initial row; `reader/tested_versions.py` parser is fault-tolerant (`(1,1)` default; `try/except ValueError` on int conversion); `SchemaFingerprint` has the two new fields with correct defaults; doctor populates them; all 10 behaviors pass; existing Phase 1 doctor tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sender/audit.py size-based rotation + --audit-log-max-bytes CLI arg</name>
  <read_first>
    - .planning/phases/03-hardening-and-distribution/03-RESEARCH.md §"Pattern 6: Audit Log Size-Based Rotation" (lines 1162-1222) — verbatim-liftable diff
    - .planning/phases/03-hardening-and-distribution/03-RESEARCH.md §"Common Pitfalls" Pitfall 5 (test sandbox must monkey-patch BOTH _LOG_DIR and _LOG_PATH)
    - .planning/phases/03-hardening-and-distribution/03-CONTEXT.md D-25 (10 MB / 5 archives), D-26 (rotation at append-time, NOT timer), D-28 (--audit-log-max-bytes arg)
    - src/whatsapp_desktop_mcp/sender/audit.py — Phase 2 _blocking_append + _LOG_DIR + _LOG_PATH constants
    - src/whatsapp_desktop_mcp/cli.py — Phase 1 argparse pattern; Phase 3 extends with --audit-log-max-bytes (set as os.environ["WHATSAPP_DESKTOP_MCP_AUDIT_LOG_MAX_BYTES"] before lazy server import so audit module reads it on first use)
    - tests/unit/test_audit_rotation.py — write FAILING tests first (RED)
  </read_first>
  <behavior>
    - Test 1 (no rotation under threshold): write 100 small audit entries; `_LOG_PATH` exists with all 100 lines; no `audit.log.1` exists.
    - Test 2 (rotation triggered at threshold): set `WHATSAPP_DESKTOP_MCP_AUDIT_LOG_MAX_BYTES=1024`, write entries until the live `audit.log` would exceed 1024 bytes; the next append rotates: `audit.log.1` contains the previous body verbatim, the new `audit.log` starts fresh with the new entry.
    - Test 3 (5-archive cap shifts oldest off): repeat rotation 6 times; `audit.log.5` exists, `audit.log.6` does NOT (the eldest was unlinked before its slot was reused).
    - Test 4 (correct shift order): after 3 rotations, `audit.log.1` contains the most-recent rotated body, `audit.log.3` the oldest; the rename order is reverse (`audit.log.4 → audit.log.5`, ..., `audit.log → audit.log.1`) so no archive overwrites another's content (Pitfall 5 inversion guard — but the rotation walks reverse order to AVOID this; test asserts content uniqueness across archives).
    - Test 5 (mode 0600 preserved on fresh audit.log after rotation): the new live `audit.log` after rotation has mode 0600 (same as Phase 2 invariant).
    - Test 6 (D-13 plaintext-body invariant — preserved across rotation): each rotated archive line still has only `body_sha256` and never `body` plaintext (rotation moves complete JSONL lines verbatim — body field is shaped by Phase 2's AuditEntry which carries `body_sha256: str` only).
    - Test 7 (--audit-log-max-bytes CLI sets env): `main(["--audit-log-max-bytes", "2048"])` sets `os.environ["WHATSAPP_DESKTOP_MCP_AUDIT_LOG_MAX_BYTES"] == "2048"` BEFORE the lazy `from whatsapp_desktop_mcp.server import run` import (verified via mocked run).
    - Test 8 (test sandbox guard — Pitfall 5): the test fixture monkey-patches BOTH `_LOG_DIR` and `_LOG_PATH` (the rotation test asserts both get patched; `_LOG_DIR.mkdir(...)` MUST resolve under tmp_path, NOT the user's real ~/Library/Logs).
  </behavior>
  <action>
    Two coordinated edits.

    1. `src/whatsapp_desktop_mcp/sender/audit.py` — extend per RESEARCH.md §"Pattern 6" verbatim diff. Add three module-level constants:
       - `_DEFAULT_MAX_BYTES = 10 * 1024 * 1024` (10 MB per D-25).
       - `_ENV_MAX_BYTES = "WHATSAPP_DESKTOP_MCP_AUDIT_LOG_MAX_BYTES"`.
       - `_ARCHIVE_COUNT = 5` (D-25 cap).
       Add two helpers:
       - `_resolve_max_bytes() -> int`: reads env var, returns int, falls back to default; ValueError-safe.
       - `_rotate_in_place(path: Path, archive_count: int) -> None`: per RESEARCH.md §"Pattern 6". Walks reverse order so no archive overwrites another's content. Eldest (`audit.log.{count}`) is unlinked before its slot is reused.
       Extend `_blocking_append`:
       - `_LOG_DIR.mkdir(parents=True, exist_ok=True)` (existing, keep).
       - **NEW BEFORE WRITE:** `if _LOG_PATH.exists() and _LOG_PATH.stat().st_size >= _resolve_max_bytes(): _rotate_in_place(_LOG_PATH, _ARCHIVE_COUNT)`.
       - Existing `is_new = not _LOG_PATH.exists()` → write → `os.chmod(_LOG_PATH, 0o600)` if new (preserved). Note: after rotation, `_LOG_PATH` no longer exists so `is_new=True` and the chmod fires automatically — mode 0600 carried.
       D-13 STRUCTURAL invariant preserved by construction: rotation moves ENTIRE JSONL lines via `Path.rename`; the AuditEntry schema (Phase 2) has `body_sha256: str` and never raw body; rotated archives are byte-identical to the live log up to the rotation point.

    2. `src/whatsapp_desktop_mcp/cli.py` — extend `main` argparse with `parser.add_argument("--audit-log-max-bytes", type=int, default=10 * 1024 * 1024, help="Audit log rotation threshold in bytes (D-25 default 10 MB; rotation keeps last 5 archives).")`. Set `os.environ["WHATSAPP_DESKTOP_MCP_AUDIT_LOG_MAX_BYTES"] = str(args.audit_log_max_bytes)` AFTER the existing `server.read_only_mode = args.read_only` line, BEFORE the lazy `from whatsapp_desktop_mcp.server import run` import — so the audit module reads it on first `_resolve_max_bytes()` call. Place this assignment ALONGSIDE the `server.fts5_mode = args.fts5_mode` from Plan 03-01 (the two CLI surfaces share the same "set BEFORE lazy server import" discipline).

    Tests (NEW): `tests/unit/test_audit_rotation.py` covers Tests 1-7. Per Pitfall 5: the fixture monkey-patches BOTH `audit._LOG_DIR` AND `audit._LOG_PATH` to tmp_path / tmp_path / "audit.log" (the Phase 2 `_isolate_live_state` fixture from `tests/integration/test_live_send.py` already does this — mirror its shape). Test 7 mocks `whatsapp_desktop_mcp.server.run` to verify the env var is set BEFORE the lazy import resolves.

    Write tests FIRST (RED).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_audit_rotation.py -x -q</automated>
    Existing Phase 2 audit tests must still pass (no regression):
    <automated>uv run pytest tests/unit/test_audit.py -x -q 2>/dev/null || uv run pytest -k 'audit' tests/unit/ -x -q</automated>
    Rotation logic present in source:
    <automated>grep -cE '_rotate_in_place|_resolve_max_bytes' src/whatsapp_desktop_mcp/sender/audit.py | xargs -I{} test {} -ge 1</automated>
    CLI arg present:
    <automated>grep -cE -- '--audit-log-max-bytes' src/whatsapp_desktop_mcp/cli.py | xargs -I{} test {} -ge 1</automated>
    D-13 plaintext-body invariant carry — AuditEntry must NOT have raw `body: str`:
    <automated>python <<'PY'
from whatsapp_desktop_mcp.sender.audit import AuditEntry
fields = AuditEntry.model_fields
# Body must be present only as body_sha256 (Phase 2 D-13 invariant); rotation does not change schema.
assert "body_sha256" in fields, f"body_sha256 missing: {list(fields)}"
assert "body" not in fields, f"raw body field present (D-13 violation): {list(fields)}"
print("ok")
PY</automated>
  </verify>
  <done>
    `audit.py` has rotation; `--audit-log-max-bytes` CLI arg parses and sets the env var; existing Phase 2 audit tests still pass; D-13 plaintext-body invariant grep gate passes; rotation correctly walks reverse order; mode 0600 preserved on fresh log post-rotation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: dev/ subpackage + reset_rate_limit subcommand + cli.py argparse subparser dispatch</name>
  <read_first>
    - .planning/phases/03-hardening-and-distribution/03-RESEARCH.md §"Pattern 8: whatsapp-desktop-mcp dev reset-rate-limit Subcommand" (lines 1318-1418) — verbatim-liftable cli + dev module
    - .planning/phases/03-hardening-and-distribution/03-RESEARCH.md §"Anti-Patterns to Avoid" — `dev` subcommands are NOT MCP tools; no `@timeout` decorator applies; per-file ruff T201 ignore is correct because dev subcommands print to stdout (one-shot CLI utility, NOT the stdio MCP server)
    - .planning/phases/03-hardening-and-distribution/03-CONTEXT.md D-27, D-28 (dev subcommand surface), D-33 (README points users at `whatsapp-desktop-mcp dev reset-rate-limit` for budget recovery — Plan 03-04 documents this)
    - src/whatsapp_desktop_mcp/sender/rate_limit.py — `_DB_PATH` is the unlink target
    - src/whatsapp_desktop_mcp/cli.py — Phase 1 argparse main; Phase 3 restructures into a subparser dispatch (default = MCP server; `dev` subcommand routes to dev/*.py)
    - pyproject.toml — Phase 0 ruff config; Phase 3 adds [tool.ruff.lint.per-file-ignores] entry for src/whatsapp_desktop_mcp/dev/*.py
    - tests/unit/test_dev_subcommand.py — write FAILING tests first (RED)
  </read_first>
  <behavior>
    - Test 1 (subparser parses): `main(["dev", "reset-rate-limit"])` reaches the dev dispatch branch (verified by mocking `whatsapp_desktop_mcp.dev.reset_rate_limit.run` and asserting it was called).
    - Test 2 (no subcommand → server runs): `main([])` does NOT route into dev; instead calls the lazy `server.run()` (verified by mocking).
    - Test 3 (non-tty refuses): with stdin not a tty (default in pytest), `dev.reset_rate_limit.run()` returns 1 AND prints to stderr "Refusing to reset rate-limit DB from a non-tty…". The DB file (sandboxed under tmp_path via monkey-patched `rate_limit._DB_PATH`) is NOT unlinked.
    - Test 4 (tty + 'y' confirms + unlinks): with `sys.stdin.isatty` mocked to return True and `sys.stdin.readline` mocked to return "y\n", `run()` returns 0 AND the DB file (sandboxed) is unlinked.
    - Test 5 (tty + non-'y' refuses): same setup but readline returns "n\n" → returns 1 AND DB file remains.
    - Test 6 (DB absent → no-op success): when `rate_limit._DB_PATH` does not exist, `run()` returns 0 with the "nothing to reset" stdout message; does NOT prompt.
    - Test 7 (T201 lint pass): the dev subpackage has stdout `print()` calls but ruff does NOT report T201 (per-file-ignore correctly configured). `uv run ruff check src/whatsapp_desktop_mcp/dev/` exits 0.
  </behavior>
  <action>
    Four coordinated edits.

    1. `src/whatsapp_desktop_mcp/dev/__init__.py` (NEW) — empty file (or one-line docstring) — Python package marker.

    2. `src/whatsapp_desktop_mcp/dev/reset_rate_limit.py` (NEW) — RESEARCH.md §"Pattern 8" provides the verbatim module. Lift it. Module shape:
       - Imports: `import sys`; `from whatsapp_desktop_mcp.sender import rate_limit`.
       - `def run() -> int`:
         - `db_path = rate_limit._DB_PATH`.
         - Early-return: if `not db_path.exists()`: `print(f"No rate-limit DB at {db_path}; nothing to reset."); return 0`.
         - tty guard: if `not sys.stdin.isatty()`: print refusal to stderr, `return 1`.
         - Confirmation prompt: `print(f"This will erase all rate-limit history at {db_path}. Continue? [y/N] ", end="", flush=True)`. Read `answer = sys.stdin.readline().strip().lower()`. If `answer != "y"`: `print("Aborted."); return 1`.
         - `db_path.unlink(missing_ok=True)` (the missing_ok guard handles a TOCTOU race between the early-return check and the unlink). `print(f"Removed {db_path}.")`. `return 0`.

    3. `src/whatsapp_desktop_mcp/cli.py` — restructure `main` per RESEARCH.md §"Pattern 8" verbatim. Two-step refactor preserving the Phase 1 `--read-only` + Plan 03-01 `--fts5-mode` + Plan 03-03 Task 2 `--audit-log-max-bytes` arg behaviors:
       - Extract the existing argparse calls into a helper `_add_server_args(parser)` so they can be applied to either the top-level parser OR a future `server` subcommand (RESEARCH.md "Apply the existing server CLI args to a parser").
       - In `main`: build the top-level parser; call `_add_server_args(parser)`; add `subparsers = parser.add_subparsers(dest="cmd")`; add `dev = subparsers.add_parser("dev", help="developer utility subcommands")`; add `dev_sub = dev.add_subparsers(dest="dev_cmd")`; add `dev_sub.add_parser("reset-rate-limit", help="clear ~/Library/Application Support/whatsapp-desktop-mcp/rate-limit.db")`.
       - After `args = parser.parse_args(argv)`: dispatch. If `args.cmd == "dev" and args.dev_cmd == "reset-rate-limit"`: `from whatsapp_desktop_mcp.dev.reset_rate_limit import run as dev_reset; return dev_reset()`. Otherwise: existing server-startup path (`from whatsapp_desktop_mcp import server; server.read_only_mode = args.read_only; server.fts5_mode = args.fts5_mode; os.environ["WHATSAPP_DESKTOP_MCP_AUDIT_LOG_MAX_BYTES"] = str(args.audit_log_max_bytes); from whatsapp_desktop_mcp.server import run; run(); return 0`).

    4. `pyproject.toml` — add `[tool.ruff.lint.per-file-ignores]` entry: `"src/whatsapp_desktop_mcp/dev/*.py" = ["T201"]` per RESEARCH.md §"Pattern 8". The reasoning: `dev` subcommands are one-shot CLI utilities, NOT the stdio MCP server, so stdout `print()` is intentional; the Phase 0 D-05 stdout-purity rule applies to `whatsapp-desktop-mcp` (server mode), NOT to `whatsapp-desktop-mcp dev *`. (Verify the `[tool.ruff.lint.per-file-ignores]` table doesn't already exist; if it does, append the new key.)

    Tests (NEW): `tests/unit/test_dev_subcommand.py` covers Tests 1-7. Use `monkeypatch.setattr` to swap `sys.stdin.isatty`, `sys.stdin.readline`, and `rate_limit._DB_PATH`. Test 1 mocks `whatsapp_desktop_mcp.dev.reset_rate_limit.run`. Test 7 invokes `subprocess.run(["uv", "run", "ruff", "check", "src/whatsapp_desktop_mcp/dev/"])` and asserts returncode==0.

    Write tests FIRST (RED).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_dev_subcommand.py -x -q</automated>
    Phase 1 CLI tests still pass (no regression in --read-only / --version / --help):
    <automated>uv run pytest -k 'cli' tests/unit/ -x -q</automated>
    Per-file ruff ignore is configured:
    <automated>grep -cE 'src/whatsapp_desktop_mcp/dev' pyproject.toml | xargs -I{} test {} -ge 1</automated>
    The dev subpackage passes ruff (the per-file-ignore must work):
    <automated>uv run ruff check src/whatsapp_desktop_mcp/dev/</automated>
    The dev subcommand is reachable end-to-end (with stdin redirected to /dev/null so the non-tty branch fires and no DB is touched):
    <automated>uv run whatsapp-desktop-mcp dev reset-rate-limit </dev/null; test $? -eq 1</automated>
  </verify>
  <done>
    `dev/` subpackage exists; `reset_rate_limit.run()` honors all 5 prompt cases (no-DB, non-tty, tty+y, tty+n, tty+other); `cli.main` dispatches; ruff per-file-ignore configured; existing Phase 1 cli tests still pass; end-to-end command line invocation works.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/tested_versions.md` (manual maintainer edits) → `reader/tested_versions.py` parser | Trusted maintainer input crosses into code at module-load time; malformed rows must NOT crash the server (DIAG-02 invariant). |
| WhatsApp.app version string → doctor.degraded_mode_warning rendering | Untrusted (vendor-controlled) string is interpolated into a structured warning consumed by the LLM client. |
| User stdin → `dev reset-rate-limit` confirmation | Interactive input controls a destructive file-system operation. |
| `audit.log.{1..5}` archive files → on-disk persistence | Rotation creates new files inheriting mode 0600 (same posture as the live log). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Denial of Service (parser crash on malformed row) | `reader/tested_versions.py` module-load | mitigate | `try/except ValueError` around the `int()` call inside `_parse_z_versions`; logger.warning + skip; `(1,1)` default on empty result; `(FileNotFoundError, PermissionError)` caught at the file-read boundary. Doctor stays callable per DIAG-02 (Pitfall 4). Test 3 in Task 1 enforces this. |
| T-03-03-02 | Information Disclosure (audit body plaintext leak after rotation) | `audit.log.1..5` archives | mitigate | D-13 STRUCTURAL invariant carries: rotation uses `Path.rename` to move COMPLETE JSONL lines verbatim; AuditEntry schema has `body_sha256: str` only (no raw `body` field). Test 6 + the AuditEntry schema grep gate enforce this. Mode 0600 preserved on fresh post-rotation log via the existing `os.chmod(_LOG_PATH, 0o600)` when `is_new=True`. |
| T-03-03-03 | Denial of Service (audit log race during cross-process rotation) | `_blocking_append` rotation | accept | Single-instance-per-user assumption from Phase 2 D-14 carries; rotation is single-threaded within one MCP server process (audit append is serialized through `asyncio.to_thread`). Cross-process race documented as out-of-scope for v1.0 per CONTEXT.md T-4. No `fcntl.flock` in v1 (RESEARCH.md anti-pattern: "Adding fcntl.flock to audit log rotation in v1.0"). |
| T-03-03-04 | Tampering / Information Disclosure (degraded_mode_warning prompt-injection vector) | `doctor.schema_fingerprint.degraded_mode_warning` rendering | mitigate | The structured warning is interpolated from two sources: `wa_version` (vendor-controlled string from `/Applications/WhatsApp.app/Contents/Info.plist:CFBundleShortVersionString`, sanitized by macOS Info.plist parsing) and `latest` (from `_load_tested_wa_versions` parsing column 1 of the maintainer-controlled `tested_versions.md`). Both are markdown-shaped strings with no LLM-instruction surface; the warning is delivered as a Pydantic-modeled string field, not as freeform tool output. The risk is low — accepted as v1.0 posture. |
| T-03-03-05 | Denial of Service (accidental `dev reset-rate-limit` invocation in CI / non-tty context) | `dev/reset_rate_limit.py` | mitigate | `sys.stdin.isatty()` check refuses non-tty stdin; explicit `y` answer required for tty stdin (RESEARCH.md §"Pattern 8" tty + explicit confirmation). Test 3 enforces. |
| T-03-03-06 | Tampering (T201 lint bypass via dev/ subpackage exfiltrating to stdout from server-mode code) | `src/whatsapp_desktop_mcp/dev/*.py` | mitigate | The per-file ruff T201 ignore is scoped to `src/whatsapp_desktop_mcp/dev/*.py` ONLY; the rest of the package (notably `server.py`, `tools/*.py`, `reader/*.py`, `sender/*.py`) still has T201 active. The dev subpackage is reachable ONLY via the `whatsapp-desktop-mcp dev <subcommand>` argparse path — there is no import path from server.py into dev/*. Verified by `grep -rE 'from whatsapp_desktop_mcp.dev|import whatsapp_desktop_mcp.dev' src/whatsapp_desktop_mcp/` returning ONLY the cli.py dispatch line. |

</threat_model>

<verification>
- All Task 1, 2, 3 automated tests pass.
- Existing Phase 1 doctor tests + Phase 2 audit tests + Phase 1 cli tests pass (no regression).
- `SchemaFingerprint.model_fields` includes `supported_version_range` and `degraded_mode_warning`.
- `AuditEntry.model_fields` does NOT include raw `body` (D-13 invariant).
- `whatsapp-desktop-mcp dev reset-rate-limit </dev/null` exits 1 (non-tty refuses).
- `uv run ruff check src/whatsapp_desktop_mcp/dev/` exits 0 (per-file-ignore works).
- Source audit: this plan covers ROADMAP SC3 (tested_versions.md matrix); CONTEXT decisions D-19, D-20, D-21, D-25, D-26, D-27, D-28 (--audit-log-max-bytes + dev reset-rate-limit portions).
</verification>

<success_criteria>
- `doctor` returns `degraded_mode_warning` when WhatsApp.app version is outside the tested matrix; remains `None` for matrix-matching versions.
- Audit log rotates at 10 MB (configurable) keeping 5 archives; D-13 body-plaintext invariant preserved.
- Maintainer can recover live-test budget via `whatsapp-desktop-mcp dev reset-rate-limit`.
- DIAG-02 (doctor stays callable when other surfaces fail) preserved via parser fault-tolerance.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hardening-and-distribution/03-03-SUMMARY.md` per the standard summary template.
</output>
