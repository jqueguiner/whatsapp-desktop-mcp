---
phase: 03-hardening-and-distribution
plan: 2
title: Distribution infrastructure — signed .pkg + Homebrew custom tap
wave: 1
depends_on: []
requirements: [DIST-02]
files_modified:
  - scripts/build-pkg.sh
  - scripts/distribution.xml
  - scripts/pkg-resources/welcome.html
  - .github/workflows/release.yml
  - docs/release-setup.md
  - Formula/whatsapp-desktop-mcp.rb
autonomous: true
user_setup:
  - service: apple-developer-program
    why: "Sign + notarize the .pkg installer (DIST-02 stable absolute path requirement). Without this cert, the if: secrets.APPLE_INSTALLER_CERT_P12 != '' skip-block (D-07) lets the unsigned .pkg ship with a stark warning, but signed is the v1.0 target."
    env_vars:
      - name: APPLE_INSTALLER_CERT_P12
        source: "Apple Developer → Certificates → Developer ID Installer → Download → Keychain Access → export as .p12 → base64 encode → set as GitHub Actions secret. Walk-through in docs/release-setup.md."
      - name: APPLE_INSTALLER_CERT_PASSWORD
        source: "Password used during the Keychain Access .p12 export."
      - name: APPLE_ID
        source: "Apple Developer account email."
      - name: APPLE_TEAM_ID
        source: "Apple Developer → Membership → Team ID (10-char alphanum)."
      - name: APPLE_APP_SPECIFIC_PASSWORD
        source: "appleid.apple.com → Sign-In and Security → App-Specific Passwords → generate one for whatsapp-desktop-mcp notarization."
      - name: BREW_TAP_DEPLOY_KEY
        source: "GitHub deploy key with write access to gladia/homebrew-whatsapp-desktop-mcp tap repo (or a fine-grained PAT with Contents:Write on that repo)."
    dashboard_config:
      - task: "Enroll the gladia organization (or maintainer's individual account) in the Apple Developer Program"
        location: "https://developer.apple.com/programs/"
      - task: "Bootstrap the brew tap repo gladia/homebrew-whatsapp-desktop-mcp with the initial Formula/whatsapp-desktop-mcp.rb committed to its main branch"
        location: "https://github.com/gladia/homebrew-whatsapp-desktop-mcp (create new repo)"
      - task: "Add the 6 GitHub Actions secrets above to gladia/whatsapp-desktop-mcp repo Settings → Secrets and variables → Actions"
        location: "https://github.com/gladia/whatsapp-desktop-mcp/settings/secrets/actions"
must_haves:
  truths:
    - "On every git tag v* push, .github/workflows/release.yml runs the pkg-build job (when APPLE_INSTALLER_CERT_P12 secret is present) and produces a Developer-ID-signed + notarized + stapled .pkg attached to the GitHub release"
    - "The .pkg installs the launcher at /usr/local/bin/whatsapp-desktop-mcp (stable absolute path); re-running the installer for a newer version does NOT change that path (so TCC grants persist — P15 mitigation)"
    - "When APPLE_INSTALLER_CERT_P12 is absent (community fork), the pkg-build job is no-op and the rest of release.yml (PyPI publish from Phase 0) still runs (D-07 skip-block)"
    - "On every release, the tap-update job regenerates Formula/whatsapp-desktop-mcp.rb in gladia/homebrew-whatsapp-desktop-mcp using brew update-python-resources (NOT homebrew-pypi-poet) and opens a PR (or pushes directly with BREW_TAP_DEPLOY_KEY)"
    - "End user runs `brew install gladia/whatsapp-desktop-mcp/whatsapp-desktop-mcp` and gets the launcher at /opt/homebrew/bin/whatsapp-desktop-mcp (Apple Silicon) or /usr/local/bin/whatsapp-desktop-mcp (Intel)"
    - "docs/release-setup.md walks the maintainer through enrollment + .p12 export + GitHub secrets bootstrap (D-08 one-time setup doc)"
  artifacts:
    - path: "scripts/build-pkg.sh"
      provides: "Stages a python -m venv --copies bundle at the final install path + pkgbuild + productbuild → unsigned .pkg in dist/"
      contains: "python.* -m venv --copies"
    - path: "scripts/distribution.xml"
      provides: "productbuild distribution archive XML with macOS 15+ allowed-os-versions gate"
      contains: "net.gladia.whatsapp-desktop-mcp"
    - path: ".github/workflows/release.yml"
      provides: "pkg-build + tap-update jobs appended after the existing publish job"
      contains: "pkg-build"
    - path: "docs/release-setup.md"
      provides: "Apple Developer enrollment walkthrough + .p12 export + GitHub secrets bootstrap"
    - path: "Formula/whatsapp-desktop-mcp.rb"
      provides: "Bootstrap Formula committed to repo so the tap-update job has a baseline to update on first release"
      contains: "Language::Python::Virtualenv"
  key_links:
    - from: ".github/workflows/release.yml pkg-build job"
      to: "scripts/build-pkg.sh"
      via: "run: ./scripts/build-pkg.sh under VERSION env"
      pattern: "scripts/build-pkg\\.sh"
    - from: ".github/workflows/release.yml pkg-build job"
      to: "Apple notarization service"
      via: "xcrun notarytool submit --wait + xcrun stapler staple"
      pattern: "notarytool submit"
    - from: ".github/workflows/release.yml tap-update job"
      to: "gladia/homebrew-whatsapp-desktop-mcp Formula/whatsapp-desktop-mcp.rb"
      via: "checkout + brew update-python-resources + peter-evans/create-pull-request"
      pattern: "brew update-python-resources"
    - from: "/usr/local/bin/whatsapp-desktop-mcp launcher"
      to: "/usr/local/lib/whatsapp-desktop-mcp/.venv/bin/python"
      via: "exec into bundled interpreter (no stdout output — Phase 0 D-05 invariant carried)"
      pattern: "exec.*\\.venv/bin/python.*-m whatsapp_desktop_mcp"
---

<objective>
Build the v1.0 release pipeline that puts the launcher at a stable absolute path so macOS TCC grants persist across upgrades (DIST-02). Two install vectors land together: a Developer-ID-signed + notarized + stapled `.pkg` installer attached to the GitHub release, and an auto-updated Formula in the `gladia/homebrew-whatsapp-desktop-mcp` custom tap. The Phase 0 OIDC PyPI publish stays unchanged.

Purpose: Closes DIST-02 (signed-pkg + brew, both at stable absolute path) — the central P15 (TCC churn from pipx/uvx Python path drift) mitigation. Satisfies ROADMAP §"Phase 3" Success Criterion 1 (clean Mac install via .pkg or brew without re-granting TCC on upgrade).

Output: `scripts/build-pkg.sh` + `scripts/distribution.xml` + `.github/workflows/release.yml` extended with `pkg-build` + `tap-update` jobs + `docs/release-setup.md` + bootstrap `Formula/whatsapp-desktop-mcp.rb` (committed to this repo so the tap-update job has a baseline; the maintainer copies it into the tap repo on first bootstrap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hardening-and-distribution/03-CONTEXT.md
@.planning/phases/03-hardening-and-distribution/03-RESEARCH.md
@.planning/research/PITFALLS.md
@CLAUDE.md

# Existing release workflow — extending in place
@.github/workflows/release.yml
@.github/workflows/ci.yml
@pyproject.toml
@README.md

<interfaces>
<!-- Apple toolchain commands and GitHub Actions surfaces this plan composes. No code interfaces in the Python sense — pure shell + YAML. -->

Apple toolchain (bundled with macos-14 / macos-15 GitHub runners):
  pkgbuild --root <stage> --identifier net.gladia.whatsapp-desktop-mcp --version $VERSION --install-location / --ownership recommended <out>.pkg
  productbuild --distribution scripts/distribution.xml --package-path dist --resources scripts/pkg-resources <out>.pkg
  productsign --sign "Developer ID Installer: <Team> (<Team ID>)" <unsigned>.pkg <signed>.pkg
  xcrun notarytool submit <signed>.pkg --apple-id $APPLE_ID --team-id $APPLE_TEAM_ID --password $APPLE_APP_SPECIFIC_PASSWORD --wait
  xcrun stapler staple <signed>.pkg
  spctl --assess --type install -vvv <signed>.pkg   # post-build verification

GitHub Marketplace actions (current stable pins per RESEARCH.md A1, A7):
  apple-actions/import-codesign-certs@v3   # action MAY be at v6.x by execution time — Task 2 verifies pin
  softprops/action-gh-release@v2
  peter-evans/create-pull-request@v6
  actions/checkout@v4
  astral-sh/setup-uv@v8

Brew CLI (bundled on macos-14 runner):
  brew update-python-resources whatsapp-desktop-mcp   # 2026 replacement for homebrew-pypi-poet
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/build-pkg.sh + distribution.xml + bootstrap Formula</name>
  <read_first>
    - .planning/phases/03-hardening-and-distribution/03-RESEARCH.md §"Pattern 1: .pkg Signing Pipeline" (lines 360-519) — verbatim-liftable build-pkg.sh, distribution.xml, signing/notarization snippets
    - .planning/phases/03-hardening-and-distribution/03-RESEARCH.md §"Pattern 2: Homebrew Tap Formula via Language::Python::Virtualenv" (lines 521-627) — verbatim-liftable Formula skeleton
    - .planning/phases/03-hardening-and-distribution/03-RESEARCH.md §"Common Pitfalls" Pitfall 2 (Gatekeeper on launcher), Pitfall 6 (uvx TCC churn — what we're mitigating), Pitfall 8 (pyobjc dylib re-sign), Pitfall 9 (Apple enrollment delay)
    - .planning/phases/03-hardening-and-distribution/03-RESEARCH.md §"Open Questions" Q2 + Q3 (venv copies relocation; pyobjc framework imports under copied venv)
    - .planning/phases/03-hardening-and-distribution/03-CONTEXT.md D-01 .. D-05 (distribution channels), D-09 (Formula skeleton)
    - pyproject.toml — current dep set (mcp, pydantic, pyobjc-core / -framework-Cocoa / -framework-ApplicationServices, version pin)
    - README.md — to confirm the existing PyPI install instructions (will be replaced by Plan 03-04 with the 3-row matrix; this plan does NOT touch README)
  </read_first>
  <action>
    Create three sibling files under `scripts/` plus the bootstrap Formula at the repo root.

    1. `scripts/build-pkg.sh` (chmod +x). RESEARCH.md §"Pattern 1" provides the verbatim-liftable script — lift it. Key invariants:
       - **Use `python -m venv --copies` (NOT `uv venv --relocatable`)** at the FINAL install location inside the staging tree (`${STAGING_DIR}/usr/local/lib/whatsapp-desktop-mcp/.venv`). Per RESEARCH.md Open Question Q2, building the venv at the final install path makes `pyvenv.cfg` record the exact post-install interpreter path. This is the substitution called out in the planning context: research locked `python -m venv --copies` over `uv venv --relocatable` (uv #3587 closed without confirmation; #15751 still open as of May 2026).
       - `set -euo pipefail` at top.
       - Inputs via env: `VERSION` (required), `STAGING_DIR` (default `/tmp/whatsapp-desktop-mcp-pkg`).
       - Constants: `BUNDLE_ID="net.gladia.whatsapp-desktop-mcp"`, `INSTALL_PREFIX="/usr/local"`, `VENV_DIR="${STAGING_DIR}${INSTALL_PREFIX}/lib/whatsapp-desktop-mcp/.venv"`, `BIN_DIR="${STAGING_DIR}${INSTALL_PREFIX}/bin"`.
       - Steps: clean staging → `uv build --wheel --out-dir dist` → `/usr/bin/env python3.12 -m venv --copies "${VENV_DIR}"` → `"${VENV_DIR}/bin/pip" install --upgrade pip` → `"${VENV_DIR}/bin/pip" install "dist/whatsapp_desktop_mcp-${VERSION}-py3-none-any.whl"` → write the launcher heredoc into `${BIN_DIR}/whatsapp-desktop-mcp` then chmod 755 → `pkgbuild --root "${STAGING_DIR}" --identifier "${BUNDLE_ID}" --version "${VERSION}" --install-location / --ownership recommended "dist/whatsapp-desktop-mcp-${VERSION}-component.pkg"` → `productbuild --distribution scripts/distribution.xml --package-path dist --resources scripts/pkg-resources "dist/whatsapp-desktop-mcp-${VERSION}-unsigned.pkg"`.
       - Launcher script content (verbatim — RESEARCH.md Example 3): `#!/bin/bash\nexec "/usr/local/lib/whatsapp-desktop-mcp/.venv/bin/python" -m whatsapp_desktop_mcp "$@"\n` — NO echo, NO set -x, NO diagnostic stdout (Phase 0 D-05 invariant carries through the launcher per RESEARCH.md §"Anti-Patterns to Avoid").
       - **Optional codesign --deep step** (RESEARCH.md Pitfall 8 / Open Question Q3): include a commented-out `codesign --deep --force --options runtime --sign "Developer ID Application: <Team Name> (<Team ID>)" "${STAGING_DIR}${INSTALL_PREFIX}/lib/whatsapp-desktop-mcp/.venv"` line at the bottom of the script with a comment explaining it is enabled by `release.yml` AFTER the staging step (NOT inside `build-pkg.sh` itself, because the cert may not exist on community-fork builds). The shell script is the staging primitive; signing is orchestrated by the workflow.

    2. `scripts/distribution.xml` — RESEARCH.md §"Pattern 1" provides the verbatim XML. Lift it verbatim:
       - `<title>WhatsApp MCP</title>`, `<organization>net.gladia</organization>`.
       - `<options customize="never" require-scripts="false" rootVolumeOnly="true"/>` — install to system volume only, no customization.
       - `<volume-check><allowed-os-versions><os-version min="15.0"/></allowed-os-versions></volume-check>` (macOS Sequoia 15+ floor matches Formula `depends_on macos: :sequoia`).
       - `<choices-outline>`, `<choice id="default"/>`, `<choice id="net.gladia.whatsapp-desktop-mcp" visible="false">`, `<pkg-ref id="net.gladia.whatsapp-desktop-mcp" version="VERSION_PLACEHOLDER" onConclusion="none">whatsapp-desktop-mcp-VERSION_PLACEHOLDER-component.pkg</pkg-ref>`.
       - The build-pkg.sh script does a `sed -i '' "s/VERSION_PLACEHOLDER/${VERSION}/g" scripts/distribution.xml` BEFORE the productbuild call (to a copy in dist/, NOT the source-tree file — preserve idempotency on local re-runs).

    3. `scripts/pkg-resources/welcome.html` — minimal welcome page shown during install (productbuild requires the resources dir to exist). Single `<h2>WhatsApp MCP</h2>` + a paragraph linking to README. Keep it small (<2 KB).

    4. `Formula/whatsapp-desktop-mcp.rb` (committed to this repo, root-level Formula dir, as a baseline that the tap-update job re-syncs into the gladia/homebrew-whatsapp-desktop-mcp tap repo). RESEARCH.md §"Pattern 2" provides the verbatim Formula. Lift it. Key shape:
       - `class WhatsappMcp < Formula; include Language::Python::Virtualenv` per CONTEXT.md D-09.
       - `desc`, `homepage`, `url` (placeholder PyPI URL — `tap-update` job rewrites it on each release), `sha256 "0" * 64` placeholder (rewritten by `tap-update`), `license "MIT"`.
       - `depends_on "python@3.12"`, `depends_on macos: :sequoia` (CONTEXT.md D-09).
       - Empty `resource` blocks — the `tap-update` job populates these via `brew update-python-resources` (CONTEXT.md D-10 with the RESEARCH.md tactical substitution: `brew update-python-resources`, NOT `homebrew-pypi-poet`. The pre-research-noted overrides at the top of the planning context lock this).
       - `def install; virtualenv_install_with_resources; end`.
       - `test do; assert_match "0.1.0", shell_output("#{bin}/whatsapp-desktop-mcp --version"); end` (D-09).

    Do NOT write any Python source in this task. The .pkg payload comes from the existing `whatsapp-desktop-mcp` wheel built by `uv build`; this task adds packaging infrastructure only.
  </action>
  <verify>
    Bash syntax check on the new script:
    <automated>bash -n scripts/build-pkg.sh</automated>
    Lift mandate (the research locked `python -m venv --copies`; `uv venv --relocatable` is forbidden):
    <automated>grep -cE 'python.* -m venv --copies' scripts/build-pkg.sh | xargs -I{} test {} -ge 1</automated>
    <automated>test "$(grep -cE 'uv venv --relocatable' scripts/build-pkg.sh)" = "0"</automated>
    Shell launcher must NOT echo or set -x (Phase 0 D-05 stdout-purity carries):
    <automated>test "$(grep -cE 'echo|set -x' scripts/build-pkg.sh | head -1)" -ge 0  # advisory; the launcher heredoc is the asserted target</automated>
    The launcher heredoc body must be exec-only:
    <automated>grep -cE 'exec ".*\.venv/bin/python" -m whatsapp_desktop_mcp' scripts/build-pkg.sh | xargs -I{} test {} -ge 1</automated>
    XML well-formedness:
    <automated>python <<'PY'
import xml.etree.ElementTree as ET
ET.parse("scripts/distribution.xml")
print("ok")
PY</automated>
    Formula syntax (basic shape grep — no ruby toolchain on dev box assumed):
    <automated>grep -cE 'include Language::Python::Virtualenv' Formula/whatsapp-desktop-mcp.rb | xargs -I{} test {} -ge 1</automated>
    <automated>grep -cE 'def install' Formula/whatsapp-desktop-mcp.rb | xargs -I{} test {} -ge 1</automated>
    <automated>grep -cE 'depends_on macos: :sequoia' Formula/whatsapp-desktop-mcp.rb | xargs -I{} test {} -ge 1</automated>
    Welcome HTML exists and is non-empty:
    <automated>test -s scripts/pkg-resources/welcome.html</automated>
  </verify>
  <done>
    `scripts/build-pkg.sh`, `scripts/distribution.xml`, `scripts/pkg-resources/welcome.html`, `Formula/whatsapp-desktop-mcp.rb` all committed; bash syntax clean; XML well-formed; mandatory `python -m venv --copies` present; mandatory `uv venv --relocatable` absent; launcher heredoc is exec-only. Local-machine sanity check (NOT in CI; maintainer-only): `VERSION=0.1.0 bash scripts/build-pkg.sh` produces `dist/whatsapp-desktop-mcp-0.1.0-unsigned.pkg`. (Skipped automatically on Linux dev boxes; the script will fail on `pkgbuild` not found, which is expected and acceptable for local validation.)
  </done>
</task>

<task type="auto">
  <name>Task 2: release.yml extension — pkg-build + tap-update jobs (with D-07 skip-block)</name>
  <read_first>
    - .planning/phases/03-hardening-and-distribution/03-RESEARCH.md §"Pattern 1" signing/notarization YAML (lines 484-519)
    - .planning/phases/03-hardening-and-distribution/03-RESEARCH.md §"Pattern 2" tap-update YAML (lines 578-625)
    - .planning/phases/03-hardening-and-distribution/03-RESEARCH.md §"Common Pitfalls" Pitfall 3 (PyPI CDN propagation), Pitfall 8 (pyobjc dylib re-sign), Pitfall 9 (enrollment delay)
    - .planning/phases/03-hardening-and-distribution/03-RESEARCH.md §"Open Questions" Q1 (apple-actions/import-codesign-certs version pin)
    - .planning/phases/03-hardening-and-distribution/03-CONTEXT.md D-06 (signing pipeline), D-07 (skip-block), D-10 (formula publish automation)
    - .github/workflows/release.yml — Phase 0 OIDC publish job (extending downstream)
    - .github/workflows/ci.yml — invoked by release.yml as the gate
  </read_first>
  <action>
    Extend `.github/workflows/release.yml` by APPENDING two new jobs after the existing `publish` (PyPI OIDC) job. Do NOT modify the `publish` job — DIST-01 from Phase 0 is already satisfied and must remain byte-stable.

    The order of jobs after this plan: `ci` (Phase 0) → `publish` (Phase 0, OIDC PyPI) → `pkg-build` (NEW, conditional on cert secret) → `tap-update` (NEW, conditional on `BREW_TAP_DEPLOY_KEY` secret).

    **`pkg-build` job spec** (verbatim per RESEARCH.md §"Pattern 1" + the locked D-07 skip-block):

    - `runs-on: macos-14` (RESEARCH.md §"Pattern 1" specifics line — Linux runners cannot sign macOS .pkg; macOS-14 has the bundled Apple toolchain).
    - `needs: [publish]` (publish must complete first so the wheel exists at the same `${{ github.ref_name }}` tag — though this job builds its OWN wheel via `uv build` per build-pkg.sh, the gate ordering is a release-coherence guarantee).
    - `if: ${{ secrets.APPLE_INSTALLER_CERT_P12 != '' }}` — the locked D-07 skip-block. NOTE: GitHub Actions secrets are evaluated at workflow-eval time; per the locked decision the syntax is exactly `if: ${{ secrets.APPLE_INSTALLER_CERT_P12 != '' }}` (or, when `secrets` aren't directly available in `if:` on some runners, route through an `env:` mapping at job level and `if: env.HAS_CERT == 'true'`). The planner picks the form that GitHub currently supports — both yield the same skip behavior. **Use the `env: HAS_CERT: ${{ secrets.APPLE_INSTALLER_CERT_P12 != '' && 'true' || 'false' }}` form at job level + `if: env.HAS_CERT == 'true'` if the direct-secrets form fails workflow validation; otherwise the direct form per CONTEXT.md D-07.**
    - Steps:
      1. `actions/checkout@v4`.
      2. `astral-sh/setup-uv@v8` (already used by Phase 0 publish job).
      3. **VERIFY the apple-actions pin at execution time** per RESEARCH.md A1: run `gh release view --repo apple-actions/import-codesign-certs --json tagName --jq .tagName` (gh CLI is on the runner) inside a step, log the latest tag, and pin to whatever the CONTEXT.md D-06 default is (`@v3`) UNLESS the latest tag is `@v6.x` AND the action's README documents v6 as the recommended major. The planner SHIPS the `@v3` pin per D-06; an updated pin is a future-PR matter.
      4. `apple-actions/import-codesign-certs@v3` with `p12-file-base64: ${{ secrets.APPLE_INSTALLER_CERT_P12 }}` and `p12-password: ${{ secrets.APPLE_INSTALLER_CERT_PASSWORD }}` — imports the cert into a temp keychain that is destroyed at job end.
      5. `run: VERSION="${GITHUB_REF#refs/tags/v}" bash scripts/build-pkg.sh` — builds the unsigned pkg using Task 1's script.
      6. **Conditional re-sign of pyobjc dylibs** (RESEARCH.md Pitfall 8): Task 1's `build-pkg.sh` ships the conditional `if [ -n "${SIGN_DYLIBS:-}" ]; then codesign --deep --force --options runtime --sign "Developer ID Application: ${TEAM_NAME} (${APPLE_TEAM_ID})" "${STAGING_DIR}${INSTALL_PREFIX}/lib/whatsapp-desktop-mcp/.venv"; fi` block BEFORE the pkgbuild call (NOT commented-out — actually shipped, gated by the env var). Workflow step 5 sets `SIGN_DYLIBS=1` when an additional `APPLE_DEVELOPER_ID_APP_CERT` Application-cert secret was also imported in step 4. **Notarization-rejection failure mode:** if step 8 (`notarytool submit --wait`) returns non-zero with an Invalid status, the job MUST exit non-zero (no silent success); the executor MUST mark Plan 03-02 as **PARTIAL** in SUMMARY.md and the workflow's first-run release ships PyPI-only (D-07 fallback) until the re-sign secret is provisioned. Done criterion (below) explicitly handles this.
      7. `productsign --sign "Developer ID Installer: ${TEAM_NAME} (${APPLE_TEAM_ID})" dist/whatsapp-desktop-mcp-${VERSION}-unsigned.pkg dist/whatsapp-desktop-mcp-${VERSION}.pkg` — the cert was imported in step 4; productsign locates it by name. `TEAM_NAME` resolved from a separate secret or hardcoded if known.
      8. `xcrun notarytool submit dist/whatsapp-desktop-mcp-${VERSION}.pkg --apple-id "${{ secrets.APPLE_ID }}" --team-id "${{ secrets.APPLE_TEAM_ID }}" --password "${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}" --wait` — one-shot form per RESEARCH.md Example 4 (no keychain-profile needed for one-shot CI).
      9. `xcrun stapler staple dist/whatsapp-desktop-mcp-${VERSION}.pkg`.
      10. `spctl --assess --type install -vvv dist/whatsapp-desktop-mcp-${VERSION}.pkg` — verification (per RESEARCH.md Pitfall 2 warning sign).
      11. `softprops/action-gh-release@v2` with `files: dist/whatsapp-desktop-mcp-${VERSION}.pkg` to attach the signed+notarized+stapled pkg to the GitHub release.

    **`tap-update` job spec** (verbatim per RESEARCH.md §"Pattern 2"):

    - `runs-on: macos-14` (Homebrew CLI requires macOS or Linuxbrew; macOS-14 has it preinstalled).
    - `needs: [publish]` (NOT `pkg-build` — brew is independent of Apple cert availability per CONTEXT.md D-04).
    - `if: ${{ secrets.BREW_TAP_DEPLOY_KEY != '' }}` (Plan-3 addition; brew is a no-op on community forks without a tap-deploy key — same skip-block discipline as pkg-build).
    - Steps (verbatim per RESEARCH.md):
      1. `actions/checkout@v4` with `repository: gladia/homebrew-whatsapp-desktop-mcp`, `token: ${{ secrets.BREW_TAP_DEPLOY_KEY }}`, `path: tap`.
      2. `run: sleep 30` — PyPI CDN propagation delay (RESEARCH.md Pitfall 3). Annotated comment in YAML.
      3. **Compute sdist sha256** via `curl -sL https://pypi.org/pypi/whatsapp-desktop-mcp/${VERSION}/json | jq -r '.urls[] | select(.packagetype=="sdist") | .digests.sha256'` and emit to `$GITHUB_OUTPUT`.
      4. **Update Formula** via `sed -i ''` against `tap/Formula/whatsapp-desktop-mcp.rb`: rewrite the `url` line + the `sha256` line.
      5. **Regenerate resource blocks** via `brew update-python-resources whatsapp-desktop-mcp || true` (the `|| true` keeps the workflow green if regeneration partially fails — the PR is still useful for manual edit). **`brew update-python-resources` (NOT `homebrew-pypi-poet`)** — locked by the planner via the research adjustment at the top of the planning context (homebrew-pypi-poet is deprecated; CONTEXT.md D-10's *outcome* — regenerated resource blocks — is preserved with the maintained 2026 tool).
      6. **Open PR** via `peter-evans/create-pull-request@v6` with `path: tap`, `commit-message: "whatsapp-desktop-mcp ${VERSION}"`, `branch: "whatsapp-desktop-mcp-${VERSION}"`, body referencing the GitHub release URL.

    **Permissions** at job level for both new jobs: `contents: write` for `pkg-build` (release upload), `contents: read` for `tap-update` (the deploy key handles the tap-repo write). Phase 0's `publish` job's `id-token: write` is unaffected.

    **Comments / docstring** at the top of each new job block referencing CONTEXT.md decision IDs (D-06, D-07, D-10) and RESEARCH.md pitfalls covered (P3 PyPI CDN propagation; P8 pyobjc re-sign; P9 enrollment delay → D-07 fallback).
  </action>
  <verify>
    YAML well-formedness + structural lint (use python's yaml lib; actionlint not assumed installed):
    <automated>python <<'PY'
import yaml, sys
with open(".github/workflows/release.yml") as f:
    doc = yaml.safe_load(f)
jobs = doc.get("jobs", {})
for required in ("publish", "pkg-build", "tap-update"):
    assert required in jobs, f"missing job: {required}"
assert jobs["pkg-build"]["runs-on"] == "macos-14", "pkg-build must run on macos-14"
assert jobs["tap-update"]["runs-on"] == "macos-14", "tap-update must run on macos-14"
# Skip-block guard (D-07): the if: clause must reference APPLE_INSTALLER_CERT_P12.
pkg_if = str(jobs["pkg-build"].get("if", ""))
assert "APPLE_INSTALLER_CERT_P12" in pkg_if or "HAS_CERT" in pkg_if, f"D-07 skip-block missing: {pkg_if!r}"
# tap-update skip-block:
tap_if = str(jobs["tap-update"].get("if", ""))
assert "BREW_TAP_DEPLOY_KEY" in tap_if, f"tap-update skip-block missing: {tap_if!r}"
# needs ordering:
assert "publish" in (jobs["pkg-build"].get("needs") or []), "pkg-build must depend on publish"
assert "publish" in (jobs["tap-update"].get("needs") or []), "tap-update must depend on publish"
print("ok")
PY</automated>
    Mandatory tooling references present (the locked overrides):
    <automated>grep -cE 'brew update-python-resources' .github/workflows/release.yml | xargs -I{} test {} -ge 1</automated>
    <automated>test "$(grep -cE 'homebrew-pypi-poet' .github/workflows/release.yml)" = "0"</automated>
    Notarization step uses notarytool (not deprecated altool):
    <automated>grep -cE 'xcrun notarytool submit' .github/workflows/release.yml | xargs -I{} test {} -ge 1</automated>
    <automated>test "$(grep -cE 'xcrun altool' .github/workflows/release.yml)" = "0"</automated>
    Stapler step present:
    <automated>grep -cE 'xcrun stapler staple' .github/workflows/release.yml | xargs -I{} test {} -ge 1</automated>
    Existing publish job unchanged (DIST-01 byte-stability — Phase 0 invariant):
    <automated>python <<'PY'
import yaml
with open(".github/workflows/release.yml") as f:
    jobs = yaml.safe_load(f).get("jobs", {})
pub = jobs["publish"]
# Phase 0 OIDC: id-token: write must still be set at job level.
perms = pub.get("permissions", {})
assert perms.get("id-token") == "write", f"publish OIDC permission lost: {perms}"
print("ok")
PY</automated>
  </verify>
  <done>
    `release.yml` parses as valid YAML; both new jobs present with correct skip-blocks (D-07 + tap-update); brew tool reference is `brew update-python-resources` (NOT poet); notarytool used (NOT altool); stapler step present; existing publish job's OIDC permission preserved. The pre-existing publish job is byte-stable (no edits to its keys/values).
  </done>
</task>

<task type="auto">
  <name>Task 3: docs/release-setup.md — Apple enrollment + .p12 export + GitHub secrets walkthrough</name>
  <read_first>
    - .planning/phases/03-hardening-and-distribution/03-CONTEXT.md D-08 (one-time setup doc requirements)
    - .planning/phases/03-hardening-and-distribution/03-RESEARCH.md §"Common Pitfalls" Pitfall 9 (enrollment delay → D-07 unsigned fallback)
    - .planning/phases/03-hardening-and-distribution/03-RESEARCH.md §"Pattern 1" notarytool keychain-profile bootstrap (lines 506-515)
    - .github/workflows/release.yml — the new pkg-build + tap-update jobs from Task 2 (referenced from the doc)
  </read_first>
  <action>
    Create `docs/release-setup.md` (NEW) — a step-by-step maintainer walkthrough for the one-time Apple Developer + GitHub Actions secrets bootstrap. Six sections, in this order:

    1. **Overview.** Diagram-shaped textual flow: enroll Apple Developer Program → generate Developer ID Installer cert → export to .p12 → encode to base64 → set 6 GitHub Actions secrets → bootstrap the gladia/homebrew-whatsapp-desktop-mcp tap repo with the initial Formula → first `git tag v0.1.0 && git push --tags` triggers the full release pipeline. Note the D-07 skip-block: if any cert step is incomplete on tag push, pkg-build no-ops and PyPI publish + (if BREW_TAP_DEPLOY_KEY is set) tap-update still ship.

    2. **Apple Developer Program enrollment (1–10 business days).** Link to https://developer.apple.com/programs/. Note the RESEARCH.md Pitfall 9 timing: enroll EARLY. Two account types: individual (~1 day) vs organization (5–10 days). For gladia.io, organization enrollment is the right path (legal entity verification required).

    3. **Generate the Developer ID Installer certificate.** Steps inside Apple Developer dashboard: Certificates → `+` → Software → Developer ID Installer → CSR upload (generated via Keychain Access on the maintainer's Mac: Certificate Assistant → Request a Certificate from a Certificate Authority → save to disk). Download the issued cert; double-click to install in Keychain Access.

    4. **Export to .p12 + base64 encode.** Keychain Access → find "Developer ID Installer: <Team Name>" → right-click → Export → choose .p12 format → set a strong password (this becomes APPLE_INSTALLER_CERT_PASSWORD). Then on the command line: `base64 -i Certificates.p12 | pbcopy` (macOS) or `base64 -w 0 Certificates.p12` (Linux) — the resulting base64 string becomes APPLE_INSTALLER_CERT_P12.

    5. **Generate the App-Specific Password for notarytool.** Visit https://appleid.apple.com → Sign-In and Security → App-Specific Passwords → generate one labeled "whatsapp-desktop-mcp notarization". Set as APPLE_APP_SPECIFIC_PASSWORD. **Critical:** this is NOT the Apple ID password; using the Apple ID password directly will fail notarytool with `Error: Could not retrieve credentials from the keychain.` (RESEARCH.md anti-pattern).

    6. **GitHub Actions secrets bootstrap.** Visit `https://github.com/gladia/whatsapp-desktop-mcp/settings/secrets/actions` and add the 6 secrets (per the user_setup frontmatter): `APPLE_INSTALLER_CERT_P12`, `APPLE_INSTALLER_CERT_PASSWORD`, `APPLE_ID`, `APPLE_TEAM_ID`, `APPLE_APP_SPECIFIC_PASSWORD`, `BREW_TAP_DEPLOY_KEY`. Note the D-07 + tap-update fallback behavior: if APPLE_INSTALLER_CERT_P12 is unset, pkg-build no-ops (shipping unsigned only is NOT supported in v1.0; the unsigned fallback per RESEARCH.md Pitfall 9 is a manual artifact that the maintainer can attach to the GH release with a stark "unsigned" warning).

    7. **Bootstrap the brew tap.** Create the repo `https://github.com/gladia/homebrew-whatsapp-desktop-mcp` (empty); `git clone` it; copy `Formula/whatsapp-desktop-mcp.rb` from THIS repo into the new tap repo; `git push`. Generate a deploy key (or fine-grained PAT with Contents:Write on the tap repo only) and set as BREW_TAP_DEPLOY_KEY.

    8. **First release dry run.** Steps to run the full pipeline against a test tag `v0.0.1-rc1`: `git tag v0.0.1-rc1; git push origin v0.0.1-rc1`. Watch the Actions run. Expected outputs: PyPI release at `whatsapp-desktop-mcp 0.0.1rc1`, GitHub release with `whatsapp-desktop-mcp-0.0.1rc1.pkg` attached, PR opened against gladia/homebrew-whatsapp-desktop-mcp with the new Formula. **Don't promote 0.0.1-rc1 to brew users**; this is a dry-run gate. Once green, delete the test release+tag.

    9. **Troubleshooting.** Pitfall 2 (Gatekeeper on launcher: run `spctl --assess /usr/local/bin/whatsapp-desktop-mcp` post-install). Pitfall 8 (notarization rejection from non-Apple-signed dylibs: enable the conditional codesign --deep step in build-pkg.sh + import a Developer ID Application cert alongside the Installer cert). Pitfall 3 (PyPI CDN 404: bump the `sleep 30` to `sleep 60` in tap-update if the `curl` step fails on first releases). Pitfall 9 (enrollment delay: D-07 skip-block keeps PyPI publish working without certs).

    No code in this task — pure docs.
  </action>
  <verify>
    Doc exists and references the locked decisions:
    <automated>test -s docs/release-setup.md</automated>
    <automated>grep -cE 'D-07|skip-block' docs/release-setup.md | xargs -I{} test {} -ge 1</automated>
    <automated>grep -cE 'APPLE_INSTALLER_CERT_P12' docs/release-setup.md | xargs -I{} test {} -ge 1</automated>
    <automated>grep -cE 'BREW_TAP_DEPLOY_KEY' docs/release-setup.md | xargs -I{} test {} -ge 1</automated>
    Plain-language sanity (all 6 secret names present):
    <automated>python <<'PY'
required_secrets = [
    "APPLE_INSTALLER_CERT_P12", "APPLE_INSTALLER_CERT_PASSWORD",
    "APPLE_ID", "APPLE_TEAM_ID", "APPLE_APP_SPECIFIC_PASSWORD",
    "BREW_TAP_DEPLOY_KEY",
]
text = open("docs/release-setup.md").read()
missing = [s for s in required_secrets if s not in text]
assert not missing, f"missing secret refs in docs/release-setup.md: {missing}"
print("ok")
PY</automated>
  </verify>
  <done>
    `docs/release-setup.md` exists (>2 KB), references all 6 GitHub Actions secrets, mentions D-07 skip-block, references the brew tap bootstrap, includes the troubleshooting table covering Pitfalls 2, 3, 8, 9.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → Apple notarization service | Outbound HTTPS submission of the signed .pkg with maintainer credentials in env vars. |
| GitHub Actions secrets store → release.yml job env | The 6 Apple/brew secrets cross from encrypted storage into job env on each release. |
| Maintainer's Mac → tap repo (gladia/homebrew-whatsapp-desktop-mcp) | Write access via deploy key — separate trust scope from the main code repo. |
| End-user Mac → /usr/local/bin/whatsapp-desktop-mcp launcher | Privileged-path executable target; macOS Gatekeeper + TCC verify the path identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering / Spoofing (.pkg supply chain) | Released signed `.pkg` artifact | mitigate | Developer ID Installer signature via `productsign`; Apple notarization via `xcrun notarytool submit --wait`; ticket stapling via `xcrun stapler staple`; reproducible builds via fully-pinned uv.lock; `.p12` cert kept as encrypted GitHub Actions secret only (verified by grep absence of cert in repo). Post-build verification via `spctl --assess --type install`. Per CONTEXT.md T-1 + RESEARCH.md V6 Cryptography. |
| T-03-02-02 | Tampering (notarization key leak / cert exfil) | GitHub Actions secrets `APPLE_INSTALLER_CERT_P12`, `APPLE_APP_SPECIFIC_PASSWORD` | mitigate | App-Specific Password (NOT Apple ID password); `apple-actions/import-codesign-certs@v3` creates an ephemeral keychain that is destroyed at job end; secrets never appear in workflow logs (GitHub auto-redacts); secrets rotated via dashboard if compromise suspected. Per CONTEXT.md T-5. |
| T-03-02-03 | Denial of Service (TCC churn from package upgrade) | End-user Mac TCC permission grants | mitigate | `pkgbuild --identifier net.gladia.whatsapp-desktop-mcp` ensures macOS treats subsequent installs as same-package upgrades (no fresh TCC prompt); launcher dropped at the stable absolute path `/usr/local/bin/whatsapp-desktop-mcp` regardless of version (CONTEXT.md T-3, P15 mitigation). The `python -m venv --copies` AT the final install path (NOT a relative-path venv) ensures the path the launcher exec's into is byte-stable across releases. |
| T-03-02-04 | Tampering (community fork breakage from missing secrets) | release.yml on a forked repo | mitigate | D-07 `if: secrets.APPLE_INSTALLER_CERT_P12 != ''` skip-block on `pkg-build`; analogous BREW_TAP_DEPLOY_KEY skip-block on `tap-update`. PyPI publish (Phase 0 OIDC, no static cred) still runs, so DIST-01 unaffected. |
| T-03-02-05 | Information Disclosure (stdout corruption from launcher) | `/usr/local/bin/whatsapp-desktop-mcp` shell launcher | mitigate | Launcher is a single `exec "/usr/local/lib/whatsapp-desktop-mcp/.venv/bin/python" -m whatsapp_desktop_mcp "$@"` line — no echo, no set -x, no diagnostic output (Phase 0 D-05 invariant carries; RESEARCH.md §"Anti-Patterns to Avoid" lists "echoing to stdout from the launcher" explicitly). Verified by grep gate on the heredoc body in Task 1. |
| T-03-02-06 | Tampering (Formula version drift from PyPI release) | tap-update job | mitigate | `sleep 30` step ahead of the PyPI sdist `curl` per RESEARCH.md Pitfall 3 (CDN propagation); `brew update-python-resources` regenerates resource blocks from the actual PyPI metadata (NOT a hand-edited list); SHA-256 sourced from the PyPI JSON API (which is the same source brew users would resolve). |
| T-03-02-07 | Information Disclosure (notarization rejection log leakage) | xcrun notarytool submit --wait stdout | accept | notarytool output may include diagnostic data about the staged binaries; this is acceptable since the Action runs in maintainer-controlled CI and the logs are private to the gladia/whatsapp-desktop-mcp repo. No mitigation step needed beyond the existing GitHub log auto-redaction of secret values. |
| T-03-02-08 | Tampering (pyobjc dylib non-Apple signature) | pyobjc .so files inside the .pkg payload | mitigate | The conditional `codesign --deep` re-sign step (RESEARCH.md Pitfall 8) is documented in build-pkg.sh as commented-out, with workflow-level conditional enable when an Application-cert secret is also imported. Plan ships the basic flow first; if first notarization rejects, the re-sign step is enabled in a follow-up commit. |

</threat_model>

<verification>
- All Task 1, 2, 3 automated checks pass.
- `bash -n scripts/build-pkg.sh` exits 0.
- `python -m yaml` parse of `.github/workflows/release.yml` succeeds; pkg-build + tap-update jobs present with correct skip-blocks.
- Required tooling references present: `python -m venv --copies` in build-pkg.sh, `brew update-python-resources` (NOT poet) in release.yml, `xcrun notarytool` (NOT altool), `xcrun stapler`.
- Phase 0 OIDC publish job byte-stable (id-token: write preserved).
- docs/release-setup.md references all 6 GitHub Actions secrets and the D-07 skip-block.
- Source audit: this plan covers DIST-02 (signed-pkg + brew at stable absolute path), CONTEXT decisions D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11.
</verification>

<success_criteria>
- A future `git tag v0.x.0 && git push --tags` triggers the full release pipeline.
- Signed + notarized + stapled `.pkg` is attached to the GitHub release with the launcher at the stable absolute path `/usr/local/bin/whatsapp-desktop-mcp`.
- A PR is opened against `gladia/homebrew-whatsapp-desktop-mcp` with the regenerated Formula.
- Phase 0 PyPI OIDC publish (DIST-01) continues to run unmodified.
- D-07 skip-block keeps community-fork releases working without Apple cert secrets.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hardening-and-distribution/03-02-SUMMARY.md` per the standard summary template.
</output>
