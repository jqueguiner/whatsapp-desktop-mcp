# whatsapp-mcp

MCP stdio server for the macOS WhatsApp Desktop app — read history and send messages from Claude Desktop / Claude Code.

> **Status:** Phase 0 skeleton. The full README (ToS warning, 60-second quickstart, requirements, development notes) is produced by Plan 05 of Phase 0. This file is a placeholder so `hatchling` can resolve `readme = "README.md"` during `uv build`.

See `.planning/PROJECT.md` for the project description and `.planning/ROADMAP.md` for the phase plan.
